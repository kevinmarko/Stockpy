# Known Issues

Dated write-ups of real production issues found and root-caused in this codebase —
kept even after a fix lands, because the "why" (root cause, what was tried and
rejected, what's still unverified) is exactly the context a future regression needs
and a commit message won't carry. See [`docs/README.md`](../README.md) for the full
documentation library index.

| File | Status |
|------|--------|
| [`cross_process_throttle_monotonic_clock_reboot_reset.md`](cross_process_throttle_monotonic_clock_reboot_reset.md) | **Fixed.** A pipeline cycle showed "running" with zero progress for 20+ minutes and no open network connections. `data/cross_process_throttle.py::wait_turn()`'s cross-process FMP/EDGAR spacing lock computed `elapsed = now - last` without checking whether `last` came from a *prior* boot session (monotonic clocks reset to near-zero on reboot) — a stale timestamp made `elapsed` deeply negative, triggering a multi-day `time.sleep()` while holding the exclusive cross-process lock, freezing every process on the machine (any worktree, `main.py`, the daemon, backfill/validation scripts) that ever calls FMP or EDGAR |
| [`cnn_lstm_tf_deadlock.md`](cnn_lstm_tf_deadlock.md) | **Fixed** (Round 8) — TensorFlow eager-execution deadlock in the CNN-LSTM forecaster; see CLAUDE.md's CNN-LSTM subprocess-isolation bullet for the production fix this informed |
| [`lightgbm_faiss_libomp_collision_segfault.md`](lightgbm_faiss_libomp_collision_segfault.md) | **Fixed** (two rounds — a segfault, then a separate deadlock) — OpenMP `libomp` collision between lightgbm and faiss sharing a process; see CLAUDE.md's rag_index/faiss bullet for the production fix |
| [`robinhood_device_approval_login_hang_risk.md`](robinhood_device_approval_login_hang_risk.md) | **Mitigated by design, not yet verified against a real Robinhood account** — device-approval login has no built-in timeout; see CLAUDE.md's Robinhood login bullet for the killable-subprocess fix. 2026-08 follow-up: the login worker now also ingests real order history in the same session |
| [`robinhood_order_history_window_and_fifo_limits.md`](robinhood_order_history_window_and_fifo_limits.md) | **By design, not a bug.** Real, disclosed limits of the Robinhood closed-trade ingest: equities only, short sales dropped (never fabricated), and a persisted-fill union that closes coverage gaps over time rather than promising a complete ledger up front |
| [`react_router_dom_ghsa_jjmj_open_redirect.md`](react_router_dom_ghsa_jjmj_open_redirect.md) | **Resolved** — fixed in [PR #475](https://github.com/kevinmarko/Stockpy/pull/475), `react-router-dom` bumped to `7.18.2` |
| [`vite_plugin_pwa_workbox_dev_chain_unfixable.md`](vite_plugin_pwa_workbox_dev_chain_unfixable.md) | **Resolved** — targeted `overrides` entry in `webapp/package.json`; `npm audit` now 0 findings (was 8 high) |
| [`pip_audit_stale_ambient_env_false_positive.md`](pip_audit_stale_ambient_env_false_positive.md) | **Resolved — false positive.** A reported 36-vulnerability pip-audit result was scanning the wrong Python environment; a rebuilt `.venv` showed zero |
| [`2026_08_security_quality_review.md`](2026_08_security_quality_review.md) | **Findings fixed, documented.** A 2026-08-05 proactive security/quality review (no open GitHub issues existed at the time) |
| [`watchlist_env_inline_comment_hang.md`](watchlist_env_inline_comment_hang.md) | **Pipeline-hang fix landed; operator `.env` fix + 9-key wider blast radius still open.** A `.env` `WATCHLIST=   # comment` line's trailing comment silently became the real env value (python-dotenv doesn't strip it) and hung the whole orchestrator cycle for 1h+ on a bad-symbol network fetch; `load_env_watchlist()` now rejects implausible ticker strings before they reach a live fetch |
| [`data_pipeline_fred_unbounded_timeout_stall.md`](data_pipeline_fred_unbounded_timeout_stall.md) | **Fixed**, plus a **2026-08-27 follow-up sweep, also fixed.** Same symptom class as the WATCHLIST hang above (a cycle wedged in `state: "running"` for 2.5 days, restart the only recovery), different root cause: `fredapi.Fred.get_series()` calls a bare `urlopen()` with no timeout anywhere in the library. Fixed at both reachable call paths (`fetch_all_data_async`'s per-task `asyncio.wait_for`, plus a `socket.setdefaulttimeout()`-scoped bound directly on the FRED calls in `data_engine.py`) and backed by a new read-only daemon stall alert. The follow-up sweep (3 parallel audits: subprocess calls, executor/multiprocessing waits, LLM client timeouts) found and fixed the same bug class in `execution/alpaca_broker.py`/`data/market_data.py`'s `AlpacaProvider` (alpaca-py's `RESTClient` has no timeout anywhere — worse in kind than the FRED bug, since `AlpacaBroker` calls it synchronously on its own event loop, not even off a background thread), `pipeline/runner.py`'s generic per-step dispatcher, 3 unbounded `subprocess.run` calls, and 5 LLM chat-client construction sites (`google-genai`'s own default is confirmed to be no timeout at all); one item (`api/ws_api.py`'s WS idle-timeout gap) is disclosed and deliberately deferred |
| [`forecast_tracker_local_data_root_split.md`](forecast_tracker_local_data_root_split.md) | **Fully resolved (2026-08-19).** Code fixed (PR #720); the split `forecast_errors` data (1,974,166 stale rows) was merged into the live DB with zero duplicates, verified, and the old DB archived. A second, previously-undocumented instance of the same bug class was found and fixed in `investyo_mcp_server.py` during the reconciliation — see CLAUDE.md's `settings.LOCAL_DATA_ROOT` bullet |
| [`webapp_memory_leak_investigation.md`](webapp_memory_leak_investigation.md) | **Resolved & Verified** — Memlab V8 heap profiling identified focus retention and WebSocket reconnect edge cases; hardened `useLiveTick.ts`, `LogStream.tsx`, `Modal.tsx`, and `CommandPaletteModal.tsx` with automated test coverage |
| [`codeql_advanced_outage_and_bandit_addition.md`](codeql_advanced_outage_and_bandit_addition.md) | **Resolved.** CodeQL Advanced was blocked first by a real default-setup/advanced-setup config conflict (cleared on its own), then by a live GitHub.com code-scanning-ingestion outage (external, not a code defect). Added `bandit` as an independent CI SAST job that never depends on GitHub's code-scanning API; 44-finding baseline individually triaged and suppressed with `# nosec BXXX` |
| [`scenario_matrix_field_mismatch.md`](scenario_matrix_field_mismatch.md) | **Fixed** ([PR #808](https://github.com/kevinmarko/Stockpy/pull/808)) — Paper Broker screen crashed on every visit against a live backend because `ScenarioHeatmap.tsx` and `pilots/scenario_matrix.py` disagreed on almost every field name; one instance of the "Options desk mock/live API parity" gap CLAUDE.md already documents, worse than the named ones since it's mounted unconditionally |
| [`duplicate_orchestrator_daemon_processes.md`](duplicate_orchestrator_daemon_processes.md) | **Root cause found and fixed (2026-08-24).** Two independent `orchestrator_daemon` processes (one launchd-managed, one manually started) recurred a second time, this time traced to a real race in `launch_webapp.command`'s single-shot health check racing a daemon that's alive but not yet answering `/health` (engine warm-up, or `launchd`'s own crash-restart window) — fixed by checking for the process itself (`pgrep -f`) before deciding to start a competing one. The 2026-08-19 occurrence was resolved on the spot without a known root cause; this is that root cause |
| [`options_desk_mock_live_parity_sweep_2026_08_19.md`](options_desk_mock_live_parity_sweep_2026_08_19.md) | **Fixed.** The gap above turned out systemic, not isolated — a full sweep of the Paper Broker screen's dependency tree found 21 more crash/fabrication bugs across 13 backend modules and 10 frontend files (Dispersion, ZeroDte, VPIN, Smart Order Router, GEX, LOB Depth, Vol Surface, Earnings Crush, Unusual Flow, Vol Forecast, Gamma Scalper, Market Maker, Research Copilot, Multi-Broker Gateway, SEC 606, FIX Gateway, Options Chain); verified live against a real (non-mock) backend, not just typecheck/unit tests |
| [`daily_signals_missing_table.md`](daily_signals_missing_table.md) | **Fixed.** `investyo_mcp_server.py` tools (`get_signal_breakdown` and others) errored with `no such table: DailySignals` because `database_setup.py` — the only place that table is created — is never wired into any automated pipeline path; ran it against the live `LOCAL_DATA_ROOT` db with a verified backup, confirmed zero data loss via a full 38-table row-count diff, and confirmed the affected tools now return an honest no-data message |
| [`lgbm_ranker_query_group_bug.md`](lgbm_ranker_query_group_bug.md) | **Fixed and verified.** `LGBMCrossSectionalRanker.train()` computed correct per-date LambdaRank query groups but never actually passed them to LightGBM — every fit treated the ENTIRE training set as one giant query instead of one query per date, which is silently wrong (ranks tickers across different dates) and, once the validation universe widened past ~100 tickers, crashed outright on LightGBM's internal ~10000-row-per-query limit. Affects the real production training path (`scripts/train_lgbm.py`) too, not just this validation script. A full real re-run confirmed the crash is gone (1365/1365 CPCV paths complete); the run's reported Sharpe surfaced a SEPARATE, already-tracked harness-wide annualization-frequency gap, not this bug recurring — see `docs/VALIDATION_STRATEGY_FIX_LOG.md`'s 2026-08-21 follow-up entry |
| [`macro_killswitch_fail_open_on_missing_fred_data.md`](macro_killswitch_fail_open_on_missing_fred_data.md) | **Fixed.** `MacroEconomicDTO.killSwitch`/`market_regime` silently computed a "risk on" verdict off hardcoded benign defaults (VIX=15.0, T10Y2Y=0.5, etc.) substituted for genuinely missing FRED series, instead of failing closed — the two named fields in the original report turned out to be the *less* load-bearing half of the bug; a design-review pass before implementation also caught a landmine in the first-draft fix and a second live gate (`stress_scenario_check`) the `killSwitch`-only approach would have missed. A second, independent dead-code bug (`main.py` reading a `macro_raw` key `fetch_macro_raw()` never populates) was found and fixed in the same pass. Verified against real, live FRED data and a full 32-symbol `main.run_once()` cycle, zero errors |
| [`graduated_degrade_all_or_nothing_blends.md`](graduated_degrade_all_or_nothing_blends.md) | **Fixed.** A diagnostic showed 29/30 symbols pinned at a uniform 0.2 forecast-skill weight despite ~2,800 completed observations each, because one newly-added ensemble model (`cnn_lstm`, only 7 observations) tripped an `any(n < min_obs)` all-or-nothing gate for everyone. The same bug class was found in two more unrelated modules (`risk/etf_transmission.py::build_etf_return_composite`'s composite-weighting-basis selection, `signals/registry.py::compute_all`'s missing-feature abort) and fixed at all three sites; see CLAUDE.md's "Graduated-degrade convention for N-way blends" bullet for the resulting convention |
| [`xsec_universe_coverage_concurrency_variance.md`](xsec_universe_coverage_concurrency_variance.md) | **Fixed.** `cross_sectional_momentum`/`sector_quality_rank`'s `deployable` verdicts swung wildly across dozens of real runs with no code changes — empirically confirmed (3x bit-identical reruns) to be *other* concurrent validation runs on this shared machine hitting FMP's rate-limit cooldown at different points, each ending up with a differently-incomplete ~500-ticker universe. `validation/harness.py` now tracks and fails closed on universe coverage below 90%, visible in the JSON summary, HTML report, and CLI table |
| [`options_vpin_fabricated_live_data.md`](options_vpin_fabricated_live_data.md) | **Fixed and verified against real live data.** The live `GET /pilots/options/vpin/metrics` endpoint unconditionally computed VPIN from `generate_synthetic_option_trades()`'s fabricated random-walk data, indistinguishable from a real measurement anywhere in the response, docs, or UI. Fixed by fetching real hourly bars via `data.market_data.get_provider()` (a bar-level BVC approximation, mirroring `desktop/daemon_runtime.py::maybe_update_circuit_breaker`'s own precedent) and degrading to an explicit `data_available: False`/`vpin: None` response — never a fabricated fallback — when real bars are unavailable; `VpinGauge.tsx` now renders that state honestly instead of a `?? 0.25` fake default |
| [`forecast_tracker_early_actualization.md`](forecast_tracker_early_actualization.md) | **Fixed.** `ForecastTracker.update_actuals`'s `tolerance_days` (default 5) subtracted from the eligibility cutoff instead of adding to it — actualizing every forecast up to 5 days *early*, not late as its own docstring claimed, systematically biasing the RMSE that `FORECAST_SKILL_WEIGHTING_ENABLED` weights are computed from. Empirically confirmed a 30-day forecast actualized on day 25 (a 10-day forecast on day 5 — 50% relative timing error). Fixed by removing the parameter and requiring the full nominal horizon to elapse; decision documented to leave already-recorded `forecast_errors` rows unreconciled (going-forward-only fix, self-heals over the 180-day rolling skill window) |
| [`hmm_regime_state_mislabeling_spherical_tied.md`](hmm_regime_state_mislabeling_spherical_tied.md) | **Fixed and verified (2026-08-22).** `HMMRegimeDetector.identify_states_by_vol()` mislabeled hidden states for `covariance_type="spherical"` (hmmlearn's public `covars_` getter returns a malformed shape, silently tripping an unlogged arbitrary-index fallback on every fit) and could mislabel `"tied"` (ranked states by an undirected mean-vector norm instead of a directional risk proxy) — reproduced a genuine crash regime labeled `"bull"`, which could have silently under-reported risk to `MacroEconomicDTO.killSwitch`. Also fixed: the silent fallback now logs, and `n_states >= 4` now correctly labels the worst state `"bear"`. A separate, structural finding: `tied` covariance is not recommended for regime detection even post-fix (see the write-up) |
| [`options_risk_fabricated_spy_spot.md`](options_risk_fabricated_spy_spot.md) | **Fixed.** `pilots/options_risk.py::calculate_portfolio_greeks` fabricated a hardcoded `$500.0` SPY spot price for any book with no direct SPY position, while `main.py`'s automated delta-hedge cycle filled at the REAL SPY price — confirmed sizing 4 already-placed live paper hedge orders off the fabricated price (one selling 1,226 shares, ~$943k notional). A second bug (`_resolve_symbol_beta`'s dead-on-arrival `compute_beta` fallback, always `TypeError`, silently swallowed) and a third (a dead post-hedge success-log key mismatch that meant the confirmation log never printed) were fixed in the same pass |
| [`options_meta_labeler_serving_time_gaps.md`](options_meta_labeler_serving_time_gaps.md) | **Fixed.** Six compounding bugs in the Stage 4 Options ML Meta-Labeler, every one making the gate MORE permissive on unclean input, never more conservative: 4 of 10 live inference features (`vrp`/`vix`/`short_delta`/`credit_to_width_ratio`) were always hardcoded constants, never real data; `trend_bias` meant a different thing at train time vs. serve time (dropped as a feature entirely); an unresolvable required feature (present but NaN) reached the model as a normal value and produced a confident, INCREASED prediction; a degenerate single-outcome training run reported unclipped 100%/0% confidence forever; any scoring exception silently failed OPEN to full un-gated trade size at DEBUG-only logging; and `train()` reported in-sample metrics with no disclosure |
| [`options_lifecycle_daemon_gate_gap_2026_08_22.md`](options_lifecycle_daemon_gate_gap_2026_08_22.md) | **Partially fixed.** `main.py`'s automated options-lifecycle outer gate omitted `OPTIONS_0DTE_ENABLED` — enabling ONLY 0DTE lifecycle management (a documented, self-contained flag) silently skipped the whole block, so `manage_0dte_exits()`'s +75%/-30%/15:45 ET hard exit never ran; fixed with a regression test. A second, broader gap is documented but NOT fixed: the automated options lifecycle (exit management, strategy auto-execution, delta hedging) has no equivalent wiring at all in `main_orchestrator.py`/`desktop/daemon_runtime.py` — only 0DTE is separately wired there — so flipping `ORCHESTRATOR_DAEMON_ENABLED=True` (the documented future cutover) would silently stop those three behaviors; deferred to its own follow-up task given the daemon's `macro_dto`-threading and cadence design questions |
| [`synthetic_diffusion_reverse_sde_sign_error.md`](synthetic_diffusion_reverse_sde_sign_error.md) | **Sign error and VaR/CVaR-vs-paths consistency both fixed and verified; calibration gap substantially (not fully) mitigated across two follow-up rounds.** `generate_synthetic_crash_paths`/`generate_guided_crisis_paths` integrated the reverse-time OU SDE with a negated drift at both call sites, causing the live diffusion-stress-test endpoint's generated paths to diverge instead of denoise (verified three independent ways). Fixed via a single shared, tested `_reverse_sde_drift` helper plus distributional-recovery regression tests. A reported VaR/CVaR-vs-paths inconsistency was fixed in a same-day follow-up — a first naive attempt introduced a worse regression (negative VaR, reverted); the real fix derives VaR/CVaR directly from the compounded price paths themselves. The residual calibration gap (generated scale far too large even after an epoch bump) was further mitigated via early-stop + analytic Tweedie denoising (~2-3x further improvement, verified via a multi-seed sweep) plus a new request-contract horizon bound — a real flaw in the fix's first design (CFG guidance amplifying the score network's own inaccuracy) was found and corrected mid-implementation, with an honestly-disclosed trade-off: CFG's directional effect on the exact final output is now weaker (can invert on some data) even though overall calibration measurably improved. Not claimed fully fixed |
| [`paper_trade_strategy_id_vocabulary.md`](paper_trade_strategy_id_vocabulary.md) | **Disclosed, not fixed.** Paper-trade writers stamp `strategy_id` using at least five different, mutually-inconsistent conventions (pilot-registry kebab-case slugs, human-readable Title Case labels, the literal `"Manual Trade"`, the store's own `"untagged"` default, and the advisory bridge's flat `"advisory"`/`"composed"` bucket) — since `sizing/kelly.py`'s per-strategy warm-up pools trades by exact string equality on this field, near-duplicate keys silently fragment what should be one strategy's trade history. Two genuinely wrong hardcodes (a `dict.get` default that doesn't fire on an explicit `None`, and a 0DTE exit path that re-hardcoded its entry tag instead of reading the position's real one) were fixed in the same pass; standardizing the vocabulary itself is deferred |
| [`paper_options_zero_fill_price.md`](paper_options_zero_fill_price.md) | **Mitigated.** `PaperAccountStore`'s atomic order processing functions (`apply_multi_leg_fill`, `apply_roll_fill`) silently defaulted to a zero fill price if a leg was missing one and didn't validate that a `fill_price` was genuinely positive, letting corrupt zero-cost-basis options positions through; fixed with a rigid `fill_price > 0.0` validation that rejects the entire atomic order (CONSTRAINT #6), plus a purge script for previously-corrupted rows. See CLAUDE.md's "CONSTRAINT #6 and #4 fix (2026-08-22, Paper Execution)" bullet |
| [`pr872_math_regression_sweep_2026.md`](pr872_math_regression_sweep_2026.md) | **Clean sweep — no quant-math regression found in scope.** An audit pass (Agent 6a of the PR 872 remediation) regex-swept the branch's diff for numeric-guard/threshold-adjacent changes; found and fixed two documentation/test-data regressions (a test's data-loading path reverted to an unthrottled `yfinance` call, and a docstring that lost its pointer to the real registry numbers), confirmed the rest as either pre-cleared annotation-only churn or a genuine, correctly-guarded feature addition (`paper_closed_trades` replacing the old triple-barrier proxy for paper-feature outcomes). One unrelated, pre-existing gap in `pilots/vol_mispricing.py` (an unguarded `None * float` on a narrow double-data-outage path) was noticed and disclosed, not fixed, as outside this agent's file scope |
| [`pr872_live_db_test_contamination_2026.md`](pr872_live_db_test_contamination_2026.md) | **Fixed and verified.** The same PR 872 remediation's new `transactions_store` bridge (`data/paper_account_store.py::_record_closed_trade`) added an implicit write path with no test-isolation guard, and the live, shared `~/.stockpy_local/quant_platform.db`'s `trades` table was found holding 260 rows of synthetic test data (confirmed 100% contamination — the table had 0 rows before the bridge feature existed, every row bridge-tagged). Fixed via a new autouse `conftest.py` fixture routing both `PaperAccountStore` and `TransactionsStore` to an isolated per-test temp-file DB by default; live DB cleaned up by the human operator (backed up first) after the agent's destructive-write classifier blocked the agent from running the DELETE itself |
| [`daemon_universe_watchlist_divergence.md`](daemon_universe_watchlist_divergence.md) | **Fixed and verified.** `pipeline/production_steps.py::AsyncDataFetchStep` — the universe builder the persistent orchestrator daemon (`main_orchestrator.py`/`desktop/daemon_runtime.py`) actually runs every cycle — never read `WATCHLIST`/`watchlist.txt` at all, and silently dropped `settings.DEFAULT_TICKERS` outright whenever scan-discovery had any candidate, instead of unioning it in like `main.py::_build_universe()` correctly does. A symbol added via watchlist.txt, `POST /agentic/watch`, or the Paper Broker order ticket's "+ Add to Watchlist" button reliably reached `main.py`'s universe but never the daemon's — the likely root cause of an operator-observed "symbols falling out of the universe" symptom. Fixed via two new shared functions in `data/portfolio_sync.py` (`compute_tracked_universe`/`load_env_watchlist`) both entry points now delegate to; 190 pre-existing + new tests pass |
| [`earnings_crush_bmo_amc_bar_alignment.md`](earnings_crush_bmo_amc_bar_alignment.md) | **Fixed.** `pilots/earnings_crush.py::get_historical_earnings_moves` always measured the realized post-earnings gap as `Open[event_date] - Close[event_date-1]` — correct only for before-market-open (BMO) reporters. For after-market-close (AMC) reporters (the majority case for large-cap tech, this module's own default universe), the real reaction lands one day later and was never measured, understating the realized-move denominator and inflating `crush_edge_ratio` enough to fire a real trade alert on a candidate with no genuine edge. Verified (via FMP's own published API docs) that no real BMO/AMC field exists anywhere in this codebase's earnings data; fixed by computing both BMO and AMC candidate gaps from real bar data and taking the larger (conservative, never fabricated) — plus tightening the front-week expiration picker to always clear the earnings date |
| [`options_matrix_pop_truncation_and_vol_skew_drift.md`](options_matrix_pop_truncation_and_vol_skew_drift.md) | **Fixed.** `OptionsMatrix.tsx`'s "Prob of Profit" integrated a probability density over `computePayoff`'s finite chart grid instead of the true unbounded price domain, silently dropping tail probability — a credit spread's flat profitable plateau runs past the grid edge, understating POP by 35 points in a hand-verified longer-dated/higher-vol case. Fixed via a closed-form log-normal integration (`computeProbabilityOfProfit`) over `(0, +Infinity)`, partitioned by the position's real breakevens. A second, latent finding was fixed pre-emptively in the same PR: `VolSurface3D.tsx`'s 25Δ skew card always recomputed a moneyness-proxy value instead of reading the real backend `skew_25delta` the sibling `VolSurfaceView.tsx` screen already renders under the identical label — harmless today (no live call site wires real vol data in yet) but a live trap for the next one that does |
| [`earnings_crush_uoa_followup_audit_findings.md`](earnings_crush_uoa_followup_audit_findings.md) | **Mixed — 8 findings across 3 branches.** A combined follow-up audit of `pilots/earnings_crush.py` and `pilots/unusual_options_flow.py` (findings #2–#9). Finding #7 — `GET /pilots/options/flow/unusual` and `GET /pilots/options/earnings-crush/candidates` both returning an empty list on two structurally different conditions ("nothing qualified" vs. "the scan degraded") with no way to tell them apart — is fixed and verified in full via a new `diagnostics` kwarg on both scan functions plus `degraded`/`symbols_errored`/`symbols_fetch_failed` response fields. The other seven findings (IV-burst HV30 wiring, a mid-block sentiment deadband, `price_is_estimated`/`spot_price_is_estimated` honesty flags, per-contract isolation, an atomic `save_uoa_records` write, `historical_moves`/`company_name` wiring, and a missing `net_credit` in `execute_earnings_crush_trade`'s success branch) are implemented on two sibling branches and described here at a summary level |
| [`lob_simulator_uncalibrated_live_arrival_rates.md`](lob_simulator_uncalibrated_live_arrival_rates.md) | **Partially fixed; live calibration deliberately not pursued.** An audit of `pilots/options_sor.py`/`pilots/lob_simulator.py` found the live `POST /pilots/options/lob/simulate-queue` endpoint runs on fixed Poisson-rate constants for every symbol, never `compute_lob_arrival_rates()` (zero production callers, confirmed by grep) — contradicting the module's own "empirically calibrated" docstring claim. Two independent, real `mu_cancel` unit bugs in `compute_lob_arrival_rates()` were fixed and regression-tested defensively (the primary branch divided by cancel event count instead of canceled shares; the no-depth fallback returned a raw events/sec rate that, empirically, flips `fill_probability` from ~0.03% to 100% the moment it's multiplied by a real queue depth), and a separate, live-reachable sign bug in the sibling `options_sor.py` (an unsigned delta silently flooring PUT-active-leg hung-leg-probability at 0.02) was fixed in the same PR. Two follow-up attempts at wiring live `theta_market` calibration were built/investigated and both explicitly REJECTED, not merely deferred: a full, tested Alpaca `trade_count`-based implementation (PR #909) was closed unmerged on data-source policy grounds (FMP/Yahoo only for new features, not Alpaca), and a Robinhood Level-2 price-book alternative was rejected before implementation (device-approval-gated login can't serve a stateless request; a depth snapshot isn't an arrival rate) — see the doc's own "Alpaca- and Robinhood-based calibration both investigated and rejected" section and CLAUDE.md's resulting data-source-policy bullet |
| [`ci_offline_suite_flakes_2026_08_24.md`](ci_offline_suite_flakes_2026_08_24.md) | **Investigated and resolved — not a `main` regression.** Two `test (offline suite)` CI failures observed around PR #898 turned out to be three unrelated things: `main`'s own push-run failure was a pre-existing, self-documented timing-sensitive throttle test (green on re-run, no code change); a `docs/settings_liveness.json` staleness failure seen on PR #899's branch was caused by *that PR's own* line-number-shifting diff, already fixed there; and a `test_build_command_manifest.py` real-subprocess-invocation test failed with a native `SIGABRT` ("terminate called without an active exception") under CI's `-n auto` parallel load — confirmed not reproducible locally and not correlated with any code change (same commit's content passed in an earlier CI run). Hardened with a bounded retry rather than a code fix, since none was needed |
| [`robinhood_snapshot_fabricated_zero_on_swallowed_api_failure.md`](robinhood_snapshot_fabricated_zero_on_swallowed_api_failure.md) | **Fixed and verified.** `data/robinhood_portfolio.py::_fetch_live_snapshot()`'s `... or {}` / `... or "0"` fallbacks silently turned a swallowed `robin_stocks` API failure (confirmed against its source: `request_get(..., dataType='indexzero')` never raises on a non-200/malformed response, it returns bare `None`) into a fabricated `$0` equity/buying-power reading, persisted as a genuinely "fresh" cached snapshot for up to 20h — reproduced two ways end-to-end (a malformed profile call with real holdings; `build_holdings()`'s own internal profile round-trip transiently failing while this module's later, separate profile calls succeeded). Fixed by raising when either profile call returns empty/malformed, correctly re-engaging the existing DB → JSON-cache fallback that had never actually been triggered by this failure mode; `build_holdings()` returning `{}` is deliberately left unguarded (genuinely ambiguous with a real zero-position account) — a disclosed, intentional scope boundary, not a gap. A related asymmetry (the exception-fallback path checked only the JSON cache, never DB) was fixed in the same pass |
| [`prompt_registry_unsigned_remote_adoption.md`](prompt_registry_unsigned_remote_adoption.md) | **Fixed and verified.** `PromptRegistry._safe_adopt()`'s HMAC-SHA256 signature gate was a silent no-op whenever `PROMPT_REGISTRY_SIGNING_KEY` was unset — for EVERY backend (`HTTPStore`/`FirestoreStore` alike), not narrowly scoped to `LocalJSONStore` offline dev as the module docstring implied. A constructed tampered `PromptRecord` (genuine body + an injected instruction worded to dodge the guardrails deny-list) with no signing key was confirmed to be silently adopted and reach every live LLM call site (Gravity, chart insight, commentary, research, portfolio context). `PROMPT_REGISTRY_ENABLED` defaults `False` (fresh installs unaffected), but `PROMPT_REGISTRY_BACKEND` defaults to `"http"` the moment it's enabled, with nothing warning an operator who forgets to also set a signing key. Fixed via three layers: `PromptRegistry.__init__` now raises `ValueError` when constructed with an `HTTPStore`/`FirestoreStore` and no signing key; `_build_registry_from_settings()` refuses to build that remote store at all (CRITICAL log + alert, falls back to cache/baseline-only) so the new constructor guard can't crash `get_registry()`; and a new blocking `scripts/preflight_check.py` gate catches the misconfiguration at go-live time. Everything already correct when a key WAS configured (constant-time comparison, pin/rollback, the CONSTRAINT #4/#6 fallback chain) was left untouched |
| [`options_gex_100x_dollar_scaling.md`](options_gex_100x_dollar_scaling.md) | **Fixed and verified.** `pilots/options_gex.py` computed every dollar GEX figure (Net/Call/Put GEX, every per-strike value) missing the industry-standard `×0.01` "per 1% move" normalization the module's own separately-computed `dealer_hedging_flow` field already correctly applied — overstating the primary displayed figures by exactly 100x (a single ATM SPY strike computed to $3.46B). Also fixed: missing/zero/NaN implied volatility and missing/unparseable expiration previously fabricated a plausible-looking `σ=0.25`/`dte=30.0` instead of excluding the contract (CONSTRAINT #4, reachable on real yfinance data), and `GexProfileView.tsx` never surfaced the backend's own honest `chain_source`/`spot_price_source` fields, so a fully synthetic fallback chain rendered indistinguishably from real market structure. A second Black-Scholes gamma implementation (not routed through the canonical `pilots/options_risk.py`) was found, numerically verified identical, and disclosed but not consolidated in this pass |
| [`sec_606_price_improvement_fabricated_zero.md`](sec_606_price_improvement_fabricated_zero.md) | **Fixed and verified.** The audited file (`execution/multi_broker_gateway.py`) turned out to contain zero NBBO/price-improvement logic at all — the real bug was in `data/execution_audit_store.py`/`execution/sec_rule_606_reporter.py`: every real production fill omits NBBO (no wiring exists to populate it), and `price_improvement` defaulted to `0.0` identically for "genuinely zero improvement" and "unmeasurable" — making the live SEC Rule 606 report's price-improvement rate/dollars structurally always ~0%/`$0.00`, a regulatory-shaped report presenting a coverage gap as a measured fact. A related dead-code bug (`classify_limit_order`, unit-tested but zero write-path callers) made the "Marketable Limit" category structurally unreachable too. Fixed via a new `nbbo_available` column distinguishing the two cases, coverage-aware rate denominators plus an explicit `nbbo_coverage_pct` surfaced everywhere a rate is, and wiring the limit-order classifier into the write path. A defensive crossed-market guard was also added to `execution/fix_gateway.py`'s (currently fully-simulated) NBBO synthesizer |
| [`sector_selection_similarity_lookahead.md`](sector_selection_similarity_lookahead.md) | **Fixed and verified.** `sector_selection_engine.py`'s semantic-similarity ranking term had no point-in-time awareness at all — it always embedded a target's CURRENT business description regardless of what `as_of` date a backtest/replay caller was scoring, defeating the lookahead-safety design the sibling Sector Heat Factor term already had (and had zero test coverage, since every existing test mocked the description resolver away). Fixed via a new `HistoricalStore.get_fundamentals_raw_json_asof` point-in-time lookup (mirroring the existing `get_fundamentals_asof` convention) threaded through `resolve_target_description`/`_rank_one_target`. Verified this has a real, disclosed effect on the live daily pipeline too (not just future backtest callers) by reading every real fundamentals-provider's return shape and reproducing the exact degrade case in a test, rather than assuming it's a no-op. A second, unrelated `degraded_reason` operand-order bug (a routine heat-side flag silently masking the actual blocking similarity-side reason) was found and fixed in the same pass |
| [`llm_prompt_injection_undelimited_headlines.md`](llm_prompt_injection_undelimited_headlines.md) | **Fixed and verified.** `llm/research.py` interpolated real, externally-sourced (FMP/Finnhub) news headlines directly into the LLM user prompt with no delimiter separating untrusted data from instructions — reproduced statically that an instruction-shaped headline would land verbatim in the synthesis block. Bounded risk (the four Tier 9 schemas have zero numeric/action fields and nothing downstream in `sizing`/`signals`/`execution` reads their output — confirmed by grep), but a real trust-erosion/social-engineering risk to a human operator. Fixed via explicit `<headline>`/`<research_context>` fencing plus a matching system-prompt instruction never to follow instructions found inside them, with a sanitizer neutralizing embedded angle-brackets/newlines that could otherwise forge a fake closing delimiter |

Three of these (`cnn_lstm_tf_deadlock.md`, `lightgbm_faiss_libomp_collision_segfault.md`,
`robinhood_device_approval_login_hang_risk.md`) are also individually cross-linked from
CLAUDE.md's own architecture bullets, since the production fixes they informed are
load-bearing. The rest are indexed here only.
