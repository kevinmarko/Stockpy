# Feature & Tier History — Gemini doc (detailed)

This file holds the detailed, dated changelog of every Tier/Stage/Task feature shipped
since Stage 4, moved out of `GEMINI.md` on 2026-07-05 to keep that file under the
context-budget char limit (mirrors the equivalent split done for `CLAUDE.md` /
`docs/FEATURE_TIER_HISTORY.md` the same day). `GEMINI.md` still carries the **current,
load-bearing** architecture reference (standing operator rules, tech stack, flat
module list, agent directives / enforced conventions); this file is the **archival
"why does X exist and what shipped in the PR that added it" record** — read it when
you need the full backstory, test surface, or Gravity-audit-step details for a
specific subsystem.

This file covers the same underlying features as `docs/FEATURE_TIER_HISTORY.md` but
is written in GEMINI.md's own denser prose style (ALL-CAPS inline section headers,
one paragraph per feature) rather than CLAUDE.md's Markdown-heading style — kept as a
separate file rather than merged so each agent's own doc and its own archival history
stay independently attributable and easy to diff.

Sections in this file (search for the ALL-CAPS inline header):
- ML Stage 4: Triple-Barrier Labeling & Meta-Labeling, qlib-style ML architecture
- Stage 5: Disk-persisted cadence cache
- Stage 6: Holding-aware advisory engine (engine/advisory.py)
- .env loading convention, pipeline smoke tests + verify tooling
- Daily HTML report redesign, Streamlit dashboard improvements
- Technical Options Matrix & premium-selling helper
- Reports tab Brinson-Fachler attribution, Launcher tab dual entry points
- Safety/Analytics/Control tabs (circuit breakers, dependency map, version registry)
- Observability tab (system telemetry, latency heatmap, error log)
- Tier 1 / 1.2 / 1.3 — conviction calibration, decision journal
- Tier 1 decision support — snapshot diff ("Δ Since Last Run")
- Market Data tab diagnostics
- Tier 5.1 — ADVISORY_ONLY mode quarantine
- Tier 5.3 — kill switch pause gate + macro-triggered advisory gating
- (and further tiers/stages continuing to the end of the file)

**Critical invariants that must never regress are still summarized in `GEMINI.md`
where they're load-bearing for every session** (e.g. ADVISORY_ONLY quarantine,
CONSTRAINT #3/#4/#6, no-fabricated-metrics, dead-letter resilience, order-idempotency,
kill-switch semantics). This file adds the full detail, test surface, and Gravity step
numbers behind each shipped feature.

---

TRIPLE-BARRIER LABELING & META-LABELING (ml/triple_barrier.py, ml/meta_labeling.py — Stage 4):
Triple-barrier labeling (Lopez de Prado AFML Ch. 3) generates +1/-1/0 labels by racing three barriers: upper (profit-take = entry + pt_mult * sigma * entry), lower (stop-loss = entry - sl_mult * sigma * entry), and vertical (timeout = entry + N business days). `get_volatility(close, span=100)` uses causal EWMA (`adjust=False`) — vol at t uses ONLY returns ≤ t. `cusum_filter(close, threshold)` samples events when cumulative log-return drift crosses ±threshold (resets after each event). The CUSUM loop is intentionally sequential (not vectorizable). `apply_triple_barrier(events, close, pt_sl_multiples, vertical_barrier_days)` pre-computes the full vol series (causal), indexes at each event time, and finds first-touch barriers looking ONLY at close[t0+1:t1]. Perturbing prices after t0 must NOT change barriers — verified by perturbation tests.

MetaLabeler trains a binary LightGBM classifier to predict P(primary_signal_correct). Meta-label = 1 when primary direction matches barrier outcome (both +1 or both -1); vertical timeout counts as wrong. The `MetaLabelerRegistry` (global_meta_registry in ml/meta_labeling.py) maps signal_id → MetaLabeler. SignalAggregator queries it for each active module; if P < settings.META_LABEL_MIN_CONFIDENCE (default 0.4), meta_label_composite is forced to 0.0 (hard gate, not a gradual suppression). This zeroes the Kelly Target while leaving the directional signal score (BUY/HOLD/etc.) unchanged. Empty registry = pre-Stage-4 behavior (composite = 1.0, no-op).

QLIB-STYLE ML ARCHITECTURE (ml/ — Prompt 4.3, no qlib dependency):
Three-layer structure: ml/data/ (PIT feature store + label construction), ml/models/ (Model ABC + implementations), ml/strategies/ (StrategySpec). ALL ML models must implement ml.models.base.Model: fit(X, y, t1)/predict(X)/save(path)/load(path). Both LGBMCrossSectionalRanker and MetaLabeler satisfy this ABC. ml/registry.yaml lists production models with trained_date, cpcv_dsr, pbo, and deployable flag; parse with PyYAML (added to requirements.txt). PITFeatureStore (ml/data/store.py) caches daily cross-sectional features as Parquet files (ml/data/cache/) for expanding-window retraining. StrategySpec (ml/strategies/__init__.py) links a Model to a signal_id and flags is_meta_labeler.

DISK-PERSISTED CADENCE CACHE (cache/cache_store.py — Stage 5):
A reusable, SQLite-backed cache layer at cache/cache.db that prevents redundant network calls across all data-fetch categories. No new third-party dependencies — uses only stdlib (sqlite3, gzip, json, threading, hashlib) plus pandas.

Core types: Cadence enum (INTRADAY=5 min, DAILY=20 h, WEEKLY=7 d, MONTHLY=30 d, QUARTERLY=90 d, YEARLY=365 d). CADENCE_TTL dict is the single place to retune any TTL. CADENCE_REGISTRY maps logical names ("quotes", "daily_bars", "fundamentals", "financials", "dividends_meta", "analyst_ratings", "earnings_calendar", "company_profile", "macro_regime_inputs") to their Cadence. CacheEntry (frozen dataclass) holds value + fetched_at (tz-aware UTC) + expires_at + cadence; exposes age_seconds and is_fresh.

Cache class: SQLite WAL mode + threading.RLock (one writer at a time, unlimited concurrent readers). isolation_level=None (autocommit) with manual BEGIN IMMEDIATE / COMMIT / ROLLBACK for atomic writes. Two tables: cache_entries (JSON key-value; PRIMARY KEY (namespace, key)) and history_cache (gzip-compressed JSON blobs for OHLCV time-series; PRIMARY KEY (symbol, namespace)). Methods: get / set / invalidate / clear(namespace=None). explicit expires_at override: set() accepts an optional expires_at datetime to pin expiry to a known event (e.g., next-earnings date) rather than the cadence TTL.

Incremental history (get_history_incremental): Three-case logic: (1) cold cache → full fetch via fetch_fn(symbol); (2) warm cache within TTL → return cached DataFrame, no network call; (3) expired → delta fetch via fetch_fn(symbol, start="YYYY-MM-DD") starting the day after the last cached bar, merged and de-duplicated on DatetimeIndex. Falls back to full re-fetch via TypeError catch if fetch_fn doesn't accept start. Logs "Fetching history delta from <date> for <symbol> (namespace=<ns>)" at INFO on every delta fetch. tz-aware DatetimeIndex is normalised to tz-naive on write so yfinance (tz-aware) and FRED (tz-naive) sources concat without collision.

get_default_cache() / _inject_cache(): thread-safe module-level singleton (the production instance). _inject_cache(cache) replaces it for test isolation — preferred over monkeypatching.

@cached(namespace, cadence) decorator: transparent cache lookup + store around any fetch function. Accepts force=True kwarg (consumed before forwarding to wrapped function). Logs cache hits at INFO. Attaches ._cache_namespace and ._cache_cadence for Gravity auditing.

Serialisation: custom JSON encoder/decoder supports pd.DataFrame (orient="split"), pd.Series (orient="split"), and datetime (ISO). OHLCV DataFrames stored as gzip-compressed JSON blobs (no pyarrow). Cache keys > 128 chars are SHA-256-hashed. SECRETS MUST NEVER BE PASSED AS CACHE VALUES — callers are responsible; the cache stores no credentials.

Gravity Step 25 (cache system audit): Checks Cache API completeness, all Cadence enum values present in CADENCE_TTL with non-zero TTLs, CADENCE_REGISTRY has all required keys, @cached second call is a hit (network called once), TTL expiry triggers refresh, force=True bypasses cache, and no known secret-pattern strings appear in cache values.

HOLDING-AWARE ADVISORY ENGINE (engine/advisory.py — Stage 6):
New engine/ package providing per-symbol BUY/SELL/HOLD recommendations tailored to actual Robinhood holdings. Public API: evaluate(symbol, position, market, snapshot, macro_dto=None, transactions_store=None) -> Recommendation.

Recommendation (frozen dataclass): symbol, action: Literal["BUY","SELL","HOLD"], strategy: str, conviction: float [0,1], rationale: str, suggested_position_pct: float, forecast: float|None, key_indicators: dict[str,float], data_quality: Literal["OK","STALE","PARTIAL"].

CONFIG dict (module top): 16 named thresholds — score gates (strong_buy=75, buy=55, sell=35), P&L thresholds (gain_hold_bias=10%, loss_sell_threshold=-10%), dividend bias thresholds (yield≥4% or cumulative≥$50), max_single_position_pct=5%, Kelly params (fraction=0.5, cap=0.20), conviction levels (strong_buy=0.85, buy=0.70, hold=0.55, sell=0.65, strong_sell=0.80), bearish_forecast_pct_threshold=-3%. No magic numbers in the decision logic.

Pipeline (13 stages, each wrapped in try/except for dead-letter resilience): (1) quote + OHLCV bars via MarketDataProvider; (2) MarketBarDTO construction; (3) FundamentalDataDTO via market.get_fundamentals(); (4) ProcessingEngine technical indicators; (5) TechnicalOptionsEngine GJR-GARCH vol; (6) ForecastingEngine 30-day blended forecast; (7) neutral MacroEconomicDTO default when not injected (NOT added to partial_flags — intentional fallback, not a data failure); (8) StrategyEngine.evaluate_security() for raw signal + score + Kelly fraction; (9) holding-aware overlay (three mutually-exclusive cases below); (10) fractional-Kelly sizing clamped to max_single_position_pct; (11) one-paragraph plain-English rationale citing top 2-3 drivers; (12) key_indicators dict (NaN for unavailable values, never omitted); (13) data_quality: PARTIAL > STALE > OK.

Holding-aware overlay — three mutually-exclusive cases evaluated only when position is not None and quantity > 0:
• Case A (SELL escalation): unrealized_pl_pct ≤ -10% (effective cost = avg_cost − divs_per_share) AND 30-day forecast is bearish (< -3% change) → override final_action to SELL, raise conviction to max(current, 0.80).
• Case B (DIVIDEND HOLD BIAS RULE): forward_dividend_yield ≥ 4% OR dividends_received ≥ $50, AND base action is BUY/HOLD, AND raw score < buy_score_threshold → override to HOLD. Rationale explicitly cites forward yield, cumulative dividends, and ongoing income compounding. Fires ONLY on weak-signal readings — a genuinely strong BUY (score ≥ 55) is preserved.
• Case C (gain protection): unrealized_pl_pct ≥ 10% AND base action == BUY AND forecast not bearish → override to HOLD. Avoids piling into a winner already past the gain threshold.

Source-of-truth rules (CONSTRAINT #4 — enforced in evaluate()): PortfolioPosition / AccountSnapshot (Robinhood) are the ONLY source for cost_basis, quantity, dividends_received, buying_power, equity. MarketDataProvider is the ONLY source for prices, OHLCV bars, fundamentals, indicators, forecasts. These roles never cross.

Module-level engine imports: ProcessingEngine, ForecastingEngine, TechnicalOptionsEngine, StrategyEngine, TransactionsStore are imported at module level (not lazily inside the function body) so mock.patch("engine.advisory.<ClassName>") resolves correctly in tests.

Tests (tests/test_advisory.py — 16 fully offline tests): All engines patched. Three acceptance criteria tested: AC1 (held + dividends + gain + neutral → HOLD, rationale cites dividends and gain); AC2 (held + below cost + bearish → SELL, elevated conviction); AC3 (non-held + strong bullish + positive Kelly → BUY, suggested_position_pct in (0, 0.05]). Data-quality mechanics tested independently. Uses TransactionsStore(db_url="sqlite:///:memory:") for DB isolation.

.ENV LOADING CONVENTION (CRITICAL — main.py, main_orchestrator.py):
Both entry points IMPORT `from dotenv import load_dotenv as _load_dotenv` at module top but CALL _load_dotenv(override=False) only inside their entry-point function: main.py calls it as the first line of main(); main_orchestrator.py calls it inside async def main(). run_once() deliberately does NOT call it (its docstring says so) — direct callers that bypass main() (make verify, verify.command, ad-hoc `import main; main.run_once()`) call load_dotenv() themselves in their own wrappers first, and both already do. NOT at module top — module-top invocation would pollute the pytest session and break tests/test_settings.py::test_settings_defaults (which asserts ALPACA_API_KEY is None for a clean Settings()). settings.py uses pydantic-settings with env_file=".env", which populates Settings() but does NOT propagate values to os.environ. Modules that read credentials via os.environ.get(...) directly — notably data/robinhood_portfolio.py (RH_USERNAME / RH_PASSWORD / RH_MFA_SECRET) — would otherwise see empty strings even with a fully-populated .env, producing the misleading runtime error: "Required environment variable 'RH_USERNAME' (or 'ROBINHOOD_USERNAME') is missing or empty." override=False so explicit shell exports still win. Regression test: tests/test_env_loading.py AST-walks both entry points to assert a load_dotenv() call exists somewhere in the file (module top OR inside any function body) — any refactor that removes it entirely fails CI immediately. The empty-universe warning in main.run_once() spells out all four remediation paths (RH_* env vars / WATCHLIST env var / watchlist.txt / Sheet2 column A) so the operator can act without spelunking through source.

PIPELINE SMOKE TESTS + VERIFY TOOLING (tests/test_pipeline_smoke.py, Makefile, verify.command):
Three test classes, all fully offline. TestRunOncePipeline exercises main.run_once() at orchestrator level with all six network call sites patched (fetch_account_snapshot, get_provider, _build_macro_dto, _fetch_bars_for_universe, _build_context_extras, advisory_evaluate): tests correct RunResult shape, dead-letter behavior (one rigged failure → 1 error + N-1 recommendations, no unhandled exception), and all-failure still returns a RunResult. TestAdvisoryTailoringRules exercises engine.advisory.evaluate() directly with mocked heavy engines (same mock.patch("engine.advisory.ProcessingEngine") etc. pattern as test_advisory.py, TransactionsStore in-memory): Case B (dividends_received=$75 ≥ $50 + score=45 < 55 → HOLD override on BUY signal), Case A (unrealised_pl_pct=-15% + forecast -8.2% < -3% → SELL override), non-held (BUY + GARCH vol=0.20 → BUY with 0 < pct ≤ 0.05). TestNoOrderFunctions: AST walk over all .py files EXCLUDING execution/, tests/, .venv/, Gravity AI Review Suite.py, ai_verification_prompts.py; fails if any function definition's name matches submit_order, buy_order, sell_order, place_order, place_equity_order, place_option_order, or starts with "place_". Backtrader notify_order callbacks do NOT match these patterns and are correctly NOT caught. Makefile: make verify (env check → pytest → live run_once() + summary), make test, make smoke. verify.command: macOS double-click equivalent of make verify; verifies Python 3.12 in .venv, aborts on pytest failures, prints summarize_run() output.

Gravity Step 27 (advisory audit): Checks module importable, Recommendation dataclass fields and frozen status, evaluate() accepts correct parameters, CONFIG has all 16 required keys, three acceptance criteria end-to-end (AC1/AC2/AC3 with patched engines), data_quality STALE/PARTIAL/OK mechanics, suggested_position_pct bounded by max_single_position_pct on BUY and 0.0 on SELL/HOLD, no magic numbers in logic section (all decision literals match CONFIG values).

DAILY HTML REPORT REDESIGN (2026-06 — diagnostics_and_visuals.py, main.py):
diagnostics_and_visuals.generate_html_report(portfolio_data, regime, output_path, *, yield_curve, credit_spread, sahm_rule, real_yield, audit_log=None, account_summary=None) is the ACTIVE report path called by BOTH main.py and main_orchestrator.py. (reporting_engine.py's daily_report_template.html path is NOT wired into either entry point — only test_fast_render.py uses it.) The embedded HTML_REPORT_TEMPLATE now leads with HOLDINGS & P&L (shares, avg cost, current price, market value, signed unrealized P&L $/%) and ACTION & RATIONALE (action signal with colour class + conviction meter, suggested position %, click-to-expand plain-English rationale). NEW kwarg account_summary: dict renders a top portfolio summary band (total_equity, buying_power, total_unrealized_pl, total_dividends, num_positions, n_buy/n_hold/n_sell tally); when None (main_orchestrator.py path) the band is hidden and rows degrade to "—". Backward-compatible field normalization accepts BOTH spaced pipeline keys ("Action Signal", "Robinhood Unrealized PL") AND underscored advisory keys ("Action_Signal", "UnrealizedPL"); market value / unrealized P&L are DERIVED (shares×price, (price−avgCost)×shares) only when snapshot fields are absent — never fabricated for non-held symbols. Dependency-free vanilla-JS search box + sortable columns (no DataTables/jQuery). main.py._write_html_report() sources holdings/P&L from the Robinhood AccountSnapshot positions (CONSTRAINT #4 source of truth) and builds account_summary defensively. Tests: tests/test_html_report.py (9 offline tests — holdings/rationale render, account_summary band, backward-compat with no band, NaN/Inf sanitisation, derived market value, empty universe, no-secrets guard).

STREAMLIT DASHBOARD IMPROVEMENTS (observability/dashboard.py):
NEW _load_account_snapshot() reads cache/account_snapshot.json (Robinhood holdings + P&L — CONSTRAINT #4 source of truth for account state, never contains secrets). NEW "Account Holdings & P&L" panel: Total Equity / Buying Power / Unrealized P&L / Dividends metrics + per-position table with green/red-coloured unrealized P&L via _style_holdings()/_color_pnl() pandas Styler (Styler.map, currency/percent format dict). NEW sidebar "🔄 Refresh now" button → st.cache_data.clear() + st.rerun() for on-demand refresh without waiting for the TTL. Degrades gracefully to an instructional note when the snapshot is absent. All st.dataframe calls use width='stretch' (use_container_width deprecated, removed after 2025-12-31).


TECHNICAL OPTIONS MATRIX & PREMIUM-SELLING HELPER (technical_options_engine.py):
NEW top-level helper build_premium_directive(symbol, bars, *, spot_price, is_stale=False, target_dte=30, macro_dto=None, vrp=None, risk_free_rate=settings.RISK_FREE_RATE) -> dict fuses GJR-GARCH σ, realized-vol IVR proxy, Aroon+Coppock trend bias (deterministic _determine_trend_bias sign-agreement rule), full ATM Black-Scholes Greeks, and the deterministic strategy directive from OptionsPricingRecommender.generate_strategy_pricing_matrix into a single hydrated row used by BOTH the GUI Technical Options Matrix tab AND Gravity STEP 38. Output keys: Symbol, Price, Stale, Sigma_GARCH, IVR_Proxy, Aroon_Oscillator, Coppock_Curve, Trend_Bias, Strategy, Action, Net_Premium, Realizable_Daily_Theta, ATM_Delta/Gamma/Vega/Theta_Daily, Short_/Long_Strike, Short_/Long_Delta, Legs, Integrity_OK, Integrity_Issues. NaN — never fabricated zeros — for fields that cannot be computed (CONSTRAINT #4). NEW companion validator validate_directive_integrity(directive, *, delta_tolerance=0.05, strike_grid=STRIKE_GRID_USD=0.50) -> {"ok": bool, "issues": [str], "checks": [dict]} asserts every leg strike lies on the $0.50 USD exchange grid (via _on_strike_grid) AND (where the leg dict carries a resolved Delta) the BS delta is within delta_tolerance of the conventional EXPECTED_DELTA_TARGETS entry — Put Credit Spread (-0.30/-0.15), Call Credit Spread (+0.30/+0.15), Iron Condor (±0.16/±0.05), Debit Spreads (±0.50/±0.30), Covered Call (+0.30). Cash/Wait directives pass trivially; Iron Condor legs intentionally omit Delta so the validator SKIPS (never fabricates) the delta check on those legs. NEW Gravity step_38_options_matrix_integrity_audit (7 checks: full schema hydration, $0.50 strike-grid compliance on high-IVR bullish Put Credit Spread, ±0.05 delta tolerance against -0.30/-0.15 targets, validator rejects off-grid 95.37 strike but accepts engine output, VIX>30 degrades to Cash/Wait, CREDIT EVENT regime degrades to Cash/Wait, low-IVR + bullish routes to Call Debit Spread not Cash). NEW tests: tests/test_options_matrix.py (13 tests). gui/panels.py.render_options_matrix now auto-iterates the union of held positions ∪ watchlist ∪ last signals (via NEW gui.panels._active_symbols, which composes NEW _held_symbols read-only from cache/account_snapshot.json + NEW _watchlist_symbols matching main._load_watchlist semantics + the existing _signal_symbols), per-symbol dead-letter resilient, with a progress bar and a flagged-rows expander.


REPORTS TAB — BRINSON-FACHLER ATTRIBUTION (gui/panels.py, 2026-06):
The Reports tab now exposes a full interactive Brinson-Fachler section, replacing the prior placeholder expander. Five pure helpers (module-level so they are testable without Streamlit) form the boundary to EvaluationEngine.calculate_brinson_fachler: default_brinson_fachler_frame() seeds a five-column editor (Sector, Portfolio Weight (%), Portfolio Return (%), Benchmark Weight (%), Benchmark Return (%)) with the GICS-11 sector list (GICS_SECTORS constant); parse_pasted_sector_matrix(text) auto-detects TSV vs CSV from the first line and supports both header-bearing and header-less 5-column blocks — header-less is sniffed by trying to float() columns 2-5 of line 1 (without this, pd.read_csv would promote a data row to the header and silently drop a sector); build_brinson_fachler_inputs(editor_df) splits the editor into the (portfolio_df, benchmark_df) compat shape AND divides every numeric column by 100 (unit-consistency invariant — editor stores percents, engine multiplies weight×return directly; regression here silently inflates the result by 100×); validate_brinson_fachler_weights(editor_df, tolerance_pct=1.0) warns if portfolio or benchmark weights sum outside 100%±1% or contain negatives (long-only attribution convention); compute_brinson_fachler(editor_df) returns the engine's canonical 8-key dict (Portfolio Return, Benchmark Return, Active Return, Allocation Effect, Selection Effect, Interaction Effect, Attribution Sum, Sector Details). The Sector Details map uses the per-row schema weight_p/weight_b/return_p/return_b/allocation_effect/selection_effect/interaction_effect/total_attribution. UI state persists under st.session_state["bf_editor_df"] and st.session_state["bf_result"] so tab swaps don't lose work; per-sector breakdown + editor-input CSV downloads are wired. NEW tests: tests/test_brinson_fachler_ui.py (11 tests — paste parser branches, percent→fraction conversion, validation, end-to-end attribution-sum ≈ active-return identity). NEW Gravity step_40_brinson_fachler_attribution_audit (5 checks) pins the same wiring in production.


LAUNCHER TAB — DUAL ENTRY POINTS + TELEMETRY (gui/panels.py + gui/orchestrator_runner.py, 2026-06):
The Launcher tab now exposes TWO launch buttons: ▶️ Launch Pipeline → orchestrator_runner.launch_orchestrator → python main_orchestrator.py (async, broker, HTML report), AND 🔄 Refresh Data (Advisory) → NEW orchestrator_runner.launch_advisory_main → python main.py (synchronous advisory loop, broker-free; canonical .env-loading entry point per project convention). RunHandle now carries mode (orchestrator/advisory) and an explicit log_path so callers know which stream to tail; compute_stage_status() follows handle.log_path rather than a hard-coded constant. NEW log files: output/gui_run.log (orchestrator) and output/gui_advisory.log (advisory) — kept distinct so orchestrator stage-marker scans don't false-positive on advisory output. NEW orchestrator_runner.validate_required_env(required=REQUIRED_ENV_VARS) -> dict[str, bool] runs on every render and surfaces missing variables (default ("FRED_API_KEY",)) as an inline st.error BEFORE the buttons are clicked — fixes the "Refresh Data does not produce observable results" failure mode where the subprocess silently degraded to neutral defaults. NEW orchestrator_runner.read_telemetry_tail(max_lines=120) reads logs/investyo.log (written by alerting.setup_logging(), rotated 10 MB × 5 backups) into a separate expander, giving the operator a single entry-point-agnostic structured-logging stream. NEW opt-in "Auto-refresh while running" checkbox (launcher_auto_refresh) — sleeps 5 s then calls st.rerun() while a run is active. Finished-run banner distinguishes exit 0 (✅) from any non-zero code (❌) and labels the mode. NEW tests: tests/test_orchestrator_runner.py (12 tests — env-validation truth table, log-routing contract, telemetry idle hint, advisory/orchestrator mode tagging, distinct log paths). NEW Gravity step_41_launcher_telemetry_audit pins the same wiring in production.


SAFETY / ANALYTICS / CONTROL TABS — CIRCUIT BREAKERS, DEPENDENCY MAP, VERSION REGISTRY, MODE TOGGLE, DRILL-DOWN (gui/circuit_breakers.py + gui/dependency_map.py + gui/strategy_registry.py + gui/panels.py, 2026-06):
THREE NEW HELPER MODULES (no Streamlit imports, headlessly testable) plus updates to render_gravity_audit / render_strategy_matrix / render_report_viewer.

(1) gui/circuit_breakers.py — derivation layer over output/KILL_SWITCH + output/risk_gate_blocks.jsonl. Public API: read_block_log(path, max_lines=500) tolerant JSONL reader (corrupt lines dropped + logged at DEBUG, never raised); derive_kill_switch_trip(path, reason=None) emits CRITICAL CircuitBreakerTrip when sentinel exists (mtime → triggered_at; sentinel text → detail); derive_block_log_trips(blocks, *, window=24h, now=…) projects risk-gate blocks into typed trips, keeps newest per (check_name, strategy_id), drops outside-window; unknown check_name values BUBBLE THROUGH tagged WARNING (signal to add a row to _KNOWN_CHECKS); collect_circuit_breaker_trips(...) top-level helper (kill switch first, then newest-first block trips); summarise_trips(trips) → {CRITICAL, WARNING, TOTAL}. ARCHITECTURAL RULE: adding a new breaker means adding a check inside execution/risk_gate.py and tagging the emitted block — the panel auto-picks it up. NEVER re-implement risk logic in circuit_breakers.py.

(2) gui/dependency_map.py — declarative DataSource (enum) → Consumer graph. CONSUMERS dict is HAND-CURATED by design: inferring from imports would over-couple to call sites that gate sources on config flags. Public helpers: impacted_consumers(degraded) → list[ImpactRecord] (string inputs not matching a known DataSource resolve to DataSource.UNKNOWN with EMPTY impact — never fabricated, CONSTRAINT #5); all_consumers() (deduped by name); render_edges() flat (source, consumer, kind) tuples. EXTENSION RULE: new consumer of existing source → append to the right _*_CONSUMERS tuple; new source → add DataSource enum value + _LABELS entry + CONSUMERS row; carry every map change through to Gravity step 44.

(3) gui/strategy_registry.py — strategy file versioning + global Simulation/Paper/Live mode toggle. StrategyVersion(name, file_path, version_hash, last_modified, enabled, weight): version_hash = sha256(file)[:12] hex, NONE when file missing/unreadable (CONSTRAINT #5). list_strategy_versions(*, module_names=None, weights=None, disabled=None, signals_dir=None) joins live signals.registry.global_registry with settings.SIGNAL_WEIGHTS / settings.DISABLED_SIGNAL_MODULES; all four kwargs injectable for tests. "Version" means *was the file touched since last run*, NOT semver. ExecutionMode enum (SIMULATION/PAPER/LIVE): read_active_mode() → ModeState resolves from settings.DRY_RUN winning over settings.ALPACA_PAPER (OrderManager intercepts regardless of ALPACA_PAPER when DRY_RUN=true). set_active_mode(mode) writes BOTH DRY_RUN AND ALPACA_PAPER ATOMICALLY (no half-flips) via the allowlist-bounded gui/env_io.write_setting. Effect on NEXT launch only — never patches a running settings.Settings. NEW gui/env_io.py ALLOWED_KEYS entry: ALPACA_PAPER (Alpaca *secret* keys remain in SECRET_KEYS, write-protected).

gui/panels.py wiring:
- render_gravity_audit is now the SAFETY tab: _render_circuit_breaker_dashboard + _render_dependency_map ABOVE the existing Gravity audit launcher; KPI strip (CRITICAL/WARNING/Total trips), table with severity icon + threshold + observed + raw payload expander, multi-select degraded-source picker projecting impacted consumers + a "full graph" expander.
- render_strategy_matrix gains _render_strategy_mode_toggle (Simulation/Paper/Live radio with "🔴 CONFIRM LIVE PRODUCTION" button when LIVE is chosen — atomic two-flag write via env_io) and _render_strategy_version_registry (table of module / enabled / weight / version_hash / mtime / source-file path).
- render_report_viewer shows _render_report_provenance_banner: blue "🔵 Live data" st.info when an orchestrator snapshot exists AND mode ∈ {PAPER, LIVE}; grey "⚪ Backtested / simulated" Markdown blockquote otherwise. New "🔬 Drill down by symbol" expander surfaces the full signal row + recent closed trades from transactions_store.TransactionsStore (CONSTRAINT #7 — integrate, don't reinvent).

NEW tests (34 total): tests/test_circuit_breakers.py (12), tests/test_strategy_registry.py (12), tests/test_dependency_map.py (10). Coverage: corrupt-line tolerance, kill switch with reason, known-vs-unknown check classification, window + per-(name, strategy) dedup; strategy-version happy + missing-file + hash-changes-on-edit; mode truth table + invalid rejection + two-flag atomicity; map registry sanity + UNKNOWN no-fabrication + edge-count symmetry. NEW Gravity step_44_safety_analytics_control_audit pins all of the above. NEW docs HOW_TO_GUIDE.md and RUNBOOK.md sections on the Safety tab + global mode toggle.


OBSERVABILITY TAB — SYSTEM TELEMETRY + LATENCY HEATMAP + ERROR LOG (gui/observability_telemetry.py + gui/panels.py, 2026-06):
The Observability ("Health") tab now exposes three new sections after Strategy P&L, all backed by NEW module gui/observability_telemetry.py (no Streamlit imports, headlessly testable). (1) System Telemetry: collect_system_telemetry(disk_path='/') -> SystemTelemetry — psutil-backed CPU%, logical-core count, 1-min load avg, memory% + bytes, disk% + bytes, and per-process RSS / CPU% / thread count. psutil unavailability OR sampling failure returns NaN floats + -1 byte counts + psutil_available=False (CONSTRAINT #4 — never zero-fabricated). cpu_percent uses interval=None (non-blocking) so first paint is meaningless and auto-refresh is required. format_bytes(n) renders B/KiB/MiB/GiB/TiB (n<0 → "—"). Red banners at CPU≥90% / mem≥90%, yellow at CPU≥75%. (2) Data Latency Heatmap: LatencySampleStore(max_samples=500) is a bounded ring buffer of LatencySample(symbol, source, quote_timestamp, ingested_at, latency_seconds, is_stale). record(...) computes (ingested - quote_ts).total_seconds() — both naive timestamps are promoted to UTC. CROSS-TAB WIRING (must not regress): the store lives in st.session_state["obs_latency_store"] and is populated by render_market_data on every successful fetch, so one click on Market Data → Fetch quotes feeds the Observability heatmap. summarise_latency(samples) -> {count, p50, p95, worst_symbol, worst_p95}. Heatmap rendering uses pandas Styler.background_gradient(cmap='RdYlGn_r') over Latency (s) with a plain-table fallback. (3) Error Aggregation: parse_log_lines(lines) parses alerting.setup_logging()'s formatter "%(asctime)s  %(levelname)-8s  %(name)s — %(message)s" (comma OR dot millis). Unparseable lines (multi-line traceback continuations) are KEPT as LogEntry(level="", parsed=False, raw=…) so context survives. filter_log_entries(entries, *, min_level='INFO', contains=None) filters ordinally over VALID_LEVELS = ('DEBUG','INFO','WARNING','ERROR','CRITICAL') AND a case-insensitive substring; unparsed lines are KEPT regardless of level. tally_levels(entries) drives the KPI metrics row (CRITICAL/ERROR/WARNING/INFO + UNPARSED bucket). read_log_tail(path, max_lines=500) is the IO (missing → [], never raises). The panel reads gui.orchestrator_runner.TELEMETRY_LOG_PATH = logs/investyo.log, renders the level dropdown defaulting to INFO + substring filter, and caps the st.code body at 300 most-recent matching lines so a runaway run can't freeze the browser. NEW tests: tests/test_observability_telemetry.py (26 tests — psutil ImportError → NaN shape, format_bytes ladder, latency record + roll-off + summary, log parser + filter + tally, missing-file → []). NEW Gravity step_43_observability_telemetry_audit pins the three-surface contract.


TIER 1 / 1.2 — CONVICTION CALIBRATION TRACKER (evaluation_engine.calibration_curve, transactions_store.TransactionsStore, gui/panels._render_calibration_section, 2026-06):
New function evaluation_engine.calibration_curve(transactions_store, n_bins=10, min_trades_per_bin=5) -> pd.DataFrame: bins closed trades by their recorded conviction score and computes the empirical win rate per bin — answering "when the system says 0.80, does it actually win 80%?". Win logic is side-aware: long → exit_price > entry_price; short → exit_price < entry_price (equality = loss). Uses pd.cut(bins=np.linspace(0, 1, n_bins+1), include_lowest=True). Columns: bin_low, bin_high, bin_center, conviction_mean, win_rate (NaN for bins below min_trades_per_bin), count, perfect_calibration. SCHEMA MIGRATION: TransactionsStore._ensure_conviction_column() uses SQLAlchemy inspect() to detect the column before issuing ALTER TABLE ADD COLUMN conviction REAL — handles both fresh installs and legacy DBs with pre-existing seeded trades. TransactionsStore.record_trade() now accepts conviction: Optional[float] = None; persisted as a REAL nullable column. Dead-letter contract (CONSTRAINT #4 + #6): conviction column absent → empty DataFrame with correct schema; all-null conviction → empty; store read failure → empty; any exception → empty + logged at DEBUG. GUI: _render_calibration_section() in gui/panels.render_report_viewer() (after Brinson-Fachler); renders the reliability diagram via matplotlib.pyplot embedded with st.pyplot(). Diagonal "perfect calibration" reference line shown. If no conviction-annotated trades exist (default state for the 169 seeded trades which have no conviction field), shows an informative "no data yet" placeholder. Module-level constants _CALIBRATION_COLUMNS and _empty_calibration_df() form the schema contract so tests can import them without running calibration. NEW tests: tests/test_calibration.py (24 tests, 5 classes — TestSchema: empty store, zero rows, no conviction fallback, all-null, read failure, count int dtype; TestWinRateLogic: long win/loss, short win/loss, mixed 60%, exit==entry not a win; TestBinning: n_bins param, bin bounds, bin_center midpoint, perfect_calibration = bin_center, trades land in correct bins; TestMinTradesGate: below threshold NaN, at threshold not NaN, empty bin conviction_mean NaN; TestRecordTradeConviction: kwarg accepted, persisted, None→null, column in open trades). NEW Gravity step_52_calibration_audit pins 10 checks.

TIER 1 / 1.3 — MANUAL EXECUTION DECISION JOURNAL (gui/decision_log.py + gui/panels._render_decision_journal_section, 2026-06):
NEW module gui/decision_log.py (no streamlit imports — headlessly testable). Public API: DecisionEntry (frozen dataclass: symbol, action_taken ["acted"|"passed"|"modified"], signal_action, conviction, notes, timestamp, signal_ts, trade_id); ActionTaken Literal type; DEFAULT_LOG_PATH = Path("output/decision_log.jsonl"); append_decision(entry, log_path) — atomic JSONL line-append, creates parent dirs; read_decisions(log_path) — tolerant reader, corrupt/blank lines logged at DEBUG and skipped, remaining entries always returned (CONSTRAINT #6); decisions_df(log_path) — typed DataFrame with Int64 nullable trade_id, empty schema when log absent (CONSTRAINT #4); join_to_store(entry, transactions_store, window_hours=24.0) — finds closest matching trade by symbol within ±window_hours of entry.timestamp; returns None on no-match or any failure (never fabricates — CONSTRAINT #4); log_decision(..., now_fn=None) — orchestrates: normalize symbol to uppercase, build entry, optionally join (only when action_taken="acted"), append. JOIN CONVENTION (must never regress): join_to_store called ONLY for "acted" — "passed" and "modified" never set trade_id. Symbol matching is case-insensitive. GUI: _render_decision_journal_section(signals) inserted in render_report_viewer() BEFORE the Brinson-Fachler section so the operator sees signal → logs decision → sees attribution → calibration (UX: most intuitive flow). Presents: symbol selectbox from signal list, signal-context KPI strip (system action, conviction), notes textarea, three buttons (✅ Acted / ⏭ Passed / 🔁 Modified — Modified warns if notes blank), success banner citing trade match or "no match within 24h", past-decisions collapsible log with CSV export. State persisted in st.session_state["dj_last_result"] across rerenders. NEW tests: tests/test_decision_log.py (30 tests, 5 classes — TestDecisionEntry: frozen, fields, action_taken values; TestAppendAndRead: round-trip, multiple entries order, missing file, corrupt/blank skip, trade_id + None-conviction round-trips, parent-dir creation; TestDecisionsDf: empty schema, Int64 dtype, nullable NaN, row count; TestJoinToStore: within window, outside window, symbol not found, picks closest, store failure, case-insensitive; TestLogDecision: field wiring, append, passed/modified skip join, acted joins, acted no match → None, None conviction). NEW Gravity step_53_decision_log_audit pins 10 checks: import, frozen + fields, round-trip, empty schema, corrupt line skip, join within window, join None outside, passed skips join, acted joins, test file exists.

TIER 1 DECISION SUPPORT — "Δ SINCE LAST RUN" SNAPSHOT DIFF (scripts/snapshot_diff.py + diagnostics_and_visuals.py, 2026-06):
NEW module scripts/snapshot_diff.py is the single source of truth for snapshot ROTATION and the "what changed since yesterday" DIFF that the daily HTML report now leads with. Public surface: SnapshotDiff (frozen dataclass: prev_ts, curr_ts, regime_change, new_buys, action_flips, conviction_deltas, added_holdings, dropped_holdings, notes, .is_empty, .to_dict()), load_snapshot, list_rotated_snapshots, rotate_snapshot(snapshot, output_dir, *, max_age_days, now=None), compute_diff(prev, curr, *, conviction_delta_threshold), compute_diff_from_history(output_dir, *, conviction_delta_threshold), format_markdown. CLI: `python -m scripts.snapshot_diff prev.json curr.json [--format markdown|json] [--conviction-threshold 0.2]`; with no positional args defaults to the two most-recent rotated snapshots under --output-dir. TOLERANCE CONTRACT (CONSTRAINT #4 + #6): every load/diff/rotate path is wrapped — missing file → None, corrupt JSON → None, non-object JSON → None, write/prune failures → logged at WARNING/DEBUG and skipped; the diff engine NEVER raises so the daily HTML report renders even with a degraded history dir. First-run case (prev is None): all current BUYs become new_buys, all current holdings become added_holdings, regime_change stays None. CLASSIFICATION RULES (pinned by tests/test_snapshot_diff.py + Gravity step 51): New BUY = (no prior signal OR prior action did not contain "BUY") AND current action contains "BUY" — takes precedence over action_flips (a HOLD→BUY is reported once, as a new_buy). Action flip = both sides present, both non-empty, different, AND not already in new_buys. Conviction delta = advisory_conviction (fallback conviction) present on both sides AND |after − before| ≥ conviction_delta_threshold (default 0.2). Holdings added/dropped = set diff over snapshot's holdings list (or backfilled from signals[].shares > 0). Regime change = prev.market_regime and curr.market_regime both non-empty AND different. ROTATION: rotate_snapshot writes output/history/state_snapshot_<UTC>.json via atomic write-then-rename; filename encodes the snapshot's own ISO timestamp (else wall-clock UTC), formatted state_snapshot_YYYYMMDDTHHMMSSZ.json (colon-free, FAT/NTFS-safe); files older than max_age_days are pruned in the same call; max_age_days=0 disables pruning; non-matching filenames in history/ are ignored. WIRING: main_orchestrator._write_state_snapshot() now writes a holdings: list[str] field (sorted Shares>0 symbols) and calls rotate_snapshot — moved BEFORE the HTML report renders so the Δ-band diff sees curr=this-run / prev=previous-run. The HTML-report block then calls compute_diff_from_history() and passes snapshot_diff_payload to generate_html_report(snapshot_diff=...). main._write_state_snapshot(result, macro_dto) is a NEW helper in the advisory entry point that emits the SAME JSON schema (timestamp, holdings, signals, market_regime, vix, kill_switch_active, macro_regime_gate_enabled) so the diff engine sees a consistent shape across both entry points; called from _write_html_report() before HTML render, _load_snapshot_diff_for_report() returns the .to_dict() payload (or None on first run / any failure). diagnostics_and_visuals.generate_html_report(..., snapshot_diff=None) adds a new keyword-only kwarg — when non-None, the template renders a top-of-report "Δ Since Last Run" band (CSS class .delta-band) above the macro/regime cards with grid cells for new_buys, action_flips, conviction_deltas, added_holdings, dropped_holdings and a banner for regime_change; when None the band is hidden entirely. NEW SETTINGS: SNAPSHOT_HISTORY_DAYS (int, default 30 — pruning window), SNAPSHOT_CONVICTION_DELTA_THRESHOLD (float, default 0.2 — material-movement floor). NEW tests: tests/test_snapshot_diff.py (24 tests: TestLoadSnapshot missing/empty/corrupt/non-object/valid round-trip; TestRotation rotation+filename encoding+prune+ignore-unrelated; TestComputeDiff first-run-buys+identical-empty+action-flip+new-buy-precedence+0.21-surfaces-0.19-suppressed+regime-change+holdings±+backfill-from-shares; TestHistoryIntegration two-rotations-real-diff+single-rotation-first-run+no-history-empty-with-note; TestModuleSurface default-threshold-0.2 + JSON-serialisable to_dict). NEW Gravity step_51_snapshot_diff_audit pins 10 invariants: module surface, default threshold = 0.2, settings defaults, generate_html_report kwarg, both entry-point wirings (AST scan), rotate_snapshot round-trip, first-run BUY classification, corrupt-file → None tolerance, tests file exists.


MARKET DATA TAB — DIAGNOSTICS, THROTTLING, CONNECTIVITY HEALTH (gui/market_data_diagnostics.py + gui/panels.py, 2026-06):
The Market Data Provider tab previously surfaced provider failures as opaque "None" cells. NEW module gui/market_data_diagnostics.py introduces four pure helpers (no Streamlit imports, unit-testable headlessly): (1) classify_market_error(exc) -> ErrorCategory maps a raised exception to one of RATE_LIMIT / NOT_FOUND / NETWORK_TIMEOUT / MALFORMED / UNKNOWN by walking the __cause__/__context__ chain AND inspecting getattr(exc, "status_code", None) — so a MarketDataError wrapping a TimeoutError still resolves to NETWORK_TIMEOUT instead of falling through to UNKNOWN, and a FinnhubAPIException with status_code=429 resolves to RATE_LIMIT even when its str() is empty; category_label(cat) returns the operator-facing string ("API Rate Limited", "Symbol Not Found", etc.). (2) validate_quote(quote) -> QuoteValidation flags NaN/non-positive price, missing timestamp, and inverted bid/ask BEFORE the row is allowed into the rest of the pipeline (CONSTRAINT #4 — no fabricated metrics) — asymmetric tolerance: a missing-only-bid OR missing-only-ask is NOT flagged (legitimate outside RTH), but a missing PRICE always flags. (3) FetchHealthTracker (sliding window default 20, healthy ≥ 0.9, degraded ≥ 0.5; otherwise DOWN) — empty state is HEALTHY-neutral so first paint isn't a red scare; HealthReport.badge() returns the emoji+label string for the GUI. (4) BatchQuoteFetcher(fetch_fn, spacing_seconds=0.1, health_tracker=None, sleep_fn=time.sleep).iter_fetch(symbols) yields a BatchResult(index, symbol, quote, validation, error, category) per symbol so the Streamlit panel can stream progress-bar updates; spacing is enforced via a rolling _last_call_ts shared across back-to-back batches; sleep_fn is pluggable for tests. summarise_categories(results) rolls results up into the {"ok": …, "rate_limit": …} toast/caption summary. The rewritten render_market_data() in gui/panels.py displays four KPI columns (Provider, Mode, Quote TTL, Connection badge), a persistent yfinance-delayed info banner when Mode is delayed, a throttle slider (0–1000 ms, default 100 ms), a streaming progress bar (i/N — SYMBOL), a per-symbol Status column ("OK" / "⚠ price missing/NaN") and Error column ("API Rate Limited: 429 …"), and two reset buttons (provider singleton + connection health). State persists across reruns under st.session_state["md_health_tracker"] (FetchHealthTracker) and st.session_state["md_last_results"] (last table). NEW tests: tests/test_market_data_diagnostics.py (33 tests — error-category matrix, quote validation invariants, sliding-window thresholds + roll-off, throttle spacing observed via injected sleep_fn, summarise_categories tally). NEW Gravity step_42_market_data_diagnostics_audit pins the four-surface contract in production.


TIER 5.1 — ADVISORY_ONLY MODE QUARANTINE (settings.py, main_orchestrator.py, gui/app.py, gui/panels.py, gui/strategy_registry.py, scripts/preflight_check.py, 2026-06):
settings.ADVISORY_ONLY: bool = True is the new project-wide quarantine flag that quarantines the entire broker-execution surface. Three-layer enforcement: (1) ORCHESTRATOR BOUNDARY: main_orchestrator._execute_broker_orders() returns immediately with an INFO log when settings.ADVISORY_ONLY — no AlpacaBroker import reached, no OrderManager constructed, no network contact. (2) GUI: gui/app.py renders a persistent "📋 ADVISORY MODE" banner in place of the Simulation/Paper/Live badge when ADVISORY_ONLY=true; gui/panels.py render_strategy_matrix() suppresses the Global Execution Mode radio selector (replaced by a read-only caption showing underlying flags) so an operator cannot accidentally switch to Paper/Live while in advisory mode. (3) PREFLIGHT: scripts/preflight_check.py defines _ADVISORY_AUTO_SKIP: dict[str, str] with EIGHT entries (Stage 2+3, 2026-06-26 cleanup): four broker-stack checks (alpaca_configured, alpaca_paper_mode, dry_run_disabled, paper_trading_duration), ONE key-rotation check (alpaca_key_rotation_recent — Alpaca keys have no blast-radius risk while quarantined; Stage 3 addition), PLUS three runtime-state false-positive checks (heartbeat_fresh — heartbeat written only by main_orchestrator.py, not main.py; validation_reports — gates live order submission, irrelevant when no orders are submitted; no_unexpected_risk_blocks — risk-gate blocks only occur on order submission); when ADVISORY_ONLY=True each skipped check gets a distinct per-check reason string (not a generic message); a new advisory_only_active check is always run first (PASS-loud when true, PASS-with-warning when false). NEW TEST: tests/test_advisory_only.py (38 tests, 6 classes): TestSettingsAdvisoryOnly (default=True, env override, type=bool); TestOrchestratorBoundary (broker method early-return when ADVISORY_ONLY, broker never called, return type matches, live-mode path unchanged when False); TestGUIPanels (mode-toggle suppressed, advisory-mode caption present, app.py references ADVISORY_ONLY banner); TestPreflightAutoSkip (all 8 checks auto-skip, advisory_only_active=True PASS, advisory_only_active=False is not blocking, main() exits 0); TestAdvisoryPauseGate (kill switch + ADVISORY_ONLY coexist, pause-then-resume flow); TestGravityStepReference (step_54 present in Gravity Suite source). NEW Gravity step_67_key_rotation_audit (10 checks — see Stage 3 section below). tests/test_preflight.py gains TestAdvisoryModeAutoSkip (6 tests: heartbeat_fresh auto-skipped when advisory=True; validation_reports auto-skipped when advisory=True; heartbeat_fresh FAILs when advisory=False + file missing; validation_reports FAILs when advisory=False + no reports dir; per-check reasons are distinct; original four broker checks still skipped). NEW Gravity step_54_advisory_only_audit (9 checks): settings.ADVISORY_ONLY default True, orchestrator short-circuit, GUI banner reference, GUI mode-toggle suppression, advisory_only_active check exists, _ADVISORY_AUTO_SKIP is dict with eight entries (Stage 3 added alpaca_key_rotation_recent as the 5th broker-dependent check), functional skip path under True (all eight), advisory_only_active warns under False, test file exists, heartbeat_fresh reason references main_orchestrator.py, validation_reports reason references "order submission". NO new env vars beyond ADVISORY_ONLY. NO new Python dependencies. docs/HOW_TO_GUIDE.md: §11 advisory-mode caveat, §13 preflight table updated with auto-skip column, §15 kill-switch behaviour in advisory mode, new Advisory-Only Mode section. docs/RUNBOOK.md: fully rewritten (Tier 5.2) for advisory-platform operation — §3.1–3.3 are now advisory-relevant incident playbooks (stale account snapshot, missing recommendation for held symbol, calibration score below threshold); §1 paper-to-live procedure and §6 emergency shutdown are retained with ⚠ N/A markers; §6 replaced with Advisory Pause-and-Restart procedure using the kill-switch sentinel as a pause-recommendations gate. No Gravity step for docs-only RUNBOOK rewrite (Tier 5.2).

TIER 5.3 — KILL SWITCH AS "PAUSE RECOMMENDATIONS" GATE + MACRO-TRIGGERED ADVISORY GATING (main.py, main_orchestrator.py, engine/advisory.py, 2026-06):
Two independent mechanisms added. (A) FILE-BASED SENTINEL PROTOCOL: main.run_once() checks GlobalKillSwitch().is_active() after Stage B (universe build) but before Stage C (macro compute) — when the sentinel exists, logs "Advisory paused by kill-switch sentinel — skipping evaluation cycle" with the reason text, and returns an early RunResult(recommendations=[], errors=[{stage="kill_switch_gate", ...}]). main_orchestrator._main_body() applies the same check after data fetch and before run_pipeline() — returns immediately so the last state_snapshot.json is untouched and the GUI shows the last known state. CLI operator flow: `python -m execution.kill_switch --activate --reason "..."` → next run pauses; `python -m execution.kill_switch --deactivate` → resumes. (B) MACRO-TRIGGERED ADVISORY GATING (engine/advisory.py): New Step 8b block BEFORE the holding-aware overlay. Three tiers — HARD GATE: market_regime in ("RECESSION","CREDIT EVENT") → any BUY/STRONG BUY downgraded to HOLD with adjusted_score capped below buy_score_threshold; SOFT GATE: vix_value > 30.0 OR sahm_rule_indicator ≥ 0.5 → adjusted_score = max(0, score - 25) used for all subsequent threshold comparisons including holding-overlay Case B; SECTOR VETO: Financials/Financial Services/Real Estate sector AND (yield_curve_10y_2y < 0 OR high_yield_oas > 6) → BUY → HOLD. macro_gate_reason string assembled from whichever conditions fired is passed to _build_rationale() as new kwarg (default "") and prepended as "Driver 0" in the rationale. Six new CONFIG keys: macro_vix_gate_threshold=30.0, macro_sahm_gate_threshold=0.5, macro_score_penalty=25, macro_veto_sectors=["Financials","Financial Services","Real Estate"], macro_veto_yield_curve_threshold=0.0, macro_veto_oas_threshold=6.0. NEW TEST: tests/test_advisory_pause_gate.py (3 classes, ≈22 tests): TestKillSwitchPauseGate (active sentinel → empty recs, pause reason in errors, inactive → pipeline runs), TestOrchestratorKillSwitchGate (pipeline skipped, source-grep), TestMacroTriggeredGating (RECESSION/CREDIT EVENT/RISK ON/VIX/Sahm/sector veto Finance/Real Estate/non-vetoed Tech/gate reason in rationale/clean run no noise), TestMacroGateConfig (six keys, types, defaults, sector membership). Gravity step_55_advisory_pause_gate_audit (10 checks: CONFIG keys, defaults, sector veto list, Step 8b + macro_gate_reason in source, main.py pause strings, main_orchestrator.py pause string, test file, _build_rationale signature, functional RECESSION→HOLD). NO new env vars. NO new Python dependencies.

TIER 1.4 — SYMBOL WATCH WITH THRESHOLD ALERTS (watch_engine.py, watch_rules.yaml, main.py, settings.py, 2026-06):
NEW module watch_engine.py (headlessly testable, no Streamlit imports). Fills the critical intraday visibility gap: evaluates watch_rules.yaml rule definitions against advisory pipeline output at the end of every run_once() cycle, then dispatches ntfy push notifications for matched rules via alerting.notify(). Public API: WatchRule (frozen dataclass: symbol, alert_on ["action_change"|"conviction_above"|"conviction_below"], threshold [0.0–1.0], priority ["high"|"default"|"low"], label); WatchAlert (frozen dataclass: symbol, rule_type, priority, title, message, trigger_detail); SymbolWatchState (mutable dataclass: action, conviction, alerted_conviction_above: Dict[str,bool], alerted_conviction_below: Dict[str,bool], timestamp — .to_dict()/.from_dict() for JSON persistence); load_watch_rules(path) → list[WatchRule] (PyYAML yaml.safe_load; returns [] on missing/malformed, never raises; validates alert_on, threshold, priority; skips invalid rules at WARNING); load_watch_state(path) → dict[str,SymbolWatchState] (reads output/watch_state.json; returns {} on missing/corrupt; never raises — CONSTRAINT #6); save_watch_state(state, path) → None (atomic write-then-rename, swallows failures — CONSTRAINT #6); evaluate_watch_rules(rules, recommendations, prev_state) → (list[WatchAlert], dict[str,SymbolWatchState]) (PURE COMPARISON LOGIC — never calls any market-data provider, forecaster, or model; no-lookahead invariant verified by Gravity step 56 via monkeypatching get_provider); dispatch_watch_alerts(alerts, *, dashboard_url=None) → None (inline `from alerting import notify` per call so mock.patch("alerting.notify") resolves correctly; per-alert try/except; silent when NTFY_TOPIC unset). ALERT TYPES: action_change fires once per action flip (HOLD→BUY, BUY→SELL, etc.) — never fires on first run (no prior action); conviction_above is EDGE-TRIGGERED: fires on first run where conviction ≥ threshold, silent while condition persists, resets when conviction drops below threshold; conviction_below is the mirror edge-trigger for falling conviction. Edge suppression is keyed by str(threshold) in alerted_conviction_above/below dicts — consistent across write and read to avoid float serialisation mismatch. WILDCARD: symbol "*" matches ALL symbols in the current recommendation universe; specific symbol rules are checked independently. STATE FILE: output/watch_state.json — per-symbol JSON record written atomically every run (including quiet runs). Missing file = first run (empty state). Symbols leaving the universe are dropped from state so stale state cannot produce phantom alerts on re-entry. INTEGRATION: added inside main.run_once() immediately after the advisory evaluation loop completes (before Sheet/HTML sinks), wrapped in outer try/except (CONSTRAINT #6). WATCH_RULES_FILE: default config at project root with two active rules: (1) universe-wide conviction siren ("*", conviction_above, threshold=0.85, high priority); (2) universe-wide action-change tracker ("*", action_change, default priority). SETTINGS: settings.WATCH_RULES_FILE: str = "watch_rules.yaml". NEW ENV VARS: WATCH_RULES_FILE (path to YAML rule file), NTFY_DASHBOARD_URL (optional deep-link URL appended to each notification body — read via os.environ.get() in dispatch_watch_alerts, consistent with the NTFY_TOPIC pattern in alerting.py; never added to Settings). NEW TESTS: tests/test_watch_alerts.py (60 tests, 7 classes — TestWatchRule: frozen+defaults+fields; TestWatchAlert: frozen+fields; TestSymbolWatchState: round-trip+from_dict-defaults; TestLoadWatchRules: 11 cases covering missing/malformed/empty/valid/threshold-skip/unknown-alert_on/out-of-range/invalid-priority/uppercase-normalised/multiple/bad-rule-resilience; TestLoadSaveWatchState: 7 cases; TestEvaluateWatchRules: 19 cases including no-lookahead structural verify; TestDispatchWatchAlerts: 6 cases; TestMainPyIntegration: 5 source-guard + settings + yaml-exists checks). NEW Gravity step_56_watch_alerts_audit (14 checks: module importable, frozen dataclasses+fields, SymbolWatchState round-trip, load_watch_rules missing→[], malformed→[], valid conviction_above parsed, load_watch_state missing→{}, action_change HOLD→BUY fires, conviction_above edge-trigger no-spam, no-lookahead structural monkeypatch, settings.WATCH_RULES_FILE, main.py source refs, watch_rules.yaml exists, test file exists).

TIER 1.5 — PLAIN-ENGLISH "WHY" FOR EVERY RECOMMENDATION (EXPANDED) (engine/advisory.py, settings.py, 2026-06):
Extends engine/advisory._build_rationale() with four institutional-grade narrative sections gated behind new env var RATIONALE_VERBOSITY. STANDARD MODE ("standard", default): unchanged single-paragraph rationale citing top 2-3 drivers — backward-compatible with all existing callers. VERBOSE MODE ("verbose"): standard paragraph PLUS four labelled sections separated by a blank line. [A] REGIME CONTEXT: "{macro_regime} — HMM {strongly confirms/uncertain/risk-off pressure} (p=X.XX). VIX=X, Sahm Rule=X, 10y-2y spread=±X." — three-tier HMM prose: p ≥ 0.70 → "strongly confirms risk-on"; p 0.30–0.70 → "uncertain"; p < 0.30 → "risk-off pressure". When hmm_risk_on_probability is None: "HMM regime estimate unavailable (insufficient FRED history or first run)". [B] HISTORICAL CALIBRATION: "{p*100:.0f}% win rate over N closed trades (payoff B:1; Kelly edge E — positive/negative)." — computed from TransactionsStore.closed_trades_df() via estimate_win_rate_and_payoff(lookback_trades=100); when win_rate_data is None (< 30 trades), degrades to "Insufficient closed-trade history (< 30 trades); position sizing defaults to volatility targeting." [C] SIGNAL INVALIDATION THRESHOLDS: Score flip point (BUY/HOLD → "score drop below {sell_score_threshold} converts signal to RISK REDUCE"; SELL → "score recovery above {buy_score_threshold} warrants re-evaluation"); conditional RSI/RSI(2) mean-reversion voids using CONFIG["rsi_mean_reversion_exit_level"]/CONFIG["rsi_2_mean_reversion_exit_level"] (both = 35); macro tripwires (VIX/Sahm thresholds, always shown); sector-veto condition (only for Financials/Financial Services/Real Estate); SMA-200 trend break when provided. [D] INDICATOR THEORY NOTES: first-line type(module).__doc__ for ≤4 regime-active signal modules from signals.registry.global_registry, title-cased. Section omitted entirely when active_module_docs is empty. NEW evaluate() STEP 10b: pre-computation block (only when RATIONALE_VERBOSITY=="verbose") wraps two try/except blocks per CONSTRAINT #6 — (1) calls estimate_win_rate_and_payoff on the already-bound TransactionsStore; (2) lazy-imports global_registry, filters by module.is_active_in_regime(macro_dto), extracts first non-boilerplate doc line into _verbose_module_docs dict. _build_rationale() extended signature adds 9 new optional kwargs (all safe defaults); the function is pure string-builder with no I/O. TWO NEW CONFIG ENTRIES (magic-number enforcement): "rsi_mean_reversion_exit_level": 35, "rsi_2_mean_reversion_exit_level": 35 — prevent literal '35' in logic section per TestConfigCompleteness::test_no_magic_numbers_in_logic. NEW SETTING: settings.RATIONALE_VERBOSITY: str = Field(default="standard"). NEW ENV VAR: RATIONALE_VERBOSITY=standard (added to .env.example with inline documentation of all four sections). NO new Python dependencies. NEW TESTS: tests/test_rationale_verbosity.py (49 tests, 7 classes — TestSettingsField: field exists + default; TestStandardMode: 6 backward-compat tests; TestVerboseModePresence: 5 tests; TestRegimeContextSection: 6 tests; TestCalibrationSection: 6 tests; TestInvalidationSection: 10 tests; TestIndicatorTheorySection: 6 tests; TestGracefulDegradation: 3 tests; TestEndToEndIntegration: 5 end-to-end tests through evaluate() with patched engines). NEW Gravity step_57_rationale_verbosity_audit (10 checks: settings default, CONFIG RSI keys, _build_rationale signature, standard mode no markers, verbose mode [A/B/C] present, HMM ≥ 0.70 prose, HMM < 0.30 prose, win_rate_data=None fallback, sector veto Financials/not Technology, test file exists).

TIER 2.1 — REGIME-CONDITIONAL SIGNAL WEIGHTS (signals/aggregator.py, settings.py, 2026-06):
New module-level function resolve_regime_weights(market_regime, regime_weights, default_weights) merges per-regime override dicts onto the flat SIGNAL_WEIGHTS base. When regime_weights is empty (the project default), returns default_weights unchanged (same object, zero-overhead no-op — fully backward-compatible). Exact regime match: returns {**default_weights, **override} so only listed keys change and uninvolved modules inherit their default weight. Falls back to regime_weights["_default"] when no exact match, then returns default_weights if still no match. SignalAggregator.aggregate() resolves effective weights once per call at the top of the loop (resolves via settings.REGIME_SIGNAL_WEIGHTS), replacing the former self.weights.get(name) lookup. NEW SETTING: settings.REGIME_SIGNAL_WEIGHTS: dict[str, dict[str, float]] = {} (default empty — backward-compatible flat weights). Configure via JSON in .env: REGIME_SIGNAL_WEIGHTS={"RECESSION": {"rsi2_mean_reversion": 0.0}, "RISK ON": {"timeseries_momentum": 40.0}}. The "_default" key acts as a catch-all for unmapped regimes. NEW TESTS: tests/test_regime_weights.py (33 tests). NEW Gravity step_58_regime_weights_audit (8 checks: resolve_regime_weights importable, empty dict returns same object, exact match + inherit, _default fallback, no-match-no-default, settings default is {}, aggregate docstring references regime weights, test file exists).

TIER 2.2 — FORECAST ENSEMBLE WEIGHTED BY RECENT SKILL (forecasting/, forecasting_engine.py, settings.py, 2026-06):
New package forecasting/ with forecasting/forecast_tracker.py. ForecastTracker backs a SQLite table forecast_errors (columns: id, symbol, model_name, horizon_days, forecast_ts, forecast_price, actual_price, squared_error, recorded_at; WAL journal mode; index on symbol+model+horizon+ts). Public API: record_forecasts(symbol, horizon_days, model_prices, forecast_ts) — inserts per-model prices (skips 0/negative; CONSTRAINT #4). update_actuals(symbol, horizon_days, actual_price, as_of, tolerance_days=5) -> int — matches past forecasts to realized prices using a ±5-day tolerance window to absorb weekends/holidays. get_skill_weights(symbol, horizon_days, window_days=60, min_obs=30) -> dict[str, float] — inverse-RMSE normalized weights; empty dict when no history; cold-start equal weights when any model has < min_obs completed rows; _MIN_RMSE=0.01 prevents infinite weight on perfect predictions. pending_count / completed_count — monitoring helpers. ALL methods wrapped in try/except (CONSTRAINT #6). ForecastingEngine.__init__ accepts optional tracker= kwarg (non-ForecastTracker values silently discarded → self._tracker=None for backward compatibility). Static _blend_with_skill(model_forecasts, skill_weights, preferred_model, current_price) method: when skill_weights is non-empty uses weighted average over intersection of model_forecasts and skill_weights (renormalized); falls back to original static sector-preference blending when skill_weights={}. generate_forecast() lifecycle per symbol per horizon: (1) update_actuals before loop; (2) per-horizon get_skill_weights → _blend_with_skill; (3) record_forecasts to persist new predictions. NEW SETTINGS: FORECAST_SKILL_WINDOW_DAYS: int = 60 (rolling RMSE window), FORECAST_SKILL_MIN_OBS: int = 30 (cold-start threshold). NEW TESTS: tests/test_forecast_tracker.py (56 tests, 8 classes). NEW Gravity step_59_forecast_skill_audit (10 checks: ForecastTracker importable, DDL columns, cold-start equal weights, warm-path RMSE ordering, _MIN_RMSE > 0, ForecastingEngine tracker kwarg, _blend_with_skill callable, both new settings exist and > 0, package re-export, test file exists).


TIER 2.4 — SENTIMENT / NEWS CATALYST SIGNAL (signals/news_catalyst.py, signals/base.py, signals/__init__.py, config.py, settings.py, main_orchestrator.py, requirements.txt, 2026-06):
NEW signal module signals/news_catalyst.py (auto-registers via global_registry.register at module end, triggered by `import signals.news_catalyst` added to signals/__init__.py). NewsCatalystSignal.name = "news_catalyst"; default weight in settings.SIGNAL_WEIGHTS = 10.0. TWO-PHASE HOOK PATTERN: pre_compute(universe_df, context) batch-fetches from Finnhub for all symbols and populates both (a) self._news_scores/self._earnings_dt (instance cache) AND (b) context.news_sentiment_scores/context.earnings_dates (SignalContext new fields — see signals/base.py changes below); compute(row, context) reads exclusively from instance cache, never calls the network. HEADLINE SCORING: Two paths — (1) FinBERT (ProsusAI/finbert via transformers.pipeline): lazy process-level singleton _FINBERT_PIPELINE, loaded once per process with _FINBERT_LOAD_ATTEMPTED guard; maps positive/negative/neutral labels to +confidence/-confidence/0. (2) Keyword lexicon fallback (used on ImportError or RuntimeError from transformers, or when FinBERT disabled via settings.FINBERT_ENABLED=False): _POSITIVE_WORDS and _NEGATIVE_WORDS frozensets (~80 words total); _lexicon_sentiment(headline) = (pos − neg) / max(1, pos + neg) ∈ [-1, 1]; tokenises by lowercasing and splitting, strips ".,!?;:\"'()[]" from each token. _score_headline(headline, pipeline) selects the correct scoring path. EARNINGS PROXIMITY GATING: _earnings_proximity_multiplier(next_earnings, now, suppress_hours, dampen_days) returns 0.0 within suppress_hours (default 48h — completely silent near earnings to avoid fabricated signals), 0.5 within dampen_days (default 7 days — half-weight during earnings approach), 0.5 for 24h post-earnings (fresh noise period), 1.0 beyond. FINNHUB WIRING: _build_finnhub_client() reads FINNHUB_API_KEY from os.environ; returns None when absent (pre_compute short-circuits silently). _fetch_company_news(client, symbol, lookback_days) calls client.company_news, aggregates headlines, falls back to empty list on any error. _fetch_next_earnings(client, symbol) calls client.earnings_calendar; returns None on missing data. Courtesy time.sleep(0.12) per symbol ≈ 8 calls/sec, safely under Finnhub free-tier 60/min ceiling. SignalContext EXTENDED (signals/base.py): two new optional fields (Dict defaults): news_sentiment_scores: Dict[str, float] = field(default_factory=dict); earnings_dates: Dict[str, str] = field(default_factory=dict). COLUMN_SCHEMA ADDITIONS (config.py): three new columns appended to the ADVISORY SIGNALS section: {"header":"News Sentiment","key":"News_Sentiment","format":"number"}, {"header":"Earnings Date","key":"Earnings_Date","format":"string"}, {"header":"Cluster","key":"Correlation_Cluster","format":"number"}. ORCHESTRATOR WRITEBACK (main_orchestrator.py): after the multifactor writeback block, reads context.news_sentiment_scores/earnings_dates and writes dashboard_df['News_Sentiment']/dashboard_df['Earnings_Date'] via .map(); always initialises dashboard_df['Correlation_Cluster'] = float('nan') (computed only on-demand in GUI). NEW SETTINGS: NEWS_LOOKBACK_DAYS: int = 7 (Finnhub news fetch window), FINBERT_ENABLED: bool = True (toggle neural vs. lexicon path), NEWS_EARNINGS_SUPPRESS_HOURS: float = 48.0 (zero-weight window near earnings), NEWS_EARNINGS_DAMPEN_DAYS: float = 7.0 (half-weight window before earnings). NEW ENV VARS: NEWS_LOOKBACK_DAYS, FINBERT_ENABLED, NEWS_EARNINGS_SUPPRESS_HOURS, NEWS_EARNINGS_DAMPEN_DAYS. OPTIONAL DEPENDENCY: transformers>=4.35.0 added to requirements.txt with comment noting PyTorch or TF backend required; ImportError handled gracefully → lexicon fallback, never a crash. NEW TESTS: tests/test_news_catalyst.py (46 tests, 8 classes: TestLexiconSentiment, TestEarningsProximity, TestScoreHeadline, TestSignalCompute, TestPreCompute, TestRegistration, TestFetchHelpers, TestSettings — all Finnhub/transformers calls monkeypatched). NEW Gravity step_60_news_catalyst_audit (10 checks: importable, name attribute, FinBERT None-safe, lexicon pos>0/neg<0, suppress within 48h, dampen within 7d, pre_compute populates context fields, no-key resilience, SIGNAL_WEIGHTS entry, test file exists).


TIER 2.5 — CORRELATION CLUSTER AWARENESS (research_engine.py, gui/panels.py, config.py, settings.py, 2026-06):
NEW module-level functions in research_engine.py (no new module, pure additions). compute_correlation_clusters(returns_df, distance_threshold=0.4, min_obs=20) -> Tuple[Dict[str,int], pd.DataFrame]: (1) accepts a pre-built returns DataFrame (columns=symbols, index=dates); (2) drops symbols with < min_obs valid returns and assigns them cluster_id=0 (excluded; CONSTRAINT #4 — no fabricated cluster assignment); (3) computes pairwise correlation matrix and converts to Lopez de Prado distance d = sqrt(0.5 * (1 − ρ)); (4) runs scipy.cluster.hierarchy.linkage with method='ward' then fcluster(criterion='distance', t=distance_threshold) to assign cluster IDs; (5) builds per-cluster summary DataFrame with columns cluster_id, symbols (list), n_symbols, avg_intra_corr (NaN for singletons; never fabricated); (6) returns (labels: Dict[str, int], cluster_summary: pd.DataFrame) — empty dict + empty summary with correct schema on any fatal error (CONSTRAINT #6). fetch_returns_for_clustering(symbols, lookback_days=60) -> pd.DataFrame: fetches yfinance daily closes via yf.download (lazy import inside function body so test patching targets yfinance.download directly), returns pct_change() DataFrame; returns empty DataFrame on any error or empty symbols list. GUI INTEGRATION (gui/panels.py): _render_correlation_cluster_section(signals: list) helper inserted in render_report_viewer() before _render_decision_journal_section. UI surfaces: lookback slider (30–250 days), threshold slider (0.05–1.5), "Compute Clusters" on-demand button (CONSTRAINT #5 — no scheduler/cron). Results stored in st.session_state["cluster_labels"] and st.session_state["cluster_summary"]; renders an assignment table (Symbol, Cluster ID, Size, Avg Intra-Cluster Corr) and a concentration table (cluster → aggregate position %, with ">30%" warning annotation). DESIGN RATIONALE: correlation clusters require simultaneous returns for all symbols at once — incompatible with the orchestrator's per-symbol loop — so on-demand GUI computation is the correct architectural choice rather than a pipeline-computed column. Correlation_Cluster defaults to NaN in all orchestrator output (column exists in COLUMN_SCHEMA for forward-compatibility). NEW SETTINGS: CORRELATION_CLUSTER_LOOKBACK_DAYS: int = 60, CORRELATION_CLUSTER_THRESHOLD: float = 0.4. NEW ENV VARS: CORRELATION_CLUSTER_LOOKBACK_DAYS, CORRELATION_CLUSTER_THRESHOLD. NEW TESTS: tests/test_correlation_clusters.py (27 tests, 7 classes: TestComputeCorrelationClusters 7 tests, TestDistanceThreshold 3 tests, TestSummaryDataFrame 6 tests, TestEdgeCases 4 tests, TestInsufficientHistory 0 — covered by TestEdgeCases::test_insufficient_obs_excluded, TestFetchReturnsHelper 3 tests, TestSettings 7 tests — all pure function tests, no mocking needed for compute_correlation_clusters; fetch_returns_for_clustering patches yfinance.download directly). NEW Gravity step_61_correlation_cluster_audit (10 checks: compute_correlation_clusters importable, fetch_returns_for_clustering importable, known-correlated A/B share cluster, uncorrelated C in different cluster, empty DataFrame → empty labels+summary, Correlation_Cluster in COLUMN_SCHEMA, CORRELATION_CLUSTER_LOOKBACK_DAYS=60, CORRELATION_CLUSTER_THRESHOLD=0.4, insufficient obs → cluster_id=0, test file exists).


TASK 3 — OPERATOR ERGONOMICS (scripts/daily_briefing.py, diagnostics_and_visuals.py, settings.py, scripts/preflight_check.py, gui/panels.py, launch.command, 2026-06):
Four ergonomics improvements for advisory operators who check the platform infrequently or from a phone.

3.1 DAILY BRIEFING DIGEST (scripts/daily_briefing.py, NEW): Single module with three public callables. generate_briefing(output_dir) -> str assembles a Markdown briefing with five labelled sections: [MACRO REGIME] reads market_regime/vix/hmm_risk_on_probability/kill_switch_active from output/state_snapshot.json; [TOP 3 ACTION SIGNALS] sorts signals by action priority (BUY > HOLD > SELL) then conviction descending; [Δ SINCE LAST RUN] calls scripts.snapshot_diff.compute_diff_from_history; [DEAD-LETTERED SYMBOLS] calls gui.dead_letter.read_dead_letter; [30-DAY CALIBRATION] calls evaluation_engine.calibration_curve + TransactionsStore (both imported lazily inside function body — no module-level heavy imports; CONSTRAINT #7 + enables monkeypatching). Every section is independently wrapped in try/except — CONSTRAINT #6 — so first-ever runs (no snapshot, no dead_letter.json, no trade history) degrade to "No data yet" placeholders and still produce a valid Markdown file. write_briefing(output_dir) -> Path writes to output/briefing_YYYY-MM-DD.md atomically (mkdir -p + write); never raises. main(argv) is the CLI entry point: python -m scripts.daily_briefing [--print] [--output-dir DIR]; --print echoes the written briefing to stdout after writing. WIRE-UP IN launch.command: appended `python -m scripts.daily_briefing --print || true` as the final step so every launch (single-run AND interval-loop) prints a briefing digest to the Terminal window. The `|| true` ensures a briefing failure never aborts the launcher. No new Python dependencies; no network calls in the briefing itself — all data comes from pre-written output files and SQLite. NEW TESTS: tests/test_operator_ergonomics.py::TestDailyBriefingImport (4 tests: importable, generate_briefing/write_briefing/main callable); TestBriefingSections (11 tests: regime/VIX/kill-switch appear in text, top-actions sorted correctly, dead-letter read, calibration MAE rendered); TestGenerateBriefing (5 tests: never raises, required headers present, snapshot wired, file creation, date in filename).

3.2 MOBILE-FRIENDLY DAILY REPORT (diagnostics_and_visuals.py): Added a @media (max-width: 600px) responsive CSS block to HTML_REPORT_TEMPLATE embedded <style>, immediately before the closing </style> tag. No new dependency — pure CSS. Key rules: exec-grid collapses to single column (grid-template-columns: 1fr); summary-band collapses to 2-column (grid-template-columns: repeat(2, 1fr)); signal table wrapped in overflow-x: auto + -webkit-overflow-scrolling: touch so it scrolls horizontally on narrow viewports instead of overflowing; tr.data-row td and th both get min-height: 44px for WCAG 2.5.5 touch-target compliance; delta-grid collapses to 1fr; reduced font sizes and padding for 600px. The desktop layout is completely unchanged. NEW TESTS: TestMobileResponsiveCSS (5 tests: @media present in template, 600px breakpoint, 44px tap target, 1fr grid collapse, overflow-x:auto).

3.3 SECRETS-ROTATION REMINDER (settings.py, scripts/preflight_check.py, Stage 3 — 2026-06-26 cleanup): New settings FRED_KEY_ROTATED_DATE: Optional[str] = Field(default=None) AND ALPACA_KEY_ROTATED_DATE: Optional[str] = Field(default=None) — ISO dates (YYYY-MM-DD) set by the operator after generating new API keys. New functions check_key_rotation_recent(max_age_days=90) (FRED) and check_alpaca_key_rotation_recent(max_age_days=90) (Alpaca) in scripts/preflight_check.py: both WARNING-ONLY, NEVER BLOCKING (passed is always True, never False). Three outcomes per check: (a) date not set in .env → warning to start tracking; (b) valid ISO date within max_age_days → clean PASS with age in days; (c) valid ISO date but older than max_age_days → warning citing exact age and the key rotation URL. ALPACA_KEY_ROTATED_DATE check is AUTO-SKIPPED when ADVISORY_ONLY=True (Alpaca keys have no blast-radius risk while the broker surface is quarantined; `alpaca_key_rotation_recent` is the 8th entry in _ADVISORY_AUTO_SKIP). Wired into ALL_CHECKS as checks #2 and #3 (after check_fred_key_configured, before check_advisory_only_active). TOTAL CHECK COUNT: 16 (was 15). New env vars: FRED_KEY_ROTATED_DATE, ALPACA_KEY_ROTATED_DATE (both optional; unset = warning, never failure). Both added to .env.example with explanatory comments. docs/GO_LIVE_CHECKLIST.md: rotation checkboxes updated with real check function names (not Stage-3 stub parentheticals). docs/HOW_TO_GUIDE.md §13 table: count updated to 15; alpaca_key_rotation_recent row added with "Yes" advisory skip column; heartbeat_fresh and validation_reports now also show "Yes" in the advisory skip column. NEW TESTS in tests/test_preflight.py: TestKeyRotationChecks (8 tests: fred unset→warning, fred fresh→pass, fred stale→warning, alpaca unset→warning, alpaca fresh→pass, alpaca stale→warning, alpaca invalid-iso→warning, alpaca auto-skipped under ADVISORY_ONLY=True). NEW Gravity step_67_key_rotation_audit (10 checks: importable, settings field, unset→warning, fresh→clean-pass, stale→warning-pass, invalid-format→warning, in _ADVISORY_AUTO_SKIP, auto-skip fires under True, both in ALL_CHECKS in order, test file contains TestKeyRotationChecks).

3.4 "QUICK ADD TO WATCHLIST" GUI (gui/panels.py): render_live_inventory() extended with a text input + "➕ Add to watchlist" button between the Robinhood snapshot refresh UI and the Sync Now button. FILE-ONLY: writes to watchlist.txt at the repo root — never to .env and never via gui.env_io.write_setting (avoids GUI-induced env churn; CONSTRAINT #3). VALIDATION: ticker normalized to uppercase; empty input → st.warning; any character outside [A-Z0-9.-] → st.warning. DEDUPLICATION: reads existing non-comment lines before appending (comment lines start with #, consistent with main.py._load_watchlist); if the uppercase ticker already exists → st.info "already in watchlist.txt" without a second write. FILE CREATION: if watchlist.txt does not exist, the open("a") call creates it automatically. OSError on write → st.error with the OS message. PICKED UP AUTOMATICALLY: main.py._load_watchlist() reads watchlist.txt on every run_once() call with no restart needed. No new settings, no new env vars — file path is the repo-root watchlist.txt constant already used by main.py. NEW TESTS: TestWatchlistQuickAdd (7 tests: append writes line, dedup skips second add, comment lines not counted as existing, ticker uppercase-normalized, missing file created, OSError surfaced via st.error, source guard that "watchlist.txt" appears in render_live_inventory source).

GRAVITY STEP 63 (step_63_operator_ergonomics_audit, 10 checks): scripts.daily_briefing importable + generate_briefing callable; generate_briefing returns Markdown containing a regime section header; write_briefing produces a file named briefing_YYYY-MM-DD.md; HTML_REPORT_TEMPLATE contains @media (max-width: 600px); mobile CSS block contains 44px tap target + overflow-x:auto; check_key_rotation_recent in ALL_CHECKS; check_key_rotation_recent is always warning-only (never passed=False — verified by calling with stale/invalid dates); Settings.FRED_KEY_ROTATED_DATE declared as Optional field; render_live_inventory source references watchlist.txt quick-add; tests/test_operator_ergonomics.py exists.

## GUI HELP EXPLAINERS — PROMPT 6 (Docs sync + Gravity step_68_help_explainers_audit, 2026-06)

DOCS CHANGES: (1) docs/HOW_TO_GUIDE.md — new section "## In-App Help & Glossary" (anchor: #in-app-help--glossary) appended at end of file; covers: what-you'll-find table (explain() expanders / metric tooltips / glossary / section help), first-run onboarding tour (4-step checklist + dismiss via output/.gui_onboarded), help-key convention ("<tab>.<metric_name>" → metric_help(), missing → "", never raises), anchor-contract invariant (guide_anchor must match real heading slug, enforced by test + Gravity check 3). (2) docs/RUNBOOK.md §0 — self-serve note added before "The Reports tab includes:" block: "Not sure what a term means? The ❓ Help tab has a searchable glossary of 60+ terms … Every tab also carries a collapsible ❓ What is this & how do I use it? expander at the top." (3) CLAUDE.md — 4 new convention bullets added before "All data crossing into calculation code…": in-app help content SSOT (gui/help_content.py), help-key convention (<tab>.<metric_name> → empty string never raises), anchor-contract invariant (HOW_TO_GUIDE.md slug must match), thresholds in help text (import from settings/thresholds/CONFIG never literals). (4) GEMINI.md — this entry. (5) gui/panels.py — added `from gui import help_widgets` import; added `help_widgets.explain("<tab_id>")` as first rendered call in all 10 render_* tab functions (launcher/reports/settings/strategy_matrix/paper_monitor/gravity/options/market_data/observability/live_inventory); added render_help() (11th tab body: onboarding tour + glossary search). (6) gui/app.py — 11th tab "❓ Help" wired to panels.render_help(); sidebar quick-help widget added (3 most-common questions: action signals / Kelly Target / advisory mode). NEW GRAVITY FUNCTION step_68_help_explainers_audit (10 checks from plan §7): (1) imports gui.help_content exports; (2) all 10 tab IDs in TAB_HELP; (3) all guide_anchors resolve to valid HOW_TO_GUIDE.md heading slugs; (4) metric_help("__nonexistent__") == ""; (5) required glossary terms present (kelly target/pbo/dsr/sahm rule/iv rank/hmm/advisory mode/conviction/calibration); (6) no duplicate GLOSSARY keys; (7) explain callable; (8) mark_onboarded/should_show_tour round-trip; (9) ≥10 help_widgets.explain() calls in gui/panels.py covering all 10 tab IDs; (10) tests/test_help_content.py exists. PRE-EXISTING TYPEERROR in Gravity json.dumps (step_52_calibration_audit returns a dict with numpy bool values): pre-existing bug not introduced by this PR; fixed by adding NumpyEncoder to export_machine_readable_report(). CONSTRAINTS HONORED: #4 (metric_help returns "" not fallback), #6 (explain/glossary_chip/why_callout never raise), #7 (thresholds import from settings/CONFIG, never literals).

## TIER 4 — VALIDATION & HONESTY (2026-06)

4.1 LIVE-VS-RECOMMENDATION TRACKING (evaluation_engine.py + gui/panels.py): evaluation_engine.recommendation_tracking_report(log_path, transactions_store, horizon_days=30, *, historical_store, _today) -> Dict[str, Any]. "If you'd taken every BUY at the published conviction-weighted size, the paper-equivalent return over 30 days would be X%; actual decisions returned Y%." Reads the 1.3 decision log (output/decision_log.jsonl) via gui.decision_log.read_decisions() (lazy import — avoids circular import). Filters to signal_action containing "BUY" (also catches "STRONG BUY"). For each BUY entry: (a) fetches bars via HistoricalStore.get_bars(symbol, lookback_days=756) (3 years covers signals from up to 2 years ago); (b) model entry price = _price_at_or_before(bars, signal_dt); (c) model exit price = _price_at_or_before(bars, signal_date + horizon_days) when completed (exit_date <= today); (d) for action_taken=="acted" entries with a trade_id, reads TransactionsStore.get_trade_history(symbol) for entry_price/exit_price — when exit_ts is None (open trade), uses latest bar close as surrogate. Aggregation: model_return_30d = conviction-weighted mean of completed BUYs with valid prices; operator_return_30d = simple mean of actual returns; delta = operator − model (positive = operator adds value). Returns dict with keys: rows (per-signal), model_return_30d, operator_return_30d, delta, n_signals, n_acted, n_completed, n_with_exit, horizon_days. MODULE-LEVEL EXPORTS: _TRACKING_EMPTY (sentinel with all 9 keys), _DEFAULT_DECISION_LOG_PATH (Path). HELPER: _price_at_or_before(bars, target) — returns Close ≤ target date as float; NaN on empty/no-match — CONSTRAINT #4. All I/O in try/except, CONSTRAINT #6. HistoricalStore imported lazily (no circular import). GUI section _render_recommendation_tracking_section() in gui/panels.py inserted in render_report_viewer() between decision journal (1.3) and Brinson-Fachler attribution; horizon slider 5–90 d; 4 KPI columns; @st.cache_data(ttl=300); narrative markdown. NEW TESTS: tests/test_recommendation_tracking.py (≥30 tests, 8 classes: TestEmptyLog/TestNoBuySignals/TestModelReturn/TestActualReturn/TestPassedSignal/TestHorizonNotElapsed/TestConvictionWeighting/TestDelta/TestDeadLetterResilience/TestModuleSurface). GRAVITY STEP 64 (step_64_recommendation_tracking_audit, 10 checks): recommendation_tracking_report importable; _TRACKING_EMPTY has all 9 required keys; _DEFAULT_DECISION_LOG_PATH is pathlib.Path; _price_at_or_before(empty, now) returns NaN (CONSTRAINT #4); missing log → n_signals=0 and all floats NaN; passed BUY → n_signals=1 / n_acted=0; recent signal (5 days ago, horizon=30) → n_completed=0; HistoricalStore failure degrades gracefully (CONSTRAINT #6); gui/panels.py references recommendation_tracking_report; tests/test_recommendation_tracking.py exists.

4.2 WALK-FORWARD VALIDATION CADENCE (scripts/refresh_validations.py + scripts/refresh_validations.sh): Monthly runner that re-validates registered strategies so reports never go stale. STRATEGY_REGISTRY: Dict[str, Tuple[Callable, float]] maps strategy_id → (adapter_fn, turnover). Two registered strategies: "rsi2_mean_reversion" (adapter=_build_rsi2_adapter, turnover=0.02) and "timeseries_momentum" (adapter=_build_tsmom_adapter, turnover=0.005). ADAPTERS: _build_rsi2_adapter(spy_close) mirrors tests/test_validation_rsi2.py — RSI(2) + SMA-200 trend filter + crash/recession RISK-OFF gate; returns (X[RSI_2, SMA_200], y=daily_ret, precomputed={RSI2_Gated, RSI2_Ungated}). _build_tsmom_adapter(spy_close) mirrors tests/test_validation_ts_momentum.py — 4 variants (12M/6M × 10%/20% vol target); returns (X[ROC_12M, ROC_6M, Vol], y, precomputed with 4 series named TSMOM_{ROC}_{vol}pct). _make_strategy_fn(precomputed, turnover) — closure returning harness-compatible strategy_fn(X_train, y_train, X_test, y_test) → list[dict] with keys params/train_returns/test_returns/turnover. _download_spy(start_date, end_date) — yfinance; raises RuntimeError on empty result. run_validations(strategies, start_date, end_date, output_dir, n_cpcv_splits, n_test_splits) — downloads SPY once, loops strategies, runs StrategyValidationHarness; per-strategy failure → dead-letter with deployable=False + error key (CONSTRAINT #6). main(argv) — argparse CLI; exit 0 = all pass, 1 = any fail. scripts/refresh_validations.sh — bash wrapper verifying .venv + Python 3.12, then runs python3 -m scripts.refresh_validations "$@"; designed for monthly cron. CONSTRAINTS: no fabricated returns to harness (CONSTRAINT #4), yfinance only (CONSTRAINT #7), per-strategy try/except (CONSTRAINT #6). NEW TESTS: tests/test_refresh_validations.py (≥40 tests, 7 classes: TestModuleSurface/TestRegistryStructure/TestBuildRsi2Adapter/TestBuildTsmomAdapter/TestMakeStrategyFn/TestRunValidations/TestMainCLI). GRAVITY STEP 65 (step_65_refresh_validations_audit, 10 checks): scripts.refresh_validations importable; STRATEGY_REGISTRY contains rsi2 and tsmom; entries are (callable, positive turnover); RSI(2) adapter returns X with RSI_2/SMA_200 columns; TSMOM adapter returns 4 precomputed variants; _make_strategy_fn closure returns list with required harness keys; unknown strategy dead-lettered (CONSTRAINT #6); main exit-code 0 on all-pass / 1 on any-fail; scripts/refresh_validations.sh exists and is executable; tests/test_refresh_validations.py exists.


## PROMPT REGISTRY (prompt_registry/, 2026-06)

SECURITY BOUNDARY (MUST NEVER BE VIOLATED): Fetched prompts are advisory text only. They can change what an AI is told — they cannot change what the platform is permitted to do. Order submission, advisory quarantine, risk gates, and the kill switch are enforced in Python code, not in any prompt body. This invariant is audited on every Gravity run (step 69, check 7).

PACKAGE (prompt_registry/): models.py (PromptRecord / PromptVersion / RegistryManifest frozen dataclasses); signing.py (HMAC-SHA256 sign() / verify() / compute_sha256()); guardrails.py (validate_prompt() — rejects ADVISORY_ONLY=false body, submit_order body, size overflow > PROMPT_MAX_CHARS); cache.py (CacheManager — signed-version disk cache + read_baseline() / list_baseline_ids() + write() / read() / delete()); store.py (PromptStore ABC + LocalJSONStore + HTTPStore via stdlib urllib, zero new dependencies); registry.py (PromptRegistry — resolution chain, sync(), rollback(), get_registry() module-level singleton + reset_registry()); __main__.py (CLI subcommands: list, get, sync, pin, rollback, diff, verify, publish); baseline/ (git-committed fallback bodies — always available, zero network).

RESOLUTION CHAIN (CONSTRAINT #4 — never ""): Pin (PROMPT_REGISTRY_PINS dict) → Remote latest (verified HMAC) → Disk cache (verified HMAC) → Baseline → one-line sentinel. get() never returns an empty string.

SECRET KEYS (CONSTRAINT #3 — 4 keys in gui/env_io.SECRET_KEYS, NOT in ALLOWED_KEYS, never GUI-writable): PROMPT_REGISTRY_URL (protected HTTPS manifest URL), PROMPT_REGISTRY_TOKEN (bearer read-token), PROMPT_REGISTRY_PUBLISH_TOKEN (higher-privilege publish credential — author machine only, runtime platform never needs this), PROMPT_REGISTRY_SIGNING_KEY (HMAC-SHA256 key shared between author and runtime).

NON-SECRET TUNABLES (in gui/env_io.ALLOWED_KEYS): PROMPT_REGISTRY_ENABLED (bool, default false — master switch; baseline-only when false), PROMPT_REGISTRY_BACKEND ("http" | "local" | "firestore", default "http"), PROMPT_REGISTRY_PINS (JSON dict {"prompt_id": "version"} — rollback lever, also in _JSON_KEYS for correct .env serialization). PROMPT_REGISTRY_PINS is in _JSON_KEYS frozenset so pydantic-settings round-trips the dict correctly via json.dumps / json.loads.

ADDITIONAL SETTINGS (settings.py): PROMPT_REGISTRY_REFRESH_SECONDS (int, default 0 — CONSTRAINT #5: on-demand only, never a timer); PROMPT_CACHE_DIR (str, default "output/prompt_cache"); PROMPT_CACHE_KEEP_VERSIONS (int, default 5 — rollback depth); PROMPT_MAX_CHARS (int, default 50000 — guardrails hard ceiling).

CONSTRAINT SUMMARY for this package: #3 (4 creds secret, GUI can't write them), #4 (fail-closed to baseline then sentinel, never ""), #5 (REFRESH_SECONDS=0, sync only when operator explicitly triggers it), #6 (every network/parse/verify path dead-letter tolerant — no exception past GUI boundary).

GUI TAB "📝 Prompts" (gui/app.py tab 10, gui/panels.render_prompt_registry, wrapped in safe_panel): mandatory security banner ("Prompts are advisory text; safety gates are enforced in code"); disabled-registry warning when PROMPT_REGISTRY_ENABLED=false; on-demand "🔄 Sync" button (disabled when not enabled, never called on a timer — CONSTRAINT #5); status DataFrame (Prompt ID / Resolved version / Source badge / Pinned / Cached count); per-ID selectbox drill-down with three expanders (View body, Diff two versions, Rollback/pin); pin/rollback writes via env_io.write_setting("PROMPT_REGISTRY_PINS", json.dumps(pins_dict)) — effective on next launch, never hot-swaps running process.

CLI (python -m prompt_registry): list → table of all known IDs with resolved version and source; get <id> → print body to stdout; sync → pull remote manifest, verify + cache; pin <id> <version> → write PROMPT_REGISTRY_PINS; rollback <id> → pin to previous cached version; diff <id> <v1> <v2> → unified diff; verify → re-check HMAC + guardrails on all cached files (non-zero exit on failure); publish <id> <version> <path> → sign + upload (requires PUBLISH_TOKEN, exits cleanly non-zero without it). All subcommands dead-letter tolerant — clear error message + non-zero exit, never traceback.

BASELINE PROMPT IDS (prompt_registry/baseline/): gravity_step_01.md, gravity_step_02.md, gravity_step_03.md, gravity_step_04.md, gravity_step_05.md, gravity_step_06.md, gravity_step_07.md, gravity_system.md, master_preprompt.md.

OPERATOR WORKFLOW: `python -m prompt_registry get master_preprompt` fetches and prints the resolved body (pipe to pbcopy on macOS). Publishing v1.1.0 and moving the "latest" pointer is the "update over the internet" mechanism; `python -m prompt_registry sync` pulls it to the disk cache. See docs/HOW_TO_GUIDE.md §16 for the full workflow and docs/RUNBOOK.md §7 for the publish/rollback incident playbooks.

TESTS: tests/test_prompt_registry_signing.py, tests/test_prompt_registry_guardrails.py, tests/test_prompt_registry_store.py, tests/test_prompt_registry_resolution.py, tests/test_prompt_registry_cli.py, tests/test_prompt_registry_gui.py (303 tests total, all passing).

GRAVITY STEP 69 (step_69_prompt_registry_audit, 10 checks): (1) prompt_registry importable, get_registry/PromptRegistry/PromptRecord exist; (2) fail-closed: get("gravity.system") returns baseline (non-empty) with no URL/cache; (3) verify(tampered_body)=False AND verify(signed_body)=True; (4) guardrails rejects ADVISORY_ONLY=false body and submit_order body; (5) 4 secret keys in SECRET_KEYS AND not in ALLOWED_KEYS; (6) disabled registry → Gravity prompts byte-identical to baseline; (7) no eval/exec/import in prompt_registry/ or ai_verification_prompts.py source; (8) PROMPT_REGISTRY_REFRESH_SECONDS default=0 (CONSTRAINT #5); (9) CLI verify exits non-zero on corrupt cache; (10) tests/test_prompt_registry_resolution.py exists.

AI CONTROL CENTER TAB (gui/ai_control_center.py + gui/panels/__init__.py::render_ai_control_center, 2026-07, extended 2026-07-02 for flexible routing): A single "🎛️ AI Control Center" Streamlit tab (tab 14 in gui/app.py) — the one operator-facing surface for EVERY AI option (analyst rationale commentary + alert commentary — both FLEXIBLY ROUTED to Claude OR Gemini per-job via LLM_COMMENTARY_RATIONALE_PROVIDER / LLM_COMMENTARY_ALERT_PROVIDER, independently operator-chosen — Gemini chart vision (fixed), Gravity AI runner, Opal research). OPERATOR-ONLY / nothing autonomous: every action is a button or an operator-started `main.py --interval N` / `--agent` run they can stop (honors the standing "no automatic AI invocation" rule). It CONSOLIDATES existing helpers (reuses _render_llm_commentary_button, _render_gemini_chart_section, _render_gravity_ai_runner_section, orchestrator_runner) — it does not duplicate logic. Headless helper gui/ai_control_center.py (Streamlit-free): AICapability dataclass, CAPABILITIES registry (5 options), capability_status(settings, cap) → four-state {not_built > disabled > missing_key > ready}, control_center_overview, validate_toggle_write (raises SecretWriteError on secret / DisallowedKeyError on non-allowlisted — CONSTRAINT #3), opal_built() soft-import probe. Four panel sections: A capability grid + master-switch toggles (write via gui.env_io.write_setting, effect NEXT launch); B on-demand per-symbol Claude/Gemini-vision/Opal (Opal auto-activated now that llm/research.py has shipped — Tier 9 Scope 4); C Gravity AI audit; D operator Start/Stop of orchestrator_runner.launch_scheduled_advisory(mode, interval_seconds)/stop_run (subprocess.Popen → main.py, log output/gui_scheduled.log, interval clamped ≥ 30 s — NO cron/daemon/threading.Timer). env_io: ALLOWED_KEYS gained GRAVITY_AI_RUNNER_ENABLED + OPAL_RESEARCH_ENABLED/PROVIDER/MODEL; OPENAI_API_KEY added to SECRET_KEYS (never GUI-writable). TESTS: tests/test_ai_control_center.py, tests/test_gui_env_io_control_center_keys.py. GRAVITY step_78_ai_control_center_audit (11 checks: registry completeness, ALLOWED_KEYS toggles, OPENAI_API_KEY secret-only, capability_status truth table, launcher callable + no autonomous scheduler, tab wired in gui/app.py, toggle-write guard, Opal auto-activation confirmed (built=True, default status=disabled), test files exist, flexible-routing regression check — Gemini serving rationale + Claude serving alerts both resolve ready off the correct key). Also see llm/router.py (get_rationale_provider/get_alert_provider now dispatch to either ClaudeProvider or GeminiProvider via a shared _construct_provider helper) and NEW tests/test_llm_router.py.

## PERFORMANCE HOT-PATH SERIES (PR A–D, main_orchestrator.py / engine.advisory / gui, 2026-07)

Four staged efficiency passes over the per-cycle hot loops, all default-preserving except PR D. PR A (CONNECTION REUSE + ENGINE DI + PARALLELIZATION): HistoricalStore, forecasting.forecast_tracker.ForecastTracker, and TransactionsStore each now hold ONE cached sqlite connection (check_same_thread=False, WAL + busy_timeout=5000, threading.Lock) instead of opening a fresh sqlite3.connect per call (~N/ticker/cycle before); engine/advisory.py's heavy engines (ProcessingEngine/TechnicalOptionsEngine/ForecastingEngine/StrategyEngine + ONE TransactionsStore) became lazily-built module-level singletons injectable by the orchestrator's EngineContext; and main_orchestrator.py's two serial per-ticker loops (options/IV + advisory overlay) fan out over ThreadPoolExecutor with byte-identical outputs (loop bodies were order-independent + try/except-guarded; iv_store.record_iv() writes deferred to the main thread after the pool joins). PR B (GUI PANEL CACHING): previously-uncached per-rerun panel loads (Analytics realized-performance + equity-curve, AI-Insights bars, Pairs close, and the Observability Forecast-Skill table's ~120-connection-per-rerun double loop) moved behind @st.cache_data mtime-keyed loaders + one batched GROUP BY RMSE query; Pairs Scan parallelized — WHAT renders is identical, only WHEN/HOW data is fetched changed. PR C (CNN-LSTM + PROPHET MODEL PERSISTENCE, opt-in FORECAST_MODEL_PERSISTENCE_ENABLED default False): new forecasting/model_persistence.py caches ONE fitted artifact per ticker under ml/models/forecast_cache/ (mtime = staleness clock, FORECAST_MODEL_RETRAIN_DAYS default 7) so a fresh forecast skips the 50-epoch Keras fit / Prophet fit within the retrain window; any load failure degrades to a fresh fit (CONSTRAINT #6). PR D (ADVISORY PIPELINE-COMPUTE REUSE, OUTPUT-CHANGING opt-in ADVISORY_REUSE_PIPELINE_COMPUTE default False): the advisory overlay reuses run_pipeline's already-fit GARCH_Vol/Forecast_30 for a ticker (precomputed_garch/precomputed_forecast) instead of a SECOND independent GARCH + full ensemble fit; StrategyEngine.evaluate_security() is DELIBERATELY not deduped (run_pipeline calls it WITHOUT context_extras, so reusing its strategy_output would silently zero cross-sectional-momentum/multifactor contributions). New env vars: DATA_FETCH_MAX_CONCURRENCY (default 8), MARKET_DATA_BARS_TTL_SECONDS (300), FORECAST_MODEL_PERSISTENCE_ENABLED, FORECAST_MODEL_RETRAIN_DAYS, ADVISORY_REUSE_PIPELINE_COMPUTE. Tests: tests/test_perf_gui_cache.py.

## FUNDAMENTALS — FINNHUB → YAHOO STATEMENT-COMPUTED ENGINE (data/yahoo_fundamentals.py, 2026-07-08)

Dropped the paid/rate-limited (chronically-429) Finnhub company_basic_financials primary path for fundamentals in favor of a pure, I/O-free compute_fundamentals(...) that derives 14 equity fundamentals from Yahoo's FREE statement frames (income_stmt + quarterly, balance_sheet, cashflow + quarterly, dividends, institutional_holders) plus price/shares the caller already fetched via yfinance. Returns a yfinance .info-style dict so FundamentalDataDTO.from_raw_dict() is UNCHANGED; every metric is computed in its own try/except and degrades independently to NaN (CONSTRAINT #4 — a missing statement row never fabricates 0.0 nor nukes the other 13), and module-level alias tables (_row_latest/_ttm/_match_row) tolerate yfinance statement-label drift. TWO SCALE-CRITICAL RULES preserved: dividendYield emitted as a FRACTION (0.0257, consumed as-is, NOT routed through normalize_yfinance_dividend_yield), debtToEquity ×100 (150.0 — two downstream consumers divide by 100); payoutRatio uses abs() of the negative "Cash Dividends Paid" over TTM net income; beta = Cov(stock,SPY)/Var(SPY) over BETA_LOOKBACK_DAYS (new setting, default 504, ≥60 overlapping obs, SPY series cached). data/market_data.py rewired: YahooFundamentalsProvider (SOURCE="yahoo_computed") is now PRIMARY, raw yfinance .info the EMERGENCY fallback (on empty primary); FinnhubProvider retained but UNWIRED from fundamentals — FINNHUB_API_KEY now feeds ONLY signals/news_catalyst.py. New setting FUNDAMENTALS_SOURCE ("yahoo" default | "yfinance_info"); CompositeProvider gained a source_name property. Tests: NEW tests/test_yahoo_fundamentals.py (35 offline); Gravity Step 26 gained checks (p)/(q)/(r) — source_name, the two scale rules, empty-safe degradation.

## FORECASTING — GARCH VOL INTO MONTE CARLO + PROPHET INTO THE ENSEMBLE (forecasting_engine.py, 2026-07-08)

Two long-standing weaknesses fixed. (1) GARCH → MONTE CARLO σ: the Monte Carlo GBM σ is now sourced from the forward-looking, fat-tailed GJR-GARCH(1,1) estimator (technical_options_engine.estimate_gjr_garch_volatility) via new helper ForecastingEngine._estimate_daily_sigma(history_df, fallback_daily_sigma) instead of a naive backward-looking historical log-return stdev — making the MC_Lower/MC_Upper band regime-responsive (widens in turbulent vol, tightens in calm); threaded into EVERY run_monte_carlo call site; behind FORECAST_USE_GARCH_SIGMA (default True). THE SCALE RULE (documented so nobody "fixes" it): GARCH returns ANNUALIZED vol, MC needs DAILY, so _estimate_daily_sigma divides by sqrt(252) — mandatory because run_monte_carlo's auto-normalize guard keys on mu ONLY, not σ, so a stealth annualized σ would inflate the band ~16×. Degrades to historical stdev (never raises) when flag off / history_df None / <22 rows / estimator raises. (2) PROPHET → ENSEMBLE: run_prophet_forecast's h=30 yhat (previously computed but DISCARDED from the Forecast_30 blend) now enters model_forecasts["prophet"] at h=30 only and folds into _blend_with_skill's static branch as final = base*(1-w) + prophet*w, w = FORECAST_PROPHET_WEIGHT (default 0.25, clamped [0,1]); Prophet still runs at most once/call and Prophet-absent behavior is byte-identical. New settings FORECAST_USE_GARCH_SIGMA, FORECAST_PROPHET_WEIGHT.

## FORECASTING FIT-ONCE + GARCH REUSE, 4 SETTINGS TO GUI, ADVISORY-SNAPSHOT TELEMETRY PARITY (2026-07-09)

An efficiency + UI-connect pass. FIT-ONCE HOT-LOOP REFACTOR: run_arima/run_holt_winters_grid_search split into fit-once + forecast helpers (run_arima_fit/forecast_from_arima_fit, run_holt_winters_fit/forecast_from_hw_fit); generate_forecast now fits ARIMA and Holt-Winters exactly ONCE per ticker (both fits are horizon-independent) and reuses each across the target-days forecast AND every horizon in [10,30,60,90], collapsing ~5 ARIMA + ~12-15 HW statsmodels fits/ticker to 1+3 — byte-identical output; old names retained as back-compat shims. GARCH DOUBLE-FIT ELIMINATED: generate_forecast gained precomputed_garch_annual_vol; _estimate_daily_sigma uses precomputed_garch_annual_vol/sqrt(252) (same SCALE RULE) when supplied; main_orchestrator.py (~line 410) passes the dashboard_df['GARCH_Vol'] it already fit (~line 300) so no ticker is GARCH-fit twice/cycle (main.py + engine.advisory pass None → unchanged internal fit). 4 SETTINGS TO GUI: FORECAST_USE_GARCH_SIGMA, FORECAST_PROPHET_WEIGHT, FUNDAMENTALS_SOURCE, BETA_LOOKBACK_DAYS added to gui/env_io ALLOWED_KEYS + settings_manager _SETTINGS_LAYOUT (non-secret). ADVISORY SNAPSHOT TELEMETRY PARITY: reporting/state_snapshot.py::write_state_snapshot (the advisory main.py path) now emits top-level sahm_rule/high_yield_oas/yield_curve/hmm_risk_on_probability (from the injected macro_dto) + per-signal garch_vol/hmm_risk_on + the five multifactor keys, matching main_orchestrator's _write_state_snapshot so switching to the advisory orchestrator no longer blanks the GUI Observability / Report-Viewer tabs (the multifactor keys serialized null until the same-day threading fix below). Tests: test_forecasting_engine.py (shim-equality + fit-count), test_gui_env_io_forecast_keys.py, test_state_snapshot_advisory.py, test_forecasting_lookahead.py.

## ADVISORY MULTIFACTOR-Z THREADING (engine/advisory.py, 2026-07-09)

Closes the PR #192 follow-up. The five per-signal multifactor keys (value_z/quality_z/lowvol_z/size_z/multifactor_composite) always serialized as JSON null on the advisory state-snapshot path because evaluate() received context_extras and threaded it into StrategyEngine.evaluate_security() for scoring but never copied the values onto the returned Recommendation. Added _mf_scores = (context_extras or {}).get("multifactor_scores", {}).get(symbol, {}) right before the key_indicators literal (Step 12) plus five NaN-defaulting entries (value_z/quality_z/lowvol_z/size_z/multifactor_composite) — an absent context_extras, a failed pre-compute, or a microcap-excluded/out-of-universe symbol all degrade to NaN, never a fabricated 0.0 (CONSTRAINT #4). Zero cost (a dict lookup on already-computed data); no changes to reporting/state_snapshot.py (it already read the correct snake_case keys) or main._build_context_extras(). Tests: test_advisory.py::TestContextExtrasThreading.

## DEAD-CODE RESOLUTION — REUSE, DELETE, ACTIVATE (2026-07-09)

A repo-wide dead-code sweep resolved three classes of cruft with no silent behavior change on a fresh checkout. REUSE: grossMargins (emitted but previously unconsumed) now feeds quality_factor_score — processing_engine.calculate_fundamental_metrics now sets it = mean of whichever are present among {returnOnEquity, operatingMargins, grossMargins} (MEAN not SUM, so 1/2/3-metric tickers share one cross-sectional z-scale; identical Quality_Z to the old two-metric sum when all tickers carry the same two), falling back to -debt_to_equity only when none present; currentRatio now rides through the new FundamentalDataDTO.current_ratio field into engine/advisory key_indicators["current_ratio"]. DELETE: revenueGrowth + its _prior_annual helper (emitted-fundamentals count 15→14); six dead momentum ROC intermediates (kept only ROC_12M/ROC_6M, the ones consumed downstream); the legacy reporting_engine.py + daily_report_template.html pair (superseded by diagnostics_and_visuals.generate_html_report, never wired into either entry point); the Google-Cloud-NLP analyze_sentiment + orphaned fetch_and_compile_macro (sentiment is owned by signals/news_catalyst.py FinBERT) — but _fallback_sentiment was RESTORED and is RETAINED solely as the load-bearing BUG-1 regression-guard reference (tests + Gravity BUG-1 assert main_orchestrator uses calculate_sahm_rule, NOT _fallback_sentiment, for the Sahm rule). ACTIVATE (opt-in): new FORECAST_SKILL_WEIGHTING_ENABLED (default False, mirroring FORECAST_USE_GARCH_SIGMA) injects a persistent forecasting.forecast_tracker.ForecastTracker (self-provisioning its forecast_errors table) into every ForecastingEngine so the blend weights ARIMA/Monte Carlo/Holt-Winters/CNN-LSTM by inverse recent RMSE instead of fixed fractions; default-off ⇒ tracker=None ⇒ byte-identical static blend. FORECAST_SKILL_WINDOW_DAYS default raised 60→180 (it MUST exceed the 90-day max horizon or h=60/h=90 could never warm up). Tests: test_processing_engine.py, test_yahoo_fundamentals.py, test_advisory.py, test_macro_engine.py, NEW test_forecast_skill_uplift.py (slow-marked).

## PROGRESS-INDICATOR SERIES — FILE-BACKED PIPELINE PROGRESS + LIVE GUI BAR (reporting/progress.py, PR #206, 2026-07-10)

New reporting/progress.py provides a cross-cutting, dead-letter-safe progress contract shared by the orchestrator, the desktop daemon, and the GUI (distinct from the OLDER Market-Data BatchQuoteFetcher streaming bar). ProgressReporter(stages, *, run_id, output_dir) is instantiated once/cycle with the ordered stage names: start_stage(name, symbols_total), thread-safe advance_symbol(message) (safe from the parallelized per-ticker worker pools), set_message(msg), finish(state="succeeded"|"failed"|"cancelled") — every call ATOMICALLY writes output/progress.json (write-then-rename, mirroring kill_switch.activate()) with {run_id, state, stage, stage_index, stage_total, symbols_done, symbols_total, percent, message, started_at, updated_at}. read_progress(output_dir) degrades a missing/unreadable/malformed file to None, never a fabricated/partial dict (CONSTRAINT #6); clear_progress() resets at run start. Consumed by desktop/daemon_runtime.RunRecord (new progress field, surfaced via api/control_api /status + gui/daemon_client) and gui/orchestrator_runner.compute_run_progress(handle) → RunProgress(percent, label, indeterminate) which normalizes BOTH a subprocess run's progress.json and a daemon run's RunRecord.progress into one shape for the Launcher tab's live st.progress bar polling at PROGRESS_POLL_SECONDS (new setting, default 5s, GUI-writable non-secret allowlist key). NEW gui/progress_ui.py::busy(label): a lightweight busy-indicator context manager independent of ProgressReporter, applied across previously-silent one-shot GUI buttons (e.g. "Sync Now") for immediate visual feedback. Gravity step_74_progress_indicator_audit.

## KELLY SIZING DEGRADES INSTEAD OF DEAD-LETTERING ON DB BACKEND OUTAGE (transactions_store.py, commit 92d37004, 2026-07-10)

TransactionsStore() construction does an eager connection (Base.metadata.create_all), so a remote-backend connectivity failure (DNS/network — e.g. an unreachable Supabase host) raised straight out of engine.advisory._get_transactions_store() and StrategyEngine.transactions_store, DEAD-LETTERING EVERY symbol's advisory evaluation for the cycle over what is, for the advisory pipeline, an optional position-sizing refinement. NEW _OfflineTransactionsStore: a read-only stub whose closed_trades_df()/open_trades_df()/get_trade_history() return empty DataFrames (the same "zero closed trades" cold-start shape sizing/kelly.py already falls back from to volatility_target_weight). Both lazy-construction call sites now CATCH a construction failure, LOG ONCE, and substitute the stub — degrading Kelly sizing to the vol-target fallback instead of aborting the cycle; the stub is CACHED on the singleton/property so a DB outage doesn't retry-storm the failing host once per ticker. record_trade()/close_trade() STILL RAISE on the stub — a trade that was never persisted must not be fabricated as recorded (CONSTRAINT #4). Tests: tests/test_transactions_store.py; Gravity step_74_db_backend_resilience_audit (renumbered step_75_db_backend_resilience_audit by PR #209 — see below).

## GRAVITY 6-CHECK REPAIR (commit 087c49aa, 2026-07-10)

A Gravity run reported 6 failures out of 74 steps — each a STALE CHECK ASSERTION, not a production bug (the audited code had legitimately moved during prior refactors and the check wasn't updated to follow it). Repaired: step_26 (fundamentals-cache dedup test never forced the primary YahooFundamentalsProvider to fail, so with live network it never exercised the yfinance fallback under test); step_45 (the exc_info=True crash-handler scan only looked at _main_body, but the progress-indicator-core work / PR #206 split it into a thin wrapper + _main_body_impl where the crash-logging call now lives); step_51 (checked main.py's raw text for state-snapshot wiring extracted into reporting/state_snapshot.py + reporting/html_publisher.py); step_55 (kill-switch string assertions moved into pipeline/steps.py); step_65 (STRATEGY_REGISTRY entries are now 3-tuples adapter_fn/turnover/universe, not 2-tuples); step_66 (ALL_CHECKS count updated to 22).

## GRAVITY SPLIT-BRAIN CONSOLIDATION (PR #209, commit 2f461a70, 2026-07-10)

Two parallel Gravity implementations had drifted apart: the canonical Gravity AI Review Suite.py launcher (the one actually invoked by the GUI Gravity tab and CI) and an UNIMPORTED gravity/__init__.py package carrying 11 unique audit steps no runner ever executed — a split-brain where the package looked authoritative but was dead code, so its checks silently protected nothing. DELETED gravity/__init__.py (no importer anywhere — confirmed by grep/AST sweep) and MIGRATED its 11 unique steps into the launcher as step_76–86: help-content (76) + help-explainers/anchor-contract (77), advisory agent (78), trade signals (79), Robinhood orders (80), Robinhood execution bridge (81), LLM commentary (82), AI Gravity runner (83), AI Insights (84), Opal research (85), AI Control Center (86). RENUMBERED internal step references to resolve the pre-existing collision where the progress-indicator audit (PR #206) and the DB-backend-resilience audit (commit 92d37004) had both shipped as step_74 the same day: progress-indicator keeps step_74_progress_indicator_audit and DB-backend-resilience moved to step_75_db_backend_resilience_audit; the help-anchor/explainers check is now step_77_help_explainers_audit.

TIER 9 SCOPE 4 — OPAL RESEARCH AGENT (llm/research.py, OpenAI/GPT, 2026-07): A third advisory-only AI agent — "Opal" — running on OpenAI/GPT via NEW OpenAIProvider (llm/providers.py): lazy `import openai`, client constructed as `openai.OpenAI(api_key=..., timeout=timeout_seconds)` (timeout at CLIENT INIT, mirroring ClaudeProvider — never signal.alarm), call_structured() uses the openai>=1.40 SDK helper `client.beta.chat.completions.parse(response_format=schema_model)` which returns an already-validated pydantic instance via `.message.parsed` (deliberately NOT a hand-rolled `response_format={"strict":True,...}` payload, which 400s against a bare pydantic model_json_schema()). Opal is a FRONT-OF-PIPELINE research agent: for a symbol it produces a qualitative ResearchBrief (thesis_context, catalysts, risk_factors, recent_developments, data_confidence, sources_note — NO numeric field, structurally enforced via llm/schemas.py::ResearchBrief) grounded on REAL retrieved Finnhub company_news + earnings_calendar via signals/news_catalyst.py's now-PUBLIC fetch_company_news/fetch_next_earnings/build_finnhub_client (promoted from private `_fetch_company_news`/`_fetch_next_earnings`/`_build_finnhub_client` — same rename, same behavior, single-diff commit — so llm/research.py never reaches into another module's private surface). llm/research.py::generate_research_brief(symbol, context=None, *, provider=None, grounding_fn=None) is opt-in gated on NEW settings.OPAL_RESEARCH_ENABLED (default False, INDEPENDENT of LLM_COMMENTARY_ENABLED) → cache_get (score=0.0/action="RESEARCH" pinned, mirrors llm/chart_insight.py's non-scored-artifact pattern) → _gather_grounding() → provider.call_structured → cache_put → return; soft-fails to None at every step (CONSTRAINT #6, never invents grounding data — CONSTRAINT #4). llm/router.py::get_research_provider() gated on OPAL_RESEARCH_ENABLED + OPAL_RESEARCH_PROVIDER=="openai" (Opal is NOT flexibly routed like Claude/Gemini rationale/alerts — one supported provider today) + OPENAI_API_KEY. THREAD-IN: engine/advisory.py adds Recommendation.research_brief (additive trailing field, Optional[Dict[str,Any]]=None); enrich_with_llm_rationale() now runs Opal FIRST into a local working_context copy (never mutates the caller's dict), injects working_context["research_brief"] BEFORE the Claude call so llm/commentary.py::_format_rationale_user_prompt(rec_skeleton, context) can append a "Research context" block (purely additive — absent context is byte-identical to pre-Opal); Opal's and Claude's failures are independently soft-fail. CLI: `python -m engine.opal_research SYMBOL` (always exits 0, preview tool). GUI: gui/ai_insights_panel.format_research_brief_markdown (mirrors format_chart_pattern_markdown's None/partial-safe contract) + gui/panels._render_opal_research_section rendered as Section 0 at the TOP of the AI Insights tab (front-of-pipeline), gated on Opal's OWN master switch. The pre-existing AI Control Center Opal button needed ZERO changes — gui.ai_control_center.opal_built() now returns True automatically since llm/research.py genuinely exists (confirmed by Gravity step_78 check 9, updated from asserting not_built to asserting the auto-activation). NEW SETTINGS: OPAL_RESEARCH_ENABLED(False)/OPAL_RESEARCH_PROVIDER("openai")/OPAL_RESEARCH_MODEL("gpt-4o")/OPAL_RESEARCH_TIMEOUT_SECONDS(15)/OPENAI_API_KEY(None, SECRET_KEYS-only, CONSTRAINT #3). requirements.txt: openai>=1.12.0 bumped to openai>=1.40.0 (needed for .beta.chat.completions.parse()). TESTS (5 new files): tests/test_openai_provider.py, tests/test_research_brief.py, tests/test_opal_pipeline_integration.py, tests/test_gui_env_io_openai_key.py, tests/test_opal_research_panel.py. GRAVITY step_77_opal_research_audit (10 checks: module surface, OpenAIProvider.call_structured callable, ResearchBrief bounds + NO numeric field via type-based model_fields check, no top-level openai import, no order verbs, opt-in default-off, threading grep, OPENAI_API_KEY secret-only, all 5 test files exist) + ai_verification_prompts.STEP_8_PROMPT + engine/gravity_ai_runner._STEP_FILE_MAP[8].
