# Implementation Plan: feat-settings-census-gate

This implements Work Package E.

- Add test in `test_measure_settings_census.py` for all currently known fields.
- Add test in `test_no_missing_call_timeouts.py` guarding against new instances of missing timeouts.
