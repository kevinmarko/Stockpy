# InvestYo Quant Platform — runtime configuration template.
# Copy this file to `.env` and fill in real values. NEVER commit `.env`.
# All keys map 1:1 to fields on the Settings class in settings.py.

# --- Secrets / credentials ---
# Required for live macroeconomic data. Get a free key:
#   https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=

# Optional broker credentials (leave blank if unused).
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_PAPER=true

# --- Financial constants (override only if you know what you're doing) ---
RISK_FREE_RATE=0.045
MARKET_RISK_PREMIUM=0.055
REQUIRED_RETURN_RATE=0.08
MAX_PORTFOLIO_HEAT=0.06

# --- Position sizing (sizing/kelly.py, sizing/vol_target.py) ---
KELLY_FRACTION=0.5
KELLY_CAP=0.20
VOL_TARGET=0.10
MAX_LEVERAGE=2.0
MAX_POSITION_WEIGHT=1.0

# --- Multifactor signal (signals/multifactor.py) ---
# Tickers with Market Cap below this (USD) are excluded from cross-sectional
# z-scoring and receive a neutral score rather than fabricated factor exposure.
MULTIFACTOR_MICROCAP_THRESHOLD=300000000

# --- Pre-trade risk gate (execution/risk_gate.py) ---
# Maximum pairwise absolute return correlation with any existing position before
# a new BUY order is blocked. Default: 0.85.
MAX_CORRELATION=0.85
# Halt new BUY orders when intraday P&L drops below this fraction of start-of-day
# account equity. Default: 0.02 (= 2%).
DAILY_LOSS_LIMIT_PCT=0.02
# Maximum order submissions in any 60-second rolling window. Default: 10.
MAX_ORDER_RATE_PER_MIN=10
# Block new long orders when HMM risk-off probability exceeds this. Default: 0.80.
HMM_RISK_OFF_BLOCK_THRESHOLD=0.80
# Enforce NYSE RTH (09:30–16:00 ET). Set to false for crypto / 24h markets.
RISK_GATE_ENFORCE_MARKET_HOURS=true

# --- Kill switch (execution/kill_switch.py) ---
# If True, log CRITICAL reminder to flatten positions when kill switch activates.
# Automatic flattening is not yet implemented; manual action is still required.
FLATTEN_ON_KILL=false

# --- Order manager (execution/order_manager.py) ---
# Log intended orders but do not submit to broker. Safe default for CI.
DRY_RUN=false
# Slack or Discord incoming-webhook URL for reconciliation-drift CRITICAL alerts.
# Leave blank to disable webhook alerting (drift is still logged to CRITICAL).
ALERT_WEBHOOK_URL=

# --- Runtime / IO ---
OUTPUT_DIR=./output
# JSON array of ticker symbols.
DEFAULT_TICKERS=["AAPL", "MSFT", "JNJ", "AGNC"]
LOG_LEVEL=INFO
