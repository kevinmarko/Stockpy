# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Branch Workflow

Antigravity IDE has resumed working on this repo alongside Claude Code (as of 2026-07-20). There is
**no fixed multi-agent domain split or per-agent branch-prefix convention** this time — the operator
assigns work to each agent per-task, ad hoc, rather than via a structural file-domain split. Do not
defer to, flag PRs for, or avoid editing any file on the assumption another agent owns it; any file
may be worked on by whichever agent is assigned the task at hand. Branch naming (lowercase-kebab, e.g.
`fix-hmm-lookahead`) is unchanged and applies the same way regardless of which agent opens the branch.

### Start-of-session checklist
1. `git fetch origin && git rebase origin/main` — sync from main before starting.
2. **Low-risk changes** — docs, `.claude/` config (settings, hooks, skills, agents), comments,
   test-only additions, and other non-behavioral edits — may be committed directly to `main`
   after a self-review pass (re-read the full diff, confirm it does what it's supposed to and
   touches nothing else). No branch or PR required for these.
3. **Everything else** — anything touching engines, signals, execution, sizing, validation,
   orchestrators, or other runtime/trading logic — always goes through
   `git checkout -b <short-description>` (lowercase-kebab, e.g. `fix-hmm-lookahead`) and a PR.
   Never commit these directly to `main`.
4. Open a PR when a feature-branch change is complete; do not squash or amend published commits.
5. **PR Artifacts & Unique Naming**: When submitting a Pull Request, you must copy and commit the implementation plan, task tracker, and walkthrough from your local brain artifacts directory to the `.claude/` folder on your branch. To prevent collisions and accidental overwrites when multiple plans or concurrent agent tasks run in parallel across the repository, artifact and plan files must use unique, project/feature-scoped names matching the task or branch (e.g., `.claude/fmp_pipeline_optimization_implementation_plan.md`, `.claude/fmp_pipeline_optimization_task.md`, `.claude/fmp_pipeline_optimization_walkthrough.md`) alongside standard base files. Test files created for a feature must likewise carry clear, project-scoped names.
   **This rule is not limited to PR-submission time and applies to every AI agent working in this
   repo (Claude Code, Antigravity, or any other), not just whichever one authored a given file.**
   Bare, unscoped filenames — `plan.md`, `implementation_plan.md`, `walkthrough.md`, `task.md`,
   `tracker.md`, or any other generic name with no task/feature/branch qualifier — must never be
   created anywhere in the repo (`.claude/`, `.agents/`, docs, scratch dirs, or elsewhere the
   repo tracks), including transient ones an agent intends to clean up later: a second concurrent
   agent can read, edit, or collide with it before that happens. Always prefix with the
   task/feature/branch slug the same way the `.claude/fmp_pipeline_optimization_*` example above
   does, for every plan, implementation plan, walkthrough, and task-tracker file, whether it's a
   local working artifact or one committed with a PR.
6. **After merging any PR**, sync the local main checkout so the next session (in this or any
   other worktree) starts from the merged state instead of a stale `main`:
   `git -C <main-checkout-path> fetch origin && git -C <main-checkout-path> merge --ff-only origin/main`.
   Do this immediately after the merge, not at the start of some future session.

## Agent Workflow: Verification & Planning

Applies to whichever agent is doing the work — Claude Code or Antigravity — same framing
as Branch Workflow above: no per-agent carve-out.

- **Verification is mandatory, not advisory.** A task is not done until the relevant check
  has actually been run and shown to pass — not "should pass." Python changes: the
  corresponding `tests/test_<module>.py` (or the fuller `make ci` gate) must show zero
  failures. `webapp/src/` changes: `npm run --prefix webapp typecheck` clean AND, for
  anything UI-visible, an actual `npm run dev` + browser check (console errors + visual
  confirmation) — a clean typecheck alone proves the code compiles, not that a screen
  renders or behaves as intended.
- **Claude Code enforces the Python half automatically.** `.claude/hooks/verify_targeted_tests.sh`
  (`PostToolUse`, non-blocking) runs the mapped `tests/test_<module>.py` after every edit to a
  tracked `.py` file and surfaces a failure inline. `.claude/hooks/verify_before_stop.sh` (`Stop`)
  is the actual enforcement gate — it blocks ending a turn while a targeted test tied to
  uncommitted changes is still failing, capped at 2 consecutive blocks per session before
  releasing itself with a warning instead, so a genuinely stuck check can't deadlock the
  session. `/verify` and `/verify-webapp` (`.claude/commands/`) run the fuller gate and the
  browser check on demand. `.claude/hooks/block_env_write.sh` (`PreToolUse` on `Edit|Write`)
  hard-denies any write to a file named exactly `.env`. `.claude/hooks/webapp_typecheck.sh`
  (`PostToolUse` on `Edit|Write`) runs `npm run --prefix webapp -s typecheck` after any edit
  under `webapp/src/**` — the mock/live API parity gate.
- **Antigravity has no equivalent automatic blocking gate.** Live validation confirms that its `Stop` hook does not expose "force continuation" semantics, and its `PreToolUse`/`PostToolUse` shell hooks do not natively intercept file-editing tools in the IDE runtime. Therefore, on the Antigravity side, "don't mark a task done until tests pass" remains strictly **policy-only**, enforced by prompt adherence rather than system-level blocking. This is a real, open gap, not a claimed equivalence. `.agents/hooks/stop_test.sh` (`Stop`) is the advisory-only port of the Python check that does exist: it re-runs whichever mapped `tests/test_<module>.py` files correspond to uncommitted `.py` changes and surfaces a failure to stderr, but — unable to block — never enforces it. `.agents/hooks/block_env_write.sh` and `.agents/hooks/webapp_typecheck.sh` port the two hooks above to Antigravity's own I/O contract (`.agents/hooks.json`'s `PreToolUse`/`PostToolUse`, matching `default_api:write_to_file|default_api:replace_file_content|default_api:multi_replace_file_content`).
- **Plan before building in the "Everything else" tier** (engines, signals, execution, sizing,
  validation, orchestrators — as scoped in the Start-of-session checklist above): produce an
  Implementation Plan and get it reviewed before writing code. Claude Code: `EnterPlanMode`.
  Antigravity: its native `implementation_plan.md` artifact.
- **Every Implementation Plan must include an explicit documentation-update step** —
  identify which of `CLAUDE.md`/`AGENTS.md`, `docs/architecture/*.md`,
  `docs/signals/<name>.md`, or other `docs/` files need touching for the change, and
  scope those edits into the plan itself. This applies to both agents and to any task
  producing a plan, not just the "Everything else" tier above — documentation is part
  of the deliverable, not a separate pass to get to later.
- Steer a plan or diff you're unhappy with via inline comments on the artifact itself, not a
  fresh open-ended re-prompt — targeted feedback keeps the agent from losing the thread.
- **Decompose isolated, mechanical subtasks to a subagent** (writing/extending a test file, a
  migration, a schema stub) instead of doing them inline in the primary agent's context.
  `test-writer` (`.claude/agents/test-writer.md` / `.agents/agents/test-writer.md`) is the
  ready-made subagent for "write tests for `<module>`." For genuinely hard multi-file
  refactoring or complex logic, don't default a subagent to a fast/cheap model tier: Claude
  Code — leave `model:` unset so it inherits the calling session's model (as `test-writer`
  does); Antigravity — use `model: pro` (as `.agents/agents/test-writer.md` does).
- **`CLAUDE.md`/`AGENTS.md` now auto-sync.** These two files are meant to be exact mirrors
  (`AGENTS.md`'s own first line is literally `# CLAUDE.md`) but had already drifted by one
  real bullet before this existed. `.claude/hooks/sync_agent_docs.sh` /
  `.agents/hooks/sync_agent_docs.sh` now copy whichever of the two files was just edited onto
  the other automatically — you should no longer need to hand-mirror an edit to one into the
  other.

## Project

InvestYo Quant Platform ("Stock Dashboard Py") — an automated quantitative analysis pipeline: fetches market/macro data, computes technical & fundamental indicators, runs multi-horizon forecasts, backtests strategies, persists signals to SQLite, and publishes results to Google Sheets / an HTML report.

### Key documentation files
| File | Purpose |
|------|---------|
| `docs/README.md` | Master index of the full `docs/` library (~74 files) — start here if you don't already know a doc's path; this table covers only the load-bearing subset |
| `docs/architecture.md` | Mermaid data-flow diagram (Engines → DTOs → Signals → Strategy → Advisory → Broker [quarantined]) |
| `docs/architecture/*.md` | Per-module architecture reference, split out of this file's `## Architecture` section by subsystem (data layer, signal engines, execution, GUI/webapp, testing, etc.) — see the index table under `## Architecture` below |
| `docs/signals/README.md` | Index of all 17 registered `SignalModule` implementations with academic references, logic, and failure modes |
| `docs/signals/<name>.md` | Per-strategy README for each signal module |
| `docs/known_issues/README.md` | Index of 28 dated production-issue write-ups (root cause, what was tried, fixed/resolved/mitigated status) |
| `docs/incident_log.md` | Template + log for production incidents (referenced by RUNBOOK.md §6) |
| `docs/HOW_TO_GUIDE.md` | End-user guide for every platform feature |
| `docs/regime_model_tuning_guide.md` | Regime model tuning guidebook covering Hamilton 1989 Gaussian HMM, hyperparameters, and CLI audit tooling |
| `docs/RUNBOOK.md` | Operational runbook — pre-market checklist, incident playbooks, advisory pause procedure |
| `docs/GO_LIVE_CHECKLIST.md` | Pre-live checklist (all automatable items covered by `preflight_check.py`) |
| `docs/FEATURE_TIER_HISTORY.md` | Detailed dated changelog for every Tier/Task/Scope feature shipped since 2026-06 (ML Stage 4, GUI tab build-outs, Tier 1-9 advisory/LLM features, AI Control Center, Prompt Registry) — full backstory, test surface, and Gravity step numbers per subsystem |
| `docs/test_coverage_analysis.md` | Test-coverage audit — suite inventory, module-level gap table, and prioritized roadmap for improving test coverage |
| `docs/BUG_HUNTING_PROCESS.md` | Bug hunting SOP, 4-tier severity model, 5-phase workflow, domain checklists, and verification tooling guide |
| `docs/VALIDATION_STRATEGY_FIX_LOG.md` | Dated rollup of `STRATEGY_REGISTRY` deployability-gate fix attempts (`scripts/refresh_validations.py`) — before/after PBO/DSR/Sharpe/MaxDD per strategy, the causal lever used, and (for strategies that stayed honestly `deployable=False`) the measured, evidence-backed reason why. Cross-references the fixing PR and each strategy's `docs/signals/<name>.md` **Backtest Validation** section |
| `docs/AGENTIC_TRADING_SAFETY_FRAMEWORK.md` | Capability map, guardrail status, and structural gaps for the agentic trading surface |

## Frontend strategy: web app only — desktop app decommissioned

**The Pilots PWA (`webapp/`) is the platform's one actively-developed frontend.** All new
features, UI improvements, and bug fixes for the operator-facing surface go into `webapp/`
going forward. If a `gui/`-only feature has no `webapp/` equivalent yet, treat closing that
gap as `webapp/` work — not as a reason to extend `gui/`.

**The desktop app is decommissioned — do not develop it further.** This covers:
- `gui/` — the Streamlit "InvestYo Command Center" (all 18 tabs, `gui/panels*.py`, `gui/app.py`).
- `app_shell.py` and `desktop/`'s native-shell support modules (`net_util.py`, `ui_server.py`,
  `engine_supervisor.py`) — the pywebview wrapper that pops `gui/` into a native window.
- Their launchers: `launch_app.command`, `launch_gui.command`, `scripts/build_macos_app.command`.

These remain in the repo and still run for existing local setups — a severe bug blocking use
of the platform entirely may still be patched — but treat them as frozen: no new tabs, panels,
settings widgets, or capability.

**Unaffected — this is the data/compute layer both frontends sit on, and `webapp/` depends on
it being maintained:** `main.py`, `main_orchestrator.py`, every `api/*.py` service, and the
orchestrator daemon (`desktop/daemon_runtime.py`, `desktop/orchestrator_daemon.py`,
`desktop/run_history_store.py` — these live under `desktop/` alongside the decommissioned
native-shell modules but are backend infrastructure the web app's Control/Pilots API depends
on, not part of the desktop app). Only the Streamlit GUI and its native-window wrapper are
being sunset.

## Commands

```bash
# ── Web app (primary interface) ────────────────────────────────────────────────
./launch_webapp.command              # Starts Pilots PWA + backend APIs automatically (Mock or Live)
cd webapp && npm install
npm run dev                          # http://localhost:5173 — offline mock data by default
VITE_USE_MOCK=false npm run dev      # against the live backend (see below)
uvicorn api.pilots_api:app --port 8602    # backend the PWA talks to (+ data_api:8603 / metrics_api:8604 as needed)

# ── Legacy desktop app — decommissioned, no new development ───────────────────
# gui/ (Streamlit "InvestYo Command Center") and app_shell.py/desktop/'s native-shell
# modules are frozen; see "Frontend strategy" above. Kept runnable for existing setups.
#   chmod +x launch.command launch_app.command   # one-time setup, already done
# REFRESH_INTERVAL_SECONDS at the top of launch.command controls single-run (=0)
# vs interval-loop (>0, default 60 s) mode.
# The script verifies .venv exists and that Python is exactly 3.12.x before
# launching, then pauses ("Press any key") on exit so errors are always visible.

./launch_app.command                 # legacy: opens the unified Command Center in a native
                                      # desktop window (pywebview) with an always-on
                                      # background refresh loop tied to the window's lifecycle
python3 app_shell.py [--interval N]  # what launch_app.command runs under the hood
./scripts/build_macos_app.command    # ONE-TIME (legacy): builds InvestYo.app (real macOS app
                                      # bundle, custom icon, no Terminal flash) that runs
                                      # launch_app.command — drag the result into /Applications
                                      # or the Dock; not committed to git (bakes in an absolute
                                      # path — see .gitignore)
streamlit run gui/app.py             # legacy: InvestYo Command Center — full 18-tab operational GUI
./launch_gui.command                 # legacy: same as above, macOS double-click launcher

# ── Backend pipeline (unaffected by the frontend decommission) ────────────────
./setup.sh                       # creates .venv (Python 3.12, via uv — https://astral.sh/uv), installs requirements.txt
# Everything below works either after `source .venv/bin/activate`, or prefixed
# with `uv run` (e.g. `uv run pytest`) to skip activation entirely — uv
# auto-discovers .venv in the repo root, no pyproject.toml required.
python3 main.py                  # clean advisory orchestrator — runs one full cycle (or loops with --interval N); use --refresh-account to force Robinhood re-auth
python3 main.py --interval 60   # refresh market data every 60 s; Robinhood account fetched at most once/day
python3 main.py --refresh-account  # force fresh Robinhood login on this launch, then resume normal daily-cache behavior
python3 main_orchestrator.py     # newer async master orchestrator (data acquisition, schema validation, HTML report compilation)
pytest                           # run test suite
pytest tests/test_quantitative_models.py            # run a single test file
pytest tests/test_quantitative_models.py::test_graham_number_imaginary_bounds  # run a single test
python3 database_setup.py        # (re)build the SQLite schema in quant_platform.db from config.COLUMN_SCHEMA
python3 -m validation.harness --strategy <name> --start YYYY-MM-DD --end YYYY-MM-DD # run strategy validation harness
python -m scripts.refresh_validations --strategies <name> --workers 4 # walk-forward strategy validation cadence (concurrent)
python scripts/preflight_check.py            # pre-live readiness gate (exit 0 = all pass)
python scripts/preflight_check.py --json     # machine-readable JSON output
python -m execution.kill_switch --status     # check / activate / deactivate the global kill switch
make verify                                  # env-var check + pytest + one live run_once() + print summary
./verify.command                             # same as make verify, macOS double-click
```

`main.py` and `main_orchestrator.py` both auto-re-exec themselves under `.venv`'s interpreter if not already running inside it — no need to manually activate the venv before running them.

`main.py` symbol universe = `AccountSnapshot.positions` ∪ `WATCHLIST` env var (comma-separated) ∪ `watchlist.txt` (one ticker per line, `#` = comment). Held symbols are always included. **Last-resort fallback:** when held positions AND `WATCHLIST`/`watchlist.txt` are ALL empty, `_load_tickers_from_sheet2()` reads column A of the "Sheet2" tab in the "Stock Dashboard Py" Google Sheet (via `credentials.json`) and uses those tickers instead — it never merges with the other sources, is consulted only when they yield nothing, and degrades silently to `[]` (logged warning, never a crash) when `credentials.json` / the tab / the API is unavailable. New env var: `WATCHLIST`.

## Architecture

Flat, modular "Engine" architecture using dependency injection — no package directories, every engine is a top-level module imported directly by the orchestrators.

The full per-module architecture reference (previously inlined here) is split by subsystem under `docs/architecture/`. Load the file(s) relevant to what you're touching:

| File | Covers |
|------|--------|
| [`docs/architecture/data-layer.md`](docs/architecture/data-layer.md) | `config.py`, `dto_models.py`, `data_engine.py`, `data/market_data.py`, Robinhood client/portfolio/orders, `data/portfolio_sync.py`, `data/historical_store.py`, `data/brokerage_credentials.py` |
| [`docs/architecture/signal-engines.md`](docs/architecture/signal-engines.md) | `processing_engine.py`, `macro_engine.py`, `regime/hmm_regime.py`, `technical_options_engine.py`, `forecasting_engine.py`, `strategy_engine.py`, `sizing/` (position sizing single source of truth) |
| [`docs/architecture/simulation-eval-reporting.md`](docs/architecture/simulation-eval-reporting.md) | `simulation_engine.py`, `universe_engine.py`, `research_engine.py`, `evaluation_engine.py`, `transactions_store.py`, `.agents/skills/`, `database_setup.py`, `diagnostics_and_visuals.py`, `reporting/html_publisher.py`, `reporting/progress.py` |
| [`docs/architecture/execution.md`](docs/architecture/execution.md) | `execution/cost_model.py`, `broker_base.py`, `alpaca_broker.py`, `kill_switch.py`, `risk_gate.py`, `order_manager.py`, `queue_builder.py`, `compose.py` |
| [`docs/architecture/observability-and-apis.md`](docs/architecture/observability-and-apis.md) | `observability/*`, `api/state_api.py`, `api/control_api.py`, `api/pilots_api.py`, `llm/status_store.py`, `investyo_mcp_server.py`, `mcp_remote_adapter.py` |
| [`docs/architecture/webapp-and-gui.md`](docs/architecture/webapp-and-gui.md) | `webapp/` (Pilots PWA), `api/data_api.py`, `api/metrics_api.py`, `gui/daemon_client.py`, `app_shell.py`/`desktop/` (daemon runtime, orchestrator daemon), `gui/` (InvestYo Command Center), `scripts/*`, `docs/GO_LIVE_CHECKLIST.md`, `docs/RUNBOOK.md` |
| [`docs/architecture/validation-and-signals.md`](docs/architecture/validation-and-signals.md) | `validation/purged_cv.py`, `metrics.py`, `harness.py`, `stress_scenarios.py`, `signals/` package (registry, aggregator, all `SignalModule` implementations) |
| [`docs/architecture/ml-and-reports.md`](docs/architecture/ml-and-reports.md) | `ml/` pipeline, `reports/*.html.j2` templates, `ai_verification_prompts.py` (Gravity AI Auditor) |
| [`docs/architecture/testing.md`](docs/architecture/testing.md) | Index of every file under `tests/` and what each covers |
| [`docs/architecture/orchestration-entrypoints.md`](docs/architecture/orchestration-entrypoints.md) | `engine/__init__.py`, `engine/advisory.py`, the `.env` loading convention, `alerting.py`, `Makefile`, `verify.command`, `main.py`, `main_orchestrator.py` |

 ## Conventions enforced in this codebase

 - **In-app help content (`gui/help_content.py`)**: All operator-facing explainer prose MUST live in `gui/help_content.py` — never hard-code tooltip/expander text directly in `gui/panels.py`. The single-source-of-truth is `GLOSSARY` (terms), `TAB_HELP` (10 tab descriptions), `SECTION_HELP` (section-level tooltips), and `METRIC_HELP` (KPI tooltips keyed by `"<tab>.<metric>"`).
 - **Help-key convention**: Metric tooltip keys follow the form `"<tab>.<metric_name>"` — looked up by `gui.help_content.metric_help(key)`. A missing key returns `""` and renders no tooltip; it **never raises** (CONSTRAINT #6). Never add a default-fallback value in `metric_help` — empty string is the correct sentinel.
 - **Anchor-contract invariant**: Every `GlossaryEntry.guide_anchor` and `TabHelp.guide_anchor` field **must** resolve to a real heading slug in `docs/HOW_TO_GUIDE.md`. Enforced by `tests/test_help_content.py::TestAnchorValidity` (CI) and `Gravity AI Review Suite.py`'s `step_77_help_explainers_audit` check 3. When renaming a heading in HOW_TO_GUIDE.md, grep `gui/help_content.py` for the old slug and update it.
 - **Thresholds in help text**: Values cited in help strings (VIX thresholds, Kelly caps, score gates, …) MUST be read from `settings`, `validation.thresholds`, or `engine.advisory.CONFIG` at module import time — never re-typed as literals in `help_content.py`. This ensures help text stays in sync with the live config without a separate update step.
 - **In-app help content, webapp (`webapp/src/help/helpContent.ts`)**: the Pilots PWA's own curated TypeScript port of the above pattern, scoped to its screens — NOT auto-generated from `gui/help_content.py`, kept independently. `TAB_HELP` (one entry per screen — as of PR #366, all 19 screens have one, keyed by a stable per-screen slug documented in each screen's `<TabGuide tabKey="...">` call) + `GLOSSARY` (term → static string, or `(t: Thresholds | null) => string` for any value sourced from live `GET /thresholds` — never a hard-coded literal for a tunable value; a fixed algorithm constant that isn't part of the `Thresholds` API shape, e.g. the pairs z-score/half-life bands or the macro kill-switch's Sahm/VIX/HY-OAS levels, is fine to state literally IF commented explaining why it isn't threshold-sourced, mirroring the file's own `iv rank`/`vrp` precedent). Rendered by `<TabGuide tabKey=… />` (`webapp/src/components/TabGuide.tsx`) as a dismissible "How this works" panel, expanded on a screen's first visit only. **No anchor-contract field** — `TabHelp`/glossary entries here have no `guide_anchor` equivalent and are NOT checked against `docs/HOW_TO_GUIDE.md`; don't assume the Python GUI's anchor-validity test/Gravity check applies to this file, it doesn't. `helpContent.test.ts`'s `TAB_HELP content integrity` test does enforce that every `keyConcepts` entry resolves to a real `GLOSSARY` key.
 - All data crossing into calculation code goes through the DTOs in `dto_models.py`, not raw dicts/lists.
 - Data fetching always goes through `IDataProvider` implementations in `data_engine.py`.
 - Technical/fundamental math is vectorized — no per-row Python loops.
 - Loops over tickers (in `data_engine.py`, orchestrators) wrap each ticker in try/except so one bad symbol doesn't abort the whole run.
 - New/changed indicators need a corresponding test in `tests/`; numeric drift on existing indicators must stay below 1e-5.
 - Every indicator and forecaster must be verified to have zero lookahead bias using the perturbation tests in `tests/`.
 - Options premium selling (e.g. Put Credit Spreads, Iron Condors) is gated by VRP regime rules: must have `true_ivr > 50`, `VRP > 0.02`, `VIX < 30`, and no `CREDIT EVENT`. If gated, recommender returns `Cash/Wait`. The VRP leg fails closed (CONSTRAINT #6) on an unmeasurable value: `vrp=None` (no options chain available to derive one — 5 read-only display-path callers, `gui/panels/options_matrix.py`, `reporting/options_snapshot.py`, `investyo_mcp_server.py`, `options_ondemand.py`, `api/metrics_api.py`, pass this deliberately and so only enforce the other 3 conditions) SKIPS the VRP leg entirely, but a *computed* VRP that came back `NaN` (`get_vrp()` couldn't resolve a real options-chain-derived IV — the production pipeline path, `pipeline/production_steps.py::OptionsAnalysisStep`) must gate closed exactly like a VRP below threshold — see `technical_options_engine.py::generate_strategy_pricing_matrix`'s VRP gate comment and `tests/test_vrp_gate.py`.
 - Pairs trading signals are driven by Engle-Granger cointegration test, half-life of mean reversion (between 5 and 60 days), Kalman filter dynamic hedge ratio, rolling spread z-score (entry at |Z| > 2.0, exit at 0 cross or cointegration break rolling ADF p > 0.10, stop loss at |Z| > 4.0).
 - Every backtest execution (e.g. in `simulation_engine.py`) must display the survivorship bias warning and report.
 - Backtests must use the custom `TieredCostModel` for transaction commissions and slippage calculations instead of static assumptions.
 - Gate deployable status of trading strategies on PBO < 0.5 AND DSR > 0.95 in verification audits.

- Deployability gate for strategy harness strictly enforces PBO < 0.5, DSR > 0.95, net net-of-cost Sharpe > 0.5, and Max Drawdown < 30%.
- **Documenting a deployability-gate fix or honest FAIL**: when a `STRATEGY_REGISTRY` adapter is changed to move a strategy toward (or honestly away from) `deployable=True`, add a **Backtest Validation** section (before/after PBO/DSR/Sharpe/MaxDD, the causal lever used, and — for a strategy that stays `deployable=False` — the measured, evidence-backed reason) to the corresponding `docs/signals/<name>.md` (where a live signal module exists) and append an entry to `docs/VALIDATION_STRATEGY_FIX_LOG.md`. See that log's 2026-07 entry for the full worked example (12 strategies, `garch_vol_target`/`multifactor_lowvol_size`/`cross_sectional_momentum`/etc.) and the reusable fix levers it documents (Faber SMA-200 trend gate, empirically-measured turnover correction, variant-count reduction backed by measurement).
- **Options-selling strategies carry an additional tail-scenario stress gate** (`validation/stress_scenarios.py`): they are deployable only if max drawdown is < 50% AND the account survives (no blow-up) in EVERY dated shock window (OCT_2008, FEB_2018, MAR_2020, AUG_2024). Construct the harness with `is_options_selling=True` and a `stress_returns_fn(start, end) -> daily returns`; the gate fails closed if an options-selling strategy is never stress-tested. The stress summary is printed at the top of every options-selling validation report.
- Strategy validation harness cost modeling scales linearly with average daily turnover.
- Quantitative scoring in `StrategyEngine.evaluate_security` must be decoupled into pluggable signal modules under the `signals/` package, using the weighted-sum aggregator. Weights are defined in settings.SIGNAL_WEIGHTS.
- Cross-sectional signals (e.g. `CrossSectionalMomentumSignal`) use the **two-phase hook pattern**: `pre_compute(universe_df, context)` runs once per cycle on the full dashboard DataFrame before the per-ticker loop (called via `global_registry.run_pre_compute()`); `compute(row, context)` reads pre-computed ranks from `context.xsec_percentile_ranks`. All existing per-ticker signals inherit the no-op `pre_compute` and are unaffected.
- The 12-1m cross-sectional return is computed by `compute_xsec_momentum_ranks()` in `main_orchestrator.py` using vectorized `iloc` indexing with `skip_days=22` (1 month skip) and `lookback_days=252` (12 months) — no iterrows, no current-month leakage.
- `config.COLUMN_SCHEMA` now includes `XSec_12_1M` (percent) and `XSec_Momentum_Rank` (percent). These must be present in any Pandera-validated DataFrame.
- New trading rules must be optimized in `vectorbt` and validated in `backtrader` before being added to `strategy_engine.py`.
- Option overlay trend filter in StrategyEngine uses a lookahead-free strong uptrend filter (ROC_12M > 0 and price > SMA_200) falling back to legacy trend strength when not provided.
- `SignalModule.is_active_in_regime(macro: MacroEconomicDTO) -> bool` (default `True`) lets a module opt out of contributing to the aggregate score entirely for a cycle. `SignalAggregator.aggregate()` checks this before adding a module's weighted contribution/explanation — `compute()` still runs (its raw output remains in the returned `outputs` dict for introspection), but a `False` module adds nothing to `final_score`/`score_log`. Use this for regime-fragile signals (e.g. `rsi2_mean_reversion`) rather than relying on `compute()` to self-zero, so the suppression is enforced centrally and is impossible to forget per-module.
- **Operator module disable (`settings.DISABLED_SIGNAL_MODULES`)**: a `list[str]` of signal-module names (JSON array in `.env`, default `[]`) that `SignalAggregator.aggregate()` skips — checked right alongside the `is_active_in_regime` gate, so a disabled module adds nothing to `final_score`/`score_log` and does not affect `meta_label_composite`, while its raw `compute()` output still appears in the returned `outputs` dict for introspection. Empty list reproduces the legacy behavior exactly; honored by BOTH orchestrators. The GUI Command Center's Strategy Matrix tab writes this (and `SIGNAL_WEIGHTS`) via `gui/env_io.py`. New env var: `DISABLED_SIGNAL_MODULES`.
- **Macro Regime Gate (`settings.MACRO_REGIME_GATE_ENABLED`, default `True`)**: operator-controlled bypass for `PreTradeRiskGate.macro_kill_switch_check`. When `True` (autonomous mode), the check vetoes new BUY orders whenever `MacroEconomicDTO.killSwitch` is `True` (Sahm Rule ≥ 0.5, VIX > 30, or HY OAS > 6%). When `False` (hybrid mode), the check returns pass immediately without reading `context.macro` — technical signals run without macro override, useful when idiosyncratic volatility produces a false-positive systemic alarm. The GUI Observability tab (Mission Control) provides a toggle button that writes `MACRO_REGIME_GATE_ENABLED` via `gui/env_io.py` and shows a persistent red warning banner when the gate is off. `scripts/preflight_check.py`'s `check_macro_regime_gate_enabled` is a **blocking** failure when gate is off AND `ALPACA_PAPER=False`; warning-only in paper mode. The state snapshot (`output/state_snapshot.json`) surfaces `sahm_rule`, `high_yield_oas`, and `macro_regime_gate_enabled` so the GUI can display recession telemetry without a live FRED call. New env var: `MACRO_REGIME_GATE_ENABLED`. **Fail-closed on missing FRED data (2026-08)**: this is orthogonal to the operator-disable branch above and does NOT change it — it covers the case where the gate is enabled but the FRED snapshot behind `MacroEconomicDTO` was itself incomplete this cycle. `main.py::_build_macro_dto()` and `pipeline/production_steps.py::OptionsAnalysisStep` previously substituted hardcoded benign literal defaults (e.g. `VIXCLS=15.0`, `T10Y2Y=0.5`) whenever a FRED series was genuinely missing (e.g. a total fetch failure), letting `killSwitch`/`market_regime` silently compute a "risk on" verdict off fabricated data instead of failing closed. Fixed via `MacroEconomicDTO.data_unavailable: bool` (set by both construction sites through the shared `macro_engine.py::macro_killswitch_data_unavailable()` helper, plus `MacroEngine.calculate_sahm_rule`'s own fallback signal): `killSwitch` now returns `True` and `market_regime` is forced to `"RECESSION"` unconditionally whenever `data_unavailable` is set, before computing anything off the (possibly fabricated) raw fields. `execution/risk_gate.py::stress_scenario_check` (the premium-sell VIX gate, which reads `context.macro.vix` directly and bypasses `killSwitch` entirely) independently checks the same flag. A second, independent bug fixed in the same pass: `main.py::_build_macro_dto()` used to read `macro_raw.get("SAHMREALTIME", 0.0)` — a key `DataEngine.fetch_macro_raw()` never populates — so this DTO's Sahm-driven kill-switch input was structurally dead (always `0.0`) regardless of FRED health; now sourced from a real `MacroEngine._calculate_sahm_rule_detailed()` call. See `docs/known_issues/macro_killswitch_fail_open_on_missing_fred_data.md` for the full incident write-up and `docs/architecture/signal-engines.md`'s `macro_engine.py` entry for the technical detail. **Populated-but-fabricated blind spot closed (2026-08 follow-up)**: the fix above only caught a genuinely-*missing* FRED key; `data_engine.py::DataEngine.fetch_macro_raw()`'s hardcoded emergency fallback instead *populates* every key with a benign literal, so plain presence-checking alone reported "available" even during a full outage routed through that fallback (the worse of the two failure modes, since it's the more common real-world FRED-down case). Closed via `DataEngine.fetch_macro_raw_detailed()`/`macro_killswitch_data_unavailable(..., fabricated_keys=...)` — see `docs/known_issues/macro_killswitch_fail_open_on_missing_fred_data.md`'s "Follow-up fix" section for the full detail, including a narrower VIX-only silent sub-fallback the same pass found and closed, and the `investyo_mcp_server.py::trigger_macro_engine` fix that stopped hardcoding `kill_switch_active: False`.
- **Persistent orchestrator daemon (`settings.ORCHESTRATOR_DAEMON_ENABLED`, default `False`)**: cutover flag for the always-on background daemon that keeps every heavy pipeline engine warm across cycles instead of a fresh `main_orchestrator.py` subprocess paying full import+construction cost every run (see `desktop/daemon_runtime.py`, `desktop/orchestrator_daemon.py`, `api/control_api.py`, `gui/daemon_client.py` above for the full architecture). `False` (the default) preserves today's exact subprocess behavior everywhere — fresh clones, CI, and other operators are unaffected until the flag is explicitly set. `True` switches BOTH: the desktop shell's always-on refresh loop (`desktop/engine_supervisor.start_engine`) spawns the daemon as a subprocess instead of `main.py --interval N`, and the Launcher tab's manual "Run Pipeline" button (`gui.orchestrator_runner.launch_orchestrator`) triggers a cycle over HTTP against the running daemon instead of spawning a new process. Enabling the daemon locally requires setting it in `.env` (`ORCHESTRATOR_DAEMON_ENABLED=true`) — NOT flipping the code default, which stays `False`. The Launcher tab's HTTP fast path additionally requires `ORCHESTRATOR_DAEMON_TOKEN` to be set (the Control API's `POST /run` is fail-closed by design — see `api/control_api.py` above); `ORCHESTRATOR_DAEMON_ENABLED` alone is enough for the always-on refresh loop (its internal timer calls `trigger_run()` in-process, never through the token-gated HTTP layer). **Operational note:** a stale/orphaned daemon process left holding `ORCHESTRATOR_API_PORT` (default 8601) causes a NEW daemon's own Control API bind to fail; the new daemon still starts (advisory pipeline unaffected) but logs a WARNING and its `output/daemon.json` write is delayed up to 5 s while it waits out a bounded readiness poll — check `lsof -i :8601` / `ps aux | grep orchestrator_daemon` if a fresh daemon launch seems slow to report ready, and kill any stale process before relaunching. **New env vars:** `ORCHESTRATOR_DAEMON_ENABLED`, `ORCHESTRATOR_DAEMON_TOKEN` (secret), `ORCHESTRATOR_API_PORT` (default 8601), `ORCHESTRATOR_INTERVAL_SECONDS` (default 0 = on-demand only; the daemon's own internal timer cadence, distinct from the `--interval` CLI flag which overrides it and distinct from `launch_daemon_engine`'s always-on-loop cadence). **Independent flag: `settings.PILOTS_API_ENABLED`** (default `False`) controls what the daemon PROCESS hosts once launched (the Pilots API alongside the Control API — see `desktop/orchestrator_daemon.py` above) — orthogonal to `ORCHESTRATOR_DAEMON_ENABLED`, which controls which subprocess the DESKTOP SHELL spawns in the first place. Running `python -m desktop.orchestrator_daemon` directly (e.g. for the Pilots PWA alone, without the desktop shell) honors `PILOTS_API_ENABLED` regardless of `ORCHESTRATOR_DAEMON_ENABLED`.
- **Extended hours gate (`settings.ORCHESTRATOR_EXTENDED_HOURS_ONLY`, default `True`)**: default True — this changes today's behavior for existing deployments, fixing unconditional 24/7 automatic runs. When True, `main.py --interval N`'s subprocess loop, `main.py --agent`'s adaptive-cadence loop, and the daemon's own internal `_timer_loop` (`desktop/daemon_runtime.py`) all skip automatic runs outside the 04:00-20:00 ET **weekday** window (`engine.advisory_agent.is_extended_hours` — no holiday calendar, same known limitation as `is_us_market_open`). Manual runs (via the UI, the Control API, or omitting `--interval`/`--agent`) always bypass this gate. **Runtime hot-reloading (2026-08)**: `settings.RUNTIME_FLAGS_REFRESH_ENABLED` defaults to `True`, and the daemon's internal `_timer_loop` calls `self.maybe_refresh_settings()` on loop wake-up and iteration, ensuring runtime updates written via the UI take effect immediately without requiring a manual restart. Two downstream consumers were made gate-aware so a legitimately-paused automatic cycle doesn't read as a false alarm: `gui/engine_status.py`'s sidebar badge shows `🌙 Automatic runs paused (outside market hours)` instead of `🟠 Engine idle` when the staleness is explained by the gate, and `scripts/preflight_check.py::check_state_snapshot_fresh` degrades to a warning-only PASS instead of a blocking FAIL under the same condition. **New env var:** `ORCHESTRATOR_EXTENDED_HOURS_ONLY`.
- **Runtime settings auto-refresh (`settings.RUNTIME_FLAGS_REFRESH_ENABLED`, default `True`)**: enables running daemon and API server instances to dynamically re-read updated runtime flags from `output/runtime_flags.json` without requiring process restarts. In the Pilots PWA (**Settings → Data & Automation**), the **Pipeline Schedule** section provides an explicit master toggle (**"Enable Scheduled Pipeline Runs"**) that sets interval to 0 (parking the daemon timer) when toggled OFF, alongside quick interval presets (1m, 5m, 15m, 30m, 1h) and positive phrasing for the **"Limit to Extended Market Hours (4 AM – 8 PM ET)"** gate.
- **Durable pipeline run history (`desktop/run_history_store.py`)**: `OrchestratorDaemon._run_history` (see `desktop/daemon_runtime.py` above) is a plain in-memory ring capped at `run_history_size` (default 10) and lost on every daemon restart. `RunHistoryStore` gives each completed run cycle a second, durable home — a `pipeline_runs` SQLite/Postgres table resolved through `db_config.py`, matching `transactions_store.py`'s conventions (own `Base`, a `readonly=True` database-level read-only engine for consumers) — so the Pipeline Dashboard's run history can survive a restart. `OrchestratorDaemon._run_one_cycle` best-effort persists every completed `RunRecord` here immediately after building it (lazy import); a DB write failure only logs a warning and never affects the run's own SUCCEEDED/FAILED state. `api/control_api.py`'s `GET /runs/history?limit=` (read-token guarded, `limit` clamped `[1, 200]`, degrades to `[]` on a DB read failure) serves it independently of whether a daemon is currently attached — the whole point is to keep working across a restart. The webapp Pipeline screen (`webapp/src/screens/PipelineDashboard.tsx`) renders this as a separate "Full run history" section below the existing live (in-memory) "Run history" table, with a manual Refresh button plus an auto-refresh when an in-flight run completes. Only terminal (succeeded/failed) runs are ever written — a run still in flight only ever appears in the in-memory ring, never this table.
- **Durable backtest/validation run history (`validation/validation_history_store.py`, 2026-08)**: `StrategyValidationHarness.run()` used to only write files — `reports/<strategy>_validation_summary.json` (overwritten every run) and `reports/history/<strategy>_validation_history.jsonl` (append-only) — both under the repo's `reports/` directory, which is **worktree-local** (this repo runs many simultaneous git worktrees; an untracked file written in one is invisible from every other one — the same class of gap `settings.LOCAL_DATA_ROOT` closed for models/caches/logs, see that field's own bullet). A validation run in one worktree was therefore invisible to `pilots/validation_trend.py`/the Pilots PWA's Validation Trend chart running against a different worktree's API process. `ValidationHistoryStore` gives every run a second, durable home — an append-only `validation_runs` SQLite/Postgres table resolved through `db_config.py`, matching `desktop/run_history_store.py`'s `RunHistoryStore`/`sizing/cap_audit_store.py`/`data/sector_correlation_store.py`'s established convention (own `Base`, own table, a `readonly=True` database-level engine for read-only consumers) — see the "Durable pipeline run history" bullet above for the sibling pattern this mirrors. `run()`'s existing history-append step is followed by a new best-effort, dead-letter-resilient `_record_validation_run_to_db` call (lazy import; a DB hiccup never aborts an otherwise-successful validation run). `pilots/validation_trend.py`'s `cross_strategy_snapshot()`/`validation_history_trend()` read the DB FIRST and fall back to the worktree-local file read only for a strategy the DB has no row for yet (e.g. one validated before this feature shipped) — the `GET /strategy/validation-trend` response contract is unchanged, so no webapp changes were needed. **A test-suite-wide autouse fixture is required, not optional, for this kind of implicit internal write**: unlike `RunHistoryStore`, whose every test call site constructs the store directly with its own `db_url`, `_record_validation_run_to_db` fires implicitly deep inside `run()` with no way for the ~25 pre-existing test files that call `.run()` for real to opt out individually — left unguarded, running the test suite would silently write fake `strategy_id` rows into a real operator's shared `~/.stockpy_local/quant_platform.db` on every run. Closed by `conftest.py`'s `_isolate_validation_runs_db_in_tests` (root-level, session-wide autouse, mirroring the existing `_no_gdelt_throttle_in_tests`/`_no_fmp_throttle_in_tests` pattern there) rather than `tests/conftest.py`'s usual per-file opt-in convention — the same reasoning that justified a global fixture for the FMP/GDELT throttles applies here: a cross-cutting resource shared by dozens of pre-existing test files, not something a new test author would think to guard against on their own.
- **Command execution from the webapp Commands screen (`settings.COMMAND_EXECUTION_ENABLED`, default `False`)**: adds a new `"command"` `JobType` to the orchestrator Control API's `POST /jobs` (`api/_jobs.py`), letting the Pilots PWA's Commands screen actually RUN a manifest-listed CLI target instead of only composing/copying it for the operator to paste into their own terminal — layered on top of, not instead of, the existing `JOBS_API_ENABLED` + `ORCHESTRATOR_DAEMON_TOKEN` guard already protecting `POST /jobs` (both must ALSO be true; `COMMAND_EXECUTION_ENABLED=False` is the default and preserves today's compose-only behavior even if the other two are on). The `command`/`subcommand` params are resolved server-side via `pilots.commands.resolve_command` against the SAME committed `cli_introspect/command_manifest.json` artifact `GET /commands` already serves — the handler never trusts a client-supplied path/module string, only ever launching a target the manifest itself lists. `gui/orchestrator_runner.py::launch_manifest_command` builds argv from the resolved `CommandSpec`'s `invocation` field (splitting off the `python`/`python3` prefix and substituting `sys.executable`, exactly like every other launcher in this module) plus the caller-supplied `args` list appended literally (no shell involved, so argument values carry no shell-injection risk). Two safety carve-outs live in `launch_manifest_command`: `app_shell.py` is in `DISALLOWED_EXECUTE_COMMANDS` (it pops a native pywebview window on the server host, not something a browser click can show or use, so it stays copy-only and always raises `ValueError`); and `HIGH_STAKES_COMMANDS` maps `execution.kill_switch --activate`/`--deactivate` and `main.py --refresh-account` to a required, explicit `confirm=true` param on the job request — omitting it raises `ValueError` (mapped to HTTP 400) instead of silently running. `JobManager.start_job`'s single-flight check is narrowed for `JobType.COMMAND` to the SAME resolved command name (via the new `JobRecord.command_name` field) rather than the job type as a whole, so two different manifest targets can run concurrently while a duplicate invocation of the identical command still 409s. Deliberately excluded from `gui/env_io.py`'s `ALLOWED_KEYS`/`SECRET_KEYS` (hand-set in `.env` only) — same treatment as `AUTOMATION_WRITES_ENABLED` — because this flag gates execution of the global kill switch, a forced Robinhood re-login, and arbitrary flags to the orchestrators, a materially bigger risk than the fixed-dispatch `JOBS_API_ENABLED` alone already covers. New env var: `COMMAND_EXECUTION_ENABLED`.
- **Commands screen strategy-registry sync (2026-08)**: the Commands screen's `--strategy`/`--strategies` dropdown (for `validation.harness --strategy` and `refresh_validations.py --strategies`) was driven by a hand-maintained TypeScript constant (`REGISTERED_STRATEGIES` in `webapp/src/commandParse.ts`) that had drifted 13 of 29 strategies stale against the real `STRATEGY_REGISTRY` in `scripts/refresh_validations.py`. Fixed by having `scripts/build_command_manifest.py` embed a new `strategy_registry` field (sourced from `STRATEGY_REGISTRY` via an isolated subprocess, degrading to `[]` on any failure) into the committed `cli_introspect/command_manifest.json`, which the webapp now reads live via `GET /commands` — the hardcoded constant survives only as a last-resort fallback, corrected to the current 29 names. A new `tests/test_command_manifest_freshness.py` now fails CI if this drifts again (exact set-equality between `STRATEGY_REGISTRY` and the committed manifest's `strategy_registry` field). A genuine pre-existing bug was fixed in the same pass: `commandParse.ts`'s free-text autocomplete for `--strategy`/`--strategies` was dead code (its `choices`-non-null branch guard could never be satisfied for these two options), now actually working. `refresh_validations.py --strategies` (already supporting a full-registry bulk run at the CLI level) now has a genuine multi-select UI in `CommandFormBuilder.tsx` defaulting to all-selected, plus a one-click "🧪 Bulk Validate All Strategies" button on `Commands.tsx` that opens the same builder pre-defaulted to all-selected. Note the options-strategy Title-Case namespace (`STANDARD_OPTIONS_STRATEGIES` in `validation/options_harness.py`, e.g. `"Iron Condor"`) remains a separate, un-synced, explicitly out-of-scope gap — this change does not make `--strategy` autocomplete fully complete. See `docs/architecture/webapp-and-gui.md`'s `COMMAND_EXECUTION_ENABLED` entry for the full detail. Tests: `tests/test_command_manifest_freshness.py`, `tests/test_build_command_manifest.py`, plus extended `webapp/src/commandParse.test.ts`, `webapp/src/components/CommandFormBuilder.test.tsx`, `webapp/src/screens/Commands.test.tsx`.
- **Global job-status visibility in the Pilots PWA (`GET /jobs`, `JobConflictError`, `JobStatusContext`, 2026-08)**: before this, an in-flight background job (a Commands-screen launch, a `train_lgbm`/`train_meta` retrain, a Console quick action) was only visible to the exact screen component that launched it — navigate away, close the launching modal, or open a second tab and there was no way to tell whether it was still running, had finished, or a duplicate launch had just been silently rejected. `api/_jobs.py::JobManager.list_jobs()` + `GET /jobs` (`api/control_api.py`, gated identically to the existing `GET /jobs/{id}` — `require_read_token` + `JOBS_API_ENABLED`; `active_only`/`limit` params) lists every job this process has tracked, newest first — still in-memory only, still lost on an API restart, same as `JobManager._jobs` already was. `JobConflictError(RuntimeError)` replaces the old bare-string `RuntimeError` `JobManager.start_job` raised on a same-type/same-`command_name` conflict, carrying the blocking job's id/type/command name as structured fields, surfaced by `create_job` as a structured 409 body — the same shape `api/pilots_api.py::run_forecast_backfill_endpoint`'s pre-existing conflict handling already used. `webapp/src/components/JobStatusContext.tsx` (+ its `context`/`hooks` split, mirroring `ExecutionModeContext.tsx`'s existing 3-file pattern) polls `GET /jobs` every 3s app-wide and feeds a new "Jobs: idle"/"Jobs: N running" chip in `TopStatusBar.tsx` — visible from every screen regardless of which one launched a job. `RunCommandControl.tsx`/`Console.tsx`/`Models.tsx` each catch `JobConflictError` and attach to the existing job instead of showing a dead-end error (a real fix in `Models.tsx`: a double-click or a second tab's "Retrain Now" previously had no way to recover). **Explicit non-goal: this is not a real job queue** — the single-flight-reject-with-409 behavior is unchanged; only its visibility improved. `cli_introspect/targets.py`'s `TARGETS` also gained the four data-ingestion backfill scripts (`scripts/backfill_edgar_fundamentals.py`, `scripts/backfill_news_history.py`, `scripts/backfill_news_history_from_audit.py`, `scripts/backfill_sentiment_history.py`) so they're launchable/trackable through the existing `COMMAND_EXECUTION_ENABLED` + `JobType.COMMAND` pipeline for the first time, previously CLI-only with no status signal at all; `tests/test_command_manifest_freshness.py::test_manifest_commands_match_targets` now catches a `TARGETS` edit whose manifest regeneration (`python scripts/build_command_manifest.py`) was forgotten. **Audit fix, same date**: `JobRecord.handle` is briefly `None` between `start_job` reserving a job id under its lock and the actual subprocess launch completing (deliberate — so `Popen` doesn't block the lock); `GET /jobs`/`GET /jobs/{id}`/`GET /jobs/{id}/stream` originally touched `.handle` directly and crashed (uncaught `AttributeError`, not caught by `install_redacting_exception_handler` which is `HTTPException`-only) if hit during that window — fixed via a null-safe `JobRecord.is_running` property and a bounded SSE-side wait. See `docs/architecture/webapp-and-gui.md`'s "Background job execution" entry for full detail. New env vars: none — rides entirely on the existing `JOBS_API_ENABLED`/`COMMAND_EXECUTION_ENABLED` gates.
- **GUI Command Center env-write safety (`gui/env_io.py`)**: any GUI-driven `.env` edit MUST go through `gui.env_io.write_setting`/`write_many`, which enforce an `ALLOWED_KEYS` allowlist (NON-secret tunables only) and a `SECRET_KEYS` denylist — secrets are masked (`read_settings`) and raise `SecretWriteError` on write, non-allowlisted keys raise `DisallowedKeyError` (CONSTRAINT #3). Never add a new GUI-writable setting without adding it to `ALLOWED_KEYS`; never add a credential to `ALLOWED_KEYS`. `STATE_API_TOKEN` (the read-only State API bearer token — see `api/state_api.py`) is a `SECRET_KEY` (masked, never GUI-writable); `CORS_ALLOWED_ORIGINS` is a non-secret `ALLOWED_KEY`. `ORCHESTRATOR_DAEMON_TOKEN` (the Control API's fail-closed command token — see `api/control_api.py`) is likewise a `SECRET_KEY`; `ORCHESTRATOR_DAEMON_ENABLED` is a non-secret `ALLOWED_KEY`. `FOLLOW_API_TOKEN` (the Pilots API's fail-closed follow-write command token — see `api/pilots_api.py`) is likewise a `SECRET_KEY`. `BROKERAGE_CONNECT_ENABLED` (the Pilots API's brokerage-credential connect/disconnect master switch — see `api/pilots_api.py` and `data/brokerage_credentials.py`) was previously deliberately excluded from both `SECRET_KEYS` and `ALLOWED_KEYS` (it carries no credential material itself, but a GUI bug enabling credential intake was judged a materially different risk than a GUI bug enabling a forecasting tunable); as of 2026-08-02 it is a non-secret `ALLOWED_KEY` by explicit operator decision — the connect/disconnect endpoints remain independently gated by `FOLLOW_API_TOKEN` and a loopback-only request check regardless of this flag's own GUI-writability, so the flag alone is not sufficient to enable intake. `UNIVERSE_SYNC_ENABLED` (`api/data_api.py`'s `POST /data/sync` master switch) and `AGENTIC_DISCOVERY_ENABLED` (the Pilots API's Agentic Trading Discovery WRITE master switch) received the same treatment on the same date — both are now non-secret `ALLOWED_KEYS`, each still independently gated by its own endpoint's command token (`STATE_API_TOKEN` via `require_write_token` / `FOLLOW_API_TOKEN` respectively). **Policy generalized 2026-08-08 (PR #630 audit)**: the remaining 12 fail-closed write/execution master switches — `AI_GENERATION_API_ENABLED`, `AUTOMATION_WRITES_ENABLED`, `BROKERAGE_REFRESH_ENABLED`, `CACHE_LONG_SHORT_WRITES_ENABLED`, `COMMAND_EXECUTION_ENABLED`, `DEAD_LETTER_RETRY_ENABLED`, `GENERAL_SETTINGS_WRITES_ENABLED`, `LLM_WRITES_ENABLED`, `MACRO_GATE_WRITES_ENABLED`, `PROMPT_REGISTRY_WRITES_ENABLED`, `RAG_QUERY_API_ENABLED`, `STRATEGY_WRITES_ENABLED` — moved out of `EXCLUDED_FROM_GUI` into `ALLOWED_KEYS` by the same explicit-operator-decision mechanism, per operator instruction that "not secret information" is the sole bar for GUI-writability going forward, superseding the older per-flag "a GUI bug must never flip this on, hand-set in `.env` only" convention this file previously documented for each of them individually. Each endpoint these flags gate remains independently protected by its own command token (`FOLLOW_API_TOKEN`/`STATE_API_TOKEN`, plus a loopback check for the brokerage ones) regardless of the flag's own GUI-writability — this is a GUI-*visibility* change, not a new capability, and it did not touch any default value (all defaulted `False` at the time of the 08-08 audit). All 12 were also added to `settings_keysets.SAFETY_CRITICAL_KEY_REASONS` (moved out of the now-empty `HAND_SET_ONLY_KEYS`), so each remains a `DANGEROUS_KEYS` member requiring typed confirmation on write through any settings editor that exposes it, on top of its command-token gate — see `settings_keysets.py`'s own comments for the full per-flag reasoning. `EXCLUDED_FROM_GUI` now holds only the 8 filesystem-path fields. `PILOTS_API_ENABLED` (hosts `api/pilots_api.py` inside the orchestrator daemon — see `desktop/orchestrator_daemon.py` above) is a non-secret `ALLOWED_KEY`, surfaced in `_SETTINGS_LAYOUT` next to `ORCHESTRATOR_DAEMON_ENABLED`; `PILOTS_API_PORT` is `.env`-only like `ORCHESTRATOR_API_PORT` (no GUI widget). `PROGRESS_POLL_SECONDS` (int, default 5 — the Launcher tab's live pipeline-progress bar poll interval; see `reporting/progress.py` above) is likewise a non-secret `ALLOWED_KEY`, grouped next to `DASHBOARD_REFRESH_SECONDS`. Six forecasting/data tunables are now GUI-writable (non-secret `ALLOWED_KEYS`, surfaced in `gui/panels/settings_manager.py`'s `_SETTINGS_LAYOUT`): `FORECAST_USE_GARCH_SIGMA` (bool — GJR-GARCH→Monte-Carlo sigma rollback lever), `FORECAST_PROPHET_WEIGHT` (float [0,1] — Prophet ensemble overlay weight), `FUNDAMENTALS_SOURCE` (`"yahoo"` | `"yfinance_info"`), `BETA_LOOKBACK_DAYS` (int — beta computation lookback), `FORECAST_SKILL_WEIGHTING_ENABLED` (bool — opt-in inverse-RMSE skill-weighted forecast blend), and `FORECAST_SKILL_WINDOW_DAYS` (int — rolling RMSE window for skill weighting). A further set of previously allowlisted-but-widgetless levers is now also surfaced in `_SETTINGS_LAYOUT`: `ORCHESTRATOR_DAEMON_ENABLED`, `PROMPT_REGISTRY_ENABLED`, `PROMPT_REGISTRY_BACKEND`, `SECTOR_FORECAST_CONFIG_PATH`, plus the two JSON-structured keys `CORS_ALLOWED_ORIGINS` and `SECTOR_FORECAST_CONFIGS` via a new `"json"` widget kind (a `st.text_area` that `json.loads`-validates on submit and skips a malformed paste rather than writing garbage — env_io re-encodes the parsed object for `_JSON_KEYS`). The four LLM provider/model selectors (`LLM_COMMENTARY_RATIONALE_PROVIDER`, `LLM_COMMENTARY_ALERT_PROVIDER`, `OPAL_RESEARCH_PROVIDER`, `OPAL_RESEARCH_MODEL`) — previously ALLOWED_KEYS with no control — now render as `st.selectbox`/`st.text_input` next to their master-switch toggle in the AI Control Center panel (`gui/panels/ai_control_center.py`, driven by `AICapability.provider_selector_setting`). The GUI Observability tab gained a **Forecast Skill** read-only sub-section (`gui/panels/observability.py::_render_observability_forecast_skill`) surfacing per-model RMSE (via a local read-only `forecast_errors` SELECT helper `_forecast_rmse_by_model`), `pending_count`/`completed_count`, and the live `get_skill_weights` per symbol×horizon — so an operator enabling `FORECAST_SKILL_WEIGHTING_ENABLED` can see whether skill-weighting is actually improving accuracy instead of tuning blind (empty DB → info message, never fabricated). Three more non-secret `ALLOWED_KEYS`/`_SETTINGS_LAYOUT` additions for the CNN-LSTM subprocess isolation fix (issue #381 — see the dedicated bullet below): `CNN_LSTM_SUBPROCESS_ISOLATION_ENABLED` (bool), `CNN_LSTM_PROCESS_POOL_WORKERS` (int), `CNN_LSTM_SUBPROCESS_TIMEOUT_SECONDS` (int).
- **Convention change (2026-08-03): new admin/write/execution capabilities default to ON.** Creating a fresh fail-closed `_ENABLED` flag (defaulting `False`, excluded from `gui/env_io.py`'s `ALLOWED_KEYS` per the bullet above) for every new admin/API capability was, per explicit operator decision, judged too much overhead going forward. The 11 (12 counting `CACHE_LONG_SHORT_WRITES_ENABLED`) write/execution gates this bullet originally listed — `AI_GENERATION_API_ENABLED`, `AUTOMATION_WRITES_ENABLED`, `BROKERAGE_REFRESH_ENABLED`, `CACHE_LONG_SHORT_WRITES_ENABLED`, `COMMAND_EXECUTION_ENABLED`, `DEAD_LETTER_RETRY_ENABLED`, `GENERAL_SETTINGS_WRITES_ENABLED`, `LLM_WRITES_ENABLED`, `MACRO_GATE_WRITES_ENABLED`, `PROMPT_REGISTRY_WRITES_ENABLED`, `RAG_QUERY_API_ENABLED`, `STRATEGY_WRITES_ENABLED` — predate this decision and their **default values** are unaffected by it; do not flip their defaults without being explicitly asked to. **Update 2026-08-07**: the operator explicitly asked to flip the frozen 12 (now 16, folding in `PILOTS_API_ENABLED`/`JOBS_API_ENABLED`/`UNIVERSE_SYNC_ENABLED`/`AGENTIC_DISCOVERY_ENABLED`), superseding "do not flip without being explicitly asked" for exactly this named list, and added a new Feature Flags screen + registry to prevent silent fail-closed behavior. **This is orthogonal to the 2026-08-08 GUI-writability policy generalization documented in the bullet above**: that change moved these same 12 flags into `ALLOWED_KEYS` (a GUI-*visibility* change, `DANGEROUS_KEYS`-confirmation-gated), it did not touch any default value. The defaults were independently changed to True by the 2026-08-07 update. A *new* admin/API write or execution capability built from this date forward should ship active by default instead of behind a freshly-added opt-in flag. This does **not** extend to feature flags that change trading behavior (new signal modules, sizing changes, new data sources, forecasting changes) — those keep defaulting `False`/opt-in exactly as every "New settings default to today's exact behavior" bullet elsewhere in this file establishes, since a silent behavior change on `git pull` is a real risk for a live capital account. See `AGENTS.md`'s "Safety posture" section for the canonical statement of this policy.
- **Cache Long/Short advisory strategy (`engine/cache_long_short_engine.py`, `data/cache_long_short_store.py`, 2026-08)**: a tax-loss-harvesting overlay — advisory only in V1, no broker order is ever submitted regardless of settings. Single-operator schema (no `User_ID`/`Portfolios` — matches `transactions_store.py`'s convention, not a multi-tenant design): `CacheLongShortPosition`/`CacheLongShortTaxLot`/`SecurityProxy` tables via `data/cache_long_short_store.py`'s `CacheLongShortStore` (SQLAlchemy declarative idiom). Real math, not mocked: `calculate_beta` reuses `processing_engine.calculate_rolling_beta` over `HistoricalStore.get_bars`; `find_correlated_proxy`/`check_correlation_drift` reuse `pairs_ondemand.analyze_pair` via `data.market_data.get_provider()` (the same lightweight `CompositeProvider` `POST /data/pairs/analyze` uses — NOT `DataEngine`, which lacks the `get_intraday_bars` method `pairs_ondemand` needs); `check_wash_sale` is a real SQL query against closed tax lots in the last 30 days. Split across the two existing FastAPI services rather than a new `APIRouter` (no such pattern exists elsewhere in this codebase): `POST /data/cache-long-short/simulate` (interactive, on-demand — mirrors `POST /data/pairs/analyze`'s exact pattern) lives in `api/data_api.py` because `api/pilots_api.py` is AST-guarded against importing `processing_engine`, even transitively-in-intent (see `pilots-endpoint` skill); the read/write endpoints (`GET/POST /pilots/cache-long-short/*`) live in `api/pilots_api.py` and read/write only through `pilots/cache_long_short.py` (a dependency-light helper, stdlib+settings+the store only), never the heavy engine. `settings.CACHE_LONG_SHORT_ENABLED` (default `False`) gates the `main_orchestrator.py` background TLH/correlation-drift scanner; `settings.CACHE_LONG_SHORT_WRITES_ENABLED` (default `False`) gates `POST /pilots/cache-long-short/{start,approve-bulk}` alongside the command token, per the pilots-endpoint skill's "own risk class, dedicated flag" rule — both default `False` because this changes what a trading strategy recommends, exactly the carve-out the bullet above describes. `CACHE_LONG_SHORT_WRITES_ENABLED` is a non-secret `ALLOWED_KEY` as of 2026-08-08 (see the GUI Command Center env-write safety bullet above) and is exposed as a toggle on the Cache Long/Short Strategy settings screen (`webapp/src/screens/SettingsCacheLongShort.tsx`, `GET/PUT /settings/cache-long-short`) — this changes only who can see/flip the boolean via a settings editor gated by `GENERAL_SETTINGS_WRITES_ENABLED` plus a typed confirmation (it's a `settings_keysets.DANGEROUS_KEYS` member), not what the flag itself gates: the `/pilots/cache-long-short/{start,approve-bulk}` endpoints still separately require `CACHE_LONG_SHORT_WRITES_ENABLED=True` at request time regardless of how it got set.
- **FMP-based paper trading engine (`execution/fmp_paper_broker.py`, `data/paper_account_store.py`, 2026-08)**: `FMPPaperBroker(BrokerBase)` — a drop-in broker adapter (gets `OrderManager`'s kill-switch + `PreTradeRiskGate` protection for free, no separate wiring) that fills orders against real FMP quotes adjusted by `execution.cost_model.TieredCostModel`'s realistic commission/slippage/spread, instead of Alpaca's paper sandbox. `data/paper_account_store.py`'s `PaperAccountStore` tracks virtual cash/positions/orders in SQLite across restarts. Selected via `settings.BROKER_BACKEND` (`"fmp_paper"` default | `"alpaca"`) in `main_orchestrator.py`'s broker construction — now defaults to `"fmp_paper"`. A runtime force-fallback safety guard ensures fallback to paper if live trading is requested without valid Alpaca credentials, verified by a preflight check. `settings.PAPER_BROKER_WRITES_ENABLED` gates all write operations to the paper store, which is managed via the new Paper Broker webapp UI surface. `settings.FMP_PAPER_STARTING_CASH` (default `100000.0`) seeds a fresh paper account. **Multi-Leg Option Paper Trading (2026-08)**: Supports both equity and full multi-leg options trading (e.g. Bull Call Spreads, Bear Put Spreads, Iron Condors, Straddles, Strangles, Calendar Spreads). Multi-leg orders execute atomically via `PaperAccountStore.apply_multi_leg_fill` with honest bid/ask spread pricing (net debit/credit calculation), $0.65/contract/leg commission modeling, short position / sell-to-open tracking (`qty < 0`), and dynamic mark-to-market option valuation via Black-Scholes using underlying spot quotes. Market orders instant-fill; LIMIT orders fill if already marketable against the net quote (otherwise rejected cleanly). **Quick Trade / any-symbol access + auto-execute symbol override (webapp, 2026-08)**: the paper-order path was already unrestricted end-to-end (`FMPPaperBroker`, `PaperAccountStore`, and the `/brokerage/options/order` endpoint never checked a symbol against `WATCHLIST`/`watchlist.txt`/held positions) but the Paper Broker screen exposed no way to reach an untracked symbol directly. `webapp/src/screens/PaperBroker.tsx` now has a "Quick Trade — Any Symbol" panel (a free-text `SymbolInput` → `GET /data/quotes` for a live quote → an inline `OptionsOrderTicket` in stock mode, plus a "View Options Chain" link into the existing `/symbol/:ticker/options` route for options on that same untracked ticker) so any FMP-quotable symbol can be paper-traded without first adding it to the watchlist/pipeline universe; a failed/missing quote shows an honest error rather than opening a ticket with a fabricated `$0` spot price. The "⚡ Automated Strategy Options Execution" auto-scan also gained an optional operator-supplied symbol-list field — blank preserves the exact existing default (`getStrategyOptionsCandidates`/`executeStrategyOptions` called with no `symbols` arg, which `pilots/paper_broker.py`/`execution/options_paper_executor.py` resolve to `settings.WATCHLIST`), populated it scans exactly those symbols instead, independent of the pipeline's tracked universe — both API methods already accepted a `symbols?: string[]` argument that the UI simply never surfaced. A full sector/industry/strategy symbol *screener* (browse-by-attribute, not just trade-by-ticker) was explicitly scoped OUT of this change and remains a follow-up: no FMP symbol-search-by-name or screener endpoint exists anywhere in this codebase yet (`data/fmp_client.py` only wraps per-symbol lookups), and tier availability for one is unverified. **Symbol search & sector/industry screener (webapp, 2026-08, closing the item above)**: `data/fmp_client.py::search_name`/`search_symbol`/`company_screener`/`available_sectors`/`available_industries` wrap FMP's real `/search-name`, `/search-symbol`, `/search-company-screener`, `/available-sectors`, `/available-industries` endpoints — live-verified 2026-08 via an external FMP MCP connector (a separate, real FMP account; NOT yet exercised through this repo's own client or the operator's own `FMP_API_KEY`/tier, see `docs/FMP_INTEGRATION.md` §9 for the honest verification-status statement and the manual eyeball-gate before trusting it live). `data/fmp_screener.py` (a new standalone module, deliberately NOT added to `CompositeProvider`/`FMPProvider` — mirrors the `peers()`/`fetch_peer_group` precedent: a universe-browse capability has no per-symbol fallback shape) exposes `search_symbols`/`screen_companies`/`list_sectors`/`list_industries`, gated by a single `settings.FMP_SCREENER_ENABLED` (default `True`), never raises (degrades to `[]`). Three new `api/data_api.py` endpoints (`GET /data/symbol-search`, `GET /data/screener`, `GET /data/screener/filters`) follow `GET /data/peers/{symbol}`'s exact shape (flag check → honest `{"reason": ...}` short-circuit, never a 500). New webapp screen `webapp/src/screens/SymbolScreener.tsx` (nav section `"research"`, plus a Marketplace "Explore" tile for mobile reachability) offers free-text search, a sector/industry/market-cap/price/beta/dividend filter form, and a few pure client-side filter presets ("Large Cap Tech", "Dividend Income", "Low Vol" — no new quant scoring introduced). Selected symbols hand off via URL query params — this codebase's one existing cross-screen pattern (`Commands.tsx`'s `?builder=`) — into Paper Broker's existing Quick Trade panel (`?quickTradeSymbol=`) or auto-execute scan input (`?scanSymbols=`), both read via a mount-once `useSearchParams()` effect. New env var: `FMP_SCREENER_ENABLED`.
- **Spot data download + honest "add to watchlist" flow (`api/data_api.py::trigger_symbol_backfill`, `webapp/src/components/options/OptionsOrderTicket.tsx`, 2026-08)**: Phase 1 of the Symbol Screener universe-integration scoping. New `POST /data/backfill/{symbol}` (`require_write_token`-gated, matching `PUT /data/universe`'s fail-closed posture) forces a full `settings.BARS_BACKFILL_DAYS` bar backfill for one arbitrary symbol into a write-mode `HistoricalStore()` — every REST-facing symbol GET in this file deliberately uses `readonly=True`, whose live-fetch-but-never-persist short-circuit is exactly why a symbol discovered via the Symbol Screener or Quick Trade never actually landed in local storage before this endpoint existed; ports `investyo_mcp_server.py::trigger_data_engine`'s write-mode MCP-tool body into a REST route the webapp can call directly. Returns `{"symbol", "rows_persisted", "last_bar_date", "status": "ok"|"no_data"}`, never a fabricated success (CONSTRAINT #4). The existing "+ Add to Watchlist" button (`OptionsOrderTicket.tsx` → `POST /agentic/watch`) was previously `console.error`-only on failure — a `409 watchlist_env_precedence` or a missing `FOLLOW_API_TOKEN` produced zero on-screen feedback. It now surfaces failures visibly, and a successful add additionally fires the new backfill endpoint (non-blocking — a backfill hiccup never rolls back the watchlist-add confirmation, CONSTRAINT #6), showing a secondary "Backfilled N bars of price history" note. `webapp/src/api/client.ts::watchCandidate` was also fixed to extract `POST /agentic/watch`'s structured `{"error": tag, "message": ...}` error detail — the shared `http()` helper's generic `String(body.detail)` would have stringified that object literally (`"[object Object]"`), defeating the point of surfacing an honest server error. A successful fill on a symbol NOT in the tracked universe (checked via the existing `webapp/src/components/universeCache.ts` cache) now shows an inline "isn't in your tracked universe — add it?" prompt (Add / Not now) instead of auto-clearing after 2 seconds — an explicit user action, never a silent auto-add.
- **Widened symbol lookup — FMP-backed dropdown everywhere (`webapp/src/components/SymbolInput.tsx`, 2026-08)**: Phase 2 of the Symbol Screener universe-integration scoping. The shared `SymbolInput` combobox (9 call sites — Data Explorer, Signal Breakdown, Forecast Viewer, Sentiment Dynamics, Sector Selection, Pairs Radar ×2, Cache Long/Short's `ConfiguratorWizard`, Paper Broker's Quick Trade, Universe Manager) previously suggested only from the tracked universe (`GET /universe`). Now also debounce-queries `GET /data/symbol-search` (FMP's full symbol universe) and renders untracked matches under a "Not yet tracked" section, with any already-tracked duplicate excluded. New `enableFmpSuggestions` prop (default `true`) lets a screen opt out — `SectorSelection.tsx` does, since `GET /sector/selection` only reads persisted DB state and an untracked symbol there is a guaranteed honest-empty dead end. New `trackedSymbols` prop lets a caller override the shared `universeCache` fetch with its own list — fixes a real pre-existing bug in `UniverseManager.tsx`: this screen's entire purpose is editing `DEFAULT_TICKERS` (`GET/PUT /data/universe`), but its `SymbolInput` was suggesting from a DIFFERENT list, the pipeline-snapshot `GET /universe`. `UniverseManager.tsx::addSymbol` also now fires `POST /data/backfill/{symbol}` (Phase 1's spot-data-download endpoint) non-blockingly after a successful add. Both new props default to today's exact prior behavior at the 7 call sites that don't pass them.
- **FMP-primary-source audit (2026-08-21)**: an operator-requested audit confirming FMP is genuinely the primary source (not just in `settings.py` docstrings) across data-layer routing, webapp charts, LLM/MCP grounding, and test coverage. Findings and fixes: (1) a CONSTRAINT #4 fabrication bug in `pilots/volatility_surface.py::get_volatility_surface_data()` (a hardcoded `$500`/`$150` spot-price fallback on quote failure — see that module's own bullet above for the fix and regression test); (2) five `investyo_mcp_server.py` MCP tools (`get_ticker_context`, `run_backtest`, `plot_equity_curve`, `plot_portfolio_equity`, `get_portfolio_summary`) that called `yfinance` directly, bypassing `MARKET_DATA_PROVIDER="fmp"` — now routed through `get_provider()` (see `docs/architecture/observability-and-apis.md`'s dedicated bullet); (3) a real test-coverage gap — no test previously exercised `CompositeProvider()` against genuinely untouched `Settings()` defaults, only hand-patched ones — closed by `tests/test_market_data.py::TestCompositeProviderGenuineDefaultRouting`; (4) confirmed, on evidence (not assumption), that `SENTIMENT_SOURCES`'s exclusion of `fmp_news` from its default is correct, not a gap — it would double-fetch the same `stock_news` endpoint `NewsCatalystSignal.pre_compute()` already calls FMP-first every cycle, the identical reasoning that already excludes `finnhub` — and documented that reasoning explicitly in `settings.py`'s field description (previously unexplained). One disclosed, still-open risk this audit could NOT close: `scripts/verify_fmp_bars.py` — the hard gate this file already calls mandatory before trusting `FMP_BARS_ADJUSTMENT` — has never been run against a live account (no live-market network access in this sandbox), despite `FMP_BARS_ENABLED=True` being the live default; see that setting's own field description in `settings.py` for the honest standing caveat.
- **Options Tab (2026-08)**: Dedicated Options Chain explorer and interactive Strategy Builder in the Pilots PWA. Powered by `OptionsDataProvider` ABC — `CompositeOptionsProvider` (`data/market_data.py`) itself only wraps `YFinanceOptionsProvider` for the raw chain (strikes, IV, bid/ask); the live spot-price injection from FMP happens one layer up, in the `GET /data/options/chain/{symbol}` handler (`api/data_api.py`), which separately calls `data.market_data.get_provider().get_latest_quote()` and honestly returns HTTP 503 rather than fabricating a price if that quote fails (CONSTRAINT #4) — for accurate Black-Scholes Greeks calculations. The UI features a "Builder" mode that auto-targets optimal strikes by delta (e.g., 16 delta strangles, vertical spreads, calendar spreads) using a `findClosestStrikeByDelta` utility, and visualizes the strategy with `OptionsPayoffChart.tsx`. The order ticket includes a Live/Paper execution toggle, defaulting to Paper mode. Only advisory features are supported (no live order routing).
- **Options Portfolio Risk & Aggregate Greeks Engine (`pilots/options_risk.py`, 2026-08)**: Computes position-level and portfolio-wide net Greeks ($\Delta_{\text{net}}$, $\Delta_{\$}$, $\Gamma_{\text{net}}$, $\Theta_{\text{daily}}$, $\mathcal{V}_{\text{1\%}}$, and $\beta$-weighted $\Delta_{\text{SPY}}$) across equity and multi-leg option positions. Re-derives spot quotes and live IV per leg; excludes unresolvable positions from aggregate sums (never zero-filled) and surfaces them via `positions_with_missing_data` and UI warning banners. Enforces a `< 1e-12` degenerate-denominator guard on $\sigma\sqrt{T}$, an explicit 0DTE fallback to intrinsic delta ($\Delta = \pm 1.0$ ITM, $0.0$ OTM; $\Gamma = \Theta = \mathcal{V} = 0$), and a settings-driven risk-free rate (`settings.OPTIONS_RISK_FREE_RATE`, default 0.045). Adheres to the AST import boundary by strictly excluding `processing_engine` imports from `pilots/options_risk.py`. Supported by interactive backtesting via `validation/options_harness.py`, Stage 4 ML Meta-Labeling for trade gating and sizing multipliers $\in [0.30, 1.50]$ via `ml/options_meta_labeler.py`, and automatic expiration cash settlements via `PaperAccountStore.settle_expired_options`. Surfaced via `GET /pilots/paper-broker/greeks`, `/pilots/options/backtest`, `/pilots/options/meta-model/*`, `/pilots/paper-broker/settle-expired`, and the Pilots PWA Paper Broker screen.
- **Options Dispersion & Implied Correlation Arbitrage Engine (`pilots/dispersion_trading.py`, 2026-08)**: Decomposes index variance and weighted constituent variances to derive institutional implied correlation ($\bar{\rho}_{\text{implied}}$) via the Driessen, Maenhout, Vilkov (2009) model. Computes historical weighted realized correlation ($\bar{\rho}_{\text{realized}}$) to evaluate correlation spreads ($\Delta \rho = \bar{\rho}_{\text{implied}} - \bar{\rho}_{\text{realized}}$) and regime opportunities (Long Dispersion vs Short Dispersion). Calibrates vega-neutral constituent contract allocations ($\sum \mathcal{V}_{\text{constituent}} \approx \mathcal{V}_{\text{index}}$) and constructs multi-leg `DispersionBasket` orders. Executes multi-ticker straddles atomically into `PaperAccountStore` under `strategy_name="Dispersion Arbitrage"`.
- **Dynamic Options Lifecycle, Delta Hedging, Vol Surface & Scenario Matrix (`pilots/options_hedging.py`, `pilots/volatility_surface.py`, `pilots/scenario_matrix.py`, 2026-08)**:
  1. *Position Lifecycle & Auto-Exits*: `execution/options_paper_executor.py` monitors open option spreads, automatically evaluating and executing 50% profit target exits, 2.0x stop losses, and 21-DTE gamma management exits into `PaperAccountStore`. Supports atomic multi-leg rolling (`apply_roll_fill`) closing existing contracts and opening next-cycle legs in one transaction.
  2. *Dynamic Delta Hedging*: `pilots/options_hedging.py` monitors $\beta$-weighted $\Delta_{\text{SPY}}$ and calculates required SPY shares to rebalance to neutral, filtering micro-swings via a configurable deadband (`settings.OPTIONS_DELTA_HEDGE_BAND_SPY_SHARES`, default 25 shares).
  3. *Volatility Surface & Skew*: `pilots/volatility_surface.py` constructs strike-dimension IV smile spline curves using monotonic PCHIP interpolation, total-variance term structure models across 7d–365d, 25-delta Put-Call skew ($IV_{25P} - IV_{25C}$), and Volatility Risk Premium (VRP) cones (10d, 20d, 30d, 60d).
  4. *2D/3D Scenario Matrix & Stress Grid*: `pilots/scenario_matrix.py` evaluates full portfolio P&L and shocked Greeks ($\Delta, \Gamma, \Theta, \mathcal{V}$) across Spot shifts ($\pm 10\%$), IV shocks ($\pm 20\%$), and Time decay intervals ($0\text{d}, 7\text{d}, 14\text{d}, 21\text{d}, \text{expiry}$), with historical stress presets (Lehman 2008, Volmageddon 2018, COVID 2020, Yen 2024).
  5. *PWA Visuals*: Surfaced via `POST /pilots/paper-broker/manage-exits`, `POST /pilots/paper-broker/roll`, `GET/POST /pilots/paper-broker/delta-hedge/*`, `GET /pilots/options/vol-surface`, `POST /pilots/paper-broker/scenario-matrix`, `ScenarioHeatmap.tsx`, and `VolSurfaceView.tsx` in the Pilots PWA.
  6. **CONSTRAINT #4 fix (2026-08-21, FMP-primary audit)**: `pilots/volatility_surface.py::get_volatility_surface_data()` used to silently substitute a hardcoded placeholder spot price (`$500.0` for SPY, `$150.0` for anything else) whenever the live `get_provider().get_latest_quote()` call failed — indistinguishable downstream from a real FMP-sourced quote, and inconsistent with the sibling `GET /data/options/chain/{symbol}` handler (`api/data_api.py`), which correctly 503s rather than fabricating a price. Fixed to pass `spot_price=None` through on a quote failure instead, letting `calculate_volatility_surface()`'s own already-correct fallback ladder run: infer spot from the option chain's median strike (flagged in its `warnings` list) when a real chain is available, or return an explicit `missing_data: True`/`reason` response when it can't. Regression-tested in `tests/test_volatility_surface.py::test_get_volatility_surface_data_never_fabricates_spot_price_on_quote_failure`.
  7. **CONSTRAINT #6 and #4 fix (2026-08-22, Paper Execution)**: `data/paper_account_store.py`'s atomic order processing functions (`apply_multi_leg_fill`, `apply_roll_fill`) silently defaulted to a zero fill price (`float(leg.get("fill_price", 0.0))`) if a leg was missing one, and did not validate if a `fill_price` was genuinely positive. This allowed corrupt options positions to be tracked in the paper portfolio. Fixed by adding a rigid `fill_price > 0.0` validation check that raises `ValueError` (rejecting the entire atomic order - Constraint #6). `pilots/dispersion_trading.py`'s client-supplied basket dicts were also updated to explicitly validate positive `fill_price` and strike consistency against real spot data before constructing a `DispersionBasket`. Hardcoded spot price fabrications were removed in `pilots/vol_mispricing.py`. A purge script `scripts/purge_corrupt_paper_options.py` was introduced to scan the SQLite DB for any previously opened options with a non-positive `avg_entry_price`, delete them, and reverse any residual cash impact.
  8. **PR 872 remediation (2026-08-24) — real closed-trade ledger, migration safety, attribution safety, and a live-DB test-contamination incident, all in one 6-agent pass**: PR 872's original scope (a `paper_closed_trades` table so a flattened/expired position's realized PnL, entry time, and holding period survive instead of being deleted with the position row — see `docs/architecture/execution.md`'s `data/paper_account_store.py` entry for the full detail) shipped with five real arithmetic/safety bugs, all found and fixed in this remediation pass: (1) **option PnL was inflated 100x** — `_record_closed_trade` re-applied the options ×100 contract multiplier on top of prices every writer in this codebase already stores as a per-contract dollar amount; fixed by removing the double-multiply, and `settle_expired_options` now converts its per-share intrinsic value to per-contract before handing it to `_record_closed_trade` so the ledger row agrees with the real cash settlement by construction. (2) `realized_pnl_pct` now reports `None` — never a fabricated `0.0` (CONSTRAINT #4) — when `avg_entry_price` is degenerate (`< 1e-12`), rather than blowing up or lying. (3) A new `PaperPosition.entry_ts` column (set on open/flip-through-zero, left untouched while averaging in) makes `holding_period_days` a real measurement instead of always `None`. (4) **`paper_positions` migration atomicity**: rebuilding the legacy single-column-PK schema into the current composite `(symbol, strategy_id)` PK schema used to run inside a broad `except Exception: logger.error(...)` that could commit a partial RENAME→CREATE→INSERT→DROP sequence (Python's stdlib `sqlite3` driver implicitly commits before DDL under plain `engine.begin()`, verified directly) — fixed via a raw `sqlite3` connection in explicit manual-transaction mode plus a pre-migration whole-file backup; a migration that can't complete cleanly now raises and construction fails (CONSTRAINT #6) instead of silently starting against a partially-migrated table. (5) **`allow_untagged_fallback` (Bug 7) made strict opt-in, default `False`**: `apply_fill`/`apply_multi_leg_fill`/`apply_roll_fill` used to unconditionally auto-borrow an opposite-signed position from the legacy `'untagged'` strategy bucket, silently misattributing PnL on a strategy's first order against a symbol with legacy inventory; the new `retag_position()` primitive is the explicit, deliberate replacement for moving a legacy position onto its real `strategy_id` by hand. Separately, the `transactions_store` bridge added by this same PR (gated by `settings.PAPER_TRADES_BRIDGE_TO_TRANSACTIONS_ENABLED`, default `False`) was fixed to reuse the caller's own session via a SAVEPOINT (`session.begin_nested()`) instead of opening a second connection mid-transaction, closing both a wrong-database bug (a bare `TransactionsStore()` re-resolving the default URL regardless of this store's own `db_url`) and a measured ~30-100x same-process WAL-writer contention cost; it fails OPEN, not closed, on a bridge write failure (the paper-side close still commits; only the bridge write is lost, logged at WARNING and counted). **A live-DB test-contamination incident occurred and was fixed during this same remediation, stated plainly rather than euphemized**: the new bridge's implicit write path had no test-isolation guard, and the real, shared `~/.stockpy_local/quant_platform.db`'s `trades` table was found holding 260 rows of synthetic test data (confirmed 100% contamination — the table had 0 rows before the bridge feature existed, every row bridge-tagged) with timestamps spanning roughly a day, predating the discovering session. Fixed via a new autouse `conftest.py` fixture, `_isolate_paper_and_transactions_db_in_tests`, routing both `PaperAccountStore`'s and `TransactionsStore`'s default DB resolution to an isolated per-test temp-file DB (a plain `:memory:` and a shared-cache memory URI were both tried and rejected first — see the incident doc for why). The live DB was backed up via `sqlite3 ... ".backup"` and the cleanup DELETE itself was run by the human operator, not the agent — blocked for the agent by the sandbox's destructive-write safety gate. See `docs/known_issues/pr872_live_db_test_contamination_2026.md` for the full incident write-up and the three regression tests (`tests/test_paper_account_store.py`'s `*_isolated_from_live_db`/`*_share_the_same_isolated_db` tests) that prove the fixture's contract.
  9. **Closed Trades read surface + strategy attribution UI (2026-08-24)**: item 1's `paper_closed_trades` table had a write path but no read path or UI — `data/paper_account_store.py::get_full_closed_trades(symbol=None, limit=100)` (mirroring `get_full_orders`'s readonly-safe cold-start guard and most-recent-first/`limit` shape; never coerces `realized_pnl_pct` from `None` to `0.0` — CONSTRAINT #4) is exposed via `pilots/paper_broker.py::get_closed_trades()` and a new fail-open-read-tier `GET /pilots/paper-broker/closed-trades`, rendered as a new Closed Trades table on the webapp Paper Broker screen. Fixed a real bug found along the way: `pilots/paper_broker.py::get_positions()` silently dropped `strategy_id`/`pilot_id`/`experiment_arm` from its response even though `PositionSnapshot` already carried them — both the Positions and Orders tables now render a Strategy column (Orders' backend response already included these fields; only the webapp type/table were missing). See `docs/architecture/execution.md`'s `data/paper_account_store.py` entry and `docs/architecture/webapp-and-gui.md`'s `PaperBroker.tsx` entry for full detail.


- **CNN-LSTM/TensorFlow deadlock fix (`cnn_lstm_worker.py`, `cnn_lstm_process_pool.py`, issue #381, `docs/known_issues/cnn_lstm_tf_deadlock.md`)**: `ForecastingEngine.run_cnn_lstm_forecast()` deadlocks the first time it runs a real TF eager op if `pandas`/`pyarrow` initialized before TensorFlow in that process (TF and pyarrow each ship a colliding Abseil symbol; whichever library's Python-level init runs first wins it). `main.py`/`main_orchestrator.py`/`pipeline/production_steps.py` all import `pandas` before `forecasting_engine` is ever reached, so a module-internal import reorder alone can't fix this — the constraint operates at PROCESS scope. `settings.CNN_LSTM_SUBPROCESS_ISOLATION_ENABLED` (default `True` since 2026-07-31, Round 7 of the known-issues doc — was `False`) routes the actual TF-touching work (fit+predict, cached-model load+predict) through a persistent `multiprocessing` **`spawn`** worker pool instead of in-process; a fresh spawned interpreter's import order is fully controllable regardless of what the parent already imported, so this protects EVERY caller of `run_cnn_lstm_forecast`, not just the entry points that remember an import-order guard. `cnn_lstm_worker.py` (the fit/predict/load payload, imports tensorflow before anything else) and `cnn_lstm_process_pool.py` (the pool manager) are deliberately **repo-root modules, not inside `forecasting/`** — `forecasting/__init__.py` eagerly imports `forecasting.forecast_tracker`, which imports pandas, so placing the worker inside that package would silently reintroduce the exact ordering bug via the package's own `__init__.py`. All pandas-dependent feature engineering/windowing/scaling stays in the parent process; only numpy arrays and JSON-safe primitives cross the process boundary. Any subprocess failure (timeout — `CNN_LSTM_SUBPROCESS_TIMEOUT_SECONDS`, default 300s; broken pool; a real training exception) is caught by `run_cnn_lstm_forecast`'s existing outer `try/except` and degrades to the zero-result sentinel (CONSTRAINT #6) instead of hanging the pipeline forever. `CNN_LSTM_PROCESS_POOL_WORKERS` (default 1) sizes the persistent pool shared across `pipeline/production_steps.py`'s per-ticker `ThreadPoolExecutor` fan-out. Defense-in-depth companion, kept for the `CNN_LSTM_SUBPROCESS_ISOLATION_ENABLED=False` opt-out path: `main.py`/`main_orchestrator.py`/`pipeline/production_steps.py` each also carry a guarded `import tensorflow` before their own `pandas` import, enforced against regression by a static AST test (`tests/test_cnn_lstm_import_order.py`, PR #439) — this only ever covered those three files, which is exactly why isolation being the default (rather than relying on three files' import order) is the real fix. **Verified against the real native deadlock** (2026-07-27, Round 6 of the known-issues doc) on real production data in the actual macOS arm64 + Framework-Python + TF 2.21.0 + pyarrow 24.0.0 environment the deadlock was originally confirmed on — the prior "not verified against the real native deadlock" caveat that kept this opt-in no longer applies. Tests that drive `run_cnn_lstm_forecast` through an in-process TF mock (`tests/test_forecasting_lookahead.py`, `tests/test_forecast_model_persistence.py`) pin `CNN_LSTM_SUBPROCESS_ISOLATION_ENABLED=False` via a file-wide autouse fixture, since a real spawned worker doesn't see the parent process's monkeypatches — dispatch itself (which path runs, with what arguments) is covered separately by `tests/test_cnn_lstm_isolation_dispatch.py`. **Reproducible fits (2026-07)**: `CNN_LSTM_RANDOM_SEED = 42` (defined once in `cnn_lstm_worker.py`, imported by `forecasting_engine.py`'s in-process fallback branch) is applied via `np.random.seed()`/`tf.random.set_seed()` immediately before every `Sequential([...])` build+`.fit()` call, in BOTH the subprocess-isolated path (`cnn_lstm_worker.fit_predict_cnn_lstm`) and the legacy in-process path — so weight init, dropout, and the validation-split shuffle are deterministic given the same input windows, in either mode. `load_predict_cnn_lstm`/the cached-model inference branch need no seed (no training, no randomness). This does NOT make forecasts identical run-over-run when the underlying `history_df` changes (that's real new data, not noise) — only when re-run against the same input.
- **rag_index/faiss OpenMP thread-pool deadlock (`data/rag_index.py::_cap_faiss_threads`, `docs/known_issues/lightgbm_faiss_libomp_collision_segfault.md`'s Round 2, 2026-07)**: this venv carries THREE independently-compiled copies of `libomp.dylib` (Homebrew's, faiss's own bundled copy, scikit-learn's own bundled copy — none sharing an install name, so dyld treats them as distinct images once more than one is loaded into the same process). Round 1 of the linked doc fixed a SEGFAULT from `tests/test_rag_index.py` eagerly `import faiss`-ing at module scope during pytest collection. Round 2 is a DIFFERENT manifestation that survives Round 1's fix: a genuine DEADLOCK (confirmed 0% CPU, a blocked wait not a spin) the first time `faiss.Index.search()` — specifically the call that enters faiss's OpenMP-threaded parallel code path, unlike construction/insertion of a handful of vectors, both of which complete instantly regardless of process history — runs in a process where a real (non-mocked) `lightgbm.Booster.__setstate__` unpickle (`ml.meta_bootstrap.bootstrap_meta_registry()`) already ran. This CORRECTS, not merely supplements, that doc's original "production was never actually exposed" conclusion — that reasoning rested on import order alone (`data/rag_index.py`'s lazy `import faiss` runs downstream of the cycle's early lightgbm bootstrap), but Round 2's isolated repro shows import order alone is not sufficient: only the first real parallel-region entry matters, and this codebase cannot certify a live cycle never calls `DocumentVectorStore.search()` after `bootstrap_meta_registry()` already ran in the same process. Fixed via `faiss.omp_set_num_threads(1)` (FAISS's own API, called once via `_cap_faiss_threads()` inside `_get_or_create_index()` — the single choke point every real search/add path reaches) rather than the standard `OMP_NUM_THREADS=1`/`KMP_DUPLICATE_LIB_OK=TRUE` env-var workarounds (both tested directly during root-causing; `KMP_DUPLICATE_LIB_OK=TRUE` alone does NOT resolve this specific hang, `OMP_NUM_THREADS=1` does but is a process-wide hammer that would also cap lightgbm's/numpy's/scikit-learn's own OpenMP parallelism) — scoping the fix to faiss's own thread pool alone, unconditionally, in production code, not only in tests, since the risk could not be ruled out for a real cycle. Zero real cost: this module's corpus (`settings.RAG_INDEX_MAX_DOCUMENTS`) is a single operator's sentiment-document archive, nowhere near the scale where FAISS's OpenMP parallelism would matter even if it worked. Verified: full `pytest -q -p no:randomly -m "not network"` (zero `--deselect` flags) re-run 2× clean, including all 6 `TestRealFaissRoundTrip` tests that previously hung the whole suite indefinitely.
- **Historical persistence (`data/historical_store.py`, Tier 2.3)**: `HistoricalStore` is the single SQLite write-path for OHLCV bars (Phase 1), account snapshots (Phase 2), fundamentals (Phase 3), and FRED macro series (Phase 3). `fetch_account_snapshot()` MUST follow the three-tier read order — DB first → JSON cache → live — so the DB row survives a network outage. `get_fundamentals()` caches typed columns AND full `raw_json` from the provider; missing fields → `NaN`, NEVER `0.0` (CONSTRAINT #4). `get_macro()` stores both VIXCLS and T10Y2Y from `DataEngine.fetch_macro_history()` and serves them as named pd.Series; fresh rows (age < `MACRO_REFRESH_HOURS`) skip the network call. Import `HistoricalStore` lazily (inside function bodies, not at module top) everywhere — `data/robinhood_portfolio.py`, `processing_engine.py`, and `macro_engine.py` all use lazy imports to avoid circular imports. `HISTORICAL_STORE_ENABLED=False` disables all DB routing without touching the JSON-cache or live-fetch tiers. **Phase 3 settings**: `FUNDAMENTALS_REFRESH_DAYS` (int, default 1 — days before a cached fundamentals row is re-fetched), `MACRO_REFRESH_HOURS` (int, default 12 — hours before FRED macro series are topped up). **Existing settings**: `HISTORICAL_STORE_ENABLED` (bool, default `True`), `BARS_BACKFILL_DAYS` (int, default 504). **Cache-poisoning guard (2026-07)**: `get_fundamentals()` never upserts a totally empty provider response — doing so would read back as a "fresh" all-NaN cache hit on the next call and suppress retry until `FUNDAMENTALS_REFRESH_DAYS` elapses; a non-empty response with some missing fields is still cached as-is (that's legitimate partial data, not a poisoned cache). **`schema_version` (2026-07)**: `_ensure_tables()` stamps/checks a single-row `schema_version` table (`CURRENT_SCHEMA_VERSION` in `data/historical_store.py`) on every construction. Additive migrations (`ALTER TABLE ADD COLUMN`) remain the actual upgrade mechanism and need no version bump; the stamp exists to WARN (never raise — CONSTRAINT #6) when a DB file was written by a newer build than the one currently reading it, the one drift additive migration can't self-detect.
- **Robinhood auto-refresh gate (`settings.ROBINHOOD_AUTO_REFRESH_ENABLED`, default `False` as of 2026-08 — was `True`)**: `fetch_account_snapshot()`'s Tier 3 (live login) runs automatically whenever the cached snapshot exceeds `max_age_hours` (default 20h) — every one of its ~8 call sites (GUI panels, `investyo_mcp_server.py`, `api/data_api.py`, `data/portfolio_sync.py`, `engine/llm_commentary.py`) inherits this with no shared cooldown, so once credentials start failing, every poll from every surface independently re-attempts a Robinhood login. **Default flipped to `False`** alongside the device-approval login rewrite below: an unattended Tier-3 attempt can never succeed once login requires a human tapping approve on their phone, so leaving automatic re-attempts on by default just spawns a doomed login-worker subprocess every cycle once the cache goes stale. `False` makes the live fetch strictly opt-in: Tier 3 is skipped whenever `force=False`, returning the best available cached snapshot (DB latest, else JSON cache) regardless of staleness, and raising only when nothing is cached anywhere. Only `force=True` (i.e. `python3 main.py --refresh-account`, or the webapp's Connect/Refresh flows, both of which run the login in the supervised worker described below) ever logs in. New env var: `ROBINHOOD_AUTO_REFRESH_ENABLED`.
- **Live-snapshot fabricated-$0-on-swallowed-API-failure fix (2026-08-24, CONSTRAINT #4)**: `_fetch_live_snapshot()`'s `... or {}` / `... or "0"` fallbacks around `r.load_portfolio_profile()`/`r.load_account_profile()` used to silently treat a swallowed `robin_stocks` API failure (confirmed against its source: `request_get(..., dataType='indexzero')` never raises on a non-200/malformed response — it returns bare `None`) as a fabricated `$0` equity/buying-power reading, which was then unconditionally persisted as a genuinely "fresh" cached snapshot for up to `max_age_hours` — because nothing raised, the three-tier DB → JSON-cache → live fallback described above was never actually triggered by this failure mode. Fixed: the two profile dicts are now validated as genuinely non-empty before the live fetch is treated as successful, raising `RuntimeError` otherwise so the existing fallback logic engages correctly. `build_holdings()` returning `{}` is deliberately left unguarded — it is genuinely ambiguous with a real zero-position account (e.g. a fresh cash deposit with nothing bought yet) — a disclosed scope boundary, not a gap; see `docs/known_issues/robinhood_snapshot_fabricated_zero_on_swallowed_api_failure.md` for the full reasoning and both reproduced failure scenarios. A related asymmetry was fixed in the same pass: the live-fetch-*exception* fallback path previously checked only the JSON cache tier, never DB, unlike the "auto-refresh disabled" branch a few lines above it — both now check DB then JSON, consistently.
- **Robinhood login moved from TOTP MFA to device-approval push (2026-08, `data/robinhood_login.py`, `data/robinhood_login_worker.py`, `data/robinhood_session.py`, `api/_rh_login.py`)**: Robinhood's `robin_stocks >= 3.4` login already implements a device-approval push workflow (the operator taps "approve" in the Robinhood app) — *omitting* `mfa_code` from the login call is what triggers it; passing one short-circuits Robinhood into issuing a token directly, bypassing the push entirely, which is exactly why TOTP and device-approval can't coexist. `RH_MFA_SECRET` and the `pyotp` dependency are retired from this codebase entirely — `data/robinhood_portfolio.py::verify_credentials()` no longer exists, and `_login_with(username, password, *, mode: Literal["reject", "device_approval"] = "reject")` is the sole login primitive: `mode="reject"` (default) raises `RobinhoodApprovalRequired(RuntimeError)` immediately, `mode="device_approval"` is the only mode that logs in and is guarded by a structural `os.environ.get("RH_LOGIN_WORKER") != "1"` check that raises a plain `RuntimeError` outside the isolated worker. **Why a killable subprocess is mandatory, not a nicety**: `robin_stocks`' own device-approval prompt loop has no timeout of its own, and its SMS/email fallback calls a blocking `input()` — see `docs/known_issues/robinhood_device_approval_login_hang_risk.md` for the full hazard writeup and the honest list of what is/isn't verified against a real account. `data/robinhood_login.py::start_login(mode: "connect"|"refresh", *, username="", password="")` launches `data/robinhood_login_worker.py` as a fresh, `stdin=/dev/null`, own-process-group subprocess per attempt (never a persistent pool — a pool's abandon-on-timeout semantics would leave an authenticated session alive indefinitely with no way to stop it) and returns a non-blocking `LoginJobState` (`job_id`, `mode`, `phase`, `state` — `running`/`succeeded`/`failed`/`timeout`/`cancelled` — `error_code`, `seconds_remaining`); `login_blocking()` starts a job and blocks until terminal (used internally by `_fetch_live_snapshot()` so every existing synchronous caller keeps its exact call shape while no longer being able to hang, bounded by `settings.RH_LOGIN_DEADLINE_SECONDS` default 180s / `RH_LOGIN_GRACE_SECONDS` default 5s / `RH_LOGIN_STARTUP_SECONDS` default 30s). `data/robinhood_session.py` restores/backs-up `~/.tokens/robinhood.pickle` around each login — `robin_stocks` generates a fresh random `device_token` on every login unless a loadable pickle overrides it, and the library's own write is non-atomic, so a kill mid-write (which a deadline-enforced worker will eventually do) previously left a 0-byte, unloadable file; a stable device token is what makes future approval prompts rare rather than routine. `api/_rh_login.py` glues this into `api/pilots_api.py`'s `POST /brokerage/connect`/`POST /brokerage/refresh` (now async, returning **202** + job status immediately instead of blocking) plus two new endpoints, `GET /brokerage/login/status/{job_id}` and `POST /brokerage/login/cancel/{job_id}`; credential persistence (`data.brokerage_credentials.write_rh_credentials`) happens in a background watcher thread the moment (and only if) a "connect" job reaches `state="succeeded"` — never in the request handler itself. `data/robinhood_client.py::RobinhoodClient.login()` (the separate legacy `ROBINHOOD_USERNAME`/`ROBINHOOD_PASSWORD` client used only by the frozen `gui/panels/live_inventory.py`) is likewise gated behind the `RH_LOGIN_WORKER` marker and now always returns `False` outside it, degrading watchlist discovery to unavailable rather than risking the same hang. New env vars: `RH_LOGIN_DEADLINE_SECONDS`, `RH_LOGIN_GRACE_SECONDS`, `RH_LOGIN_STARTUP_SECONDS`. Tests: `tests/test_robinhood_login.py` (launcher primitives against a stub worker script — success/failure/timeout-kill/child-start-failure/cancel), `tests/test_robinhood_login_worker.py` (pins the exact `robin_stocks` phrase literals the worker's progress-phase scanner depends on against the installed library source, so an upgrade that changes them fails CI loudly instead of silently leaving the UI stuck on "authenticating"), `tests/test_robinhood_session.py`, `tests/test_rh_login_api_glue.py` (the credential-persistence-on-success watcher thread), `tests/test_robinhood_portfolio.py`, `tests/test_brokerage_connect.py`.

- **Robinhood closed-trade ingest — the operator's real sells were invisible everywhere (2026-08, `data/broker_fills_store.py`, `data/robinhood_orders.py`, `data/robinhood_login_worker.py`, `pilots/trade_history.py`)**: root cause, confirmed by reading the code and a real operator account's local state: `data/robinhood_orders.py`'s FIFO reconstruction engine existed and was tested, but (a) its only production caller, `pilots/realized.py`, deliberately injects an empty `orders_fetcher` so a web request never triggers a login — cache-only by design — and nothing else ever called it with a real one; (b) even a real call would have crashed on a dead `from data.robinhood_portfolio import _login` import left over from the device-approval rewrite above. Net effect: the `trades` table stayed at 0 rows regardless of how many real trades the operator closed, and a sold-to-zero symbol silently vanished from the analysis universe (`main.py`'s `held ∪ WATCHLIST ∪ watchlist.txt ∪ discovered`) with no "sold" state — just gone. Fixed in three parts. **(1) Ingest repair**: `data/robinhood_orders.py:fetch_filled_orders(orders_fetcher=None)` now dispatches on `os.environ["RH_LOGIN_WORKER"] == "1"` (uses `r.get_all_stock_orders()` directly inside the worker) or raises the existing `RobinhoodApprovalRequired` outside it (caught by the existing degrade-to-cache path — zero behavior change for the two callers that already inject their own fetcher). `data/robinhood_login_worker.py`'s `refresh`-mode branch now also calls `_ingest_orders_best_effort(emit)` immediately after the account snapshot fetch, using the SAME authenticated session — one device-approval push produces both artifacts instead of a second push per refresh. Four non-negotiable properties enforce this can never turn a successful snapshot refresh into a reported failure: its own nested try/except, strictly-after-the-snapshot ordering, `refresh`-mode only (never `connect`), and a `settings.RH_ORDER_INGEST_BUDGET_SECONDS` (default 60s) wall-clock bound well inside the 180s login deadline, backed by `settings.RH_ORDER_SYMBOL_RESOLVE_MAX` (default 200) capping the per-instrument `get_symbol_by_url` network fanout via a durable resolver-cache seed/write-back (`data/broker_fills_store.py`'s `BrokerInstrumentSymbol` table) so only the FIRST ingest for a given instrument pays that cost. **(2) Durable store**: `data/broker_fills_store.py` (own `Base`/table/`session_scope`/`readonly=True` convention, matching `validation/validation_history_store.py`) persists **fills** (keyed by Robinhood's own `order_id`), not reconstructed `ClosedTrade`s — a dedup key derived from a `ClosedTrade` would be unstable, since FIFO pairs a sell with whichever buy lot is still in the fetch window, and that pairing shifts once an older buy ages out on a later fetch; `closed_trades()` simply re-runs the pure, unmodified `reconstruct_closed_trades()` over the full persisted fill history on every read. `record_fills()` is idempotent (insert-the-diff by `order_id`; a divergent re-fetch keeps the latest value and logs a WARNING; an empty `order_id` is skipped and counted, never given a fabricated key). Gated by `settings.BROKER_TRADE_INGEST_ENABLED` (default `True` — this flag IS the fix, a deliberate exception to "new settings default to today's behavior"). Requires the same session-wide autouse `conftest.py` DB-isolation fixture as `ValidationHistoryStore`/`ExecutionAuditStore` (`_isolate_broker_fills_db_in_tests`) since `recently_closed_symbols()` (below) is reachable from `main._build_universe`/`data.portfolio_sync.build_sync_report`, both exercised by dozens of pre-existing test files with no store of their own. **Sizing isolation is structural, not conventional**: this module never imports `transactions_store`, and nothing under `sizing/`/`execution/` imports it — enforced by an AST guard in `tests/test_broker_fills_store.py` — because production sizing (`strategy_engine.py`'s aggregate Kelly path at `strategy_engine.py:765`, `engine/advisory.py`'s own aggregate path) reads `TransactionsStore.closed_trades_df()` unfiltered by strategy; writing the operator's manual, discretionary Robinhood trades into that table would silently move live position sizing on every symbol the moment the closed-trade count crossed `sizing/kelly.py`'s `MIN_TRADES_REQUIRED` cold-start threshold — confirmed as a real, not hypothetical, risk: with the `trades` table empty, `_raw_kelly_or_vol_target_sizing` returns weight `0.0` for every symbol TODAY (`scale_in = min(1.0, 0/30) = 0.0`); the operator's real 17-round-trip history would have moved `scale_in` to `0.567` and silently turned position sizing ON platform-wide in a single commit had it landed in `trades`. **(3) Universe retention**: `settings.CLOSED_POSITION_RETENTION_DAYS` (default 180, a deliberate behavior change; `0` restores the pre-2026-08 universe exactly) + `settings.CLOSED_POSITION_RETENTION_MAX_SYMBOLS` (default 25) keep a fully-sold symbol in the analysis universe for a bounded window after its most recent real SELL fill (never a buy-only symbol — retention keys off the last sell only), via `data.broker_fills_store.recently_closed_symbols()`. `main.py::_build_universe` unions this 4th (really 6th, counting discovery/fallback) source LAST — after both the `SYMBOL_RATING_AUTO_DROP_ENABLED` subtraction and the `DEFAULT_TICKERS`/Sheet2 empty-fallback decision — because a retained symbol has `held=False` and would otherwise be immediately re-subtracted by auto-drop, or (if unioned even earlier) would silently suppress the empty-universe fallback on an otherwise-cold account. `data/portfolio_sync.py::build_sync_report`/`resolve_universe` carry the identical logic for the webapp/sync path via a synthetic `"closed:recent"` watchlist (`CLOSED_RECENT_LIST_KEY`); `resolve_universe`'s own auto-drop protected set needed an explicit widen to include it (unlike `main.py`, its `tracked` set is built BEFORE the auto-drop subtraction, so the same trap reappears there). A genuine bug found and fixed in the same pass: `async_sync_now()`'s `DEFAULT_TICKERS` persist previously would have baked every retained symbol into `.env` permanently (`DEFAULT_TICKERS` never expires and `resolve_universe` unions it in unconditionally) — now explicitly excludes the synthetic list's symbols from what gets persisted. **New Trade History screen**: `pilots/trade_history.py` (`GET /portfolio/trade-history`, fail-open `require_read_token`) reads the durable store with real pagination and a per-symbol filter — distinct from the pre-existing `pilots/realized.py`/`GET /portfolio/realized`, which stays cache-only and capped at 100 trades for the Portfolio screen's summary panel; `summary` is computed over the full filtered history, not just the returned page. `webapp/src/screens/TradeHistory.tsx` (nav section `"research"`, a Marketplace "Explore" tile, and a "See all →" link from Portfolio's Realized performance panel). **Opt-in analytics wiring (NOT sizing)**: `settings.EVAL_BROKER_TRADES_ENABLED` (default `False`, preserving today's exact NaN behavior) lets `evaluation_engine.evaluate_portfolio()` fall back to broker-reconstructed closed trades for MAE/MFE/'Edge Ratio' on a symbol with NO internal `transactions_store` history — internal history always wins when present, and this never writes `transactions_store`'s `trades` table or reaches any sizing path; `calibration_curve` needs no separate gate since a broker trade genuinely carries no platform-issued `conviction` to report, and its existing `dropna(subset=["conviction", ...])` already excludes these rows. `get_all_stock_orders` (the underlying Robinhood API) is equities-only and short-sale excess is dropped, never fabricated — see `docs/known_issues/robinhood_order_history_window_and_fifo_limits.md` for the full disclosed-limits writeup. New env vars: `BROKER_TRADE_INGEST_ENABLED`, `RH_ORDER_INGEST_BUDGET_SECONDS`, `RH_ORDER_SYMBOL_RESOLVE_MAX`, `CLOSED_POSITION_RETENTION_DAYS`, `CLOSED_POSITION_RETENTION_MAX_SYMBOLS`, `EVAL_BROKER_TRADES_ENABLED`. Tests: `tests/test_broker_fills_store.py`, `tests/test_universe_retention.py`, `tests/test_pilots_trade_history.py`, `tests/test_evaluation_broker_trades_fallback.py`, `tests/test_robinhood_login_worker_orders_ingest.py`, plus extended coverage in `tests/test_robinhood_orders.py`, `tests/test_pilots_api.py`, `tests/test_pilots_strategy_matrix.py`.
- **Portfolio & Watchlist Synchronization (`data/portfolio_sync.py`, Task 1.4)**: any code that needs "the complete universe the operator is tracking" — beyond the held-only or watchlist-only slice — MUST go through `build_sync_report()` / `async_sync_now()`, never re-derive the union ad-hoc. Coverage status drives downstream eligibility: pricing-dependent metrics must consume `CoverageStatus.FULL` symbols only (or `QUOTES_ONLY` if fundamentals aren't required); `EQUITY_ONLY` symbols stay in the equity / holdings view but are excluded from quote-driven calculations (no fabricated price proxy — CONSTRAINT #4). The Sync engine never raises on a coverage gap (dead-letter resilience); it logs an INFO diagnostic per affected symbol and records the failure mode on `SymbolStatus.diagnostic`. **New env var:** `SYNC_WATCHLIST_FILES` (colon-separated paths to additional plain-text watchlist files, one ticker per line, `#` = comment; missing files tolerated silently). The GUI Live Inventory tab's "🔄 Sync Now" persists the discovered universe to `DEFAULT_TICKERS` in `.env` via `gui/env_io.py` (`DEFAULT_TICKERS` is already in `ALLOWED_KEYS` from the Settings Manager tab).
- **Daemon universe-divergence fix (`data/portfolio_sync.py::compute_tracked_universe`/`load_env_watchlist`, 2026-08)**: universe resolution had three divergent implementations. `main.py::_build_universe()` correctly read `WATCHLIST` env/`watchlist.txt` ∪ discovered scan candidates ∪ `held`, falling back to `DEFAULT_TICKERS` only when that whole union was empty — but `pipeline/production_steps.py::AsyncDataFetchStep.run()`, the universe builder the **persistent orchestrator daemon** (`main_orchestrator.py`/`desktop/daemon_runtime.py`, the backend the Pilots PWA runs) actually executes every cycle, **never read `WATCHLIST`/`watchlist.txt` at all** and separately dropped `DEFAULT_TICKERS` outright whenever scan-discovery had any candidate that cycle. A symbol added via `watchlist.txt`, `POST /agentic/watch`, or the Paper Broker order ticket's "+ Add to Watchlist" button therefore reliably reached `main.py`'s universe but never the daemon's — silently disappearing from signals/forecasts/sizing on the very next cycle, no error, no warning. Very likely the actual root cause behind an operator-observed "symbols falling out of the universe" symptom. Fixed via two new shared functions in `data/portfolio_sync.py`: `load_env_watchlist(watchlist_file)` (a verbatim port of `main.py`'s original `_load_watchlist()`) and `compute_tracked_universe(*, held=(), watchlist=(), discovered=(), default_tickers=(), apply_rating_exclusion=True)` (the union + `SYMBOL_RATING_AUTO_DROP_ENABLED` exclusion + fallback-only-if-empty core of `_build_universe()`). Both `main.py::_build_universe()`/`_load_watchlist()` and `AsyncDataFetchStep.run()` now delegate to these instead of maintaining independent unions — `AsyncDataFetchStep` reads `ctx.watchlist_file` (already correctly set to `"watchlist.txt"` by `main_orchestrator.py`'s `RunContext(...)` construction sites, simply never read before this fix). **Disclosed side effect**: this also brings `SYMBOL_RATING_AUTO_DROP_ENABLED` exclusion to the daemon path for the first time — a real, if minor, behavior change for an operator who already has that flag on and runs the daemon. `resolve_universe()`'s own near-duplicate exclusion block (its `DEFAULT_TICKERS` handling is unconditional-union, not fallback-only, by design) was deliberately left untouched — a genuinely different universe semantic, out of scope for this fix. See `docs/known_issues/daemon_universe_watchlist_divergence.md` for the full write-up including the pre-existing `RunContext.build_universe_fn`/`watchlist_file` DI seam this fix routes around (too narrow a signature to carry the async step's `discovered`/watchlist inputs cleanly).
- `config.py`'s `COLUMN_SCHEMA` now includes `RSI_2` and `SMA_5` (both `"format": "number"`/`"currency"`), feeding `processing_engine.calculate_technical_metrics()`'s `ta.rsi(length=2)` / `ta.sma(length=5)` columns (added alongside the existing RSI(14)/SMA(50)/SMA(200) calculations — same causal, vectorized pattern, verified lookahead-free).
- `StrategyEngine.evaluate_security()` and both orchestrator call sites (`main.py`, `main_orchestrator.py`) now also accept/pass `rsi_2: float` and `sma_5: Optional[float]`, propagated into the per-ticker `row` Series as `RSI_2`/`SMA_5`/`Close` for signal modules that need them.
- Position sizing ("Kelly Target") is no longer derived from `score`/`sortino_ratio`/`edge_ratio` brackets. The single source of truth is `StrategyEngine._calculate_kelly_sizing(realized_vol)` → `sizing.kelly`/`sizing.vol_target`, fed by `settings.KELLY_FRACTION` (0.5), `settings.KELLY_CAP` (0.20), `settings.VOL_TARGET` (0.10), `settings.MAX_LEVERAGE` (2.0), `settings.MAX_POSITION_WEIGHT` (1.0). The vol-target fallback (insufficient trade history) can internally compute up to `MAX_LEVERAGE` (2.0x), but `_calculate_kelly_sizing` always applies a final `min(weight, settings.MAX_POSITION_WEIGHT)` clamp before returning — chosen as the middle ground between the old score-bracket system's 25% ceiling and an uncapped 2.0x. Neither `sizing.kelly` nor `sizing.vol_target` enforce this themselves; the clamp lives in `StrategyEngine`, so any other caller of those modules directly must apply its own ceiling.
- **Sizing-decision pipeline + portfolio cap + guardrail telemetry (`sizing/position_sizer.py`)**: `StrategyEngine.evaluate_security()` composes the HMM regime multiplier and meta-label composite on top of `_calculate_kelly_sizing`'s already-clamped weight via ONE ordered function, `size_position()`, instead of the previous inline arithmetic — and reports `SizingDecision.was_capped`/`.binding_constraint` (`"kelly_cap"`/`"vol_target_leverage"`/`"max_position_weight"`/`"portfolio_gross"`/`"escalation"`/`None`), telemetry that did not exist before. The routine HMM regime multiplier is deliberately EXCLUDED from this flag (it's expected signal-driven derating, not a guardrail cap — `Regime_Multiplier` stays its own separate field) so the guardrail doesn't fire on every risk-off cycle. New **portfolio-level gross exposure cap** (`apply_portfolio_gross_cap`, `settings.MAX_PORTFOLIO_GROSS` default `2.0`) is applied cycle-wide by `pipeline/production_steps.py::StrategyEvalStep` on top of — not instead of — the per-name `MAX_POSITION_WEIGHT` ceiling; a capped name's telemetry is overridden to `"portfolio_gross"`. `"Sizing_Was_Capped"`/`"Sizing_Binding_Constraint"` are new schema-driven `config.COLUMN_SCHEMA` columns (Sheet + `state_snapshot.json` on both writers, parity enforced by `tests/test_state_snapshot_parity.py`) plus new GUI surfaces (Strategy Matrix regime-impact section; Observability's "Sizing Cap-Event Audit Trail"). The advisory path (`engine/advisory.py`) keeps its own deliberately-tighter, decoupled cap (`CONFIG["max_single_position_pct"]`=5%) and gets its OWN guardrail telemetry (`Recommendation.sizing_was_capped`/`.sizing_binding_constraint`) rather than being routed through `size_position()`. New opt-in **cap-aware escalation** (`settings.SIZING_CAP_ESCALATION_ENABLED`, default `False`) down-weights a name capped for `SIZING_CAP_ESCALATION_THRESHOLD_CYCLES` (default 5) consecutive cycles by `SIZING_CAP_ESCALATION_FACTOR` (default 0.5), reading history from the new durable `sizing/cap_audit_store.py` (`sizing_cap_events` table, `RunHistoryStore`-pattern DB store, gated by `settings.SIZING_CAP_AUDIT_ENABLED` default `True`), which also drives an OPT-IN `observability.alerts.send_alert("WARNING", ...)` threshold alert (`settings.SIZING_CAP_ALERT_ENABLED` default `False` — KELLY_CAP binding is routine for an established Kelly book, so this doesn't surprise existing deployments with new WARNING logs on upgrade; `settings.SIZING_CAP_ALERT_THRESHOLD_PCT` default 30%). All seven new settings default to today's exact behavior (audit-only aside, which is additive/observational) — see `docs/architecture/signal-engines.md`'s `sizing/position_sizer.py` bullet for the full ordered pipeline, the byte-identical per-symbol equivalence proof, the portfolio-cap sensitivity sweep, and the fixed dead-lettered-ticker telemetry fabrication bug (this repo's sandboxed dev/CI environments have no live-market network access, so a `validation/harness.py` PBO/DSR/Sharpe/MaxDD re-verification could not be run as part of this change — see that doc and the introducing PR for the honest bottom line before enabling non-default settings live).
- **ETF volatility-transmission sizing derate (`risk/etf_transmission.py`, `settings.ETF_TRANSMISSION_SIZING_ENABLED` default `False`)**: Ben-David, Franzoni & Moussawi (2018, *JF*) — ETF arbitrage transmits a shock in one constituent to its otherwise-healthy peers, so a heavily ETF-wrapped name carries extra non-fundamental, non-diversifiable variance that neither `sizing/kelly.py` nor `sizing/vol_target.py` can see (both observe only a single name's own return series). Applied as ONE new `etf_transmission_multiplier` kwarg composed in `size_position()`'s **step 3** alongside `regime_multiplier` — deliberately a post-multiplier and NOT vol-inflation into Kelly, because `kelly_sizing_for_strategy` reads `realized_vol` only in its `< MIN_TRADES_REQUIRED` (30) cold-start branch and never once the book matures (a risk control that vanishes as the book matures is the worst possible failure profile). The multiplier is `transmission_multiplier(ownership_pct, comovement_r2, *, max_derate, ownership_reference, floor)`: explicit, monotone non-increasing in both arguments, bounded to `[floor, 1.0]`, two knobs, no fitted parameters. **Three invariants:** (1) the derate is EXCLUDED from `was_capped`/`binding_constraint` — it is continuous signal-driven derating, not a hard-ceiling event, exactly as for `Regime_Multiplier`, and folding it in would fire the guardrail on every ETF-heavy name and drown out genuine ceiling events in `sizing/cap_audit_store.py` and the `SIZING_CAP_ALERT_THRESHOLD_PCT` alert; it is surfaced as its own `SizingDecision.etf_transmission_multiplier` field instead. (2) **the multiplier is ALWAYS exactly `1.0` on any missing input, NEVER `NaN`** — `clamp_with_binding` passes NaN through, so a NaN multiplier makes `final_weight` non-finite, and `apply_portfolio_gross_cap` EXCLUDES non-finite weights from the gross sum, meaning a coverage gap would shrink the gross denominator and silently LOOSEN the portfolio cap for every covered name (a data outage must never relax a risk limit). This does not conflict with CONSTRAINT #4: the measured COLUMNS stay honestly NaN; the applied MULTIPLIER is an amount of derating, and "derate nothing" is `1.0`. (3) **the advisory path (`main.py` / `engine/advisory.py`) is UNTOUCHED** — it keeps its own deliberately-tighter, decoupled `CONFIG["max_single_position_pct"]` (5%) cap, is not routed through `size_position()`, and receives no derate. Wiring: `pipeline/production_steps.py::_apply_etf_transmission_multiplier` populates the single new `config.COLUMN_SCHEMA` column `ETF_Transmission_Multiplier` (`"format": "number"`) once per cycle before the per-ticker loop (NaN when the flag is off — never computed; coverage-gap fallbacks logged once per cycle with a COUNT, never per name), which `StrategyEvalStep` reads into `evaluate_security(..., etf_transmission_multiplier=...)`; surfaced in `state_snapshot.json` as `etf_transmission_multiplier`, pinned as orchestrator-only in `tests/test_state_snapshot_parity.py`. All four new settings (`ETF_TRANSMISSION_SIZING_ENABLED`, `ETF_TRANSMISSION_MAX_DERATE` 0.30, `ETF_TRANSMISSION_OWNERSHIP_REFERENCE` 0.20, `ETF_TRANSMISSION_MIN_MULTIPLIER` 0.50) default to today's exact behavior; flag-off is byte-identical (proven by a 200-case seeded randomized grid in `tests/test_position_sizer.py`). No `STRATEGY_REGISTRY` entry applies — no adapter in `scripts/refresh_validations.py`, `validation/*`, or `ml/feature_engineering.py` calls `size_position()` or `apply_portfolio_gross_cap()`, so the four deployability gates are unaffected; the sandbox has no live-market network access, so no `validation/harness.py` re-verification could be run.
- **ETF-transmission-adjusted portfolio covariance (`risk/etf_transmission.py::build_transmission_adjusted_cov`, `settings.ETF_TRANSMISSION_PORTFOLIO_ENABLED` default `False`)**: the per-name derate above cannot see the mechanism's actual claim — ETF arbitrage raises CO-MOVEMENT between co-held names, a portfolio-level effect — so this feeds `apply_portfolio_gross_cap()`'s EXISTING `cov_matrix`/`target_vol` risk-aware path (unreachable from production until the "Reduction-only guarantee" fix above) with an ETF-co-ownership-inflated covariance matrix, rather than a second portfolio-cap mechanism. `cov_adj[i,j] = cov[i,j] * (1 + inflation * overlap[i,j])` for `i != j` ONLY — the diagonal is never touched, since per-name variance is already the derate's job. `overlap[i,j]` (`_pairwise_etf_overlap`) is cosine similarity, in `[0, 1]`, of two symbols' ETF-basket weight vectors. **PSD repair is mandatory**: multiplicative off-diagonal inflation is not guaranteed to preserve positive semi-definiteness, and `portfolio_vol_target` has no PSD check of its own — an indefinite matrix would saturate the leverage scalar at `max_leverage`, levering the book up on a broken risk estimate; `_nearest_psd` eigenvalue-clips up to a floor (see below) and reconstructs. **Never a partially-covered matrix**: `portfolio_vol_target` ZEROES OUT any symbol missing from `cov_matrix` (its own correct behavior for unknowable risk, but far harsher than the sum-of-|weight| fallback this replaces), so `pipeline/production_steps.py::_build_etf_transmission_cov_matrix` insists on FULL coverage across the cycle's universe before returning anything but `None`; any gap degrades the whole cycle back to `cov_matrix=None`. `target_vol` reuses the existing `settings.VOL_TARGET` rather than a redundant new setting. New settings (`ETF_TRANSMISSION_PORTFOLIO_ENABLED`, `ETF_TRANSMISSION_COV_INFLATION` default `0.25`, `ETF_TRANSMISSION_COV_WINDOW_DAYS` default `60`) default to today's exact behavior. Tests: `tests/test_etf_transmission_portfolio.py` (pure math, incl. a hand-constructed indefinite 3-asset matrix proving the PSD repair actually engages), `tests/test_production_steps_etf_transmission_portfolio.py` (wiring, coverage-gap fallback). Same `STRATEGY_REGISTRY`/no-live-network caveats as the per-name derate above. **Two real bugs the follow-up 2-D sensitivity sweep (`tests/test_etf_transmission_sensitivity_sweep.py`, the plan's own "non-negotiable for PR 4" verification item, not delivered with the original PR) caught and fixed in-repo**: (1) the covariance was fed to `apply_portfolio_gross_cap` on its native DAILY scale while `VOL_TARGET` is annualized by convention everywhere else — `ETF_TRANSMISSION_COV_INFLATION` was a complete no-op in production until `_build_etf_transmission_cov_matrix` annualized it (`*252`, matching `Realized_Vol_60D`'s `*sqrt(252)` convention for std); (2) `_nearest_psd`'s eigenvalue floor was a fixed absolute `1e-10`, disconnected from the matrix's own scale — on a realistic 40-symbol book at high inflation this left a condition number of ~1e8 (~30,000x worse than the unadjusted matrix), now bounded near `1e6` via a floor relative to the matrix's own max eigenvalue (`max(epsilon, 1e-6 * max_eigenvalue)`). Also confirmed via the sweep: the double-count between the per-name derate and the portfolio covariance is real and measured (not merely acknowledged) — ~0.2% of baseline gross at the grid's extremes — and `MAX_DERATE` is NOT always strictly monotonic in final gross (a small, bounded, expected reactive-vol-target effect, not a defect; see the doc for why).
- **ETF transmission operator visibility (`gui/panels/observability.py::_render_observability_etf_transmission`)**: a read-only Mission Control sub-section showing each of the three master switches' (`ETF_TRANSMISSION_ENABLED`/`_SIZING_ENABLED`/`_PORTFOLIO_ENABLED`) ON/OFF state plus a per-symbol table of `ETF_Ownership_Pct`/`ETF_Comovement_R2`/`ETF_Primary_Wrapper`/`ETF_Transmission_Multiplier` sourced from `state_snapshot.json`, sorted most-derated-first. Row extraction/sorting is a pure helper, `gui.observability_panel_helpers.etf_transmission_rows` (unit-tested, `tests/test_observability_panel.py`), matching this codebase's Streamlit-free-logic convention; the panel never writes and degrades to an info message (never a table of fabricated nulls) when the measurement gate is off or no symbol has coverage. All nine `ETF_HOLDINGS_*`/`ETF_TRANSMISSION_*` settings are also GUI-writable via `gui/panels/settings_manager.py`'s standard widgets, allowlisted in `gui/env_io.py::ALLOWED_KEYS`. See `docs/signals/etf_transmission.md`'s "Where it's surfaced" section for detail.
- `processing_engine.calculate_fundamental_metrics()` now accepts an optional `realized_vol_60d_map: dict[str, float]` parameter (sourced from `calculate_technical_metrics()`'s `Realized_Vol_60D`, itself exposed from `calculate_momentum_metrics()`'s already-lookahead-free internal computation) and computes five multifactor raw inputs per ticker: `book_to_market` (1/P/B), `earnings_yield` (1/P/E), `quality_factor_score` (mean of whichever are present among `returnOnEquity`, `operatingMargins`, `grossMargins` — all fractions; mean not sum keeps 1/2/3-metric tickers on one cross-sectional z-score scale, identical `Quality_Z` to the old `ROE + operating margin` sum when all tickers carry the same two metrics; falls back to `-debt_to_equity` only when none of the three are present), `low_vol_score` (negative 60-day realized vol), `log_market_cap`. All five are `NaN` (never fabricated) when the underlying field is unavailable. `calculate_technical_metrics()` also now surfaces `Realized_Vol_60D` itself (`NaN`, not `0.0`, when <60 valid daily returns exist).
- `config.COLUMN_SCHEMA` now includes `Value_Z`, `Quality_Z`, `LowVol_Z`, `Size_Z`, `Multifactor_Composite` (all `"format": "number"`), produced by `signals/multifactor.py`'s `pre_compute()` and written back into `dashboard_df` by `main_orchestrator.py` immediately after `global_registry.run_pre_compute()` (the only signal module whose *outputs*, not just its inputs, need writing back to the dashboard — XSec momentum writes its inputs before `pre_compute` and never needs an outputs write-back since its rank dict isn't surfaced as its own columns the same way).
- `config.COLUMN_SCHEMA` now also includes `sellRange` (`"format": "string"`, header `"Sell Range"`), placed immediately after `buyRange` in the TACTICAL EXECUTION section. Produced by `strategy_engine.apply_sell_side_range` (see strategy_engine.py entry above) and written into `dashboard_df` by both `main.py` and `main_orchestrator.py` from `strategy_output['sellRange']`. Also surfaced in (a) the `main_orchestrator.py` JSON payload printed at end of run, (b) `_write_state_snapshot()`'s per-signal dict as `"buy_range"` / `"sell_range"` for the Streamlit observability dashboard, (c) the `daily_report_template.html` summary table column and per-ticker advice card. The Sheets sink (`main.py`) writes `sellRange` to the "Sell Range" column with no additional plumbing because `config.get_headers()` / `get_internal_keys()` are schema-driven.
- `config.COLUMN_SCHEMA` now also includes `HMM_Risk_On_Probability` (`"format": "percent"`). `run_pipeline()` in `main_orchestrator.py` now accepts an optional `data_engine: IDataProvider` parameter, used to call `MacroEngine.compute_hmm_risk_on_probability(tech_raw.get('SPY'))` and pass the result into `MacroEconomicDTO(hmm_risk_on_probability=...)`. `processing_engine.process_macro_regime()` surfaces it into the dict consumed by `compile_dashboard()`, which writes `HMM_Risk_On_Probability` into every row (`NaN`, never fabricated, when the HMM didn't run). `main()`'s offline-fallback branch (constructing a fresh `MockDataEngine`) now reassigns the outer `de` reference too, so `data_engine=de` passed to `run_pipeline()` is always consistent with whatever engine actually produced `tech_raw`/`macro_raw`/`fund_raw` for that run.
- **Gaussian HMM Regime Detector tuning & diagnostics (`regime/hmm_regime.py`, `validation/regime_diagnostics.py`, `scripts/audit_regime_model.py`, 2026-08)**: `HMMRegimeDetector` supports operator-tunable hidden state counts (`settings.HMM_N_STATES`, default 3), covariance structures (`settings.HMM_COVARIANCE_TYPE` `"diag"` default | `"full"` | `"spherical"` | `"tied"`, with multi-dimensional trace variance sorting for `full`), EM iterations (`settings.HMM_N_ITER`, default 150), and tolerances (`settings.HMM_TOL`, default 1e-4). `build_feature_matrix` supports optional credit spread (`BAMLH0A0HYM2`, `settings.HMM_CREDIT_SPREAD_FEATURE_ENABLED` default `False`) and 20D-60D vol term structure spread (`settings.HMM_VOL_TERM_SPREAD_FEATURE_ENABLED` default `False`). `MacroEconomicDTO` dynamically sources its downgrade threshold (`settings.HMM_RISK_ON_DOWNGRADE_THRESHOLD`, default 0.30) and recession agreement threshold (`settings.HMM_RISK_OFF_AGREEMENT_THRESHOLD`, default 0.70) from `settings`. `validation/regime_diagnostics.py` delivers causal walk-forward backtesting and model comparison (AIC/BIC), executable via `python -m scripts.audit_regime_model [--compare] [--json]`. New settings: `HMM_COVARIANCE_TYPE`, `HMM_N_ITER`, `HMM_TOL`, `HMM_RISK_ON_DOWNGRADE_THRESHOLD`, `HMM_RISK_OFF_AGREEMENT_THRESHOLD`, `HMM_CREDIT_SPREAD_FEATURE_ENABLED`, `HMM_VOL_TERM_SPREAD_FEATURE_ENABLED`, `KILLSWITCH_VIX_THRESHOLD_AGREED`, `KILLSWITCH_SAHM_THRESHOLD_AGREED`. Tests: `tests/test_hmm_no_lookahead.py`, `tests/test_hmm_synthetic.py`, `tests/test_hmm_state_persistence.py`, `tests/test_macro_hmm_integration.py`, `tests/test_regime_diagnostics.py`. **State-labeling fix (2026-08-22)**: `identify_states_by_vol()` previously mislabeled hidden states for `covariance_type="spherical"` (hmmlearn's public `covars_` getter returns a malformed shape for spherical, silently tripping an unlogged arbitrary-index fallback on every fit) and could mislabel `"tied"` (ranked states by an undirected mean-vector norm instead of a directional risk proxy) — reproduced a genuine crash regime labeled `"bull"`, which could have silently under-reported risk to `MacroEconomicDTO.killSwitch`'s HMM-agreement fast-trigger. Both are fixed (`spherical` now reads the compact internal `_covars_` array, matching this file's own warm-start precedent; `tied` now ranks by a directional risk-feature mean); the length-mismatch fallback now logs an error instead of failing silently; `n_states >= 4` now correctly labels the worst state `"bear"` instead of a generic `"state_<n-1>"`. `tied` covariance, while now correctly *labeled*, has a separate structural limitation for regime detection specifically (verified: its EM fit collapses to a single dominant state on realistic calm-vs-turbulent variance ratios) and is not recommended — see `docs/known_issues/hmm_regime_state_mislabeling_spherical_tied.md` and `docs/regime_model_tuning_guide.md`'s Covariance Structures section for full detail.
- **Advisory state-snapshot telemetry parity (`reporting/state_snapshot.py`)**: the advisory entry-point writer (`write_state_snapshot`, used by the `main.py` advisory path) now emits the SAME telemetry the `main_orchestrator.py` `_write_state_snapshot()` path already surfaced, so switching to the advisory orchestrator no longer blanks the GUI Observability / Report-Viewer tabs. Top-level macro recession telemetry (`sahm_rule`, `high_yield_oas`, `yield_curve`, `hmm_risk_on_probability`) is sourced from the injected `macro_dto`; each per-signal entry carries `garch_vol` and `hmm_risk_on` plus the five multifactor keys (`value_z`, `quality_z`, `lowvol_z`, `size_z`, `multifactor_composite`). `garch_vol` reads from `engine.advisory` `Recommendation.key_indicators`. The five multifactor keys are likewise sourced from `Recommendation.key_indicators` — `engine.advisory.evaluate()` looks up `context_extras["multifactor_scores"][symbol]` (the same dict `main._build_context_extras()` builds via `global_registry.run_pre_compute()` and threads into `StrategyEngine.evaluate_security()` for scoring) and copies `Value_Z`/`Quality_Z`/`LowVol_Z`/`Size_Z`/`Multifactor_Composite` onto the snake_case `key_indicators` keys the snapshot writer reads. `NaN` (JSON `null`, never a fabricated 0.0 — CONSTRAINT #4) when `context_extras` is `None`, pre-compute failed, or the symbol has no entry (e.g. microcap-excluded, or not part of this cycle's universe). Covered by `tests/test_advisory.py::TestContextExtrasThreading` (engine-level: real values populate, absence degrades to NaN) and `tests/test_state_snapshot_advisory.py` (writer-level serialization contract).
- **Options matrix integrity (`technical_options_engine.py` + Gravity STEP 38)**: every premium-selling directive rendered by the GUI's Technical Options Matrix tab, or returned by `OptionsPricingRecommender.generate_strategy_pricing_matrix`, MUST satisfy two structural invariants — **strike grid** (every leg on the `$0.50` USD grid via `_on_strike_grid`) and **delta-target tolerance** (resolved BS delta within `±0.05` of the conventional target in `EXPECTED_DELTA_TARGETS`). The validator is `validate_directive_integrity(directive)`. Gravity `step_38_options_matrix_integrity_audit` exercises schema hydration, strike-grid compliance, delta tolerance, off-grid rejection, VIX>30 regime gate, CREDIT EVENT regime gate, and low-IVR debit-spread routing. Tests: `tests/test_options_matrix.py`.
- **Market-data layer (`data/market_data.py`)**: All quote/bar/fundamentals fetches outside the existing `DataEngine.fetch_technical_raw()` path MUST go through `CompositeProvider` (the `MarketDataProvider` ABC). `CompositeProvider` auto-selects Alpaca (when `ALPACA_API_KEY` + `ALPACA_SECRET_KEY` present) or yfinance (zero config). `MARKET_DATA_PROVIDER` env-var can force either. Fundamentals are Yahoo statement-derived via `YahooFundamentalsProvider` (`data/yahoo_fundamentals.py`, `SOURCE="yahoo_computed"`, PRIMARY) with a raw yfinance `.info` (`YFinanceProvider`) emergency fallback; `FUNDAMENTALS_SOURCE` (`"yahoo"` default | `"yfinance_info"`) selects the primary. Finnhub is NO LONGER a fundamentals source (`FinnhubProvider` retained but unwired — `FINNHUB_API_KEY` now feeds only `signals/news_catalyst.py`). Two scale rules: `dividendYield` emitted as a fraction (consumed as-is), `debtToEquity` emitted ×100. Beta = `Cov(stock,SPY)/Var(SPY)` over `BETA_LOOKBACK_DAYS` (504). In-process quote cache (TTL `MARKET_DATA_QUOTE_TTL_SECONDS`=30 s, never persisted to disk). `is_stale=True` on any yfinance quote — treat as delayed by design. `MarketDataError` is the typed exception for dead-letter resilience. `get_provider()` / `reset_provider()` manage the module-level singleton. **New settings**: `settings.MARKET_DATA_PROVIDER`, `settings.FUNDAMENTALS_SOURCE`, `settings.BETA_LOOKBACK_DAYS`, `settings.FINNHUB_API_KEY` (news-only), `settings.MARKET_DATA_QUOTE_TTL_SECONDS`.
- **Data-source policy for NEW live-data-dependent features (2026-08-25, operator decision)**: when building a new capability that needs live market data this codebase doesn't already have, the only acceptable sources are **FMP or Yahoo (yfinance)**. Do not reach for Alpaca or Finnhub as the basis for a new capability, even though both are legitimate, already-wired `CompositeProvider`/fundamentals backends for their existing, established uses (Alpaca as an opt-in `MARKET_DATA_PROVIDER`/FMP-fallback-chain member; Finnhub as `signals/news_catalyst.py`'s opt-in news fallback per the bullet above) — this rule is about what a NEW feature is allowed to depend on, not a retroactive removal of either provider's existing role. If neither FMP nor Yahoo has the data a new feature needs, the answer is to disclose the gap (CONSTRAINT #4 — an honest "unavailable," not a fabricated or third-provider-only value), not to build the feature around a provider this codebase doesn't treat as a primary source. Concrete precedent: `pilots/lob_simulator.py`'s live LOB queue-fill endpoint needed a genuine per-bar trade-count field to calibrate `theta_market` — Alpaca's SDK has one (`Bar.trade_count`), FMP and yfinance don't — and a full implementation was built, tested, and PR'd on top of it before this rule was invoked; the PR was closed unmerged and the branch abandoned specifically because it depended on Alpaca rather than FMP/Yahoo, not because the code itself was wrong. See `docs/known_issues/lob_simulator_uncalibrated_live_arrival_rates.md`'s "Alpaca- and Robinhood-based calibration both investigated and rejected" section for the full writeup, including why a live Robinhood Level-2 price-book alternative was also investigated and rejected (device-approval-gated login can't serve a stateless API request; a point-in-time depth snapshot isn't itself an arrival rate).
- Google Sheets auth uses a service account via `credentials.json`.
- **Broker execution is best-effort**: errors in `_execute_broker_orders` are logged as ERROR and never propagate to crash the analysis pipeline. The pipeline's value (signals, HTML report, JSON payload) must not be held hostage to broker connectivity.
- **Broker abstraction**: all order submission in orchestrator and strategy code MUST go through `OrderManager` (never directly to `AlpacaBroker` or any concrete broker). Type-annotate against `BrokerBase`; swap paper ↔ live by changing one constructor call.
- **Idempotency**: every order intent submitted via `OrderManager.submit_order_with_idempotency` gets a deterministic `client_order_id` from `make_client_order_id(strategy_id, symbol, side, qty, timestamp)`. Do NOT fabricate or reuse IDs manually.
- **Dry-run is enforced at manager level**: `OrderManager._submit_with_retry` checks `intent.dry_run` before calling any broker — this means MockBroker tests in the test suite also correctly see the dry-run guard without implementing it themselves. AlpacaBroker also has its own redundant guard as a belt-and-suspenders, but the manager-level check is the authoritative one.
- **Reconciliation alerts**: set `ALERT_WEBHOOK_URL` in `.env` to a Slack/Discord incoming-webhook URL; `reconcile_state` fires it on any position drift. The webhook call uses `urllib.request` (no extra dependency). Failures in the alert path are logged but never swallowed silently in a bare except.
- **New settings for broker**: `DRY_RUN` (default `false`), `ALERT_WEBHOOK_URL` (default `None`). Both are optional; the orchestrator degrades gracefully when either is absent.
- **Free-first news/attention sourcing expansion (2026-07)**: four follow-on sources/features closing the gaps between the existing free-first sentiment pipeline (`data/sentiment_sources.py`, `signals/news_catalyst.py`) and local FinBERT actually running. (1) **Local FinBERT made real**: CPU `torch` moved to `requirements-optional.txt` (was previously undeclared, so FinBERT silently ran in keyword-lexicon fallback everywhere); `signals/news_catalyst.py::score_headlines()` is a new batched entry point (`settings.FINBERT_BATCH_SIZE`, default 16) returning the full 3-class softmax per headline instead of one collapsed scalar, backed by a content-hash `finbert_score_cache` table in `data/historical_store.py` (`settings.FINBERT_SCORE_CACHE_ENABLED`, default `True`) so unchanged headlines aren't rescored; `_score_headline()` stays a thin, cache-bypassing wrapper for existing callers. (2) **`GoogleNewsRSSSource`** (`"google_news"` in `data/sentiment_sources.py`) — Google News RSS search, `f"{symbol} stock"` query (no company-name-resolution dependency added), fuzzy-title dedup (opaque redirect links defeat URL dedup), `settings.GOOGLE_NEWS_LOOKBACK_WINDOW` (default `"7d"`). (3) **EDGAR full-text search (EFTS)** — `EdgarSource` gained an opt-in extension ingesting 10-K/10-Q filing BODY text (not just the always-on 8-K headline feed), gated by `settings.EDGAR_FULLTEXT_ENABLED` (default `False`), `settings.EDGAR_FULLTEXT_FORMS` (default `"8-K,10-K,10-Q"`), chunked to `settings.EDGAR_FULLTEXT_CHUNK_TOKENS` (default 512); shares `data/edgar_fundamentals.py`'s SEC throttle. (4) **`Sector_Heat_Factor`** (GDELT per-sector article-VOLUME/attention proxy, Gaussian-smoothed, `data/sentiment_sources.py::compute_sector_heat_factors`) and **`Attention_Score`** (Wikipedia Pageviews-based investor attention, DEG(2011)-style transform, `data/attention_sources.py`, optional 429-tolerant `pytrends` fallback that is never load-bearing) both ship as `pipeline/production_steps.py`-populated dashboard feature COLUMNS only — no `signals/` module, no `SIGNAL_WEIGHTS` entry — gated by `settings.SECTOR_HEAT_ENABLED`/`settings.WIKIPEDIA_ATTENTION_ENABLED`/`settings.PYTRENDS_ENABLED` (all default `False`), and are `ORCHESTRATOR_ONLY_FIELDS` in `tests/test_state_snapshot_parity.py` (no advisory-path source exists for either). **All ten new settings default to today's exact behavior** — nothing above is active until explicitly enabled. See `docs/architecture/data-layer.md`'s `data/sentiment_sources.py`/`data/attention_sources.py` entries and `docs/signals/sector_heat_factor.md` for full detail.
- **Google Trends Overlapping Window Stitcher & ASVI Attention Engine (`data/trends_stitcher.py`, 2026-08)**: modular data processing engine integrating Google Trends Search Volume Index (SVI) series and FMP OHLCV data for quantitative attention modeling. Implements (1) `GoogleTrendsStitcher` — overlapping 90-day daily interval scaling algorithm computing scaling factor $f = \sum SVI_A / \sum SVI_B$ across non-zero overlapping days to eliminate boundary step-discontinuities across long-term SVI series; (2) `ASVICalculator` — Da, Engelberg & Gao (2011) Abnormal Search Volume Index ($ASVI_t = \ln(SVI_t) - \ln(\text{Median}(SVI_{t-k \dots t-1}))$, shifted strictly by 1 day to guarantee zero lookahead bias); and (3) `FMPDataLoader` — standardized daily OHLCV bar generation and technical feature computation (EMA-12, EMA-26, MACD, RSI-14) for sequence input tensors. See `docs/signals/google_trends_asvi.md` and `tests/test_trends_stitcher.py`.
- **Google Trends Live Fetching & Persistence (`data/google_trends_client.py`, `data/trends_store.py`, 2026-08)**: Live SVI data fetching is enabled via `pytrends` with robust process-wide rate limiting, exponential backoff, and a cooldown circuit breaker that mirrors the `_gdelt_get` design, preventing HTTP 429 errors. Raw downloaded windows and stitched continuous SVI series are persisted in a local SQLite database (`raw_trends_downloads` and `stitched_google_trends` tables in `trends_store.py`) enabling point-in-time reads and minimizing redundant network calls.
- **Phase 4: LSTM-Attention Forecaster (`cnn_lstm_worker.py`, `ml/asvi_feature_engineering.py`, 2026-08)**: Deep LSTM-Attention model trained on a 15-feature sliding window (Target ASVI, Sector-Proxy ASVI, Open, High, Low, Close, Volume, RSI_14, EMA_12, EMA_26, MACD, MACD_Signal, MACD_Hist, Realized_Vol_60D, SMA_50). The attention mechanism exposes raw `attention_weights` for temporal lag visualization. Sector ASVI utilizes the real `FMPDataLoader.get_fundamentals()` dynamically (inheriting the native 6h `FUNDAMENTALS_CACHE_TTL_SECONDS` cache) mapping the string sector to a SPDR ETF proxy (e.g. Technology -> XLK) via `SECTOR_TO_ETF`, securely falling back to SPY on miss. Neural execution safely leverages the `cnn_lstm_worker.py` TF-isolated subprocess pool to avoid deadlocks. Diagnostics are wired through `POST /pilots/ml/lstm-attention-forecast`.

- **Shared GDELT rate limiter (`data/sentiment_sources.py::_gdelt_get`, 2026-07)**: both GDELT consumers — `GDELTSource` (`mode=artlist`, per-symbol tone documents) and `GDELTVolumeSource` (`mode=timelinevol`, the `Sector_Heat_Factor` path) — MUST issue every request through the single module-level limiter, never `requests.get` directly. The courtesy budget is per-HOST, so two independently unthrottled clients in one process is exactly how a 429 storm starts (same rule that makes `data/etf_holdings.py` reuse `data/edgar_fundamentals.py`'s `_throttle` instead of opening a second SEC client). The concrete failure: `GDELTSource.fetch()`'s windowed backfill is ONE request for a live per-cycle `since` and so looked healthy, but `scripts/backfill_sentiment_history.py --months 6` is ~26 unspaced requests per symbol — measured 2026-07-29 against a 33-symbol universe, GDELT answered HTTP 429 to substantially all of them, archiving nothing. Three bounds: minimum issuance spacing (`GDELT_MIN_REQUEST_INTERVAL_SECONDS`, default 5.0, lock held across the sleep), retry with exponential backoff honouring `Retry-After` (`GDELT_MAX_RETRIES` 2, `GDELT_RETRY_BACKOFF_SECONDS` 5.0), and a cooldown circuit breaker that SKIPS GDELT calls outright for `GDELT_COOLDOWN_SECONDS` (300) after `GDELT_COOLDOWN_THRESHOLD` (3) consecutive FAILED requests — 429, 5xx, or transport error alike — the bound that keeps an unresponsive host from turning a long backfill into hours of certain-to-fail requests while EDGAR/Finnhub/Reddit starve for wall-clock budget. Counting transport errors is deliberate and was corrected during this change: the first cut counted only 429s, and a live re-test found GDELT had switched to read-timing-out, so a single 6-month symbol still burned 262 s for zero documents (26 windows × a 10 s timeout). From the caller's side "refusing us" and "not answering us" have identical cost and identical remedy; requiring CONSECUTIVE failures is what keeps one flaky socket from opening the cooldown. Retries, by contrast, stay 429/5xx-only — an immediate retry of a read timeout just times out again at full cost, and a 404 is a bad query, not an overloaded host. Measured after the fix: 262 s → 30.3 s for that same symbol. `GDELTSource.fetch()` treats `GDELTUnavailable` — and only that — as a signal to abandon the remaining windows; every other per-window failure keeps skip-and-continue. New `SentimentSource.deadline` seam: `CompositeSentimentSource` checks its budget BETWEEN sources, which cannot interrupt a 26-window loop already running, so it now assigns `source.deadline = self._cycle_deadline` and any source that loops internally must consult `deadline_exceeded()`. **Unlike most flags in this codebase these defaults are NOT a no-op**: a live cycle now covers fewer symbols' GDELT documents per pass than the unthrottled (largely 429-ing) version attempted to, and GDELT publishes no hard rate-limit number, so these values are a conservative choice rather than a documented contract. `GDELT_MIN_REQUEST_INTERVAL_SECONDS=0` + `GDELT_MAX_RETRIES=0` + `GDELT_COOLDOWN_THRESHOLD=0` reproduces the prior behavior exactly. **New env vars**: `GDELT_MIN_REQUEST_INTERVAL_SECONDS`, `GDELT_MAX_RETRIES`, `GDELT_RETRY_BACKOFF_SECONDS`, `GDELT_COOLDOWN_THRESHOLD`, `GDELT_COOLDOWN_SECONDS`.
- **Credential reads MUST go through `settings.X`, never `os.environ` directly (`signals/news_catalyst.py::build_finnhub_client`, fixed 2026-07)**: this function read `os.environ.get("FINNHUB_API_KEY", "")`, the one outlier among this codebase's credential-gated sentiment sources — `RedditSource`/`EdgarSource` in `data/sentiment_sources.py` both correctly read `settings.REDDIT_CLIENT_ID`/`settings.EDGAR_USER_AGENT`. Pydantic-settings' `env_file=".env"` loads `.env` into the `Settings` model directly; it does NOT also copy the value into the process's real `os.environ`. An operator whose only source for a key is `.env` (the documented, normal case) got a silently `None` client forever, indistinguishable from "this source genuinely has nothing this cycle" — no error, no warning. Confirmed live 2026-07-29: a real Finnhub key in `.env` produced zero documents across a 33-symbol, 6-month backfill until fixed. Existing tests masked this because they `patch.dict(os.environ, {"FINNHUB_API_KEY": ...})`, which DOES mutate real `os.environ` — unlike a `.env` file — so they never observed the gap; corrected to `patch("settings.settings.FINNHUB_API_KEY", ...)` matching the Reddit/EDGAR test convention. Any new credential-gated source MUST follow the `settings.X` pattern, never read `os.environ` for a value that might only live in `.env`. **Two further instances of the same bug class, fixed 2026-07-31**: `prompt_registry/registry.py::_build_registry_from_settings` and `prompt_registry/__main__.py::cmd_publish` read 8 keys via `os.environ.get(...)` instead of `settings.settings.X` (`PROMPT_REGISTRY_ENABLED`, `PROMPT_REGISTRY_PINS`, `PROMPT_REGISTRY_SIGNING_KEY`, `PROMPT_REGISTRY_BACKEND`, `PROMPT_REGISTRY_URL`, `PROMPT_REGISTRY_TOKEN`, `PROMPT_CACHE_DIR`, `PROMPT_CACHE_KEEP_VERSIONS`; `PROMPT_REGISTRY_PINS` is now read as a native dict, the redundant `json.loads` removed — `PROMPT_REGISTRY_CREDENTIALS` intentionally still reads `os.environ`, since no `settings.py` field exists for it yet); and `data/market_data.py`'s `CompositeProvider` (`__init__`, `_select_quote_provider`, `get_intraday_bars`, `get_fundamentals`, `YahooFundamentalsProvider._beta_period`) read `MARKET_DATA_PROVIDER`, `ALPACA_API_KEY`/`ALPACA_SECRET_KEY`, `FUNDAMENTALS_SOURCE`, `MARKET_DATA_QUOTE_TTL_SECONDS`, `MARKET_DATA_BARS_TTL_SECONDS`, `FUNDAMENTALS_CACHE_TTL_SECONDS`, `FUNDAMENTALS_NEG_CACHE_TTL_SECONDS`, and `BETA_LOOKBACK_DAYS` the same wrong way — both switched to `settings.settings.X`.
- **Prompt Registry settings-plumbing fixes (2026-07-31)**: beyond the `os.environ`→`settings.X` fix above (`prompt_registry/registry.py`/`prompt_registry/__main__.py::cmd_publish`), a second, independent bug in the same module: `prompt_registry/__main__.py`'s `cmd_pin`/`cmd_rollback` called `json.dumps(dict(...))` on the pins payload BEFORE passing it to `gui.env_io.write_setting("PROMPT_REGISTRY_PINS", ...)` — but `write_setting` already JSON-encodes `_JSON_KEYS` values internally, so `.env` ended up with a JSON string literal wrapping real JSON (double-encoded). `_build_registry_from_settings`'s `isinstance(pins, dict)` check then failed silently and every CLI-set pin was dropped without error. Fixed by passing the plain `dict`. **Rule**: any `_JSON_KEYS` value passed to `gui.env_io.write_setting`/`write_many` MUST be a plain Python object, never pre-`json.dumps`'d — `write_setting` owns the encoding.
- **Degenerate-std guard convention**: any ratio that divides by a computed standard deviation or drawdown must guard with `< 1e-12` (or `>= 1e-12` for the "valid, proceed" branch), never an exact `== 0` / `> 0` check — floating-point noise from a constant or near-constant series lands *near*, not *at*, zero. The original incident: a flat/degenerate returns series produced `std() ≈ 1e-16`, and an exact-equality guard let `mean / std` explode to roughly `-7.7e16` in the Pilots PWA's Regime Navigator. The `1e-12` threshold matches the original site, `risk/etf_transmission.py::_DEGENERATE_STD`, and has since been propagated to: `validation/metrics.py::sharpe_ratio` and its CPCV per-path Sortino guard; `validation/harness.py`'s full-sample Sortino guard and two Calmar guards (in-sample and OOS-gate branches); `evaluation_engine.py::calculate_equity_curve_metrics`'s Sharpe and Calmar guards; `simulation_engine.py::cost_sensitivity_curve`'s std guard; `processing_engine.py::calculate_technical_metrics`'s Sortino guard (feeds the live `Sortino_Ratio` column); `signals/multifactor.py::_zscore_winsorize`'s std guard (feeds the live `Multifactor_Composite` column); and `scripts/refresh_validations.py`'s two Sortino-adapter guards (`_build_sortino_drawdown_adapter`, `_build_signal_replay_adapter`). Any new ratio-over-std/drawdown computation MUST use this same threshold, not a fresh exact-equality check.
- **Graduated-degrade convention for N-way blends**: an aggregate/blend of independent estimators (signal modules, ensemble models, ETF-wrapper composites) must never use an `any()`/`all()` all-or-nothing readiness gate that lets one immature/missing component silence the others' real signal — exclude the immature component and renormalize/proceed over survivors instead. Reserve true all-or-nothing gating for structurally coupled objects (a covariance matrix) or deliberate worst-case-dominates safety gates (a stress-scenario deployability check), and comment explicitly why when using it. Generalizes `signals/multifactor.py`'s `.mean(skipna=True)` quality score and `signals/aggregator.py`'s per-module `is_active_in_regime`/`DISABLED_SIGNAL_MODULES` skip. Fixed at: `forecasting/forecast_tracker.py::compute_skill_weights_from_stats` (+ its two callers in `pilots/observability.py`), `risk/etf_transmission.py::build_etf_return_composite`, `signals/registry.py::compute_all`/`compute_all_vectorized`. See `docs/known_issues/graduated_degrade_all_or_nothing_blends.md`.
- **ETF holdings ingestion (`data/etf_holdings.py`, 2026-07)**: live ETF constituent-basket ingestion feeding a planned "ETF volatility transmission" risk overlay grounded in Ben-David, Franzoni & Moussawi (2018), "Do ETFs Increase Volatility?", *Journal of Finance* 73(6):2471-2535 (ETF arbitrage transmits a shock in one constituent to its healthy peers, so heavily ETF-wrapped names carry extra non-diversifiable variance; the paper's measure is a quarterly ownership share). **Master switch `settings.ETF_HOLDINGS_ENABLED`, default `False`** — the gate is checked FIRST and `get_etf_holdings()` returns `{}` with ZERO network calls and zero DB reads, so a fresh clone / CI never touches EDGAR. PRIMARY source is **SEC N-PORT (NPORT-P) via EDGAR**, and all SEC HTTP MUST reuse `data/edgar_fundamentals.py`'s existing `_throttle`/`_http_get`/`get_cik` — a second, independently-throttled SEC client in the same process would blow SEC's shared ~10 req/s courtesy budget and throttle the platform's own fundamentals and 8-K paths. **Freshness must never be oversold**: N-PORT covers three month-ends and publishes ~60 days after quarter end, so holdings run **1-5 months stale, always** — fine for the quarterly ownership-share stock variable, not a real-time basket feed. `as_of` is a hard causality filter applied twice (filing selection drops anything whose report date OR publication date is after the cutoff; `HistoricalStore.get_etf_holdings(..., as_of_date=X)` re-applies `as_of_date <= X` in SQL and returns the single most recent basket, never a union across quarters). Unreported `weight`/`shares_held` are `NaN`, never `0.0`; a tickerless position is skipped rather than keyed by CUSIP; a symbol that could not be resolved is ABSENT from the result rather than present with an empty list. Never raises (`{}` on any failure, per-ETF try/except). The iShares CSV secondary is opt-in only (`settings.ETF_HOLDINGS_ISSUER_CSV_ENABLED`, default `False`) and carries a family constraint — iShares and SPDR baskets must never be mixed inside one composite. **New env vars**: `ETF_HOLDINGS_ENABLED`, `ETF_HOLDINGS_TICKERS`, `ETF_HOLDINGS_MARKET_PROXY`, `ETF_HOLDINGS_REFRESH_DAYS`, `ETF_HOLDINGS_ISSUER_CSV_ENABLED`, `ETF_HOLDINGS_MAX_SECONDS_PER_CYCLE`, `ETF_HOLDINGS_CIRCUIT_BREAKER_THRESHOLD` — all default to today's exact behavior. See `docs/architecture/data-layer.md`'s `data/etf_holdings.py` entry for the full rejected-sources list, network budget, and verification honesty note (no live fetch was ever performed — all tests are fixture-driven).
- **ETF volatility transmission (`risk/etf_transmission.py`, 2026-07)**: measurement layer for Ben-David, Franzoni & Moussawi (2018) "Do ETFs Increase Volatility?" (*Journal of Finance* 73(6)) — ETF arbitrage transmits a shock in one constituent to its healthy basket peers, so a heavily ETF-wrapped name carries extra non-fundamental, non-diversifiable variance. **DIAGNOSTIC ONLY**: three new `config.COLUMN_SCHEMA` columns (`ETF_Ownership_Pct` percent, `ETF_Comovement_R2` number, `ETF_Primary_Wrapper` string), populated by `pipeline/production_steps.py::_apply_etf_transmission` (called from `StrategyEvalStep.run` immediately after `_apply_sector_heat_factor`) and read by nothing in scoring/sizing/execution as of this PR — not a `SignalModule`, no `SIGNAL_WEIGHTS` entry; a sibling PR wires them into position sizing. **Market-residualized, not naive, R² is the crux**: a naive R² of a stock on its sector-ETF composite is high for every large-cap regardless of ETF ownership (shared market-factor loading), which would be a market-beta derate wearing an ETF costume — `compute_market_residual_r2` therefore residualizes both the stock and the ETF composite against `settings.ETF_HOLDINGS_MARKET_PROXY` (default `SPY`) first and correlates the residuals, following `processing_engine.calculate_rolling_beta`'s exact alignment contract (causal `.rolling(window)`, `join="inner"`, never forward-filled). The market proxy is EXCLUDED from the ownership-weighted return composite itself (used only as the residualization leg), so a name whose only covered wrapper IS the market proxy has an identically-zero residual and reports `NaN`, never a fabricated number. A ticker that is ITSELF an ETF is excluded from all three columns (its ownership/R² against its own basket is 1.0/1.0 — a maximum derate for a trivially wrong reason). `ETF_Ownership_Pct` uses `shares_out ≈ Market Cap / Price`, guarded on both being `> 0` (`FundamentalDataDTO.market_cap` defaults to a fabricated `0.0`, so a naive divide would yield `inf` on exactly the names whose fundamentals failed). Chooses NaN-until-full-window-coverage over an understated partial-window number for composition drift (a name added to a wrapper last week has no tethered history). `risk/etf_transmission.py` performs ZERO I/O and consumes `data/etf_holdings.py`'s `ETFHolding` dataclass duck-typed, never importing it. **Deliberately NOT implemented**: the paper's literal arbitrage-gap regression (stock return on lagged ETF-price-vs-NAV mispricing) — reconstructing the synthetic basket needs the FULL constituent set's price history (SPY = ~500 names) while `tech_raw` only carries the operator universe, and free daily NAV/IIV history is unavailable; a low-coverage basket proxy would be fabricated data, so this is a Phase-2 follow-up gated on a real NAV source. New settings, all opt-in (default `False`/today's-behavior-preserving): `ETF_TRANSMISSION_ENABLED`, `ETF_HOLDINGS_MARKET_PROXY`, `ETF_TRANSMISSION_WRAPPERS`, `ETF_TRANSMISSION_EXCLUDED_SYMBOLS`, `ETF_TRANSMISSION_WINDOW_DAYS`, `ETF_TRANSMISSION_MIN_OBS`. See `docs/signals/etf_transmission.md` and `docs/architecture/signal-engines.md`'s `risk/etf_transmission.py` entry for full detail.
- **CNN-LSTM purged internal train/val split (`forecasting_engine.py`, `cnn_lstm_worker.py`, 2026-07 quant-integrity fix)**: `make_direct_multistep_windows`'s stride-1 supervised windows overlap by `lookback-1` raw rows, so Keras's own `validation_split=0.2` (driving `EarlyStopping`) left the last training windows near-duplicates of the first validation windows, optimistically biasing `val_loss`-based epoch selection — a training-procedure leak distinct from (and in addition to) the train/inference scaler leakage `fit_scalers_on_train` already fixed. `ForecastingEngine.purged_train_val_split` (mirrored pure-numpy in `cnn_lstm_worker.py::_purged_train_val_split` for the subprocess-isolated path) purges the overlapping windows and passes an explicit `validation_data=(X_val, Y_val)` instead of `validation_split`. Applied unconditionally (no settings flag — this only affects which epoch's weights `EarlyStopping(restore_best_weights=True)` selects, not the causal correctness of the emitted forecast). See `docs/architecture/signal-engines.md`'s `forecasting_engine.py` entry. Tests: `tests/test_forecasting_lookahead.py::TestPurgedTrainValSplit`, `tests/test_cnn_lstm_worker.py`.
- **MFE/MAE fabrication risk removed (`evaluation_engine.py`, `pipeline/production_steps.py`, 2026-07)**: the core MAE/MFE computation (`evaluation_engine.py`'s `evaluate_portfolio`/`calculate_edge_ratio`) was already genuine — a real closed trade's intra-trade OHLC path from `transactions_store.py`, not fabricated — but two spots fell short of that bar and are now fixed. (1) `pipeline/production_steps.py` used to compute an "edge ratio" over a fictional 15-bars-ago-to-today window (no real trade behind it) and append it to the user-facing "Strategy Explainer Notes" as if it were a genuine post-trade evaluation; removed (its value was always overwritten anyway by the real per-trade computation later in the same step). (2) `evaluate_portfolio()`'s degraded fallback — deriving MAE/MFE from a single day's `High`/`Low` against `Entry_Price` when no real trade history existed, and hardcoding `position_type='long'` regardless of the real side — was removed; MAE/MFE now stay `NaN` (CONSTRAINT #4) in that case, matching every other "no real trade history" path. Tests: `tests/test_evaluation_no_history.py`, `tests/test_production_steps_edge_ratio_notes.py`.
- **Strategy validation harness genuine-OOS gate (`validation/harness.py`, `validation/metrics.py`, `settings.VALIDATION_HARNESS_OOS_GATE_ENABLED` default `False`, 2026-07 quant-integrity fix)**: `ValidationReport.sharpe`/`.max_dd`/`.sortino`/`.calmar`/`.hit_rate`/`.avg_trade_pct`/`.turnover` — everything feeding the `deployable` gate's `Sharpe > 0.5` / `MaxDD < 30%` criteria besides PBO/DSR — were computed from `self.strategy_fn(X, y, X, y)`, an in-sample "test" set identical to the training set, while only PBO/DSR were genuinely out-of-sample (via `CombinatorialPurgedCV`); separately, CPCV's own PBO/DSR Sharpes were computed on gross (cost-free) returns even though the in-sample leg applied `_apply_cost_model`'s turnover-scaled cost — an inconsistent cost basis between the gate's two halves. `run_cpcv_evaluation()` gained an optional `cost_model_fn` parameter (cost-adjusts every CPCV path's returns before any Sharpe/PBO/DSR/drawdown statistic) plus four new genuinely-OOS aggregates (`mean_oos_max_dd`/`mean_oos_sortino`/`mean_oos_hit_rate`/`mean_oos_avg_trade_pct`/`mean_oos_turnover` — the mean of each metric computed independently per CPCV path, not one concatenated curve, since CPCV's combinatorial test-block reuse across paths would double/triple-count dates in a naive concatenation). When the settings flag is `True`, the harness wires its cost model into CPCV and replaces the seven in-sample numbers with these OOS aggregates; `equity_curve`/`benchmark_curve`/`macro_benchmark_curve` stay on the full-sample series either way (a single non-overlapping OOS equity curve needs the AFML CPCV backtest-path-recombination algorithm, not implemented here — a real, separate follow-up, not silently faked). Default `False` reproduces every currently-recorded `docs/VALIDATION_STRATEGY_FIX_LOG.md` PBO/DSR/Sharpe/MaxDD baseline exactly — this sandboxed dev/CI environment has no live-market network access to re-verify the registered `STRATEGY_REGISTRY` fleet against the corrected numbers. `n_trials` (already persisted per-strategy but never displayed) is now also surfaced next to DSR in the main HTML validation report and the GUI Validation Lab table. See `docs/architecture/validation-and-signals.md`'s `validation/harness.py`/`validation/metrics.py` entries and `docs/VALIDATION_STRATEGY_FIX_LOG.md`'s 2026-07-29 harness-level addendum. Tests: `tests/test_metrics_cpcv_oos_aggregates.py`, `tests/test_harness_oos_gate.py`.
- **Financial Modeling Prep (FMP) data layer (`data/fmp_client.py` + `data/fmp_fundamentals.py`/`data/fmp_macro.py`/`data/fmp_feeds_company.py`/`data/fmp_feeds_market.py` + `FMPProvider` in `data/market_data.py`, 2026-07 — full reference: `docs/FMP_INTEGRATION.md`)**: primary source for quotes/bars/fundamentals with `MARKET_DATA_PROVIDER="fmp"`, `FUNDAMENTALS_SOURCE="fmp"`, and all 14 `FMP_*_ENABLED` capability booleans + `FMP_QUOTES_REALTIME` defaulting to `True` (deliberate operator decision for active FMP deployment), with automatic fallback to the existing Alpaca/yfinance/Yahoo chain on any failure. `FMP_API_KEY` in `.env` is required for live network calls. Five diagnostic feeds (analyst consensus/grades, an earnings calendar + surprise history, a treasury-rate/econ-indicator macro supplement into the existing `macro_history` table, economics calendar events (`Next_Macro_Event`/`Next_Macro_Event_Date`), and insider-trading + dated sector snapshots) are diagnostic-only dashboard columns — **never a `SignalModule`, never a `SIGNAL_WEIGHTS` entry**, since none has point-in-time history on day one (FMP serves only the CURRENT analyst consensus, for example) and "a signal the repo cannot backtest cannot earn a weight" is also the no-lookahead guarantee mechanism here, not just policy. Pipeline execution in `pipeline/production_steps.py` shares a single `_fmp_deadline` wall-clock budget across all per-symbol loops. `FMP_FALLBACK_ENABLED` (default `True`) governs whether a failure falls through the chain or propagates. **The single highest-risk setting is `FMP_BARS_ADJUSTMENT`** (default `"dividend-adjusted"`): the incumbent yfinance path is `auto_adjust=True` (split AND dividend adjusted), while FMP's `light`/`full` EOD variants are split-only — `full` looks like the obvious choice and is wrong, and a mismatch corrupts every return series, indicator, GARCH fit, and backtest *plausibly* (nothing fails loudly). `scripts/verify_fmp_bars.py` (network-marked, not part of the pytest suite) is a hard gate — it must exist and PASS (max abs relative close diff `< 1e-4` on KO/JNJ/AAPL over 2 years) before `FMP_BARS_ADJUSTMENT` is changed from its default or before trusting raw bars. **Starter-tier limitation, permanent and not fixable by this integration**: `Institutional Velocity` stays hardcoded `0.0` — Form 13F is Ultimate-only and Starter has no short-interest feed either, so no FMP endpoint on this account can fill either half of that calculation; `data/etf_holdings.py`'s SEC N-PORT path is likewise unchanged (ETF/mutual-fund holdings are also Ultimate/Enterprise-only). See `docs/FMP_INTEGRATION.md` for the full 31-setting reference, the manual eyeball-gate checklist required before flipping any flag live, and the honest risk/verification-limitations list; see `docs/architecture/data-layer.md`'s "The FMP data layer" entry for the full module-by-module architecture.
- **FMP as the primary company-news provider (`data/fmp_client.py::stock_news`/`parse_news_published_date`, `data/sentiment_sources.py::FMPNewsSource`, `signals/news_catalyst.py::fetch_company_headlines`/`fetch_next_earnings_any`, `settings.FMP_NEWS_ENABLED` default `True` (flipped from False, 2026-08 PR #737), 2026-08 — full reference: `docs/FMP_INTEGRATION.md` §7)**: prompted by an operator hitting a `FINNHUB_API_KEY is not set ... (or finnhub-python is not installed)` error from `scripts/backfill_news_history.py` alongside a separate `.env`-resolution bug (see the `.env`/venv-bootstrap bullets below) — the decision was to make FMP PRIMARY for company news/earnings dates, with Finnhub kept as an opt-in fallback rather than removed. `FMP_NEWS_ENABLED` + `FMP_API_KEY` is the two-gate convention every other FMP feed here follows. Verified live 2026-08: `/news/stock` covers ≥6 months of real history (vs. Finnhub's free-tier ~3 month cap) with working `from`/`to`/`page`/`limit` pagination (bounded by `FMP_NEWS_MAX_PAGES` default `10`, `FMP_NEWS_PAGE_LIMIT` default `100`). Deliberately does **not** wrap `/news/press-releases` — that endpoint returned a plan-entitlement `"Restricted Endpoint"` rejection on the account this was verified with. **`publishedDate` is US Eastern Time, not UTC** — verified (not assumed) by cross-referencing a real GlobeNewswire article: FMP reported `"2026-08-02 14:51:00"` for a release whose own page states `"August 02, 2026 14:51 ET"`, an exact match; `parse_news_published_date` localizes via `ZoneInfo("America/New_York")` (DST-correct year-round) before converting to UTC — getting this wrong would have silently shifted every `news_history` day-bucket by up to 5 hours. Two new dispatcher functions in `signals/news_catalyst.py` — `fetch_company_headlines(symbol, lookback_days)` and `fetch_next_earnings_any(symbol)` — try FMP first and fall back to the existing, unchanged, still-exported `build_finnhub_client()`/`fetch_company_news()`/`fetch_next_earnings()` otherwise; `NewsCatalystSignal.pre_compute()` (internal method renamed `_score_via_provider`, `_score_via_finnhub` kept as an alias), `llm/research.py`'s Opal grounding packet, and `engine/agent_sentiment.py`'s Antigravity tool were all re-pointed at the two dispatchers, and each one's gate now accepts FMP-only configuration (previously required a Finnhub client). `data/sentiment_sources.py` gained a new opt-in `FMPNewsSource` (`name="fmp_news"`, registered in `_SOURCE_REGISTRY`/`_SOURCE_PRIORITY` ahead of `"finnhub"`, added to `signals/credibility.py`'s `_INSTITUTIONAL_SOURCES`) — an operator must add `"fmp_news"` to `SENTIMENT_SOURCES` for it to run; `FinnhubSentimentSource` is intentionally left untouched and Finnhub-specific, still independently selectable. **`"fmp_news"` is deliberately excluded from `SENTIMENT_SOURCES`'s default** (re-audited 2026-08-21, decision: leave as-is) for the identical reason `"finnhub"` already is — `NewsCatalystSignal.pre_compute()`'s `_score_via_provider()` → `fetch_company_headlines()` already calls the same `data.fmp_client.stock_news()` endpoint FMP-first, per symbol, every cycle whenever `FMP_NEWS_ENABLED`+`FMP_API_KEY` are set; adding `"fmp_news"` to the default would double-fetch that same endpoint per symbol per cycle rather than pull broader or different data. See `settings.py`'s `SENTIMENT_SOURCES` field description for the full reasoning. `scripts/backfill_news_history.py`'s `_fetch_headlines`/`_fetch_earnings_dates` each gained an FMP-first half using the same one-wide-call-then-reconstruct-locally approach the Finnhub path already used. All new settings default to today's exact Finnhub-only behavior; nothing changes until `FMP_NEWS_ENABLED=true` is set. Tests: `tests/test_fmp_news.py`, plus extended coverage in `tests/test_news_catalyst.py`, `tests/test_sentiment_sources.py`, `tests/test_research_brief.py`, `tests/test_backfill_news_history.py`.
- **`universe_engine.py` fix: FMP-primary constituent-changes feed + broadened dead-letter fallback (`settings.FMP_UNIVERSE_ENABLED` default `True` (flipped from False, 2026-08 PR #737), 2026-08 — full reference: `docs/FMP_INTEGRATION.md` §8)**: Wikipedia removed the "Selected changes to the list of S&P 500 components" table from `List_of_S%26P_500_companies` entirely (confirmed live, 2026-08 — the content is gone, not a markup shift), which broke `universe_engine.py::fetch_and_cache_universe()` unconditionally on any fresh clone with no pre-existing `data/universe_cache.parquet`, cascading into 5 failing tests via `validation/harness.py:655`'s unguarded call. Two independent fixes. (1) **FMP as the primary changes source**: `data/fmp_client.py::historical_sp500_changes` + `data/fmp_universe.py::fetch_sp500_changes_via_fmp` (the two-gate `FMP_UNIVERSE_ENABLED`+`FMP_API_KEY` convention, FMP-first/Wikipedia-fallback dispatcher mirroring `fetch_company_headlines`'s FMP/Finnhub pattern in `signals/news_catalyst.py`) — never raises, degrades to `[]` on any failure so `fetch_and_cache_universe()` falls through to the Wikipedia changes-table scrape unchanged. Wikipedia's current-constituents table (confirmed still intact) stays the unconditional source of truth for the CURRENT roster regardless of this flag; only the historical-changes half is FMP-eligible. **Not verified against a live FMP account** — the endpoint path/field names are best-effort from public docs only (this integration was built in a sandbox with no live-market network access and FMP's docs site blocked automated fetches); see `docs/FMP_INTEGRATION.md` §8 for the honest verification-status note before trusting `FMP_UNIVERSE_ENABLED=True` as a working primary path rather than a safe no-op. (2) **Broadened dead-letter fallback, independent of FMP**: `fetch_and_cache_universe()`'s stale-cache fallback previously covered only a network-request exception; the three structural `ValueError`s (too-few-tables, missing-symbol-column, missing-changes-columns — the one actually triggering the live break) now get the same treatment — fall back to a stale cache if one exists, else raise the original exception type/message unchanged. `validation/harness.py::run()`'s call to `get_universe_with_survivorship_warning` is separately guarded (mirroring `simulation_engine.py::print_survivorship_warning_for_backtest`'s pre-existing try/except around the exact same call) and degrades to an honest NaN/`None`-flagged `bias_report` sentinel (CONSTRAINT #4 — never fabricate `n_current`/`estimated_bias_pct`) instead of crashing the whole validation run; `reports/validation_report_template.html.j2` renders that sentinel as an honest "unavailable" note rather than literal "nan"/"None" text. New env var: `FMP_UNIVERSE_ENABLED`. Tests: `tests/test_fmp_universe.py`, plus extended coverage in `tests/test_dead_letter_resilience.py` (`TestFetchAndCacheUniverseMalformedTable`'s new cache-present-fallback cases, `TestFetchAndCacheUniverseFMPPrimarySource`, `TestValidationHarnessUniverseFailureGuard`).
- **`scripts/refresh_validations.py`: tiered universe widening for 7 cross-sectional strategies, 2026-08-21**: `cross_sectional_momentum`/`relative_strength_xsec`/`multifactor_lowvol_size`/`macro_regime_pit`/`signal_replay_balanced_blend`/`lgbm_ranker`/`sector_quality_rank` moved off three hardcoded lists — a 30-name `_XSEC_UNIVERSE_30`, a 9-name hand list (`multifactor_lowvol_size`), and a 12-name/2-sector `SNEQR_UNIVERSE` (`sector_quality_rank`) — onto a real S&P 500 roster sourced live from `universe_engine.get_sp500_constituents()` via a new `_load_wide_universe()` loader (falls back to the old 30-name list, never raises, if `universe_engine` can't produce a roster). Two tiers: `_XSEC_UNIVERSE_WIDE` (503 names, `O(dates)`-cost adapters) and `_XSEC_UNIVERSE_CAPPED` (a 100-name slice, for the three ticker-count-scaling adapters). Measured, real before/after numbers for all 7: 5 strategies passed and stayed `deployable=True` with meaningfully lower MaxDD; `lgbm_ranker` regressed to a hard training-crash FAIL (LightGBM 4.7.0 raised `Number of rows <N> exceeds upper limit of 10000 for a query` on every fold once the widened 100-name panel crossed some internal query-size limit — not yet root-caused or fixed); `sector_quality_rank` flipped `deployable=True → False` on a real, measured, counterintuitive MaxDD increase (19.6%→34.2%) despite gaining 6 more sectors of coverage, the opposite of what `docs/signals/sector_quality_rank.md` previously expected. **Scope, honestly stated: this widens BREADTH/diversification only, NOT point-in-time survivorship-bias correction** — `universe_engine.get_sp500_constituents()` currently returns the same current ~503-name roster for every historical date (Wikipedia's historical-changes table was removed in 2026-08 and the FMP fallback needs an unconfigured `FMP_API_KEY` in this environment — see the `universe_engine.py` bullet above), so every backtest still runs today's constituent list against 2005-2026 price history regardless of when a name actually joined the index; true point-in-time reconstruction remains a disclosed, separate follow-up. See `docs/VALIDATION_STRATEGY_FIX_LOG.md`'s 2026-08-21 entry for the full before/after table, the `lgbm_ranker`/`sector_quality_rank` regressions, and each affected strategy's own `docs/signals/<name>.md` addendum.
- **`scripts/refresh_validations.py` fix: unthrottled single-shot `yfinance` bulk fetch replaced with per-ticker FMP fetches (2026-08-21)**: the 2026-08-21 universe-widening change above (`_XSEC_UNIVERSE_WIDE`, ~503 tickers) turned `_download_closes()`/`_download_ohlcv()`'s single unthrottled `yf.download()` call for the *entire* universe into a real production failure the first time it actually ran at that width — two separate `python -m scripts.refresh_validations --strategies <29 names>` runs measured Yahoo/yfinance failing 446 of 503 tickers (89%), split between yfinance's generic-empty-response "possibly delisted" message (hitting obviously-real names like SPY/AMZN/MSFT) and yfinance's own local SQLite tz-cache contention (`OperationalError: unable to open database file`, from ~500 concurrent per-ticker threads hammering one WAL-mode cache file — not a Stockpy database). This cascaded into 17 of 29 strategies hard-erroring with a misleading "insufficient history" message (the real cause was a required ticker's column existing in `closes_df` but being all-NaN) and, worse, two "completed" results silently CONTAMINATED by data starvation rather than failing loudly — `sector_quality_rank` reported `deployable=True`/Sharpe=0.80/PBO=0.0/DSR=1.0 while 90-99% of its 100-ticker universe was incorrectly excluded as "microcap" by `signals/multifactor.py`'s `market_cap.fillna(0.0)` fail-closed branch, tripped by missing market-cap data rather than these actually being small caps. Fixed by migrating `_download_closes`/`_download_ohlcv`/`_download_shares` off `yfinance` entirely onto per-ticker `data/fmp_client.py` calls (`historical_eod`/`shares_float`) inside a bounded `ThreadPoolExecutor`, reusing `data/market_data.py`'s existing `_fmp_bars_payload_to_df`/`_warn_once_if_fmp_bars_adjustment_mismatched` reshape/adjustment-convention helpers rather than reimplementing them — pacing comes from FMP's own existing module-level throttle/retry/circuit-breaker (`FMP_MIN_REQUEST_INTERVAL_SECONDS`/`FMP_MAX_RETRIES`/`FMP_COOLDOWN_THRESHOLD`, mirroring the Shared GDELT rate limiter bullet's pattern above) rather than a bespoke chunk-and-sleep loop. Per-ticker failures are still dead-lettered (logged, ticker absent from the result, never fabricated) exactly as before. Also tightened `_validate_single_strategy`'s availability check from bare column presence to `t in closes_df.columns and closes_df[t].notna().any()`, so an all-NaN-but-present column now trips the existing, correctly-worded `RuntimeError` instead of silently passing through to the generic downstream error. Verified: `python3 -m pytest tests/test_refresh_validations.py -q` — 119 passed, 0 failures.
- **Jules coding-agent API integration (`data/jules_client.py`, 2026-08 — full reference: `docs/JULES_INTEGRATION.md`)**: an opt-in, `JULES_ENABLED`-gated (default `False`, a `settings_keysets.DANGEROUS_KEYS` member) integration with Google's third-party autonomous coding agent — given a prompt and a connected GitHub repo/branch, Jules writes code and opens a real, unsupervised PR (hardcoded `automationMode=AUTO_CREATE_PR`; there is no lower-stakes mode). Exposed via `investyo_mcp_server.py`'s `list_jules_sources`/`dispatch_jules_task` MCP tools and `scripts/jules_dispatch.py`'s `list-sources`/`create-session` CLI, both requiring an explicit `confirm=True`/`--confirm` per dispatch (mirroring `gui/orchestrator_runner.HIGH_STAKES_COMMANDS`'s gate for other irreversible actions) plus a same-day dispatch-ledger dedup check (`output/jules_dispatched.jsonl`, `--force`/`force=True` to override). Stated plainly rather than papered over: `confirm=True` is a materially weaker gate than the `robinhood-execution` skill's human-narrated, per-order confirmation loop — what actually closes that gap today is the `.claude/skills/jules-delegation/SKILL.md` skill's prose hard-stop against passing `confirm=True` without the operator's per-task go-ahead, enforced by an agent following the skill, not by code. See `docs/JULES_INTEGRATION.md` for the full setup sequencing and the honest disclosed-unknowns list (Jules's pricing/quota model has not been independently verified as part of this integration).
- **Sentry error-tracking instrumentation (`observability/sentry_integration.py`)**: a dedicated `init_sentry()` hook for reporting unhandled exceptions from the persistent background orchestrator daemon (which can otherwise fail silently out-of-sight for hours). Gated by `settings.SENTRY_ENABLED` (default `True` — an explicit exception to the "new instrumentation defaults off" convention, because it is provably safe/no-op until `SENTRY_DSN` is also populated, so leaving it on carries zero risk for existing deployments while keeping it discoverable) and requires `sentry-sdk` (added to `requirements-optional.txt`, loaded via lazy import so the daemon still starts up fine without it). **Scope constraint:** this is wired into `desktop/orchestrator_daemon.py`'s entrypoint ONLY — `main.py` and the standalone `api/*.py` services are intentionally unaffected (disclosed, deliberate follow-up).
- **`.env` resolution fix + `scripts/` venv/`.env` bootstrap (`settings.ENV_PATH`, `scripts/_bootstrap.py`, 2026-08)**: root-caused an operator report of a stale-looking `RH_USERNAME` error despite a correctly-populated `.env`. Three independent `.env` locators previously disagreed: pydantic-settings' own `env_file=".env"` (CWD-relative), a bare `load_dotenv()` in `main.py`/`main_orchestrator.py`/`app_shell.py`/`desktop/orchestrator_daemon.py` (walks UP via `find_dotenv()` from the calling file's directory — in a git worktree with no `.env` of its own, this silently found a PARENT checkout's `.env`), and `gui/env_io.py`/`data/brokerage_credentials.py`'s own independently-derived repo-root paths. `settings.ENV_PATH` (`Path(__file__).resolve().parent / ".env"`) is now the SINGLE source every locator imports rather than re-deriving; all four `load_dotenv()` call sites pass it explicitly. Separately, `data/robinhood_portfolio.py::_require_env` (renamed `_require_setting`) and `data/brokerage_credentials.py`'s credential functions read via `os.environ` — the exact "pydantic-settings loads `.env` into `Settings` only, never into the real process environment" bug class this file already documents for Finnhub/EDGAR/Reddit — and now read via `settings.settings.X`; `write_rh_credentials`/`clear_rh_credentials` additionally assign the live settings singleton, not just `.env` and `os.environ`, so the write they just made isn't invisible until restart. The five standalone `api/*.py` FastAPI services (previously the ONLY caller that never loaded `.env` at all, since they have no `main()`-style entry point to hook into) now each call `load_dotenv(ENV_PATH, ...)` at true module top. **`scripts/_bootstrap.py`** (new, stdlib-only at module scope) gives every `scripts/*.py` entry point a `bootstrap()` call mirroring `main.py`'s venv-reexec guard (re-exec under `.venv`'s interpreter if not already there) plus `.env` loading — fixing the OTHER half of the original report (`finnhub-python` was installed only in `.venv`; the operator's system Python lacked it). Placement is split by import risk: the 12 scripts never imported as a library elsewhere call `bootstrap()` at true module top (catches a wrong interpreter before any heavy import crashes); the 6 scripts ALSO imported as a library by production code (`scripts/snapshot_diff.py` by `main_orchestrator.py`/`api/data_api.py`/`pipeline/production_steps.py` and more; `scripts/daily_briefing.py`/`preflight_check.py`/`refresh_validations.py`/`train_lgbm.py`/`train_meta_labelers.py` by Gravity or sibling scripts) call it only inside `if __name__ == "__main__":`, so a plain `import scripts.X` from a running process never risks a subprocess re-exec mid-import — mirroring `main_orchestrator.py`'s own existing `if __name__ == "__main__":`-guarded reexec block, which `main.py`'s unconditional one does not have. `data.portfolio_sync.resolve_universe` gained `allow_live_broker_fetch: bool = True` (threaded to `fetch_account_snapshot`'s new `allow_live_fetch` kwarg) so headless backfill scripts (`backfill_news_history.py`, `backfill_news_history_from_audit.py`, `backfill_sentiment_history.py`, `backfill_edgar_fundamentals.py`) resolve the universe from cache only, never attempting a live Robinhood TOTP/MFA login (which, absent `RH_MFA_SECRET`, falls back to a blocking interactive prompt or raises immediately headless). Tests: `tests/test_env_loading.py`, `tests/test_robinhood_portfolio.py`, `tests/test_brokerage_connect.py`, `tests/test_robinhood_client.py`, `tests/test_portfolio_sync.py`, `tests/test_run_once.py`, `tests/test_backfill_edgar_fundamentals.py`.
- **Runtime settings store — READ path (`runtime_flags.py`, 2026-08)**: lets a `Settings` field value be sourced from `output/runtime_flags.json` in addition to real env vars and `.env`. **Zero behavioral change until that file exists** — no writer ships with it (that is a separate, later task), and no install has the file, so `apply_overrides` is a no-op everywhere today. Precedence is **real shell env > store > `.env` > field default**. Four rules govern any change here. (1) **`runtime_flags.py` is a stdlib-only leaf imported BY `settings.py`** (at the very bottom, after `settings = Settings()`), so it must NEVER import `settings`/`gui.env_io`/`config` at any scope — that is a circular import that breaks `import settings` for all ~146 dependent modules; `settings_keysets.py` (itself importing nothing but `__future__`) is the one permitted project import, and `python-dotenv` is imported lazily inside the one function needing it. AST-enforced by `tests/test_runtime_flags.py::TestModuleIsADependencyFreeLeaf`. (2) **Assignment goes through `Settings.__pydantic_validator__.validate_assignment`, never `pydantic.TypeAdapter`** — `TypeAdapter` coerces to the annotated type but silently BYPASSES `@field_validator`s, verified: it passes `ROBINHOOD_EXECUTION_MODE="garbage-value"` straight through, where the real validator collapses anything outside `{off, review, live}` to the inert `off` precisely so a bad value can never arm live execution. Same mechanism as `api/pilots_api.py`'s `PUT /llm/setting`. (3) **`settings_keysets.BOOTSTRAP_KEYS` is an absolute exclusion** — a stored value for one of those 7 fields is skipped with a WARNING, never applied. (4) **Path anchoring is `Path(__file__).resolve().parent`**, re-derived independently (it cannot import `settings.ENV_PATH`/`OUTPUT_DIR` — `OUTPUT_DIR` is itself a `BOOTSTRAP_KEY` for that self-referential reason) and pinned equal to `ENV_PATH` by a test in BOTH `tests/test_runtime_flags.py` and `tests/test_env_loading.py`; anything CWD-relative or `find_dotenv()`-style would reintroduce the parent-worktree bug the bullet above documents. **JSON shape the not-yet-built writer must produce** — `{"version": 1, "flags": {"FIELD": {"value": <json>, ...optional audit metadata}}}`; the `{"value": ...}` envelope is REQUIRED, not optional, because `SECTOR_FORECAST_CONFIGS` is `dict`-typed and a bare-scalar form would be ambiguous with a dict field's real value. Failure handling is dead-letter-per-key (CONSTRAINT #6): a corrupt file / wrong `version` / non-object `flags` applies nothing and logs a WARNING, while an unknown field name, a `BOOTSTRAP_KEYS` member, an env-pinned name, or a value failing validation skips that ONE key and applies the rest. **Never logs a stored value** — a store entry may hold a credential, and `str(ValidationError)` embeds `input_value=` (verified), so only `errors()[0]["msg"]` is ever logged. Known gap left to the writer: this module does not refuse `gui/env_io.py::SECRET_KEYS` (it cannot import that set; duplicating an 80-key credential list into a leaf is a worse drift hazard) — refusing to WRITE a secret belongs in the writer. `settings.RUNTIME_FLAGS_REPORT` exposes the per-run `ApplyReport` (`applied`/`skipped_bootstrap`/`skipped_env_pinned`/`skipped_unknown`/`skipped_invalid`/`error`) for a later operator surface; nothing reads it to make a decision. Tests: `tests/test_runtime_flags.py`, `tests/test_env_loading.py::TestEnvPathAnchor`.
- **Runtime settings store — WRITE path (`runtime_flags_writer.py`, 2026-08)**: the other half of the bullet above, and **the first and only thing in this codebase that writes `output/runtime_flags.json`** — until it existed the read path was provably inert because no writer did. Two entry points, both of which NEVER raise (CONSTRAINT #6) and always return a frozen `WriteResult(key, ok, applied_value, reason, persisted, applies)`: `write_override(key, raw_value, *, actor="", path=None)` and `delete_override(key, *, actor="", path=None)`. **This module is deliberately NOT a stdlib-only leaf and must NEVER be imported by `settings.py` or `runtime_flags.py`** (source-scan enforced by `tests/test_runtime_flags_writer.py::TestModuleWiring`) — it is only ever called from request-handling code running long after `settings.py` finished importing, which is exactly what lets it import `gui.env_io` and close the one gap `runtime_flags.py`'s own docstring names as left to the writer: refusing to persist a `SECRET_KEYS` field. **Five refusal gates, in order**: (1) `gui.env_io.SECRET_KEYS`; (2) `settings_keysets.BOOTSTRAP_KEYS`, with the per-field string from `BOOTSTRAP_KEY_REASONS` as the reason, not a generic message; (3) not a real `Settings.model_fields` name (`extra="ignore"` would otherwise store a typo forever and skip it forever); (4) validation via `Settings.__pydantic_validator__.validate_assignment`, never `TypeAdapter`; (5) the COERCED value must survive `json.dumps`. Gate 1 runs before gate 3 because 38 of the 40 secret keys ARE real fields, so ordering is what makes the refusal attributable to secrecy; and the two key sets **overlap on `DATABASE_URL`/`MCP_DATABASE_URL_RO`**, where gate 1 wins — a DSN can embed credentials, and "this is a secret" is the stronger statement (`settings_keysets.py`'s own `DATABASE_URL` reason says as much). Deliberately NOT gates: `ALLOWED_KEYS` (that is the `.env` writer's allowlist for a different, durable-at-next-launch mechanism) and `DANGEROUS_KEYS` (needs an operator confirmation step, which is the calling layer's concern — this module could not tell "the operator confirmed" from "the caller forgot to ask"). **Validation runs on `settings.settings.model_copy()`, not the live singleton** (verified: `model_copy` gives a distinct `__dict__`), so a refusal at any gate leaves the process byte-identical, and the live apply is delegated to `runtime_flags.apply_overrides` — meaning the singleton is mutated by exactly ONE code path and precedence (real shell env > store > `.env` > default) has exactly one implementation the writer cannot drift from. **`applies`** is `"immediately"` | `"next_daemon_restart"` | `"env_pinned"` | `"refused"`; `"immediately"` honestly means only that the singleton's attribute moved — the 78 `restart_required` fields in `docs/settings_liveness.json` still need a restart before the change has real effect, and that per-field classification is deliberately left to the calling UI rather than duplicated here. An **env-pinned** write still persists (durable for a future unpinned process) but reports `applies="env_pinned"` with `applied_value` set to the *env-sourced value that won*, never the value written, so the gap is visible instead of a false `"immediately"`. **Concurrency** is scoped rigor for a single-operator artifact: a module-level `threading.RLock` serializes read-merge-write-replace so concurrent threads in one process (FastAPI runs `def` handlers in a threadpool — the realistic race) cannot lose each other's keys, the existing file is re-read INSIDE the lock immediately before the merge, and the file is replaced via temp + `os.replace` (same idiom as `gui/env_io.py::write_many_atomic`); a cross-PROCESS writer landing between our read and our replace is still last-writer-wins, the same residual `write_many_atomic` documents for `.env` and not made worse here. **A damaged store splits two ways**: unparseable/wrong-shape is renamed to `runtime_flags.json.corrupt.<UTC>` (bytes preserved for hand-recovery) and a fresh store written with a WARNING; an unsupported schema `version` is REFUSED outright, because that is not corruption but a valid file from a NEWER build, and overwriting it with a v1 file would destroy current state this build merely cannot read. The merge re-reads the raw file rather than rebuilding from `load_store()`, which unwraps the `{"value": ...}` envelope and would restamp every untouched key's `updated_at`/`updated_by` — destroying provenance the call was never asked to touch. **Audit log**: `output/runtime_flags_audit.jsonl`, always a SIBLING of the store (so redirecting the store path in a test redirects the audit trail too), one JSON line per call including refusals. It **never records the value, even on success** — `_append_audit` has no parameter that could carry one, so leaking is structurally impossible rather than a matter of discipline (a field not formally in `SECRET_KEYS` can still receive sensitive-shaped input). If the store write succeeds but the audit append fails the call still reports `ok=True`/`persisted=True` — the value genuinely IS persisted and live, and reporting failure would invite a retry that double-writes — with the gap logged at ERROR. `delete_override` applies **no** classification gates (deletion can only reduce what the store overrides, so refusing would only strand a hand-added entry the read path keeps applying) and reverts the live value by constructing a fresh `Settings()`, because the read path only APPLIES stored keys and has no memory of what a field held before an override landed, possibly in a previous process. Tests: `tests/test_runtime_flags_writer.py` (104 tests; 9 hand-run mutations, 8 confirmed caught — removing the secret gate, validating on the live singleton instead of a copy, `str(exc)` in place of `errors()[0]["msg"]`, dropping the write lock, and giving `_append_audit` a value-carrying parameter among them). The 9th — **replacing the atomic temp-file + `os.replace` with a direct in-place `store.write_text`** — was initially NOT caught: all 100 other tests stayed green, i.e. the atomicity claim was documented but unenforced. `TestAtomicWrite` closes that gap, pinning that exactly one `os.replace` publishes the file and its source is a temp SIBLING (same directory, or the rename is a cross-filesystem copy and no longer atomic), and that a failure at the publish step leaves the previous store byte-identical and still loadable by the read path rather than truncated.
- **Mission Control research-driven overhaul (`webapp/src/screens/Observability.tsx`, `webapp/src/screens/Dashboard.tsx`, `market_data_latency.py`, 2026-08)**: a structured operator interview (the platform's single real user) plus a heuristic/code audit found the screen's actual problem wasn't missing sections — it was that it had never earned a reason to be opened ("I really don't check it. I don't know what it does."). Fixes, in priority order: (1) a `sizing cap`/`etf transmission` GLOSSARY entry (`webapp/src/help/helpContent.ts`) plus a per-section `?` `InfoTip` affordance on `SectionHeading` (`helpKey` prop) — the one-shot, first-visit-only `TabGuide` panel was the screen's ONLY explanation surface, and it never covered several sections at all; (2) a client-side-only `AttentionStripSection` at the top of the page, derived by the pure `webapp/src/observabilityAttention.ts::deriveAttentionItems()` from fields the composite endpoint already returns (circuit-breaker counts, portfolio-heat over-limit, risk-gate-block count, a sizing-cap **escalation** event specifically — not routine capping, which CLAUDE.md already documents as "routine for an established Kelly book" — a stale >120s heartbeat, and the macro gate being off), rendering an honest "All clear" when nothing qualifies; the page's only two real controls (the macro-gate toggle, the ETF-transmission config link) were relocated above the fold into a visually distinct "control" card and given `Button variant="primary"` instead of blending into the surrounding read-only tiles; (3) every section past the attention strip/portfolio-risk/equity is now collapsed by default inside a native `<details>` "Background telemetry" disclosure; (4) `Dashboard.tsx` (the app's actual `/` landing route) runs the SAME `deriveAttentionItems()` over its own `GET /observability/summary` fetch and shows a `Notice` banner linking to `/observability` ONLY when non-empty — the cross-screen trigger the page never had. Two previously-confirmed real gaps against the retired Streamlit panel were also closed: a **per-symbol forecast-skill table** (`pilots/observability.py::forecast_skill_by_symbol_summary` — a bulk `GROUP BY symbol, horizon_days, model_name` SQL aggregate reproducing `ForecastTracker.get_skill_weights`'s exact cold-start/inverse-RMSE formula per symbol, replacing the legacy panel's N×4×3 per-cell round-trip loop with three bulk queries) and a **per-symbol data-latency heatmap**. The latency heatmap is a REPLACEMENT, not a literal port: the legacy `LatencySampleStore` lived only in Streamlit `st.session_state`, populated only on a manual "Fetch quotes" click — a stateless FastAPI process has no equivalent session, and CONSTRAINT #4 forbids fabricating a history that isn't real (the same reasoning that already kept the Heartbeat Age Trend sparkline unported). The new `market_data_latency.py` (stdlib-only, zero project imports, matching the "flat, modular Engine architecture" convention) is a thread-safe, fixed-capacity in-process ring buffer, recorded AUTOMATICALLY on every real (non-cache-hit) fetch through `data/market_data.py::CompositeProvider.get_latest_quote` — strictly more useful than the manual-trigger original, not a downgrade — gated behind new setting **`MARKET_DATA_LATENCY_TRACKING_ENABLED`** (bool, default `False`, matching this codebase's dominant convention that new instrumentation defaults off even when read-only/diagnostic). Never persisted to disk, so a restarted API process honestly reports zero samples rather than a stale cross-restart figure (`pilots/observability.py::latency_heatmap_summary` surfaces this the same way `heartbeat_summary` already does). Both new sections follow the existing `sizing_cap_audit`/`etf_transmission` end-to-end template exactly: a new `ObservabilitySummary` key on the one composite `GET /observability/summary` call (no new endpoint, no `client.ts` changes), a happy-path + exported honest-disabled mock builder pair in `mock.ts`, and matching `Observability.test.tsx` coverage. All new/changed settings default to today's exact behavior. Tests: `tests/test_market_data_latency.py`, `tests/test_pilots_observability.py::TestForecastSkillBySymbol`/`TestLatencyHeatmapSummary`, `tests/test_market_data.py::TestCompositeProviderLatencyTracking`, plus the webapp additions above.
- **`settings.LOCAL_DATA_ROOT` (2026-08)**: machine-global root for every locally-generated model/data artifact (trained ML models, `quant_platform.db`, `output/`, logs, and the API/Robinhood/feature caches) — default `~/.stockpy_local` — lives OUTSIDE every git worktree/checkout on purpose. This repo runs many simultaneous git worktrees and untracked files are worktree-local in git, so before this a model/cache/log file written in one worktree was invisible from every other one even though nothing was deleted; now every worktree/checkout on this machine reads/writes the SAME physical files with zero per-worktree `.env` setup. `settings.OUTPUT_DIR` and every other `LOCAL_DATA_ROOT`-relative module constant derive their default from this one field. See `docs/architecture/data-layer.md`'s `settings.LOCAL_DATA_ROOT` subsection for the full subfolder layout, and `scripts/migrate_to_local_data_root.py` (`--dry-run` default, `--apply` to actually move, `--verify`) for the explicit, re-runnable one-time migration of artifacts still sitting at their old repo-relative paths. **Lesson learned during rollout** — see `docs/known_issues/forecast_tracker_local_data_root_split.md` for the full incident write-up: `forecasting/forecast_tracker.py`'s `ForecastTracker.__init__` had a hardcoded, CWD-relative default `db_path="quant_platform.db"` that bypassed `db_config.resolve_database_url()`/`LOCAL_DATA_ROOT` entirely — the migration's file inventory missed this one module, so once the live daemon restarted onto the merged PR, the `forecast_errors` table alone kept writing hours of real rows (~2M) to the OLD repo-relative location while every sibling store correctly moved, undetected by any test/alert/preflight check until a manual inspection caught it; fixed in PR #720, the already-diverged data was deliberately left unreconciled pending operator sign-off. **This is now a confirmed recurring bug class in this codebase** (two independent instances: `data/historical_store.py` in #718, `forecasting/forecast_tracker.py` in #720) — any new module/class that opens its own SQLite connection directly (not via `db_config.create_db_engine()`/`resolve_database_url()`) must never hardcode a bare-literal default `db_path`; watch for this specifically in review of new stores/trackers.
- **Options desk Tier A/B/C/D build-out — Phases 19-30 of `.claude/giant_master_plan.md`, 2026-08 (PR #744, #750, #751, #753)**: `pilots/lob_simulator.py` (Phase 19, Cont-Stoikov-Talreja L3 queue sim, real Gillespie/Markov-jump Monte Carlo), `pilots/options_gex.py` (Phase 20, dealer gamma exposure/walls/zero-flip, closed-form BS Γ with verified sign convention), `pilots/copula_stat_arb.py` (Phase 21, real Clayton/Gumbel/Frank/Gaussian MLE copula fitting + Kalman-filter dynamic hedge ratio — the strongest-tested module of the whole build-out, the only one with a genuine lookahead-perturbation test), `ml/drl_market_maker.py` (Phase 22), `ml/transformer_vol_forecaster.py` (Phase 23), `validation/synthetic_diffusion_engine.py` (Phase 24), `sizing/hrp_cvar_optimizer.py` (Phase 25, real López de Prado HRP quasi-diagonalization + Rockafellar-Uryasev CVaR via SLSQP), `execution/almgren_chriss_router.py` (Phase 26, real sinh/cosh optimal trajectory), `execution/fix_gateway.py` (Phase 27, real FIX 4.4 `NewOrderSingle`/`ExecutionReport`/`OrderCancelReplace` state machine, multi-venue `VenueConfig` aggregation), `llm/research_copilot.py` + `validation/autonomous_backtest_runner.py` (Phase 28, AST-safe LLM signal synthesis with a real adversarial-tested sandbox — dunder-access/forbidden-import/forbidden-call rejection — plus genuine CPCV backtest evaluation), `webapp/src/components/charts/VolSurface3D.tsx`/`LobDepth3D.tsx` (Phase 29), `execution/multi_broker_gateway.py` + `execution/sec_rule_606_reporter.py` + `data/execution_audit_store.py` (Phase 30, a real `CircuitState.CLOSED/OPEN/HALF_OPEN` failover state machine and genuine NBBO price-improvement math) all exist and are substantively implemented — this is real, tested engineering, not stubs. Also shipped but numbered outside the plan's own phase scheme: `pilots/multi_leg_pricing.py` (real Black-Scholes multi-leg pricer with 0DTE/zero-vol guards; its introducing PR loosely labeled it "Phase 21-22," which doesn't match `.claude/giant_master_plan.md`'s actual Phase 21 (copula)/22 (DRL) modules — both of which exist separately and correctly). **A full independent audit (`.claude/giant_master_plan_audit.md`, 2026-08-17) found the code is real but several load-bearing pieces didn't do what they claimed once you looked past the file existing.** Most of that audit's findings were fixed within days (PR #765 "close 7 code-level findings", PR #766 "close F1 -- mock/live API parity", PR #771 "close remaining F3/F7 findings") — re-verified fresh 2026-08-19, current state: (1) Phase 22's "Deep RL (PPO)" claim was true of `train_market_maker_policy` (still a 2-parameter heuristic hill-climb, called by `api/pilots_api.py` via `POST /pilots/options/market-maker/train` since PR #788 but with no webapp caller) but is now **partially closed**: `ml/drl_market_maker_ppo.py` (2026-08-19) is a real actor-critic PPO agent — hand-derived backprop verified against finite-difference gradients, GAE, clipped surrogate objective, a genuinely state-dependent `[delta_bid, delta_ask]` policy — implemented in pure NumPy (not `torch`, which turned out not to be installed anywhere in this environment despite being listed in `requirements-optional.txt`). See `docs/VALIDATION_STRATEGY_FIX_LOG.md`'s 2026-08-19 entry for the honest first-training-run comparison against the closed-form quoter (higher PnL/Sharpe, but also higher inventory risk — not a clean win, a real reference point). Not yet wired to any API endpoint, webapp screen, or `STRATEGY_REGISTRY` entry — that remains a separate follow-up. (2) Phase 29's "WebGL/Three.js" claim was fixed in PR #765 (F9) — `VolSurface3D.tsx`/`LobDepth3D.tsx` still render via Canvas 2D (no `three` package, no real WebGL draw calls — this was never changed), but the misleading "WebGL 3D Active" label was corrected to "Canvas 3D Renderer"/"Canvas 2.5D Renderer" with an honest fallback-mode variant; the Three.js-specific dispose helpers in `threeDisposal.ts` remain unused dead code (cosmetic, not a bug). (3) `execution/fix_recovery.py` was deleted in PR #765 (F8) as superseded dead code — its gap-fill/resend logic is confirmed correctly and more thoroughly covered inline in `execution/fix_gateway.py` (`tests/test_fix_gateway.py::test_sequence_gap_detection_and_automatic_resend`/`test_peer_resend_request_handling`/`test_sequence_reset_reset_mode`). (4) Fixed in PR #771 (F3/F7): `tests/test_transformer_vol_forecaster.py` and `tests/test_synthetic_diffusion_engine.py` both now have genuine walk-forward lookahead-perturbation tests (matching this file's established mutate-future/assert-bit-exact-past convention), and both live `api/pilots_api.py` endpoints (`GET .../transformer-forecast`, `POST .../diffusion-stress-test`) now train on real `HistoricalStore` bars, degrading to an honest HTTP 422 on thin history rather than fabricating `np.random.randn` tensors. (5) Fixed 2026-08-19: `ml/drl_market_maker.py` (Phase 22) has a documented validation exemption (`docs/VALIDATION_STRATEGY_FIX_LOG.md`'s "2026-08: High-Frequency Market Maker (Avellaneda-Stoikov) Validation Exemption" entry) now tightened to state explicitly WHY PBO/DSR don't apply — a 2-parameter hill-climb over one fixed closed-form policy isn't the strategy-selection process PBO measures overfitting risk for, a structural mismatch rather than an overfitting-immunity claim; it stays unregistered in `STRATEGY_REGISTRY` by design. `pilots/copula_stat_arb.py` (Phase 21) now HAS a real `STRATEGY_REGISTRY["copula_stat_arb"]` entry (`_build_copula_stat_arb_adapter`, calling `generate_copula_stat_arb_signals` on a KO/PEP pair, 2005-2026 real data) — measured `Sharpe=-0.455`, `PBO=0.000`, `DSR=0.246`, `MaxDD=35.1%`, `deployable=False`, an honest FAIL (worst drawdown lands on 2008-10-13, the GFC) documented in full in `docs/VALIDATION_STRATEGY_FIX_LOG.md`'s 2026-08-19 entry and `docs/signals/copula_stat_arb.md`'s corrected "Current Status," replacing the borrowed-`pairs_trading`-numbers documentation error. (6) Fixed in PR #765 (F10): the AST import-boundary guard (`tests/test_pilots_strategy_matrix.py`) is no longer a hardcoded allowlist — it now auto-discovers via `Path.glob("*.py")`, covering 55 of 61 `pilots/*.py` modules (6 explicitly exempted with documented per-module reasons), not the stale "3 of 18."
- **Options desk mock/live API parity — fixed (PR #766, "close F1", 2026-08-17; re-verified fresh 2026-08-19)**: the systemic gap flagged by the Phase 19-30 audit above is closed. All 8 previously-flagged screens (`GexProfileView.tsx`, `LobDepthView.tsx`, `CopulaSpreadView.tsx`, `MarketMakerAgentView.tsx`, `TransformerVolForecastView.tsx`, `GenerativeDiffusionStressView.tsx`, `MultiBrokerGatewayView.tsx`, `ResearchCopilotView.tsx`) were re-checked field-by-field/route-by-route against `webapp/src/api/client.ts`, `types.ts`, and the real `api/pilots_api.py` handlers — every one matches. `MultiBrokerGatewayView.tsx`'s URL is now `/pilots/execution/brokers/{status,failover}`, matching the backend exactly (the old `/multi-broker/...` mismatch is gone). `CopulaSpreadView.tsx` and `MarketMakerAgentView.tsx` turned out to have been false positives in the original audit. The `api/pilots_api.py` HRP/CVaR endpoint's `cvar_95` placeholder was also fixed in PR #765 (F2) — it now returns the real `calculate_cvar(...)` value computed over real `HistoricalStore` bars, not a hardcoded `0.05`. Two residual, non-blocking items: `pilots/multi_leg_pricing.py`'s two endpoints and the FIX `venues`/`route` endpoints still have no frontend wiring (not a parity bug — nothing to be inconsistent with; a future "add UI for X" task, not a fix), and every affected screen's own `*.test.tsx` still mocks `api.<method>()` directly via `vi.fn()` rather than exercising the real mock/live contract end-to-end, so CI still can't catch a future regression here on its own — worth a dedicated contract-test pass if this class of bug recurs. **One additional, previously-unlisted instance found and fixed (2026-08-19)**: `webapp/src/components/options/ScenarioHeatmap.tsx` (Multi-Dimensional Scenario Matrix & Stress Grid) was never one of the 8 screens the Phase 19-30 audit or PR #766's re-check covered, and it was worse than any of them — it renders unconditionally on the Paper Broker screen rather than behind a toggle, so its field-mismatch crash (`pilots/scenario_matrix.py` returned `grid`/`time_shifts_days`/`historical_presets`-as-a-dict/nested `baseline.portfolio_market_value` against every per-cell field the frontend expected) took down the *entire* screen, not just its own panel, on any live (non-mock) load. Fixed via a `to_scenario_matrix_response()` adapter confined to `evaluate_portfolio_scenario_matrix()` (the one function the API route calls) — `evaluate_scenario_matrix()`'s own return shape and all of `tests/test_scenario_matrix.py`'s existing tests are untouched. See `docs/known_issues/scenario_matrix_field_mismatch.md` for the full write-up, including a second independent bug fixed in the same change (`time_days_forward` defaulting to `0` instead of `None` collapsed every live request to a single T+0 time slice) and the honest, never-fabricated handling of per-cell `spot_price` for a multi-underlying book. This is the same bug CLASS PR #766 closed for the other 8 screens, discovered independently because it was never in that audit's named list — worth keeping in mind that a class of bug being "fixed" for a named set of components doesn't guarantee every component in the class was actually enumerated. **Correction (2026-08-19, same-day follow-up)**: this bullet's own "every one matches" / "re-verified fresh 2026-08-19" claim for the 8 named screens was itself wrong for 4 of them. A user-reported live crash on the Paper Broker screen (after the ScenarioHeatmap fix above had already landed) triggered a full sweep of the screen's entire dependency tree, which found real, reproducible field-mismatch or fabricated-value bugs in `GexProfileView.tsx`, `MarketMakerAgentView.tsx` (a 100x unit-scale error, not a field mismatch), `MultiBrokerGatewayView.tsx`, and `ResearchCopilotView.tsx` — all four were on this bullet's "every one matches" list. `LobDepthView.tsx` (not one of the original 8, but adjacent) had the identical bug pattern too. The sweep additionally found this SAME bug class, previously unaudited entirely, in `DispersionScanner.tsx`, `ZeroDteDesk.tsx`, `VpinGauge.tsx`, `SmartOrderRouterView.tsx`, `VolSurfaceView.tsx`, `EarningsCrushScanner.tsx`, `UnusualFlowFeed.tsx`, `VolForecastScanner.tsx`, `GammaScalperView.tsx`, `SecRule606ReportView.tsx`, `FixGatewayStatusRadar.tsx`, and the separate Options Chain screen (`OptionsChain.tsx`/`OptionsOrderTicket.tsx`) — 21 distinct bugs across 13 backend modules and 10 frontend files in total. `CopulaSpreadView.tsx` was re-confirmed genuinely bug-free. All fixed via the same `to_<x>_response()` adapter pattern as `ScenarioHeatmap`'s fix, verified against a real (non-mock) running backend, not just typecheck/unit tests. Full detail: `docs/known_issues/options_desk_mock_live_parity_sweep_2026_08_19.md`. The lesson repeats: a "re-verified, every one matches" claim for a named list is only as strong as the verification method behind it — this bullet's prior claim was apparently based on a static field-by-field read of the code, not an actual live-backend render, which is exactly the gap that let 4 real bugs hide behind a "matches" conclusion.
- **Options desk ML/safety gates and findings (F1–F16) — fully remediated and verified (2026-08)**:
  (1) The Stage 4 options meta-labeler (`ml/options_meta_labeler.py`) is warmed via `_ensure_meta_labeler_loaded()` in `execution/options_paper_executor.py`, and `POST /pilots/options/meta-model/retrain` dynamic feature extraction dynamically extracts real trade features (`ivr`, `vrp`, `vix`, `trend_bias`, `credit_to_width_ratio`).
  (2) `pilots/zero_dte_engine.py`'s 15:45 ET hard-liquidation gate (`manage_0dte_exits()`) is wired into `desktop/daemon_runtime.py`'s interval timer loop (`_timer_loop`) during market hours when `OPTIONS_0DTE_ENABLED` is active, and exposed via `POST /pilots/options/0dte/manage-exits`.
  (3) Options desk pilot execution endpoints `earnings_crush`, `dispersion_trading`, and `zero_dte_engine` surface `OPTIONS_DESK_DEPLOYABILITY_GATES` — echoed inline as `gate_status` on their own `POST .../execute` responses — in `api/pilots_api.py`; each of these three is an `UNGATEABLE_DATA_GAP`. `vol_mispricing` is different in WHY it's blocked: it is a MEASURED deployability failure (Sharpe -0.499, DSR 0.027, fails the Oct-2008 stress window), and as of 2026-08-18 it has its own live execute endpoint (`POST /pilots/options/mispricing/execute`, `pilots/vol_mispricing.py::execute_vol_mispricing_trade`). See `docs/signals/vol_mispricing.md`'s "Live Paper-Execution Status" section for the full detail rather than re-deriving it here. Backed by two-place documentation in `docs/VALIDATION_STRATEGY_FIX_LOG.md` (see its 2026-08-18 "Runtime Wiring Follow-Up & Doc-Drift Correction" entry, which also closes out the `get_0dte_signals` dead `HistoricalStore.get_intraday_bars` path and `docs/signals/vrp_premium_selling.md`'s stale-numbers defect, plus the same-day "vol_mispricing Live Paper-Execution Endpoint — Enforced Override Gate" follow-up entry) and `docs/signals/*.md`. All 16 findings (F1–F16) from `.claude/giant_master_plan_audit.md` have been fully remediated and verified. **Reversed 2026-08-29 — this used to be a deliberate documented decision that this bullet stated explicitly ("the gate is informational and never blocks" for the three `UNGATEABLE_DATA_GAP` strategies); it no longer is.** All four options-desk execute endpoints (`earnings_crush`, `dispersion_trading`, `zero_dte_engine`, `vol_mispricing`) now ENFORCE their gate identically: execution is blocked by default (`{"ok": false, "blocked": true, "message": ...}`, the real `execute_*_trade` function never called) and proceeds only when the request explicitly sets `override_deployability_gate: true` — a per-request override, never a standing settings flag. The three `UNGATEABLE_DATA_GAP` strategies and the one `MEASURED_FAIL` strategy differ only in the REASON `gate_status` reports, not in whether the gate blocks. This landed as an audit fix on branch `fix-known-open-gaps`: a first pass added the `override_deployability_gate` field to all three request models but the actual enforcing check (`if gate["gate_status"] == "UNGATEABLE_DATA_GAP" and not body.override_deployability_gate: return {...}`) to only `post_options_earnings_crush_execute`/`post_options_dispersion_execute`, leaving `post_options_zero_dte_execute` calling `execute_0dte_trade` unconditionally with no gate check at all despite being self-reported as fixed; an independent re-audit caught the gap and added the missing check. See `tests/test_options_desk_deployability_runtime_gap.py` for the blocked-without-override coverage on all three `UNGATEABLE_DATA_GAP` endpoints (mirroring `TestVolMispricingExecuteDeployabilityGate` in `tests/test_pilots_api.py`). **Webapp override affordance added, same day**: the reversal above shipped with no way for the Pilots PWA to ever get past the block — `webapp/src/api/client.ts`'s `executeEarningsCrushTrade`/`executeDispersionBasket`/`executeZeroDteTrade` had no `override_deployability_gate` parameter, and `EarningsCrushScanner.tsx`/`DispersionScanner.tsx`/`ZeroDteDesk.tsx` had no affordance to trigger one — every real click on "Trade Crush Spread"/"Execute Dispersion Basket"/"Trade 0DTE Breakout" would hit the blocked branch forever, silently discarding the disclosed `message` (the generic "Failed to execute..." fallback replaced it, since a 200-status blocked response never throws for `useMutation` to catch as `error`). Worse, `mock.ts`'s three mock implementations always returned `ok: true` unconditionally, so mock-mode development gave zero signal this was broken — a real mock/live parity gap, not a deliberate design choice (confirmed: nothing in this bullet or `docs/signals/vol_mispricing.md` ever decided the webapp should stay permanently blocked). Fixed by (1) widening `EarningsCrushExecutionResult`/`DispersionExecutionResult`/`ZeroDteExecutionResult` in `webapp/src/api/types.ts` into discriminated unions with the shared `OptionsDeskGateBlockedResult` shape (`{ok: false; blocked: true; message}`); (2) adding an optional `overrideDeployabilityGate` second parameter to all three `client.ts` methods, merged into the POST body as `override_deployability_gate`; (3) each screen now shows the disclosed blocked `message`, then swaps that row/action's button to a distinct amber "⚠️ Override & Execute" control — a second, deliberate click that opens a `window.confirm()` echoing the same disclosed reason before retrying with the override set, so the override is never silent and never a single accidental click; and (4) `mock.ts`'s three mocks now honor the same override contract (blocked by default, success only when overridden), restoring parity. `vol_mispricing`'s own execute endpoint (`POST /pilots/options/mispricing/execute`) still has no webapp caller at all — out of scope here, since no screen calls it today (unlike the three above, which already had wired-but-permanently-blocked callers).
- **Options desk Phases 31-36 remediation (PR #792, `feat-quant-enhancements-phases-31-36`, 2026-08)**: a follow-up independent audit of PR #792's own six new modules — real-time risk streaming (`pilots/realtime_risk_streamer.py`, `api/ws_api.py`), a dynamic circuit breaker (`execution/dynamic_circuit_breaker.py`, `execution/risk_gate.py`), a conditional generative diffusion stress engine (`validation/synthetic_diffusion_engine.py`), an HRP/CVaR portfolio optimizer (`sizing/hrp_cvar_optimizer.py`), and a simulated multi-venue FIX 4.4 gateway (`execution/fix_gateway.py`) — found several load-bearing pieces silently didn't do what the PR claimed; this pass fixed every finding in scope. Full original findings: `.claude/pr_792_phases_31_36_audit.md` (committed on a separate review worktree/branch, referenced here by path only — not present in this checkout). **Phase 31** (`/ws/risk/portfolio`): now resolves real per-symbol quotes via `pilots.price_provider.get_latest_price` instead of a dead method name (`provider.get_latest_price`, which never existed on the quote provider) that silently no-op'd every tick behind a bare `except Exception: pass`; the blocking `PaperAccountStore.get_open_positions()` → FMP-batch-quote chain is now offloaded via `loop.run_in_executor`, matching the pattern the sibling `/ws/ticks/{symbol}` endpoint already used; per-symbol betas now come from a real `Cov/Var`-over-daily-bars computation (`data.fmp_fundamentals.compute_beta` via `data.historical_store.HistoricalStore`, 5-minute in-process cache) instead of a hardcoded β=1.0, with a documented fallback to β=1.0 per-symbol on any resolution failure. **Follow-up (same PR branch)**: the one disclosed residual gap — the per-symbol quote-fetch loop wasn't itself executor-offloaded — is now closed too: `pilots.price_provider.get_latest_prices(symbols)` batches all distinct underlyings into a single `data.fmp_client.batch_quote()` call (the same real batch endpoint `PaperAccountStore._resolve_position_prices` already uses), offloaded via one `run_in_executor` round-trip instead of N single-symbol ones, degrading per-symbol (never the whole batch) on a malformed/missing entry. **Phase 32** (circuit breaker): `execution/risk_gate.py`'s file-sentinel fallback no longer fails open on an exception — it now logs at ERROR and sets `SOFT_HALT` with a reason, and evaluates both `HARD_HALT` (previously unchecked) and `SOFT_HALT` from `GlobalKillSwitch`, taking the more severe of that and whatever `DynamicCircuitBreaker().load_metrics()` finds persisted in `output/circuit_breaker_state.json`. New settings `CIRCUIT_BREAKER_ENABLED` (default `False`) and `CIRCUIT_BREAKER_REFERENCE_SYMBOL` (default `"SPY"`) wire a real live updater, `OrchestratorDaemon.maybe_update_circuit_breaker()`, into `desktop/daemon_runtime.py`'s `_timer_loop` (called unconditionally each wake, mirroring `maybe_refresh_settings`'s pattern, since the method gates on the flag internally and never raises out of the loop) — it builds a 20-day rolling annualized-vol baseline from daily bars, a reactive window from recent hourly bars, calls `DynamicCircuitBreaker.check_volatility_jump`, and persists via `update_metrics(persist=True)`. **Follow-up (same PR branch)**: the same method now also computes VPIN — reusing `pilots/options_vpin.py`'s Bulk Volume Classification (Easley/López de Prado/O'Hara) against the same hourly bars, normalized into an explicit price/volume/time frame (the raw `Close`/`Volume` columns don't match that module's lowercase column-alias matching), bucket count sized down from the tick-stream default of 50 to fit the handful of hourly bars actually available — and loss-velocity, sampling `PaperAccountStore(readonly=True).get_account().equity` into a bounded in-process ring buffer each tick (each sample costs a real live FMP quote call per open position, a real cost tied to the configured daemon cadence). Both are now passed into `update_metrics(...)`, closing 2 of the 3 previously-manual-only sub-checks. **OFI remains explicitly and permanently unwired, not merely deferred**: no configured market-data provider (Alpaca/FMP/yfinance) populates bid/ask size anywhere in this codebase's `Quote` type — there is no real order-flow-imbalance signal to compute from, confirmed by direct research, not an oversight. Since `check_flash_crash_shield` requires both `ofi` and `vpin`, the compound flash-crash shield still cannot trigger automatically even with VPIN now real; VPIN's persisted value still has standalone diagnostic worth. The flash-crash-shield case remains reachable only via manual `python -m execution.kill_switch --activate-soft-halt`/`--activate`. **Phase 34** (diffusion stress engine): the live endpoint's price-path reconstruction (`api/pilots_api.py::_clip_and_compound_diffusion_path`) now clips each step's sampled log-return before compounding and floors the running price at a small positive epsilon, closing the negative-price/runaway-blowup failure mode; VaR/CVaR dollar conversion is fixed from a linear `var_95 * spot_price` to the correct exponential transform on log-return units (`_diffusion_logret_loss_to_dollars`, `spot * (1 - exp(-var_logret))`). `regime_labels` are now genuinely derived per training window (`_derive_diffusion_regime_labels`, reusing `scripts.refresh_validations._reconstruct_macro_regime_series` mapped through the engine's `REGIME_MAP`, plus an explicit liquidity-squeeze override for windows ending inside `validation/stress_scenarios.py`'s `AUG_2024` dated window) instead of training unconditionally — 4 of 5 regime classes now get real training signal. **Follow-up (same PR branch)**: `stagflation` (the 5th) is no longer left untrained — `data_engine.py::fetch_macro_history()` now also fetches FRED `T10YIE` (10-Year Breakeven Inflation Rate), which incidentally repairs a pre-existing, unrelated dead pathway (`settings.HMM_INFLATION_FEATURE_ENABLED`/`macro_engine.py` already referenced `T10YIE` via `HistoricalStore.get_macro()` but it always silently no-op'd since the series was never fetched). `_derive_diffusion_regime_labels` gains a second local, additive override (mirroring the `AUG_2024` one exactly, so the shared `_reconstruct_macro_regime_series` stays untouched): a window is labeled `stagflation` when T10YIE is elevated relative to its own trailing rolling range (self-relative, not a hardcoded absolute level) AND `UNRATE` is trending up year-over-year AND the base bucket isn't already the more specific `RECESSION`/`CREDIT EVENT`. Disclosed plainly as an approximate, FRED-sourced heuristic — weaker grounding than the dated-window `credit_freeze`/`liquidity_squeeze` labels — in both the code comment and `GenerativeDiffusionStressView.tsx`'s regime description, not a rigorously validated regime detector. **Phase 35** (HRP/CVaR): `PaperBroker.tsx` now imports `HrpCvarOptimizerView` from `HrpPortfolioOptimizerView.tsx` (was silently pointed at a different, unmounted component file — dead UI); `max_asset_weight` now flows end-to-end — the webapp's existing `maxAssetWeight` control state → `HrpCvarOptimizeRequest.max_asset_weight` (webapp `types.ts` + `api/pilots_api.py`'s Pydantic model, both previously missing the field) → `optimize_turnover_regularized_hrp_cvar`'s real `max_weight` parameter — instead of being silently dropped as a no-op. `sizing/hrp_cvar_optimizer.py`'s quasi-diagonalization/recursive-bisection fallback now logs a WARNING (module previously had no `logging` import at all) and sets a new `hrp_fallback: bool` on the result dict instead of silently degrading to equal-weight while the separate SLSQP-level `status` field still read `"optimal"` — the two failure modes are now independently visible rather than one masking the other. **Phase 36** (FIX gateway): `GET /pilots/execution/fix/session/status` now reports real venue data via the already-instantiated `MultiVenueAggregator.get_venues_info()` instead of a hardcoded `market_centers` dict — this honestly changes the reported venue set from the previously-fabricated equity names (NYSE/NASDAQ/BATS/IEX/ARCA) to the module's real configured venues (CBOE/MIAX/BOX/PHLX/ARCA/EDGX), not a regression; `session_uptime_sec` is now `time.time() - session.connected_at` (a new `FixSession.connected_at` field stamped at the top of `connect()`) instead of a hardcoded `14820`; the fabricated 3-message `audit_log` fallback (including a fake filled order, `ORD-99124`/`CL-3019`) is now an honest empty list when `message_log` is empty. `get_global_fix_session()` now attempts `restore_state()` on first construction (falling back to a fresh hardcoded-sequence session on any missing/corrupt state file, matching `restore_state()`'s own existing log-and-return-`False` error handling) instead of always starting fresh and ignoring any prior persisted session. `message_log`/`sent_messages`/`received_messages` are now capped at 1000 entries (oldest trimmed) and `gap_queue` at 500 (oldest dropped with a WARNING) instead of growing unbounded. `connect()` no longer leaks an orphaned heartbeat `asyncio.Task` on reconnect — it now cancels and awaits any existing task before creating a new one, mirroring `disconnect()`'s existing cleanup. `FixRouteOrderRequest.symbol` gained a Pydantic `field_validator` rejecting the FIX SOH delimiter (`\x01`), `=`, and `|` with a 400 instead of silently forwarding an injectable value. Every `self.state = X` assignment in the module now routes through a new `_set_state()` helper checked against a `_VALID_TRANSITIONS` legality table — logs a WARNING (never a hard block, since this pass didn't attempt to fully enumerate every legitimate transition path) on an unexpected transition instead of leaving the "strict state machine" doc claim unenforced. Known, disclosed limitations carried forward unchanged by this pass: the FIX gateway is still fully simulated (zero real-venue network calls anywhere — see the `docs/architecture/execution.md` correction below), and `execution/fix_recovery.py`'s correct gap-fill logic remains dead code, never imported by `execution/fix_gateway.py`'s own separate inline resend implementation.
- **Options desk Phases 31-36 remediation, third pass — remaining High/Medium/Low findings (PR #792, same branch, 2026-08)**: closes the deep-dive audits' High/Medium/Low findings that weren't in either of the two passes above (13 items). **FIX gateway** (`execution/fix_gateway.py`, `api/pilots_api.py`): `GET /pilots/execution/fix/session/status` was a plain `def` (FastAPI-threadpool-dispatched) reading `FixSession`'s mutable state with zero locking while `connect()`/`disconnect()` mutate the same singleton under `session._lock` on the main event loop — now `async def`, wrapped in `async with session._lock:`; new concurrency tests directly prove a status read now blocks while the lock is held externally and produces no corrupted read under concurrent connect/disconnect. New `settings.FIX_GATEWAY_ENABLED` (default `True` — the module is fully simulated, never touches real capital) additionally gates the route/session endpoints on top of their existing token checks, closing this repo's "own risk class, dedicated flag" convention gap; `settings.FIX_HEARTBEAT_INTERVAL_SECONDS` (default `30`) replaces a hardcoded constructor default. `FixMessage.from_fix_str` now raises `FixParseError` (not a bare `ValueError`) on a malformed Tag 34. `FixSessionManager.restore_all()`'s outer per-file except (distinct from `FixSession.restore_state()`'s own inner except, which already logged correctly) now logs a WARNING instead of swallowing silently. **Real-time risk streamer** (`api/ws_api.py`, `pilots/realtime_risk_streamer.py`): `_check_ws_token` now uses `hmac.compare_digest` instead of plain `==`, matching `api/auth.py`'s existing convention; `/ws/risk/portfolio` now constructs `PaperAccountStore(readonly=True)` (was the one write-capable holdout in the codebase for a pure-read use case); its 1Hz poll is now `settings.WS_RISK_STREAM_INTERVAL_SECONDS`-driven (default `1.0`, preserves today's behavior) instead of hardcoded; `compute_position_risk_greeks` no longer fabricates `dte=30.0` on an expiration-parse failure — it returns `None`, landing the position in `missing_positions` via the function's own already-established convention, with zero new caller-side logic needed. **Silent-fallback logging** (`ml/transformer_vol_forecaster.py`, `sizing/hrp_cvar_optimizer.py`): the vol forecaster gained its first `logging` import — `fit_quantile_output_weights`'s L-BFGS-B failure now logs a WARNING naming the quantile instead of silently falling back to the Ridge seed, and a genuinely dead no-op `except` in `_align_macro_causal` (an identical unprotected conversion three lines later made it provide zero real protection) was removed rather than left as misleading fake error handling; the HRP optimizer's separate SLSQP-level fallback (distinct from the already-logged quasi-diagonalization one) now also logs a WARNING — `status="fallback"` already honestly surfaced this to callers, so this only closes the missing server-log visibility. `FIX_GATEWAY_ENABLED` is registered in `pilots/feature_flags.py`'s `WRITE_GATE_REASONS` (a real test, `test_feature_flags_registry.py::test_require_enabled_guards_registered`, AST-scans every `require_*_enabled` function and asserts its guarded settings key is registered there — this closed a real failure the FIX-gateway commit introduced). Deliberately still out of scope, not attempted: fully operator-configurable FIX venue definitions (a data-model change, not a scalar setting) and the two structural limitations named in the second-pass bullet above (OFI, `execution/fix_recovery.py`).
- **Fabricated SPY spot price + dead beta fallback in options portfolio Greeks — fixed (2026-08-22)**: `pilots/options_risk.py::calculate_portfolio_greeks` fabricated a hardcoded `$500.0` SPY spot price (CONSTRAINT #4 violation) whenever a book held no direct SPY position, and `main.py`'s automated SPY delta-hedging cycle (`settings.OPTIONS_DELTA_HEDGE_ENABLED`) sized its hedge off that fabricated price while filling at the REAL SPY price a few lines later — confirmed against this operator's own live paper account: 4 already-placed `hedge_spy_*` orders, one selling 1,226 shares (~$943k notional), consistent with the ~1.53x inflation the $500 fabrication would produce. `calculate_portfolio_greeks` now resolves SPY through the same `market_provider.get_latest_quote()` mechanism used for every other symbol and never fabricates a price — a genuinely unresolvable SPY quote reports `beta_weighted_delta_spy=0.0` with `spy_spot_resolved=False` rather than a fabricated-price computation. `main.py`'s hedge cycle (now `_run_automated_delta_hedge_cycle`) resolves ONE real SPY quote and threads it into both the sizing call and the fill call, and fails closed (skips the cycle, logs a WARNING) when unavailable, instead of running on a guess. A second bug in the same module, `_resolve_symbol_beta`'s dead second-tier fallback (`data.fmp_fundamentals.compute_beta` called with the wrong argument count, always raising a silently-swallowed `TypeError`), was removed rather than repaired — measured to be genuinely redundant with the primary tier, not merely miscalled — and replaced with a logged `(1.0, False)` default plus a per-position `beta_is_estimated` flag and top-level `symbols_with_estimated_beta` list. A third, adjacent bug found in the same pass: the hedge cycle's post-hedge success log read dead keys (`_hedge_res.get("executed")`/`"spot_price"`) that never matched `execute_delta_hedge`'s real return contract (`"hedged"`/`fill["fill_price"]`), so the confirmation log had never once printed. See `docs/known_issues/options_risk_fabricated_spy_spot.md` for the full write-up and DB evidence.
- **Reverse-time SDE sign error in the generative diffusion stress engine — fixed and verified (2026-08-24)**: `validation/synthetic_diffusion_engine.py`'s `generate_synthetic_crash_paths`/`generate_guided_crisis_paths` (Phase 34) integrated the reverse-time OU SDE with a negated drift (`-x - 2*score` at both call sites, inlined separately) instead of the correct `x + 2*score` (Anderson 1982's `dτ̄ = -dt` discretization substitution for the loops' decreasing-tau/positive-dt stepping) — the sampler diverged instead of denoised. Verified three independent ways (hand-derivation, an analytic-score isolation test recovering a known `N(5,1)` target — buggy: `mean≈-25 to -29, var≈770-800`; fixed: `mean≈5.0, var≈1.0` — and a live-pipeline repro at the endpoint's real hyperparameters). Training itself (`train_diffusion_model`/`train_conditional_diffusion_model`) was already correct and untouched. Fixed via one shared, tested `_reverse_sde_drift(x, score)` helper (both call sites previously inlining separate copies is exactly how this shipped at one site undetected by the other) plus 3 new distributional-recovery regression tests in `tests/test_synthetic_diffusion_engine.py` — every prior generation test only checked shape/NaN/Inf/monotonicity-direction, never that the recovered distribution was actually correct, which is the real gap this closes. A pre-existing test fixture bug the sign fix surfaced (`test_classifier_free_guidance_monotonicity`'s "vol_shock" class had a fabricated *positive* mean despite being a crash regime, passing before only via the bug's own pathological divergence) was also fixed. **Two related items, same-day follow-up**: (1) the reported VaR/CVaR-vs-paths data-consistency gap in `api/pilots_api.py::post_diffusion_stress_test` (VaR/CVaR read raw unclipped returns, `paths` read clipped ones) is now **fixed** — a first naive "clip once, share the array" attempt was found to introduce a *worse* regression (an asymmetric `[-50%,+200%]` clip compounds into a systematic bias that can push VaR/CVaR negative) and was reverted; the real fix derives VaR/CVaR directly from the compounded `paths` array's own total simple returns (`final_price/spot - 1`), trivially consistent by construction, verified across 150 seed/regime/confidence-level combinations with zero violations, plus a new regression test recomputing VaR/CVaR from only the HTTP response's `paths` field and asserting an exact match; (2) the endpoint's real hyperparameters (`steps=100, dt=1/252`) produced a generated-return std roughly two orders of magnitude above the true training scale even after the sign fix — a separate, pre-existing undertraining/`tau_max`-vs-injected-noise calibration gap, **partially mitigated** (training epochs raised `15→1000`, negligible latency cost, verified ~35% std reduction and consistently-improved dollar VaR/CVaR at the endpoint's real `num_paths=500`) but explicitly **not resolved** — an epoch sweep up to 10,000 shows sharply diminishing returns well short of the true scale, confirming this is a network-capacity/architecture limit, not an epoch-count problem. **Second follow-up (2026-08-24, "fully fix" the calibration gap)**: diagnosed the actual root cause — the score network's prediction accuracy collapses below `tau~0.1-0.3` (measured: median relative error 16-17% at `tau=0.1-0.5`, 74-97% at `tau=0.01-0.001`) — and shipped `_tweedie_denoise`, an analytic closed-form finishing step (Tweedie's formula for this OU/VP-SDE, independently cross-derived two ways) that stops the noisy Euler-Maruyama loop early (`tau_stop`, new optional param on both generation functions, default `_DEFAULT_TAU_STOP=0.28`, `0.0` reproduces the exact pre-fix behavior) instead of integrating into the unreliable near-`tau=0` regime. A real flaw was found and corrected mid-implementation: the first `tau_stop=0.18` choice was tuned on a prototype with a bug (it never actually exercised CFG guidance) — re-swept against the real `generate_guided_crisis_paths` CFG path at the endpoint's actual `guidance_scale=2.0` default, the naive fix only helped ~3-5%, because CFG's `(1+w)*score_cond - w*score_uncond` combination amplifies the score network's own inaccuracy by up to `(1+2w)`. Fixed by having `_predict_score`'s final analytic step always use `guidance_scale=0` (pure conditional, no CFG) regardless of what guidance was requested for the noisy loop — the loop still uses full requested guidance, only the last step drops it — which restored the improvement (verified: ratio-to-true-scale at `L=29`/`guidance_scale=2.0` drops from ~14x to ~7x for the guided path, ~38x to ~18x for the unconditional path, both measured via new production-representative before/after regression tests). **Disclosed trade-off, not silently accepted**: discounting CFG for the final step measurably weakens (and can invert, confirmed on one fixture) classifier-free guidance's directional effect on the exact final reported numbers, even though overall calibration improved — `test_classifier_free_guidance_monotonicity` now passes `tau_stop=0.0` explicitly to isolate the CFG formula's own correctness from this separate side effect. Since the fixed `tau_stop` default is tuned for the production default `horizon=30` and calibration degrades materially beyond it (measured: ~18-22x at `horizon<=30`, ~35-42x at `horizon=45-60`), `DiffusionStressTestRequest.horizon` (previously an unbounded bare `int`) is now `Field(30, ge=5, le=35)`, with a matching cap on the webapp's horizon input. **Still not fully fixed** — a genuine resolution needs its own architecture/training redesign (explicit `tau` embedding, a noise-schedule-aware or larger network), left as a disclosed follow-up. See `docs/known_issues/synthetic_diffusion_reverse_sde_sign_error.md` for the full write-up, all verification numbers, and the honest account of the mid-implementation design correction.
- **`pilots/earnings_crush.py`'s BMO/AMC bar-alignment blind spot — fixed (2026-08-24)**: `get_historical_earnings_moves` always measured the realized post-earnings gap as `Open[event_date] - Close[event_date-1]` — correct only for before-market-open (BMO) reporters. An after-market-close (AMC) reaction — the majority case for large-cap tech, which is also this module's own default universe (`NVDA, AAPL, MSFT, TSLA, AMZN, GOOGL, META, AMD, NFLX, DIS`) — lands one trading day later and was never measured, reproducibly reporting `≈0%` for a real ~14.66% synthetic AMC reaction. Since `crush_edge_ratio = expected_move_pct / realized_move_pct`, this understated denominator inflates the edge ratio and can flip `is_recommended=True` for a candidate with no genuine edge, firing a real `dispatch_earnings_crush_alert`. The identical root cause let the front-week expiration picker select an expiration dated exactly `event_date`, which for an AMC reporter expires before the reaction happens. Verified (via FMP's own published `/earnings` API schema, not assumed) that no BMO/AMC field exists anywhere in this codebase's earnings data — `data/fmp_feeds_company.py::fetch_earnings_rows` already extracts every field FMP provides. Fixed by computing both a BMO-hypothesis and an AMC-hypothesis gap from real bar data per quarter and taking the larger (never fabricated, and the conservative direction for this bug — it can only increase the realized-move denominator, never decrease it further); each move now carries `reaction_session_inferred: "bmo"|"amc"` (explicitly inferred, not source-confirmed) and the function's return dict carries `timing_data_available: False`. The expiration picker now requires `ed > event_date` (was `ed >= event_date`). See `docs/known_issues/earnings_crush_bmo_amc_bar_alignment.md` for the full write-up and `docs/signals/earnings_crush.md`'s "Defects found while analysing this pilot" section.
- **Secondary audit pass (2026-08-24) — four previously-unaudited/partially-audited areas, all fixed and verified**: a follow-up sweep of areas explicitly flagged as lower-priority/not-yet-deep-audited in prior passes. (1) **`pilots/options_gex.py` 100x dollar-scaling bug**: every displayed dollar GEX figure (Net/Call/Put GEX, every per-strike value) was missing the industry-standard `×0.01` "per 1% move" normalization the module's own `dealer_hedging_flow` field already correctly applied, overstating the primary KPI/chart values by exactly 100x (one ATM SPY strike computed to $3.46B); fixed via a shared `PERCENT_MOVE_SCALING_FACTOR` applied once at aggregation. Also fixed in the same module: missing/zero/NaN IV and missing/unparseable expiration previously fabricated a plausible `σ=0.25`/`dte=30.0` instead of excluding the contract (CONSTRAINT #4, reachable on real yfinance data — stale-quote strikes routinely report `impliedVolatility=0.0`), and `GexProfileView.tsx` never read the backend's own honest `chain_source`/`spot_price_source` fields, so a fully synthetic fallback chain rendered indistinguishably from real market structure — now shows an honesty banner. (2) **SEC Rule 606 report structurally always `$0.00`/0% price-improvement**: `execution/multi_broker_gateway.py` (the file initially in scope) turned out to contain zero NBBO/price-improvement logic at all — the real bug was in `data/execution_audit_store.py`/`execution/sec_rule_606_reporter.py`, where every real fill omits NBBO (nothing populates it) and `price_improvement` defaulted to `0.0` identically for "measured, zero improvement" and "unmeasurable," making the live compliance report present a coverage gap as a measured fact. Fixed via a new `ExecutionAuditRecord.nbbo_available` column and coverage-aware rate denominators (`nbbo_coverage_pct` now surfaced everywhere a price-improvement rate is reported) — dollar sums were already correct, only the rates were fabricated-looking. A related dead-code bug (`classify_limit_order`, tested but zero write-path callers, making "Marketable Limit" structurally unreachable) was wired in alongside a new `limit_price` passthrough from `OrderIntent`. (3) **`sector_selection_engine.py` semantic-similarity lookahead gap**: the similarity term had zero point-in-time awareness (always embedded a target's CURRENT business description regardless of a backtest's `as_of` date), defeating the lookahead-safety design the sibling Sector Heat Factor term already had; dormant (no backtest/replay caller exists yet) but completely untested. Fixed via a new `HistoricalStore.get_fundamentals_raw_json_asof` point-in-time lookup mirroring the existing `get_fundamentals_asof` convention — verified (not assumed) this has a real, disclosed effect on the LIVE daily pipeline too, by reading every real fundamentals provider's actual return shape and reproducing the exact degrade case in a test. A second bug (`degraded_reason`'s operand order let a routine heat-side flag mask the actual blocking similarity-side reason) was fixed in the same pass. (4) **LLM prompt injection via undelimited headlines**: `llm/research.py` interpolated real, externally-sourced news headlines directly into the LLM prompt with no delimiter separating untrusted data from instructions — reproduced statically that an instruction-shaped headline lands verbatim in the synthesis block. Bounded risk (the four Tier 9 schemas have zero numeric/action fields and nothing in `sizing`/`signals`/`execution` reads their output, confirmed by grep) but a real social-engineering risk to a human operator; fixed via explicit `<headline>`/`<research_context>` fencing plus a matching system-prompt instruction never to follow instructions found inside them. See `docs/known_issues/options_gex_100x_dollar_scaling.md`, `docs/known_issues/sec_606_price_improvement_fabricated_zero.md`, `docs/known_issues/sector_selection_similarity_lookahead.md`, and `docs/known_issues/llm_prompt_injection_undelimited_headlines.md` for the full write-ups.
- **Options-desk mock/live parity sweep, round 3 — 4 more confirmed bugs, all fixed and verified (2026-08-29)**: a follow-up audit specifically hunting the "backend signals a real failure/gate, frontend silently mishandles it" bug class (same family as the earnings_crush/dispersion_trading/zero_dte_engine override-affordance fix earlier this file documents), scoped to `PaperBroker.tsx` and adjacent options-desk screens. (1) **`PaperBroker.tsx`'s strategy-options/manage-exits dead-letter fabrication**: `POST /pilots/paper-broker/strategy-options/execute` and `/manage-exits` both dead-letter a real server-side exception into a 200-status `{"ok": false, "error": "..."}` body (CONSTRAINT #6) — which never throws, so `handleExecuteStrategyOptions`/`handleManageExits` read it as a real success and rendered *"Successfully executed undefined strategy trades (undefined skipped, undefined failed)"* / *"Evaluated undefined positions: closed undefined."* to the operator. Fixed via a shared `PaperBrokerDeadLetterResult` union member on both result types (`webapp/src/api/types.ts`) and a new `execDeadLetterError` state routed into the screen's existing red error box, distinct from `execStatus`'s unconditionally-green success banner. **A second, deeper bug found in the same investigation**: `ManageExitsResult`'s declared shape (`evaluated_count`/`closed_count`/`closed_positions`/`message`) never matched the real backend at all, on EITHER path — `pilots/paper_broker.py::manage_position_exits()` returns `execution/options_paper_executor.py::OptionsPaperExecutor.execute_auto_exits()`'s actual shape verbatim (`enabled`/`evaluated_count`/`executed_count`/`failed_count`/`executed`/`failed`, confirmed against `tests/test_options_lifecycle.py`'s own assertions) — meaning the manage-exits status message had never once displayed a correct number on a real backend, success or failure. `ClosedExitPosition` (only ever referenced by the wrong type) was removed as dead/fabricated. (2) **`POST /pilots/options/alerts/test` type/implementation mismatch**: `pilots/options_alerts.py::dispatch_options_alert()`'s real return shape (`status`/`alert_type`/`level`/`title`/`message`/`payload`/`timestamp`/`success`/`error`) shared zero field names with the declared `OptionsAlertTestResult` (`ok`/`dispatched_count`/`channels[]`/per-channel `results[]`) — a real (non-mocked) call crashed `VolForecastScanner.tsx`'s render (`testAlertResult.channels.join(", ")` on `undefined`). Fixed by correcting the frontend type/component to the real, honest shape rather than having the backend fabricate a per-channel breakdown it structurally cannot measure (`dispatch_options_alert` calls `send_alert()` once, all-channels-at-once, with only one aggregate success/error — synthesizing a per-channel status would itself be a CONSTRAINT #4 violation). (3) **`TopStatusBar.tsx`'s job-cancel button silently ignores a disclosed `cancelled: false`**: `POST /jobs/{id}/cancel` honestly reports `cancelled: false` (200, not an error) when it could ask but not confirm the job actually stopped (`api/control_api.py`'s own docstring) — `Console.tsx`/`RunCommandControl.tsx` both already branch on this correctly, but `TopStatusBar.tsx`'s Cancel button only had a `catch` for a thrown exception and silently did nothing on an honest `cancelled: false`. Fixed to match the other two screens' handling (a toast on the unconfirmed case). (4) **`UnusualFlowFeed.tsx` drops two real backend honesty fields (PR #894)**: `pilots/unusual_options_flow.py`'s `price_is_estimated`/`spot_price_is_estimated` (set when a trade's price/spot came from a bid/ask-midpoint or median-strike fallback rather than a real observed value) and the endpoint's `degraded`/`symbols_fetch_failed` (a genuinely incomplete scan, per the endpoint's own docstring) were declared on `UnusualOptionTrade`/`UnusualOptionsFlowResponse` in `types.ts` but never once read by the component — an estimated price rendered with identical visual confidence to a real one (the exact CONSTRAINT #4 failure mode these fields were added to prevent), and an incomplete scan rendered identically to "nothing unusual found." Fixed via a page-level "Incomplete Scan" honesty banner (mirroring `GexProfileView.tsx`'s `chain_source` banner) and small "(est.)" badges next to an estimated fill price/spot price/notional. `mock.ts` also never exercised any of these four fields on the happy path in any of the four bugs above (`managePaperOptionsExits`'s mock fixture matched the same wrong `ManageExitsResult` shape as the type; `testOptionsAlert`'s mock fabricated the same fictional per-channel breakdown; `getUnusualOptionsFlow`'s mock never set the two honesty flags) — all three now match the corrected, real contracts, so mock-mode development actually exercises what live returns. Tests: extended `PaperBroker.test.tsx` (+2), `VolForecastScanner.test.tsx` (+2), `TopStatusBar.test.tsx` (+1), `UnusualFlowFeed.test.tsx` (+3).

---

**Detailed feature/tier changelog moved out of this file (2026-07-05).** Everything above is the current, load-bearing architecture reference. For the dated, PR-by-PR backstory of every Tier/Task/Scope feature (ML Stage 4, GUI tab build-outs, Tier 1 through Tier 9, AI Control Center, Prompt Registry) — including full test surfaces and Gravity audit step numbers — see [`docs/FEATURE_TIER_HISTORY.md`](docs/FEATURE_TIER_HISTORY.md).


## Recent Architecture Updates
- **Signal Engine Vectorization**: As of Phase 4, `SignalAggregator`'s own aggregation step is natively vectorized in pandas/numpy (O(1) block computation) and row-based ticker iteration there has been removed. **Correction (2026-08 module efficiency audit):** not every `SignalModule` implementation is -- `SignalModule.compute_vectorized()` has a per-row `df.apply(axis=1)` fallback (`signals/base.py`) that a module silently inherits if it doesn't override the method, and as of the audit 7 of 20 registered modules do exactly that (`multifactor`, `cross_sectional_momentum`, `macro_regime`, `regime_multiplier`, `sector_quality_rank`, `lgbm_ranker`, `news_catalyst`). This is a debt-visibility gap, not an active performance emergency: per `docs/module_efficiency_redundancy_audit.md`'s F1, 5 of the 7 use the two-phase `pre_compute()` hook so their per-row `compute()` is a cheap dict lookup, and the other 2 are trivial conditionals -- `lgbm_ranker` in particular does NOT do per-row model inference despite that being the audit's own original (and wrong) assumption; its LightGBM prediction is already batched inside `pre_compute()`. `tests/test_no_iterrows_in_core_engines.py::test_registered_signal_module_overrides_compute_vectorized` now enumerates this list mechanically -- a new non-overriding module fails CI unless explicitly added there with a reason.
- **Multi-Leg Option Paper Trading & Automated Strategy Paper Execution (`execution/fmp_paper_broker.py`, `execution/options_paper_executor.py`, `pilots/order_sizing.py`, `data/paper_account_store.py`, `webapp/src/screens/PaperBroker.tsx`, 2026-08)**: Replaced multi-leg order rejection with native multi-leg option paper execution across credit and debit spreads (Bull Call Spreads, Bear Put Spreads, Iron Condors, Straddles, Strangles) with atomic SQLite fills across all constituent legs and short positions (`qty < 0`). Added `OptionsPaperExecutor` to scan quantitative options directives from `technical_options_engine.py` (filtered by `passes_premium_gate` and `Integrity_OK`), enforce 1-spread-per-underlying deduplication, respect `MAX_CONCURRENT_OPTION_POSITIONS`, size contracts via `calculate_multi_leg_option_sizing` capped by `MAX_OPTION_NOTIONAL_PER_TRADE`, and execute fills automatically on cycles when `PAPER_OPTIONS_AUTO_EXECUTE_ENABLED=True` or on-demand via Pilots API (`/pilots/paper-broker/strategy-options/*`) and the Pilots PWA Paper Broker screen. Tests: `tests/test_options_paper_executor.py`, `tests/test_pilots_paper_broker.py`, `tests/test_paper_broker_options_order.py`, `tests/test_paper_account_store.py`, `tests/test_fmp_paper_broker.py`. **Automated-lifecycle gating fix + daemon gap fixed (2026-08)**: `main.py`'s `_run_cycle()` gated its whole "Automated Strategy Options Paper Execution & Lifecycle" block (exit management, new-position auto-execution, delta hedging) on an outer OR of `PAPER_OPTIONS_AUTO_EXECUTE_ENABLED`/`OPTIONS_AUTO_EXIT_ENABLED`/`OPTIONS_DELTA_HEDGE_ENABLED` — `OPTIONS_0DTE_ENABLED` was checked correctly on the inner 0DTE step but was missing from this outer gate, so an operator enabling ONLY 0DTE lifecycle management got the whole block silently skipped and `manage_0dte_exits()`'s +75%/-30%/15:45 ET hard exit never ran for open 0DTE positions. Fixed by extracting the block into a new module-level `execution/options_lifecycle.py::_run_automated_options_lifecycle(macro_dto=None)` (mirroring the existing `_run_automated_delta_hedge_cycle` extraction) with `OPTIONS_0DTE_ENABLED` added to the outer OR; regression-tested in `tests/test_options_lifecycle.py` (the full four-flag OR-gate truth table, not just the one missing branch). A second, broader gap was also fixed: this whole automated-options lifecycle had no equivalent wiring anywhere in `main_orchestrator.py`/`desktop/daemon_runtime.py`, so flipping `settings.ORCHESTRATOR_DAEMON_ENABLED` to `True` would silently stop exit management/strategy auto-execution/delta hedging from running at all. Fixed by threading `macro_dto` back from the async orchestrator pipeline and wiring `run_automated_options_lifecycle(macro_dto)` into the persistent daemon's cycle exactly matching the legacy path. See `docs/known_issues/options_lifecycle_daemon_gate_gap_2026_08_22.md` for the full write-up.
- **`OrchestratorDaemon` property-vs-method contract (`api/control_api.py`, `desktop/daemon_runtime.py`, 2026-07)**: `is_running` and `last_result` are `@property` on `OrchestratorDaemon` (read WITHOUT parens); `status()`/`get_run()`/`trigger_run()`/`set_interval()`/`start()`/`shutdown()` are methods — while `gui.orchestrator_runner.RunHandle.is_running()` **is** a method, and `api/control_api.py` calls both kinds. `POST /daemon/restart` shipped a 500 on exactly this confusion (`daemon.is_running()` evaluated the property to a bool then called it), reachable on every real deployment since a daemon is always attached there. Corollary for tests: a fake for a class with properties must be a hand-written class, **never** a bare `MagicMock` (every attribute on one is callable and truthy) and **not** `mock.create_autospec` either — `create_autospec` explicitly drops the spec for data descriptors and returns a *callable* `MagicMock` for a property, so it does not catch this class of bug. See `tests/test_control_api.py`'s `_FakeDaemon` plus its `test_daemon_properties_are_never_called_as_methods` (AST guard, derives the property set from the real class) and `test_fake_daemon_mirrors_orchestrator_daemon_member_kinds` (keeps the fake's shape from drifting from the real one).

- **`output/daemon.json` staleness fix — writer + reader, both required (`desktop/orchestrator_daemon.py`, `pilots/run_status.py`, `api/pilots_api.py`, 2026-07)**: `_write_daemon_file` was called exactly once, at startup, and never updated — a dead daemon left a file advertising `state: "running"` forever, which `GET /automation/status`'s `daemon_json` fallback (consulted precisely when the Control API is unreachable — the dead-daemon case) served as-is. **Writer half:** `_write_daemon_file` gained `state`/`started_at`/`stopped_at` kwargs (all defaulting to `None` = today's exact derived behavior) and is now called a SECOND time, with `state="stopped"`, as the last step of `_teardown()` — after `daemon.shutdown()` returns, with `started_at` threaded through from the real startup timestamp (never re-stamped to "now", which would fabricate a start time at the moment the daemon died — CONSTRAINT #4). **This only covers a graceful stop.** **Reader half — the load-bearing one:** a `SIGKILL`ed daemon can never perform that terminal write, so `pilots.run_status.read_daemon_json()` now also returns a derived `"pid_alive"` key (`True`/`False`/`None` via a private `_pid_alive(pid)` — `os.kill(pid, 0)`; rejects non-positive/`bool`/non-`int` pids before ever calling it, since pid 0 or negative targets a process group; `None` — never a fabricated `False` — when unknowable) added to the record rather than a second helper a caller could forget to call. Surfaced in `api/pilots_api.py`'s `GET /automation/status` (`daemon.pid_alive`, `None` on the `control_api` branch since no pid was ever probed there) and deliberately **not** used to null `GET /automation/schedule`'s `running_value`/`drift` for a dead pid — a dead daemon's last known interval is still the honest answer to "what was it running when it died"; `daemon.alive`/`daemon.pid_alive` already convey deadness. The file's own `state` string is deliberately never echoed directly in the API — it's an unverifiable self-report a `SIGKILL`ed process can never correct; `pid_alive` is the externally-verified signal. Webapp: `pid_alive: boolean | null` in `types.ts`'s `DaemonInfo`, mirrored in `mock.ts`'s fixture, rendered in `Settings.tsx` as three distinguishable states ("stopped — process not running" / "process alive, API not responding" / "last known state").
- **Shutdown-budget fix — one published total instead of three unreconciled hardcoded values (`settings.py`, `desktop/daemon_runtime.py`, `desktop/orchestrator_daemon.py`, `desktop/engine_supervisor.py`, 2026-07)**: `desktop/orchestrator_daemon.py`'s `_teardown()` used to hardcode a 5s Control-API join + 5s Pilots-API join + a `daemon.shutdown(timeout=10.0)` call that ITSELF hardcoded an *unbudgeted* extra 5s timer-thread join before its own 10s poll — nobody had added these up, and the actual total (20-25s) was routinely SIGKILLed by every parent supervisor at a flat 5-10s. New setting **`DAEMON_SHUTDOWN_TIMEOUT_SECONDS`** (default `25.0` — the outer bound of every configuration reachable before this fix, so no path gets a shorter budget than it already had) is captured as ONE monotonic deadline at the top of `_teardown()` (inside its `_torn_down` idempotency guard, so a second no-op call never restarts the clock); each API-thread join is capped at `_SHUTDOWN_API_JOIN_SECONDS` (5.0, a module constant, not a setting) even when more of the deadline remains, and `daemon.shutdown()` receives whatever remains after both joins — correct because `OrchestratorDaemon.shutdown()` itself was fixed to budget its OWN timer-thread join *within* whatever `timeout` it's given (previously that join was a flat unbudgeted 5s on top). **Does NOT wait out an in-flight pipeline cycle** — a cycle can take minutes and there's no safe way to abort one mid-flight (see `pipeline/runner.py`'s own docstring); the budget only bounds the genuinely-bounded stages. `desktop/engine_supervisor.stop_engine`'s default timeout (used by `app_shell.py`'s only production call site) is now **backend-aware** via `getattr(handle, "mode", None)`: `"daemon"` → `DAEMON_SHUTDOWN_TIMEOUT_SECONDS + 5.0` grace (so the parent always outwaits the child), anything else (incl. a handle missing `mode` entirely) → `5.0`, today's unchanged value for the `main.py --interval` backend, where a longer timeout would only delay giving up on a cycle that's genuinely mid-flight, never make shutdown more graceful. An explicit `timeout=` argument still always wins. **Required companion fix**: `reporting/state_snapshot.py`/`main_orchestrator.py`'s `state_snapshot.json` writers were the one place a "mid-cycle SIGKILL is safe" claim didn't hold — both used a bare `write_text()` (not write-then-rename), so a kill mid-write left a truncated file every reader treated as *missing* rather than merely stale; fixed to the same atomic idiom as `execution/kill_switch.py`/`desktop/orchestrator_daemon.py`'s `_write_daemon_file`. **Outer supervisors resized to exceed the new total** (published as a ladder in `docs/RUNBOOK.md` §3.14): `launch_app.command`'s previous-instance-replace window (`SHUTDOWN_GRACE_SECONDS`, was a flat 10s → now 40s, with a progress line every ~5s so a genuinely long wait doesn't look like a hang); `scripts/com.investyo.stack.plist`'s `ExitTimeOut` (was unset/system-default → 45s). **Major related finding**: under launchd, graceful teardown was not actually running AT ALL before this fix — `AbandonProcessGroup` means launchd's SIGTERM targets the WRAPPER script's pid, not the daemon it ran in the foreground, and bash defers a trap's execution until the current foreground command completes (verified empirically), so the daemon never received that SIGTERM and was SIGKILLed outright at `ExitTimeOut`. Fixed by backgrounding the daemon in `scripts/investyo_stack_service.sh` and `wait`ing on it (interruptible), with an explicit trap that forwards the signal to the daemon and polls (bash 3.2 compatible — no `wait -n`) before reaping the two API children — see that script's own header comment for the full signal-path writeup. `deploy/investyo-daemon.service` gained explicit `TimeoutStopSec=45`/`KillSignal=SIGTERM` and `After=network-online.target` (was `network.target` — the daemon's first cycle makes outbound calls immediately). Two independent, pre-existing bugs found and fixed in the sibling systemd units while verifying this: `deploy/investyo-mcp.service` paired `Wants=network-online.target` with `After=network.target` — the classic ineffective combination (`Wants=` alone does no ordering); and **all three** units had `StartLimitIntervalSec`/`StartLimitBurst` inside `[Service]`, which silently no-ops on any systemd since v229 (2016) — moved to `[Unit]` in all three. **New env var:** `DAEMON_SHUTDOWN_TIMEOUT_SECONDS` (GUI-writable, `gui/env_io.py` `ALLOWED_KEYS` + `gui/panels/settings_manager.py` `_SETTINGS_LAYOUT`, non-secret — a GUI bug here can only make shutdown less graceful, never leak a credential or enable a dangerous action).
- **Multi-Model & Open Source AI Chat System (`api/data_api.py`, `settings.py`, `gui/env_io.py`, `webapp/src/components/AIChatInterface.tsx`, `webapp/src/api/client.ts`, 2026-08)**: Extended the platform's AI Assistant into a multi-model architecture capable of routing queries to Google Gemini (`gemini-2.5-flash`, `gemini-2.5-pro`), Anthropic Claude (`claude-3-5-sonnet`, `claude-3-5-haiku`, `claude-3-opus`), OpenAI ChatGPT (`gpt-4o`, `gpt-4o-mini`, `o1`, `o3-mini`), and Local / Open Source LLMs (Ollama, vLLM, DeepSeek, Llama-3, Qwen, Mistral, OpenRouter) via standard OpenAI-compatible endpoints (`LOCAL_LLM_BASE_URL`). Added `GET /data/ai/models` endpoint for dynamic provider/model catalog discovery, model selection bar in the UI drawer with custom model input, and automated fallback routing. Tests: `tests/test_data_api_chat.py`, `webapp/src/components/AIChatInterface.test.tsx`.
- **Gemini Live API WebSocket Streaming & Voice Controls (`api/ws_api.py`, `api/data_api.py`, `webapp/src/chat/useGeminiLive.ts`, `webapp/src/chat/audioStreamer.ts`, `webapp/src/components/AIChatInterface.tsx`, 2026-08)**: Added real-time, low-latency, bidirectional voice and text streaming over WebSockets between the Pilots PWA frontend and Google Gemini (`gemini-3.1-flash-live-preview`). Supported audio format is 16 kHz 16-bit linear PCM mono input (Web Audio microphone capture downsampled in browser) and 24 kHz 16-bit linear PCM mono output (streamed from Gemini and played via AudioContext with instant interruption support). Integrated dynamic read-only grounding tools (`list_all_pilots`, `get_pilot_holdings`, `get_pilot_recent_trades`, `get_current_portfolio`, `get_platform_status`) executed via `session.send_tool_response`. Gated by `AI_GENERATION_API_ENABLED`, `GEMINI_LIVE_CHAT_ENABLED`, and `STATE_API_TOKEN`. Tests: `tests/test_gemini_live_chat.py`, `webapp/src/chat/audioStreamer.test.ts`, `webapp/src/components/AIChatInterface.test.tsx`.
- **Execution audit trail wiring, circuit-breaker latency trip, and client-order-id collision fix (`execution/order_manager.py`, `execution/multi_broker_gateway.py`, `conftest.py`, 2026-08)**: three findings from an `execution/` audit, all fixed together (same root cause pattern: "implemented and tested, never verified end-to-end"). (1) `data/execution_audit_store.py::ExecutionAuditStore.record_audit`/`bulk_insert_audits` had zero production callers — the durable table backing `execution/sec_rule_606_reporter.py`'s SEC Rule 606 reporting was fully implemented and tested but never wired to a real order path, so `GET /pilots/execution/sec-606/report` always read an empty table (an honest all-zero result per CONSTRAINT #4, but disconnected end-to-end from real fills) — same bug class as the previously-confirmed `pilots/zero_dte_engine.py` 15:45 ET gate incident. Fixed via a new `OrderManager._record_execution_audit(intent, result)`, called from `submit_order_with_idempotency` on every real fill (`status != ERROR` and `filled_qty > 0`); injectable via a new constructor `audit_store=` param (lazily constructed against the default DB otherwise), best-effort/dead-letter-safe (never raises, matching `RunHistoryStore`'s pattern in `desktop/daemon_runtime.py`). NBBO bid/ask are left `None` (no generic NBBO source at this layer — `price_improvement` honestly computes to `0.0` rather than being fabricated). **Known limitation, disclosed not swept under the rug**: only covers a fill returned synchronously from `broker.submit_order` (true of `FMPPaperBroker`, the default paper broker); a broker whose fills arrive later via `stream_trade_updates` is not yet covered. **Required a new autouse test fixture**: `ExecutionAuditStore()`'s default constructor resolves to the real shared `~/.stockpy_local/quant_platform.db`, and ~15+ pre-existing test files construct `OrderManager(broker, ...)` directly with no `audit_store=` of their own — left unguarded, the suite would silently write real rows into an operator's production DB on every run. `conftest.py::_isolate_execution_audit_db_in_tests` (autouse, mirrors the existing `_isolate_validation_runs_db_in_tests` fixture for `ValidationHistoryStore` exactly) points the default resolver at `sqlite:///:memory:` for any test that doesn't pass its own explicit `db_url`/`sqlite_path` — **this is the pattern to follow** for any future store with an implicit default-DB constructor reachable from a widely-called production function. (2) `execution/multi_broker_gateway.py::CircuitBreaker`'s documented CLOSED→OPEN trip conditions ("consecutive failures >= threshold OR error rate >= threshold OR latency > threshold") had a dead third leg — `record_success` only logged a WARNING on a latency breach and never tripped, so a consistently-slow-but-never-erroring broker never actually tripped. Fixed via a self-contained `_consecutive_latency_breaches` counter on `CircuitBreaker` (no call-site changes needed elsewhere — every existing caller already passes `latency_ms`): reaching `config.max_consecutive_failures` consecutive breaches now trips it. Also removed the dead `CircuitBreakerConfig.auto_reset_on_heartbeat` field (referenced nowhere else in the codebase, implying a reset-bypass capability it never had) rather than inventing new behavior for an ambiguous, never-wired flag. (3) `execution/order_manager.py::make_client_order_id`'s canonicalization switched from a raw `f"{strategy_id}|{symbol}|..."` string to `json.dumps([...])` — the old scheme let a literal `|` inside `strategy_id`/`symbol` forge a collision between two distinct order tuples (reproduced: `('X', 'A|B', ...)` and `('X|A', 'B', ...)` produced byte-identical ids), which the idempotency dedup lock would then silently drop as "already submitted" — not reachable via any current call site (all pass fixed literals/validated tickers), but no longer possible for a future free-text caller to reintroduce. Tests: `tests/test_order_manager_execution_audit_wiring.py`, `tests/test_multi_broker_gateway.py::test_circuit_breaker_latency_trip`, `tests/test_order_manager_idempotency.py::test_client_order_id_no_delimiter_collision`.
- **Options-Greeks dict-shape parity, cost-model market_cap threading, Almgren-Chriss well-posedness guard, and stress-threshold duplication fix (`pilots/options_risk.py`, `simulation_engine.py`, `investyo_mcp_server.py`, `execution/almgren_chriss_router.py`, `validation/stress_scenarios.py`, 2026-08)**: four small, independently-confirmed findings from an options-Greeks and execution-cost-model audit, none live-reachable today but each a real latent correctness/consistency risk. (1) `calculate_black_scholes_greeks`'s 0DTE branch (`t_years <= 1e-12`) omitted `rho`/`rho_1pct`/`rho_raw` while the degenerate-sigma and main computation branches both include them — a future caller doing `bs["rho"]` unconditionally on a 0DTE option would `KeyError`. Fixed by adding the three keys with the textbook-correct `rho=0.0` value for an expiring option, plus a regression test asserting all three return branches share an identical key set. (2) `simulation_engine.py`'s `optimize_strategy_vectorbt()`/`run_backtrader_simulation()` hardcoded `market_cap=None` at their `TieredCostModel` call sites, so `get_liquidity_tier(None)` always resolved `"large_cap"` regardless of the actual symbol — the liquidity-tier cost differentiation was defined but never exercised through this path. Both functions now accept an optional `market_cap` parameter (default `None`, byte-identical for any caller not yet passing one); the one real production caller, `investyo_mcp_server.py::run_backtest(symbol, period)`, now resolves a real market cap via `get_provider().get_fundamentals(symbol)["marketCap"]`, degrading to `None` (never a fabricated `0.0`, CONSTRAINT #4) on any fetch failure. `pairs/simulation.py`'s own separate `market_cap=None` call site is out of scope — a two-symbol pairs backtest has no single "the" market cap without a larger design decision. (3) `execution/almgren_chriss_router.py::compute_trading_trajectory` had no guard for the AC(2001) well-posedness condition (`eta_tilde = temp_impact - 0.5*perm_impact*tau > 0`) — a caller passing a `temp_impact` too small relative to `perm_impact*tau` would silently compute a negative "effective" temporary-impact coefficient and a nonsensical `expected_shortfall`. Not exercised by any current caller (verified against every test case and `api/pilots_api.py`'s fixed params), but now raises `ValueError` explicitly alongside the module's existing five validation checks. (4) `validation/stress_scenarios.py` defined its own local `MAX_STRESS_DRAWDOWN: float = 0.50` instead of importing `STRESS_MAX_DRAWDOWN` from `validation/thresholds.py`, even though `thresholds.py`'s own docstring already claims ownership of this value — two independent literals happened to agree (both `0.50`) but had no mechanism to stay that way. Fixed via a real import, preserving `MAX_STRESS_DRAWDOWN` as a valid public re-export (confirmed imported externally by `Gravity AI Review Suite.py` and `tests/test_stress_gate.py`). Tests: extended `tests/test_options_risk.py`, `tests/test_simulation_engine.py`, `tests/test_investyo_mcp_server.py::TestRunBacktest`, `tests/test_almgren_chriss_router.py`, `tests/test_stress_gate.py`.
- **Pipeline hang fix: FRED calls had no timeout, wedging a cycle for 2.5 days (`main_orchestrator.py`, `data_engine.py`, `desktop/daemon_runtime.py`, 2026-08-27)**: a live incident (`output/progress.json` frozen in `state: "running"` for 15+ minutes and climbing, `last_data_refresh.txt` 2.5 days stale, near-zero CPU, no open outbound socket on the daemon process) was root-caused by reading the installed `fredapi` library source directly — `Fred.get_series()` calls a bare `urlopen(url)` with no timeout parameter and no session-injection hook anywhere in the class. Two independent call paths reached this unbounded call with zero protection: `main_orchestrator.py::fetch_all_data_async()`'s `asyncio.gather()` over three concurrent sub-fetches (no overall timeout — `return_exceptions=True` isolates an *exception* from one sub-fetch, not a *hang*), and, separately, `pipeline/runner.py::AsyncPipelineRunner.run()`'s `RunPipelineStep` → `run_pipeline()` → `MacroEngine.compute_hmm_risk_on_probability()` → `HistoricalStore.get_macro()` → `DataEngine.fetch_macro_history()`, hitting the identical unbounded FRED call from a completely different pipeline stage. Fixed at three layers: (1) each of `fetch_all_data_async()`'s three sub-fetches is now wrapped in `asyncio.wait_for(..., timeout=settings.DATA_FETCH_TASK_TIMEOUT_SECONDS)` (default 180s) — a timeout is a `TimeoutError` (`Exception` subclass), caught by the SAME existing per-task dead-letter handling already used for a raised exception, no new fallback logic needed; (2) `data_engine.py` gained a `_bounded_fred_timeout(seconds)` context manager (`socket.setdefaulttimeout()`, scoped narrowly and always restored) wrapping every `self.fred.get_series(...)` call in both `fetch_macro_raw_detailed()` and `fetch_macro_history()` (new `settings.FRED_REQUEST_TIMEOUT_SECONDS`, default 10.0, mirroring `FMP_TIMEOUT_SECONDS`'s per-request scope) — mandatory, not optional, since fix (1) alone leaves path two fully exposed; (3) `desktop/daemon_runtime.py::OrchestratorDaemon.maybe_alert_on_pipeline_stall()` (new, gated on `settings.PIPELINE_STALL_ALERT_ENABLED`, default **True** — a deliberate exception to this repo's "new instrumentation defaults off" convention, since it's pure read-only alerting via `observability.alerts.send_alert` with zero blast radius when no webhook/email channel is configured) fires a `WARNING` (`dedup_key="pipeline_stall"`) whenever `progress.json` reports `state="running"` stale beyond `settings.PIPELINE_STALL_ALERT_SECONDS` (default 1800s) — called from both of `_timer_loop`'s per-wake spots and from `trigger_run()` directly, since `settings.ORCHESTRATOR_INTERVAL_SECONDS` actually defaults to 0 (on-demand only), where `_timer_loop` parks on an untimed wait and would otherwise never get a periodic chance to check. Deliberately alert-only — never cancels the wedged cycle or restarts the daemon, since that process also hosts the Control/Pilots APIs the webapp depends on. Same symptom class as (and cross-referenced from) `docs/known_issues/watchlist_env_inline_comment_hang.md`, different root cause; this PR does not attempt a full audit of the other five pipeline stages for similarly-unbounded calls (disclosed, not silently assumed complete). New env vars: `DATA_FETCH_TASK_TIMEOUT_SECONDS`, `FRED_REQUEST_TIMEOUT_SECONDS`, `PIPELINE_STALL_ALERT_ENABLED`, `PIPELINE_STALL_ALERT_SECONDS`. Tests: `tests/test_main_orchestrator.py::TestFetchAllDataAsyncDeadLetter::test_macro_fetch_hang_isolated_dict_fallback_within_bounded_time`, `tests/test_data_engine_macro_history.py`'s `TestBoundedFredTimeout`/`TestFetchMacroCallsBoundedByRequestTimeout` (the latter proves the bound against a real blocking socket, not a mock), `tests/test_daemon_runtime.py::TestMaybeAlertOnPipelineStall`. See `docs/known_issues/data_pipeline_fred_unbounded_timeout_stall.md` for the full incident write-up.
- **Comprehensive unbounded-timeout sweep (2026-08-27, follow-up to the FRED-timeout incident above)**: the prior incident's own "Scope boundary" disclosed that no full audit of the rest of the codebase had been performed for the same bug class — unbounded blocking calls with no timeout. This PR is that audit: 3 parallel passes covering every `subprocess.run`/`subprocess.Popen` call site, every `ThreadPoolExecutor`/`ProcessPoolExecutor`/`multiprocessing` wait, and every LLM SDK client construction site. Re-verified against real installed-library source (not docstrings) that FMP, GDELT, EDGAR, the Robinhood device-approval login worker, the CNN-LSTM subprocess pool, and yfinance's own library-level timeout default were all already genuinely safe. Four real gaps found and fixed: (1) **`execution/alpaca_broker.py` + `data/market_data.py`'s `AlpacaProvider`** — `alpaca-py`'s `RESTClient` (base class of both `TradingClient` and `StockHistoricalDataClient`) has NO timeout anywhere, confirmed against the installed source (`RESTClient._one_request()` never passes a `timeout` key) — worse in KIND than the FRED bug, since `AlpacaBroker`'s methods call `self._client.X(...)` synchronously inside `async def` methods, not even offloaded via `asyncio.to_thread`, so a stall freezes that cycle's own event loop directly. Fixed via new shared module `data/alpaca_http.py::mount_timeout_adapter()`, mounting a custom `requests.HTTPAdapter` on the client's own `self._session` (the only lever available — the constructor exposes no timeout parameter), called from both `AlpacaBroker.__init__` and `AlpacaProvider._build_client()`. New setting `ALPACA_REQUEST_TIMEOUT_SECONDS` (default 15.0). Deliberately NOT converting to `asyncio.to_thread` (a larger, separate refactor) — disclosed residual: blocks the event loop up to 15s instead of indefinitely. (2) **`pipeline/runner.py::AsyncPipelineRunner.run()`** — the exact `await asyncio.to_thread(step.run, ctx)` dispatch named (but not fixed) in the original FRED incident is now wrapped in `asyncio.wait_for(..., timeout=settings.PIPELINE_STEP_TIMEOUT_SECONDS)` (new setting, originally default 900.0/15 min, below `PIPELINE_STALL_ALERT_SECONDS`'s 1800s; both raised same-day to 1800.0/3600s respectively -- see the "raised to 1800s" note a few lines below); a timeout propagates as `TimeoutError` exactly like any other step exception, since this runner deliberately never wraps steps in a blanket try/except. (3) Three trivial one-line `timeout=` additions to previously-unbounded `subprocess.run` calls, all LOW blast radius (strand one background job, never a shared service): `investyo_mcp_server.py::run_platform_tests()` (`timeout=900` + `except subprocess.TimeoutExpired`), `scripts/preflight_check.py`'s `.env`-not-tracked `git ls-files` check (`timeout=10`, degrades to "not tracked" on expiry), `"Gravity AI Review Suite.py"`'s check #14 SDK-reach sentinel (`timeout=120`, degrades `no_sdk = False`). (4) **LLM chat endpoint timeouts** (`api/data_api.py`'s `POST /api/chat`, `api/ws_api.py`'s `/ws/chat/live`) — 5 client-construction sites (Gemini ×2, Anthropic, OpenAI, local/OpenAI-compatible) had no explicit timeout; confirmed against the installed SDK source that `google-genai`'s own default is NO TIMEOUT AT ALL when unset (`_api_client.py`'s `max_allowed_time = float('inf')`) and Anthropic/OpenAI silently inherit their SDK's 10-minute default. New setting `AI_CHAT_TIMEOUT_SECONDS` (default 120.0 — larger than `LLM_COMMENTARY_TIMEOUT_SECONDS`/`OPAL_RESEARCH_TIMEOUT_SECONDS` since this covers a streaming response; httpx's per-chunk read-timeout semantics mean this bounds "wait for the next token," not total conversation length) now passed explicitly at all 5 sites; both endpoints run on a separate FastAPI process from the orchestrator daemon, so a stall there is a resource-leak/bad-UX risk, not a silent multi-day pipeline outage. **Deliberately deferred, disclosed not fixed**: `api/ws_api.py`'s `client_to_gemini()`'s `await websocket.receive_text()` has no idle timeout — an idle browser client can leak one asyncio task pair + one OS socket to Gemini indefinitely; a session-lifecycle/UX design decision, not a one-line bound. New env vars: `ALPACA_REQUEST_TIMEOUT_SECONDS`, `PIPELINE_STEP_TIMEOUT_SECONDS`, `AI_CHAT_TIMEOUT_SECONDS`. Tests: `tests/test_alpaca_http.py`, extended `tests/test_alpaca_broker.py`, extended `tests/test_market_data.py`, `tests/test_pipeline_runner.py`, extended `tests/test_investyo_mcp_server.py` and `tests/test_preflight.py`, plus extended chat-endpoint tests. See `docs/known_issues/data_pipeline_fred_unbounded_timeout_stall.md`'s 2026-08-27 follow-up section for the full sweep. **Same-day follow-up 2, raised to 1800s**: the very first two live daemon cycles after deploy legitimately exceeded the original 900s per-step budget (~1380s each) while contending for CPU and shared FMP/GDELT/EDGAR rate-limit budgets with two concurrent operator-launched jobs (`refresh_validations.py`, `backfill_sentiment_history.py`) on the same machine -- not a hang. `PIPELINE_STEP_TIMEOUT_SECONDS` raised 900.0 -> 1800.0 and `PIPELINE_STALL_ALERT_SECONDS` raised 1800 -> 3600 in lockstep to preserve the original 2x safety-margin ratio between them; hang protection is unchanged, only the window of tolerated legitimate contention widened. See `docs/known_issues/data_pipeline_fred_unbounded_timeout_stall.md`'s "Follow-up 2" section for the full 8-agent investigation that ruled out a code defect first.
- **Pipeline hang fix #2: per-ticker fundamentals-refresh loop had no cycle-wide deadline, wedging the "processing" stage for ~1199s every cycle (`processing_engine.py`, `settings.py`, 2026-08-28)**: a live debug session watching the daemon process in real time found a SECOND, independent stall location from the FRED-timeout incident above — the daemon logged "Routing data through Computational Core (Processing)..." then went completely silent for roughly 15 minutes before the cycle failed, matching an exact ~1199s stall duration with zero variance across all 5 of the prior failed runs recorded in the `pipeline_runs` DB history (`desktop/run_history_store.py`). The earlier "data" fetch stage was ruled out as the cause — it correctly dead-letters at `DATA_FETCH_TASK_TIMEOUT_SECONDS` (180s) and falls back to `MockDataEngine`, exactly as the 2026-08-27 FRED-timeout fix round designed it to; the hang was specifically in the LATER "processing" stage's `ProcessingEngine.calculate_fundamental_metrics()` per-ticker loop, which that prior round's audit never covered (it scoped itself to `subprocess`/thread-pool/LLM-client call sites, not this synchronous per-ticker DB-refresh loop). **Relationship to the "same-day follow-up 2" bullet above**: that investigation attributed the SAME two run IDs (`26259a60`, `09c649aa`) this debug session also examined to legitimate FMP/GDELT/EDGAR rate-limit contention from concurrently-launched jobs, and fixed it by widening the outer per-step timeout — a real, complementary finding, not a conflicting one. That fix explains *why* the provider calls were unusually slow that day; this fix closes *where* that slowness had nowhere to be bounded once it started, since `calculate_fundamental_metrics()`'s loop had no aggregate deadline of its own. Widening the outer timeout alone still leaves this specific loop able to consume the entire (now-larger) step budget under worse contention or a genuine provider outage. Root cause: `calculate_fundamental_metrics()` loops over every ticker in `fund_dtos` and, when `settings.HISTORICAL_STORE_ENABLED=True`, calls `HistoricalStore.get_fundamentals(ticker, ..., provider=...)` — a live network call with no timeout and, critically, no AGGREGATE deadline across the whole loop, so one slow/stalled provider response blocks every ticker queued behind it too. Fixed via new setting `PROCESSING_FUNDAMENTALS_MAX_SECONDS_PER_CYCLE` (float, default `60.0`): `calculate_fundamental_metrics()` now computes one wall-clock deadline before entering its per-ticker loop; once it passes, the live `HistoricalStore.get_fundamentals()` refresh is skipped for that ticker AND every remaining one for the rest of the cycle (sticky — the deadline is never reset mid-cycle), logging a single WARNING the first time it trips rather than one per skipped ticker. A ticker whose refresh is skipped this way still gets a normal result — it falls back to the DTO-only fundamentals path exactly as if `HISTORICAL_STORE_ENABLED` were `False` for that ticker (dead-letter resilience per CONSTRAINT #6; nothing dropped, nothing raised). **Deliberately not fixed, disclosed, separate follow-up**: `PIPELINE_STEP_TIMEOUT_SECONDS` (see the bullet above) only wraps `AsyncPipelineRunner.run()`'s dispatch of an entire synchronous pipeline step; `RunPipelineStep`'s own inner, separate synchronous `PipelineRunner` — which drives `OptionsAnalysisStep → ProcessingStep → ForecastingStep → StrategyEvalStep` — has no per-substep timeout mechanism of its own, so a future unbounded call in any of those four substeps would reproduce the same class of hang with nothing to catch it; this PR closes the one concrete, live-reproduced cause, not that broader architectural gap. See `docs/known_issues/data_pipeline_fred_unbounded_timeout_stall.md`'s "Follow-up 3" section for the full incident write-up (live-reproduction timing, the `pipeline_runs` history cross-check, the reconciliation with Follow-up 2's contention finding, and the disclosed substep-timeout gap). New env var: `PROCESSING_FUNDAMENTALS_MAX_SECONDS_PER_CYCLE`. Tests: `tests/test_processing_engine.py::TestCalculateFundamentalMetrics`.

- **Feature drift detection via PSI (`validation/covariate_drift.py`, `scripts/preflight_check.py`, 2026-08)**: added Population Stability Index (PSI) drift detection for model features to monitor if recent feature distributions diverge significantly from historical reference distributions. Gated by `FEATURE_DRIFT_PSI_ENABLED` in `settings.py`. Warns in `preflight_check.py` instead of blocking go-live.

- **Gravity suite**:
| Name | File | What it actually is |
|---|---|---|
| `GravityAIAuditor` (structural) | `Gravity AI Review Suite.py` | 90 real audit methods (`step_1`-`step_94`, gapped numbering), purely structural/Pandera/regex/backtest checks. **No LLM calls.** Can't be `import`ed (space in filename + top-of-file venv re-exec) — it imports other modules, nothing imports it. |
| `GravityAIAuditor` (grader) | `ai_verification_prompts.py:268` | A **different class, same name** — a keyword-based grader for text pasted back from an external LLM session. Not automated, not touched by this plan. |
| `gravity_ai_runner` | `engine/gravity_ai_runner.py` | The real one: Claude (primary) + Gemini (cross-checker) structured-output LLM auditor, 8 steps, gated `GRAVITY_AI_RUNNER_ENABLED` (default `False`), writes `output/gravity_ai_audit.json`. **Zero MCP tool touches this today** — Gap 2 fixes that. |
